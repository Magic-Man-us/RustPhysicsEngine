// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// The simple continued-fraction expansion of a float, `[a0; a1, a2, ...]`.
///
/// Stops after `max_terms`, or earlier once the remaining fractional part
/// is too small to yield a meaningful term. Only the leading terms of the
/// result describe the intended real number: an `f64` carries about 53
/// bits, so terms beyond roughly the twentieth describe the rounding of
/// the input rather than the number itself.
///
/// Panics:
/// Panics if `x` is not finite.
///
/// Rust: `exact::contfrac::continued_fraction_f64`
#[pyfunction]
#[pyo3(name = "continued_fraction_f64", signature = (x, max_terms))]
pub fn pyfn_continued_fraction_f64<'py>(py: Python<'py>, x: f64, max_terms: usize) -> PyResult<Vec<i64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::exact::contfrac::continued_fraction_f64(x, max_terms)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The convergents `h_k / k_k` of a simple continued fraction.
///
/// Uses the standard recurrence `h_k = a_k h_{k-1} + h_{k-2}`, and the
/// same for the denominators.
///
/// Rust: `exact::contfrac::convergents`
#[pyfunction]
#[pyo3(name = "convergents", signature = (cf))]
pub fn pyfn_convergents<'py>(py: Python<'py>, cf: Vec<i64>) -> PyResult<Vec<pyo3::Bound<'py, pyo3::PyAny>>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::exact::contfrac::convergents(&cf));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> { Ok(crate::runtime::coerce::rational_out(py, &__x)?) }).collect::<PyResult<Vec<_>>>()?)
}

/// The periodic continued fraction of `sqrt(n)`, as `(head, period)` with
/// `sqrt(n) = [head; period repeated]`.
///
/// For a perfect square the period is empty. Otherwise the expansion is
/// purely periodic after the first term and the period always ends with
/// `2*a0`, which is the termination test used here.
///
/// Panics:
/// Panics if `n` is zero.
///
/// Rust: `exact::contfrac::periodic_cf_sqrt`
#[pyfunction]
#[pyo3(name = "periodic_cf_sqrt", signature = (n))]
pub fn pyfn_periodic_cf_sqrt(n: u64) -> PyResult<(Vec<u64>, Vec<u64>)> {
    let __r = crate::runtime::guard(|| rust_physics_engine::exact::contfrac::periodic_cf_sqrt(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// The fundamental solution of Pell's equation `x^2 - d y^2 = 1`.
///
/// Returns `None` when `d` is a perfect square, where the equation has
/// only the trivial solution. Otherwise the smallest solution with
/// `y > 0` is a convergent of the continued fraction of `sqrt(d)`.
///
/// Panics:
/// Panics if `d` is zero.
///
/// Rust: `exact::contfrac::pell_fundamental_solution`
#[pyfunction]
#[pyo3(name = "pell_fundamental_solution", signature = (d))]
pub fn pyfn_pell_fundamental_solution<'py>(py: Python<'py>, d: u64) -> PyResult<Option<(pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>)>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::exact::contfrac::pell_fundamental_solution(d));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(match __v { Some(__x) => Some((crate::runtime::coerce::bigint_out(py, &__x.0)?, crate::runtime::coerce::bigint_out(py, &__x.1)?)), None => None })
}

/// Evaluate a generalized continued fraction
/// `b(0) + a(1)/(b(1) + a(2)/(b(2) + ...))` to `n` levels by the modified
/// Lentz algorithm.
///
/// Lentz builds the value from the top down with multiplicative updates,
/// so it never forms the deep nested quotient directly and cannot lose the
/// tail to cancellation. Zero intermediates are nudged to a tiny value,
/// which is the "modified" part.
///
/// Panics:
/// Panics if `n` is zero.
///
/// Rust: `exact::contfrac::generalized_cf_eval`
#[pyfunction]
#[pyo3(name = "generalized_cf_eval", signature = (a, b, n))]
pub fn pyfn_generalized_cf_eval(a: pyo3::Py<pyo3::PyAny>, b: pyo3::Py<pyo3::PyAny>, n: usize) -> PyResult<f64> {
    let __cb_a = std::rc::Rc::new(crate::runtime::Callback::new(a));
    let a = { let __cb = __cb_a.clone(); move |__a0: usize| -> f64 { __cb.call::<_, f64>((__a0,), f64::NAN) } };
    let __cb_b = std::rc::Rc::new(crate::runtime::Callback::new(b));
    let b = { let __cb = __cb_b.clone(); move |__a0: usize| -> f64 { __cb.call::<_, f64>((__a0,), f64::NAN) } };
    let __r = crate::runtime::guard(|| rust_physics_engine::exact::contfrac::generalized_cf_eval(&a, &b, n));
    crate::runtime::callback::check(&[&__cb_a, &__cb_b], ())?;
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The first `n` terms of the continued fraction of `e`.
///
/// `e = [2; 1, 2, 1, 1, 4, 1, 1, 6, 1, ...]`: after the leading 2 the
/// terms run in blocks of `1, 2k, 1`.
///
/// Rust: `exact::contfrac::cf_e`
#[pyfunction]
#[pyo3(name = "cf_e", signature = (n))]
pub fn pyfn_cf_e<'py>(py: Python<'py>, n: usize) -> PyResult<Vec<i64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::exact::contfrac::cf_e(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The first `n` terms of the continued fraction of `pi`.
///
/// `pi` has no known pattern, so the terms are read off a high-precision
/// value computed here in fixed point rather than from an `f64`, which
/// would only support about twenty correct terms. The working precision
/// is chosen generously against the number of terms requested.
///
/// Rust: `exact::contfrac::cf_pi_terms`
#[pyfunction]
#[pyo3(name = "cf_pi_terms", signature = (n))]
pub fn pyfn_cf_pi_terms<'py>(py: Python<'py>, n: usize) -> PyResult<Vec<i64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::exact::contfrac::cf_pi_terms(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The orbit of `x` under the Gauss map `G(x) = frac(1/x)`, `n` steps.
///
/// The Gauss map is the shift on continued-fraction expansions: the
/// integer parts of the reciprocals along the orbit are exactly the
/// partial quotients.
///
/// Panics:
/// Panics if `x` is not finite.
///
/// Rust: `exact::contfrac::gauss_map_orbit`
#[pyfunction]
#[pyo3(name = "gauss_map_orbit", signature = (x, n))]
pub fn pyfn_gauss_map_orbit<'py>(py: Python<'py>, x: f64, n: usize) -> PyResult<Vec<f64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::exact::contfrac::gauss_map_orbit(x, n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The geometric mean of the first `n` continued-fraction terms of `x`.
///
/// For almost every irrational this tends to Khinchin's constant,
/// about 2.685452001. Convergence is very slow, so a short orbit only
/// lands in the neighbourhood.
///
/// Panics:
/// Panics if `x` is not finite.
///
/// Rust: `exact::contfrac::khinchin_estimate`
#[pyfunction]
#[pyo3(name = "khinchin_estimate", signature = (x, n))]
pub fn pyfn_khinchin_estimate(x: f64, n: usize) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::exact::contfrac::khinchin_estimate(x, n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Estimate Levy's constant from the growth of the convergent
/// denominators of `x`: `q_n^(1/n)` tends to `exp(pi^2 / (12 ln 2))`,
/// about 3.275822918.
///
/// Panics:
/// Panics if `x` is not finite.
///
/// Rust: `exact::contfrac::levy_constant_estimate`
#[pyfunction]
#[pyo3(name = "levy_constant_estimate", signature = (x, n))]
pub fn pyfn_levy_constant_estimate(x: f64, n: usize) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::exact::contfrac::levy_constant_estimate(x, n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_continued_fraction_f64, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_convergents, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_periodic_cf_sqrt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_pell_fundamental_solution, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_generalized_cf_eval, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_cf_e, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_cf_pi_terms, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_gauss_map_orbit, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_khinchin_estimate, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_levy_constant_estimate, m)?)?;
    Ok(())
}
