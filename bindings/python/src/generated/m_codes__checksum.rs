// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// Checksums and check digits: cheap ways to notice that data changed.
///
/// None of these corrects anything, and none of them resists an adversary.
/// What they do is turn a class of likely accidents into a mismatch, and the
/// useful question about each is which class. A single parity bit catches any
/// odd number of flipped bits and nothing else. A Fletcher or Adler sum
/// catches reordering, which a plain sum does not, because the second
/// accumulator weights each byte by its position. A CRC of width `w` catches
/// every burst of `w` bits or fewer, every odd number of bit errors when the
/// polynomial has `x + 1` as a factor, and all but `2^-w` of everything else.
/// The decimal check digits catch every single-digit error and, except for
/// Luhn, every transposition of adjacent digits.
///
/// For an adversary, none of this is relevant: all of it is linear or nearly
/// so, and a forger can adjust the data to hit any checksum they like.
/// Even parity: `true` when an odd number of bits are set, so that appending
/// it makes the total even.
///
/// Detects any odd number of bit errors and no even number, which is the
/// whole of what a single bit can promise.
///
/// Rust: `codes::checksum::parity`
#[pyfunction]
#[pyo3(name = "parity", signature = (bits))]
pub fn pyfn_parity<'py>(py: Python<'py>, bits: Vec<bool>) -> PyResult<bool> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::parity(&bits)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Parity of the set bits of a word.
///
/// Rust: `codes::checksum::parity_u64`
#[pyfunction]
#[pyo3(name = "parity_u64", signature = (x))]
pub fn pyfn_parity_u64(x: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::checksum::parity_u64(x));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Fletcher-16 checksum: a running byte sum and a running sum of that
/// sum, both modulo 255, packed into sixteen bits.
///
/// The second accumulator is what makes it more than a sum: it weights each
/// byte by how many bytes follow it, so swapping two bytes changes the
/// result, which a plain sum cannot notice. Modulo 255 rather than 256
/// because a modulus with a factor of two lets the high bits of a byte fall
/// out of the low accumulator entirely.
///
/// Rust: `codes::checksum::checksum_fletcher16`
#[pyfunction]
#[pyo3(name = "checksum_fletcher16", signature = (data))]
pub fn pyfn_checksum_fletcher16<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<u16> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::checksum_fletcher16(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Fletcher-32 checksum, over sixteen-bit words modulo 65535.
///
/// Odd-length input is padded with a zero byte, which is the usual
/// convention and the reason Fletcher-32 cannot distinguish `"ab"` from
/// `"ab\0"`.
///
/// Rust: `codes::checksum::checksum_fletcher32`
#[pyfunction]
#[pyo3(name = "checksum_fletcher32", signature = (data))]
pub fn pyfn_checksum_fletcher32<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<u32> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::checksum_fletcher32(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Adler-32, as used by zlib: Fletcher's idea with a prime modulus.
///
/// The accumulators start at one and zero and run modulo 65521, the largest
/// prime below `2^16`. The prime modulus spreads the values more evenly than
/// Fletcher's 65535, and the leading one makes the checksum of an empty
/// input distinguishable from the checksum of a run of zero bytes.
///
/// Rust: `codes::checksum::adler32`
#[pyfunction]
#[pyo3(name = "adler32", signature = (data))]
pub fn pyfn_adler32<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<u32> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::adler32(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A cyclic redundancy check, in the parametric form every named CRC is an
/// instance of.
///
/// The message is treated as a polynomial over `GF(2)`, shifted left by
/// `width` and divided by `poly`; the remainder is the check value. Because
/// the code is linear, the difference between a message and a corrupted one
/// has its own remainder, so a corruption goes unnoticed exactly when its
/// error pattern is itself a multiple of `poly` -- which no burst shorter
/// than `width + 1` can be, since `poly` has degree `width`.
///
/// `init` seeds the register, so a run of leading zero bytes changes the
/// result; `xor_out` is applied at the end; `reflect` reverses the bits of
/// each input byte and of the final register, which is what the
/// bit-at-a-time hardware of a serial line does naturally. The named CRCs in
/// wide use all reflect input and output together or neither, so one flag
/// covers them.
///
/// Panics:
/// Panics unless `width` is between 8 and 64.
///
/// Rust: `codes::checksum::crc`
#[pyfunction]
#[pyo3(name = "crc", signature = (data, poly, width, init, xor_out, reflect_io))]
pub fn pyfn_crc<'py>(py: Python<'py>, data: Vec<u8>, poly: u64, width: u32, init: u64, xor_out: u64, reflect_io: bool) -> PyResult<u64> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::crc(&data, poly, width, init, xor_out, reflect_io)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// CRC-32 as used by Ethernet, zip, PNG and gzip.
///
/// Polynomial `0x04C11DB7`, register seeded to all ones, reflected in and
/// out, complemented at the end. The check value of `"123456789"` is
/// `0xCBF43926`.
///
/// Rust: `codes::checksum::crc32_ieee`
#[pyfunction]
#[pyo3(name = "crc32_ieee", signature = (data))]
pub fn pyfn_crc32_ieee<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<u32> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::crc32_ieee(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// CRC-16/CCITT-FALSE: polynomial `0x1021`, seeded to all ones, unreflected,
/// no final xor. The check value of `"123456789"` is `0x29B1`.
///
/// The name records a long-standing confusion: the true CCITT parameters
/// seed the register to zero, and this variant -- which is the one actually
/// deployed, in XMODEM's successors and in many microcontroller libraries --
/// does not.
///
/// Rust: `codes::checksum::crc16_ccitt`
#[pyfunction]
#[pyo3(name = "crc16_ccitt", signature = (data))]
pub fn pyfn_crc16_ccitt<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<u16> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::crc16_ccitt(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// CRC-8/SMBUS: polynomial `0x07`, zero seed, unreflected. The check value
/// of `"123456789"` is `0xF4`.
///
/// Rust: `codes::checksum::crc8`
#[pyfunction]
#[pyo3(name = "crc8", signature = (data))]
pub fn pyfn_crc8<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<u8> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::crc8(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The 256-entry lookup table for a reflected 32-bit CRC.
///
/// `poly` is the *reversed* polynomial -- `0xEDB88320` for CRC-32 -- because
/// a reflected CRC shifts right, and the table holds the remainder of each
/// possible byte. Processing a byte becomes one table lookup instead of
/// eight conditional shifts; the table is the loop unrolled once and cached.
///
/// Rust: `codes::checksum::crc_table`
#[pyfunction]
#[pyo3(name = "crc_table", signature = (poly))]
pub fn pyfn_crc_table<'py>(py: Python<'py>, poly: u32) -> PyResult<Vec<u32>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::crc_table(poly)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.to_vec())
}

/// CRC-32 driven by a precomputed table rather than bit by bit.
///
/// The same value as `crc32_ieee`, computed eight bits at a time. Pass the
/// table from `crc_table` with the reversed polynomial.
///
/// Rust: `codes::checksum::crc32_with_table`
#[pyfunction]
#[pyo3(name = "crc32_with_table", signature = (data, table))]
pub fn pyfn_crc32_with_table<'py>(py: Python<'py>, data: Vec<u8>, table: Vec<u32>) -> PyResult<u32> {
    let table = <[u32; 256]>::try_from(table).map_err(|__v: Vec<u32>| pyo3::exceptions::PyValueError::new_err(format!("expected 256 values, got {}", __v.len())))?;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::crc32_with_table(&data, &table)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Luhn checksum test, as used on payment card numbers.
///
/// Doubling every second digit from the right and casting out nines catches
/// every single-digit error and every transposition of adjacent digits
/// except `09` against `90`, which it maps to the same sum. That one blind
/// spot is why Verhoeff and Damm exist.
///
/// The check digit is the last element of `digits`.
///
/// Panics:
/// Panics if any entry is above nine.
///
/// Rust: `codes::checksum::luhn_check`
#[pyfunction]
#[pyo3(name = "luhn_check", signature = (digits))]
pub fn pyfn_luhn_check<'py>(py: Python<'py>, digits: Vec<u8>) -> PyResult<bool> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::luhn_check(&digits)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Luhn check digit that completes `payload`.
///
/// Panics:
/// Panics if any entry is above nine.
///
/// Rust: `codes::checksum::luhn_generate`
#[pyfunction]
#[pyo3(name = "luhn_generate", signature = (payload))]
pub fn pyfn_luhn_generate<'py>(py: Python<'py>, payload: Vec<u8>) -> PyResult<u8> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::luhn_generate(&payload)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// ISBN-10, whose check digit is a weighted sum modulo eleven.
///
/// Weights ten down to one, and the modulus is prime, which is what lets it
/// catch every transposition -- swapping two digits changes the sum by a
/// non-zero multiple of their difference, and a prime modulus has no zero
/// divisors to hide that. The price is that the check digit sometimes has to
/// be ten, written `X`; pass it as the value `10`.
///
/// Panics:
/// Panics unless there are ten entries, each at most nine, except the last
/// which may be ten.
///
/// Rust: `codes::checksum::isbn10_check`
#[pyfunction]
#[pyo3(name = "isbn10_check", signature = (digits))]
pub fn pyfn_isbn10_check<'py>(py: Python<'py>, digits: Vec<u8>) -> PyResult<bool> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::isbn10_check(&digits)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// ISBN-13, the same numbering embedded in the EAN-13 scheme: alternating
/// weights of one and three modulo ten.
///
/// The modulus is composite, so unlike ISBN-10 it misses transpositions of
/// adjacent digits differing by five -- but it never needs an `X`, which is
/// what the change bought.
///
/// Panics:
/// Panics unless there are thirteen digits, each at most nine.
///
/// Rust: `codes::checksum::isbn13_check`
#[pyfunction]
#[pyo3(name = "isbn13_check", signature = (digits))]
pub fn pyfn_isbn13_check<'py>(py: Python<'py>, digits: Vec<u8>) -> PyResult<bool> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::isbn13_check(&digits)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Verhoeff check, which catches every single-digit error and every
/// transposition of adjacent digits.
///
/// It works by giving up on arithmetic modulo ten and using the dihedral
/// group of order ten instead, which is not commutative -- so swapping two
/// digits genuinely changes the product, with no cases left over. A
/// position-dependent permutation of order eight is applied first, which is
/// what extends the guarantee past the two digits nearest the check digit.
///
/// The check digit is the last element of `digits`.
///
/// Panics:
/// Panics if any entry is above nine.
///
/// Rust: `codes::checksum::verhoeff_check`
#[pyfunction]
#[pyo3(name = "verhoeff_check", signature = (digits))]
pub fn pyfn_verhoeff_check<'py>(py: Python<'py>, digits: Vec<u8>) -> PyResult<bool> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::verhoeff_check(&digits)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Verhoeff check digit that completes `payload`.
///
/// Panics:
/// Panics if any entry is above nine.
///
/// Rust: `codes::checksum::verhoeff_generate`
#[pyfunction]
#[pyo3(name = "verhoeff_generate", signature = (payload))]
pub fn pyfn_verhoeff_generate<'py>(py: Python<'py>, payload: Vec<u8>) -> PyResult<u8> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::verhoeff_generate(&payload)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Damm check, with the same guarantees as Verhoeff and none of its
/// tables.
///
/// One quasigroup operation folded across the digits, with no permutation
/// and no inverse: the check digit is simply the interim value, because the
/// table's diagonal is zero. Total anti-symmetry -- that `(a * b) * c` and
/// `(a * c) * b` differ whenever `b` and `c` do -- is exactly the property
/// that catches transpositions, and it is built into the table rather than
/// arranged around it.
///
/// The check digit is the last element of `digits`.
///
/// Panics:
/// Panics if any entry is above nine.
///
/// Rust: `codes::checksum::damm_check`
#[pyfunction]
#[pyo3(name = "damm_check", signature = (digits))]
pub fn pyfn_damm_check<'py>(py: Python<'py>, digits: Vec<u8>) -> PyResult<bool> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::damm_check(&digits)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Damm check digit that completes `payload`.
///
/// Panics:
/// Panics if any entry is above nine.
///
/// Rust: `codes::checksum::damm_generate`
#[pyfunction]
#[pyo3(name = "damm_generate", signature = (payload))]
pub fn pyfn_damm_generate<'py>(py: Python<'py>, payload: Vec<u8>) -> PyResult<u8> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::damm_generate(&payload)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The number of bit positions in which two words differ.
///
/// The distance a code needs to survive: a code whose words are all at least
/// `d` apart detects `d - 1` errors and corrects `(d - 1) / 2`, because a
/// received word within that radius of a codeword is within that radius of
/// no other.
///
/// Rust: `codes::checksum::hamming_distance_bits`
#[pyfunction]
#[pyo3(name = "hamming_distance_bits", signature = (a, b))]
pub fn pyfn_hamming_distance_bits(a: u64, b: u64) -> PyResult<u32> {
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::checksum::hamming_distance_bits(a, b));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The bitwise Hamming distance between two byte strings, or `None` if they
/// are different lengths.
///
/// Rust: `codes::checksum::hamming_distance_bytes`
#[pyfunction]
#[pyo3(name = "hamming_distance_bytes", signature = (a, b))]
pub fn pyfn_hamming_distance_bytes<'py>(py: Python<'py>, a: Vec<u8>, b: Vec<u8>) -> PyResult<Option<u32>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::checksum::hamming_distance_bytes(&a, &b)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_parity, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_parity_u64, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_checksum_fletcher16, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_checksum_fletcher32, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_adler32, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_crc, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_crc32_ieee, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_crc16_ccitt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_crc8, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_crc_table, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_crc32_with_table, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_luhn_check, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_luhn_generate, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_isbn10_check, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_isbn13_check, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_verhoeff_check, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_verhoeff_generate, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_damm_check, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_damm_generate, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_hamming_distance_bits, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_hamming_distance_bytes, m)?)?;
    Ok(())
}
