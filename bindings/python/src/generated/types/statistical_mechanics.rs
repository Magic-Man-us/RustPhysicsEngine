// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;


/// A square-lattice Ising model with nearest-neighbour coupling.
///
/// `H = -j sum_<ij> s_i s_j - h sum_i s_i` with spins `+/-1`, and `beta` the
/// inverse temperature in units where Boltzmann's constant is one.
///
/// Rust: `statistical_mechanics::ising::Ising2D`
#[pyclass(name = "Ising2D", module = "rust_physics_engine.statistical_mechanics.ising", from_py_object)]
#[derive(Clone)]
pub struct PyIsing2D { pub inner: rust_physics_engine::statistical_mechanics::ising::Ising2D }
#[pymethods]
impl PyIsing2D {
    /// Builds a `Ising2D` from its fields.
    #[new]
    #[pyo3(signature = (n, spins, j, h, beta, periodic))]
    fn __new__(n: usize, spins: Vec<i8>, j: f64, h: f64, beta: f64, periodic: bool) -> Self {

        Self { inner: rust_physics_engine::statistical_mechanics::ising::Ising2D { n: n, spins: spins, j: j, h: h, beta: beta, periodic: periodic } }
    }

    /// A lattice with every spin up.
    ///
    /// Errors:
    /// Returns an error for a lattice smaller than two or larger than 512 a
    /// side, or a non-positive inverse temperature.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::cold`
    #[pyo3(name = "cold")]
    #[staticmethod]
    #[pyo3(signature = (n, j, h, beta, periodic))]
    fn cold(n: usize, j: f64, h: f64, beta: f64, periodic: bool) -> PyResult<crate::generated::types::PyIsing2D> {
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::ising::Ising2D::cold(n, j, h, beta, periodic));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyIsing2D { inner: __v })
    }

    /// A lattice with random spins.
    ///
    /// Errors:
    /// Returns an error on the same conditions as `Ising2D::cold`.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::random`
    #[pyo3(name = "random")]
    #[staticmethod]
    #[pyo3(signature = (n, j, h, beta, periodic, rng))]
    fn random(n: usize, j: f64, h: f64, beta: f64, periodic: bool, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<crate::generated::types::PyIsing2D> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::ising::Ising2D::random(n, j, h, beta, periodic, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyIsing2D { inner: __v })
    }

    /// The total energy.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::energy`
    #[pyo3(name = "energy")]
    #[pyo3(signature = ())]
    fn energy(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.energy());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The energy per site.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::energy_per_site`
    #[pyo3(name = "energy_per_site")]
    #[pyo3(signature = ())]
    fn energy_per_site(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.energy_per_site());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The magnetisation per site, signed.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::magnetization`
    #[pyo3(name = "magnetization")]
    #[pyo3(signature = ())]
    fn magnetization(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.magnetization());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// One Metropolis sweep: `n^2` attempted single-spin flips.
    ///
    /// The acceptance rule `min(1, exp(-beta dE))` satisfies detailed balance
    /// with the Boltzmann distribution, which is what makes the chain sample
    /// it. Note that a rejected move still counts as a step: the current
    /// configuration is re-measured, and treating rejections as "nothing
    /// happened" biases every average.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::metropolis_sweep`
    #[pyo3(name = "metropolis_sweep")]
    #[pyo3(signature = (rng))]
    fn metropolis_sweep(&mut self, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<()> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.metropolis_sweep(&mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// One heat-bath sweep: each visited spin is redrawn from its conditional
    /// distribution rather than proposed and accepted.
    ///
    /// Also correct, and it never rejects -- but it is not faster in any
    /// useful sense, because a spin redrawn to its current value has moved
    /// just as little as a rejected proposal.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::heat_bath_sweep`
    #[pyo3(name = "heat_bath_sweep")]
    #[pyo3(signature = (rng))]
    fn heat_bath_sweep(&mut self, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<()> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.heat_bath_sweep(&mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// One Wolff cluster update, returning the cluster size.
    ///
    /// Grows a cluster of aligned spins by adding each neighbouring bond with
    /// probability `1 - exp(-2 beta j)`, then flips the whole thing. The
    /// acceptance is *one* -- the bond probability is chosen precisely so
    /// that the construction's bias cancels the Boltzmann weight -- which is
    /// why the method has no rejected moves at all.
    ///
    /// Only meaningful for a ferromagnetic coupling in zero field; the field
    /// breaks the cancellation, and this implementation ignores it.
    ///
    /// Errors:
    /// Returns an error for a non-positive coupling or a non-zero field.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::wolff_cluster_step`
    #[pyo3(name = "wolff_cluster_step")]
    #[pyo3(signature = (rng))]
    fn wolff_cluster_step(&mut self, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<usize> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.wolff_cluster_step(&mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// Runs the chain and returns summary statistics.
    ///
    /// One *update* is a Metropolis sweep of `n^2` attempted flips, or a
    /// single Wolff cluster step. The two are not the same amount of work,
    /// and deliberately so: a Wolff update must be a fixed number of cluster
    /// steps rather than "however many it takes to flip a lattice's worth of
    /// spins". That second rule looks like the natural way to equalise the
    /// work and it silently biases every average, because it stops right
    /// after a large cluster -- and a large cluster means an ordered
    /// configuration, so measurements are taken preferentially at low
    /// energies. Measuring at a *fixed* interval of a Markov chain is
    /// unbiased; measuring when the chain reaches a state-dependent
    /// condition is not.
    ///
    /// `thermalize` updates are discarded before measurement begins. That
    /// discard is not optional either: the chain starts from a configuration
    /// that is not a Boltzmann sample, and averaging over the approach to
    /// equilibrium biases everything.
    ///
    /// Errors:
    /// Returns an error for a zero measurement interval or no sweeps.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::sample`
    #[pyo3(name = "sample")]
    #[pyo3(signature = (sweeps, thermalize, measure_every, use_wolff, rng))]
    fn sample(&mut self, sweeps: usize, thermalize: usize, measure_every: usize, use_wolff: bool, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<crate::generated::types::PyIsingStats> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.sample(sweeps, thermalize, measure_every, use_wolff, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyIsingStats { inner: __v })
    }

    /// The spin-spin correlation at separation `r` along a lattice axis.
    ///
    /// Errors:
    /// Returns an error if the separation exceeds the lattice.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::correlation_function`
    #[pyo3(name = "correlation_function")]
    #[pyo3(signature = (r))]
    fn correlation_function(&self, r: usize) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.correlation_function(r));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The ensemble-averaged correlation function out to half the lattice,
    /// together with the mean squared magnetisation.
    ///
    /// Averaging over the run is not a refinement. A single configuration's
    /// correlation function is a sample of a random variable whose spread at
    /// large separation is comparable to its mean, so a length fitted from
    /// one snapshot is fitted to noise -- and the noise does not shrink as
    /// the lattice grows, because the number of *independent* regions does
    /// not either.
    ///
    /// Errors:
    /// Returns an error for a lattice too small to fit on, or no updates.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::sample_correlations`
    #[pyo3(name = "sample_correlations")]
    #[pyo3(signature = (updates, use_wolff, rng))]
    fn sample_correlations(&mut self, updates: usize, use_wolff: bool, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<(Vec<f64>, f64)> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.sample_correlations(updates, use_wolff, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok((__v.0, __v.1))
    }

    /// Fits a correlation length to an averaged correlation function.
    ///
    /// The connected correlation `C(r) - <m>^2` decays as `exp(-r / xi)`, so
    /// the length is minus the reciprocal slope of its logarithm. Only the
    /// separations where the connected correlation is well clear of the
    /// sampling noise are fitted: a threshold near zero admits points that
    /// are pure noise, and the fitted slope is then noise too -- which reads
    /// as a *long* correlation length in a hot lattice, exactly backwards.
    ///
    /// Returns zero when there is nothing resolvable to fit, and the lattice
    /// size when the correlation does not decay within it -- which is the
    /// honest answer near the critical point, where the true length exceeds
    /// anything a finite lattice can report.
    ///
    /// Errors:
    /// Returns an error for fewer than four separations.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::correlation_length_estimate`
    #[pyo3(name = "correlation_length_estimate")]
    #[staticmethod]
    #[pyo3(signature = (correlations, background))]
    fn correlation_length_estimate<'py>(py: Python<'py>, correlations: Vec<f64>, background: f64) -> PyResult<f64> {
        let _ = py;
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::statistical_mechanics::ising::Ising2D::correlation_length_estimate(&correlations, background)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The integrated autocorrelation time of the magnetisation, together
    /// with the mean number of spin flips an update costs.
    ///
    /// The time says how many updates a measurement is worth: `2 tau`
    /// consecutive samples carry the information of one independent one, so
    /// error bars computed as though samples were independent are too small
    /// by a factor of `sqrt(2 tau)`.
    ///
    /// The work is reported alongside because the two algorithms' updates are
    /// not comparable on their own. A Metropolis update attempts `n^2` flips;
    /// a Wolff update flips one cluster, whose size varies with the
    /// temperature. Comparing the two requires `tau` times the work, not
    /// `tau` alone -- and a comparison in bare updates would flatter whichever
    /// algorithm happened to define the larger one.
    ///
    /// Errors:
    /// Returns an error for too few updates to estimate from.
    ///
    /// Rust: `statistical_mechanics::ising::Ising2D::autocorrelation_time`
    #[pyo3(name = "autocorrelation_time")]
    #[pyo3(signature = (updates, use_wolff, rng))]
    fn autocorrelation_time(&mut self, updates: usize, use_wolff: bool, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<(f64, f64)> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.autocorrelation_time(updates, use_wolff, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok((__v.0, __v.1))
    }

    #[getter]
    #[pyo3(name = "n")]
    fn py_get_n(&self) -> PyResult<usize> { Ok(self.inner.n) }

    #[setter]
    #[pyo3(name = "n")]
    fn py_set_n(&mut self, v: usize) { self.inner.n = v; }

    #[getter]
    #[pyo3(name = "spins")]
    fn py_get_spins(&self) -> PyResult<Vec<i8>> { Ok(self.inner.spins.clone()) }

    #[getter]
    #[pyo3(name = "j")]
    fn py_get_j(&self) -> PyResult<f64> { Ok(self.inner.j) }

    #[setter]
    #[pyo3(name = "j")]
    fn py_set_j(&mut self, v: f64) { self.inner.j = v; }

    #[getter]
    #[pyo3(name = "h")]
    fn py_get_h(&self) -> PyResult<f64> { Ok(self.inner.h) }

    #[setter]
    #[pyo3(name = "h")]
    fn py_set_h(&mut self, v: f64) { self.inner.h = v; }

    #[getter]
    #[pyo3(name = "beta")]
    fn py_get_beta(&self) -> PyResult<f64> { Ok(self.inner.beta) }

    #[setter]
    #[pyo3(name = "beta")]
    fn py_set_beta(&mut self, v: f64) { self.inner.beta = v; }

    #[getter]
    #[pyo3(name = "periodic")]
    fn py_get_periodic(&self) -> PyResult<bool> { Ok(self.inner.periodic) }

    #[setter]
    #[pyo3(name = "periodic")]
    fn py_set_periodic(&mut self, v: bool) { self.inner.periodic = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Ising2D", "Ising2D", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// Summary statistics from a Monte Carlo run.
///
/// Rust: `statistical_mechanics::ising::IsingStats`
#[pyclass(name = "IsingStats", module = "rust_physics_engine.statistical_mechanics.ising", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyIsingStats { pub inner: rust_physics_engine::statistical_mechanics::ising::IsingStats }
#[pymethods]
impl PyIsingStats {
    /// Builds a `IsingStats` from its fields.
    #[new]
    #[pyo3(signature = (e_mean, e_var, m_mean, m_abs, susceptibility, heat_capacity, binder_cumulant, samples))]
    fn __new__(e_mean: f64, e_var: f64, m_mean: f64, m_abs: f64, susceptibility: f64, heat_capacity: f64, binder_cumulant: f64, samples: usize) -> Self {

        Self { inner: rust_physics_engine::statistical_mechanics::ising::IsingStats { e_mean: e_mean, e_var: e_var, m_mean: m_mean, m_abs: m_abs, susceptibility: susceptibility, heat_capacity: heat_capacity, binder_cumulant: binder_cumulant, samples: samples } }
    }

    #[getter]
    #[pyo3(name = "e_mean")]
    fn py_get_e_mean(&self) -> PyResult<f64> { Ok(self.inner.e_mean) }

    #[setter]
    #[pyo3(name = "e_mean")]
    fn py_set_e_mean(&mut self, v: f64) { self.inner.e_mean = v; }

    #[getter]
    #[pyo3(name = "e_var")]
    fn py_get_e_var(&self) -> PyResult<f64> { Ok(self.inner.e_var) }

    #[setter]
    #[pyo3(name = "e_var")]
    fn py_set_e_var(&mut self, v: f64) { self.inner.e_var = v; }

    #[getter]
    #[pyo3(name = "m_mean")]
    fn py_get_m_mean(&self) -> PyResult<f64> { Ok(self.inner.m_mean) }

    #[setter]
    #[pyo3(name = "m_mean")]
    fn py_set_m_mean(&mut self, v: f64) { self.inner.m_mean = v; }

    #[getter]
    #[pyo3(name = "m_abs")]
    fn py_get_m_abs(&self) -> PyResult<f64> { Ok(self.inner.m_abs) }

    #[setter]
    #[pyo3(name = "m_abs")]
    fn py_set_m_abs(&mut self, v: f64) { self.inner.m_abs = v; }

    #[getter]
    #[pyo3(name = "susceptibility")]
    fn py_get_susceptibility(&self) -> PyResult<f64> { Ok(self.inner.susceptibility) }

    #[setter]
    #[pyo3(name = "susceptibility")]
    fn py_set_susceptibility(&mut self, v: f64) { self.inner.susceptibility = v; }

    #[getter]
    #[pyo3(name = "heat_capacity")]
    fn py_get_heat_capacity(&self) -> PyResult<f64> { Ok(self.inner.heat_capacity) }

    #[setter]
    #[pyo3(name = "heat_capacity")]
    fn py_set_heat_capacity(&mut self, v: f64) { self.inner.heat_capacity = v; }

    #[getter]
    #[pyo3(name = "binder_cumulant")]
    fn py_get_binder_cumulant(&self) -> PyResult<f64> { Ok(self.inner.binder_cumulant) }

    #[setter]
    #[pyo3(name = "binder_cumulant")]
    fn py_set_binder_cumulant(&mut self, v: f64) { self.inner.binder_cumulant = v; }

    #[getter]
    #[pyo3(name = "samples")]
    fn py_get_samples(&self) -> PyResult<usize> { Ok(self.inner.samples) }

    #[setter]
    #[pyo3(name = "samples")]
    fn py_set_samples(&mut self, v: usize) { self.inner.samples = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("IsingStats", "IsingStats", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// The `q`-state Potts model on a square lattice.
///
/// Generalises Ising, which is the two-state case. The transition turns
/// first order above `q = 4` in two dimensions, which is why the model is the
/// standard example that the *order* of a transition is not a detail of the
/// interaction but a consequence of the symmetry.
///
/// Rust: `statistical_mechanics::ising::Potts2D`
#[pyclass(name = "Potts2D", module = "rust_physics_engine.statistical_mechanics.ising", from_py_object)]
#[derive(Clone)]
pub struct PyPotts2D { pub inner: rust_physics_engine::statistical_mechanics::ising::Potts2D }
#[pymethods]
impl PyPotts2D {
    /// Builds a `Potts2D` from its fields.
    #[new]
    #[pyo3(signature = (q, n, states, j, beta))]
    fn __new__(q: u8, n: usize, states: Vec<u8>, j: f64, beta: f64) -> Self {

        Self { inner: rust_physics_engine::statistical_mechanics::ising::Potts2D { q: q, n: n, states: states, j: j, beta: beta } }
    }

    /// A random configuration.
    ///
    /// Errors:
    /// Returns an error for fewer than two states, a bad lattice size, or a
    /// non-positive beta.
    ///
    /// Rust: `statistical_mechanics::ising::Potts2D::random`
    #[pyo3(name = "random")]
    #[staticmethod]
    #[pyo3(signature = (q, n, j, beta, rng))]
    fn random(q: u8, n: usize, j: f64, beta: f64, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<crate::generated::types::PyPotts2D> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::ising::Potts2D::random(q, n, j, beta, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyPotts2D { inner: __v })
    }

    /// The energy: minus the coupling for each agreeing bond.
    ///
    /// Rust: `statistical_mechanics::ising::Potts2D::energy`
    #[pyo3(name = "energy")]
    #[pyo3(signature = ())]
    fn energy(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.energy());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// One Metropolis sweep.
    ///
    /// Rust: `statistical_mechanics::ising::Potts2D::metropolis_sweep`
    #[pyo3(name = "metropolis_sweep")]
    #[pyo3(signature = (rng))]
    fn metropolis_sweep(&mut self, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<()> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.metropolis_sweep(&mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// The order parameter: how far the most common state's share exceeds
    /// what randomness would give.
    ///
    /// Rust: `statistical_mechanics::ising::Potts2D::order_parameter`
    #[pyo3(name = "order_parameter")]
    #[pyo3(signature = ())]
    fn order_parameter(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.order_parameter());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "q")]
    fn py_get_q(&self) -> PyResult<u8> { Ok(self.inner.q) }

    #[setter]
    #[pyo3(name = "q")]
    fn py_set_q(&mut self, v: u8) { self.inner.q = v; }

    #[getter]
    #[pyo3(name = "n")]
    fn py_get_n(&self) -> PyResult<usize> { Ok(self.inner.n) }

    #[setter]
    #[pyo3(name = "n")]
    fn py_set_n(&mut self, v: usize) { self.inner.n = v; }

    #[getter]
    #[pyo3(name = "states")]
    fn py_get_states(&self) -> PyResult<Vec<u8>> { Ok(self.inner.states.clone()) }

    #[getter]
    #[pyo3(name = "j")]
    fn py_get_j(&self) -> PyResult<f64> { Ok(self.inner.j) }

    #[setter]
    #[pyo3(name = "j")]
    fn py_set_j(&mut self, v: f64) { self.inner.j = v; }

    #[getter]
    #[pyo3(name = "beta")]
    fn py_get_beta(&self) -> PyResult<f64> { Ok(self.inner.beta) }

    #[setter]
    #[pyo3(name = "beta")]
    fn py_set_beta(&mut self, v: f64) { self.inner.beta = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Potts2D", "Potts2D", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// The two-dimensional XY model: continuous spins on a square lattice.
///
/// It has no ordered phase at any positive temperature -- a continuous
/// symmetry cannot break in two dimensions -- and yet it has a transition,
/// where vortices unbind. That the transition exists without an order
/// parameter is what makes it interesting.
///
/// Rust: `statistical_mechanics::ising::XyModel2D`
#[pyclass(name = "XyModel2D", module = "rust_physics_engine.statistical_mechanics.ising", from_py_object)]
#[derive(Clone)]
pub struct PyXyModel2D { pub inner: rust_physics_engine::statistical_mechanics::ising::XyModel2D }
#[pymethods]
impl PyXyModel2D {
    /// Builds a `XyModel2D` from its fields.
    #[new]
    #[pyo3(signature = (n, theta, j, beta))]
    fn __new__(n: usize, theta: Vec<f64>, j: f64, beta: f64) -> Self {

        Self { inner: rust_physics_engine::statistical_mechanics::ising::XyModel2D { n: n, theta: theta, j: j, beta: beta } }
    }

    /// A random configuration.
    ///
    /// Errors:
    /// Returns an error for a bad lattice size or non-positive beta.
    ///
    /// Rust: `statistical_mechanics::ising::XyModel2D::random`
    #[pyo3(name = "random")]
    #[staticmethod]
    #[pyo3(signature = (n, j, beta, rng))]
    fn random(n: usize, j: f64, beta: f64, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<crate::generated::types::PyXyModel2D> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::ising::XyModel2D::random(n, j, beta, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyXyModel2D { inner: __v })
    }

    /// The energy: minus the coupling times the cosine of each bond angle.
    ///
    /// Rust: `statistical_mechanics::ising::XyModel2D::energy`
    #[pyo3(name = "energy")]
    #[pyo3(signature = ())]
    fn energy(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.energy());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// One Metropolis sweep, proposing a bounded angle change.
    ///
    /// Rust: `statistical_mechanics::ising::XyModel2D::metropolis_sweep`
    #[pyo3(name = "metropolis_sweep")]
    #[pyo3(signature = (rng, step))]
    fn metropolis_sweep(&mut self, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>, step: f64) -> PyResult<()> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.metropolis_sweep(&mut rng.inner, step));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// The vorticity of the plaquette whose lower-left corner is `(row,
    /// column)`, as an integer winding number.
    ///
    /// Summing the angle differences around a plaquette, each reduced to
    /// `(-pi, pi]`, gives a multiple of `2 pi`. That the multiple is an
    /// integer is not approximate -- it is a topological fact about the
    /// configuration, and it is why vortices cannot be removed by a small
    /// change.
    ///
    /// Rust: `statistical_mechanics::ising::XyModel2D::plaquette_vorticity`
    #[pyo3(name = "plaquette_vorticity")]
    #[pyo3(signature = (row, column))]
    fn plaquette_vorticity(&self, row: usize, column: usize) -> PyResult<i32> {
        let __r = crate::runtime::guard(|| self.inner.plaquette_vorticity(row, column));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The number of vortices and antivortices on the lattice.
    ///
    /// Rust: `statistical_mechanics::ising::XyModel2D::vortex_count`
    #[pyo3(name = "vortex_count")]
    #[pyo3(signature = ())]
    fn vortex_count(&self) -> PyResult<(usize, usize)> {
        let __r = crate::runtime::guard(|| self.inner.vortex_count());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    /// The Kosterlitz-Thouless transition temperature, about `0.893 j`.
    ///
    /// Not exactly known: unlike Ising, the XY model has no closed-form
    /// solution, and this is the best numerical estimate.
    ///
    /// Rust: `statistical_mechanics::ising::XyModel2D::kt_transition_estimate`
    #[pyo3(name = "kt_transition_estimate")]
    #[staticmethod]
    #[pyo3(signature = (j))]
    fn kt_transition_estimate(j: f64) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::ising::XyModel2D::kt_transition_estimate(j));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "n")]
    fn py_get_n(&self) -> PyResult<usize> { Ok(self.inner.n) }

    #[setter]
    #[pyo3(name = "n")]
    fn py_set_n(&mut self, v: usize) { self.inner.n = v; }

    #[getter]
    #[pyo3(name = "theta")]
    fn py_get_theta(&self) -> PyResult<Vec<f64>> { Ok(self.inner.theta.clone()) }

    #[getter]
    #[pyo3(name = "j")]
    fn py_get_j(&self) -> PyResult<f64> { Ok(self.inner.j) }

    #[setter]
    #[pyo3(name = "j")]
    fn py_set_j(&mut self, v: f64) { self.inner.j = v; }

    #[getter]
    #[pyo3(name = "beta")]
    fn py_get_beta(&self) -> PyResult<f64> { Ok(self.inner.beta) }

    #[setter]
    #[pyo3(name = "beta")]
    fn py_set_beta(&mut self, v: f64) { self.inner.beta = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("XyModel2D", "XyModel2D", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// Which way an inhibitor acts.
///
/// Rust: `statistical_mechanics::kinetics::Inhibition`
#[pyclass(name = "Inhibition", module = "rust_physics_engine.statistical_mechanics.kinetics", from_py_object, eq, eq_int)]
#[derive(Clone, Copy, PartialEq)]
pub enum PyInhibition {
    Competitive,
    Uncompetitive,
    NonCompetitive,
}
impl PyInhibition {
    pub fn to_rust(&self) -> rust_physics_engine::statistical_mechanics::kinetics::Inhibition { match self {
        Self::Competitive => rust_physics_engine::statistical_mechanics::kinetics::Inhibition::Competitive,
        Self::Uncompetitive => rust_physics_engine::statistical_mechanics::kinetics::Inhibition::Uncompetitive,
        Self::NonCompetitive => rust_physics_engine::statistical_mechanics::kinetics::Inhibition::NonCompetitive,
    } }
    pub fn from_rust(v: &rust_physics_engine::statistical_mechanics::kinetics::Inhibition) -> Self { match v {
        rust_physics_engine::statistical_mechanics::kinetics::Inhibition::Competitive => Self::Competitive,
        rust_physics_engine::statistical_mechanics::kinetics::Inhibition::Uncompetitive => Self::Uncompetitive,
        rust_physics_engine::statistical_mechanics::kinetics::Inhibition::NonCompetitive => Self::NonCompetitive,
    } }
}
#[pymethods]
impl PyInhibition {
    fn __repr__(&self) -> &'static str {
        match self {
            Self::Competitive => "Inhibition.Competitive",
            Self::Uncompetitive => "Inhibition.Uncompetitive",
            Self::NonCompetitive => "Inhibition.NonCompetitive",
        }
    }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// One elementary reaction, as species indices with their stoichiometric
/// coefficients.
///
/// Rust: `statistical_mechanics::kinetics::Reaction`
#[pyclass(name = "Reaction", module = "rust_physics_engine.statistical_mechanics.kinetics", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyReaction { pub inner: rust_physics_engine::statistical_mechanics::kinetics::Reaction }
#[pymethods]
impl PyReaction {
    /// A reaction from reactant and product lists.
    ///
    /// Rust: `statistical_mechanics::kinetics::Reaction::new`
    #[new]
    #[pyo3(signature = (reactants, products))]
    fn __new__(reactants: Vec<(usize, u32)>, products: Vec<(usize, u32)>) -> PyResult<crate::generated::types::PyReaction> {
        let reactants = reactants.into_iter().map(|__e| (__e.0, __e.1)).collect::<Vec<_>>();
        let products = products.into_iter().map(|__e| (__e.0, __e.1)).collect::<Vec<_>>();
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::kinetics::Reaction::new(&reactants, &products));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyReaction { inner: __v })
    }

    /// The molecularity: how many molecules meet.
    ///
    /// Rust: `statistical_mechanics::kinetics::Reaction::order`
    #[pyo3(name = "order")]
    #[pyo3(signature = ())]
    fn order(&self) -> PyResult<u32> {
        let __r = crate::runtime::guard(|| self.inner.order());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The net change in each species, indexed by species.
    ///
    /// Rust: `statistical_mechanics::kinetics::Reaction::net_change`
    #[pyo3(name = "net_change")]
    #[pyo3(signature = (species))]
    fn net_change<'py>(&self, py: Python<'py>, species: usize) -> PyResult<Vec<i64>> {
        let _ = py;
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.net_change(species)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "reactants")]
    fn py_get_reactants(&self) -> PyResult<Vec<(usize, u32)>> { Ok(self.inner.reactants.clone().into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>()) }

    #[getter]
    #[pyo3(name = "products")]
    fn py_get_products(&self) -> PyResult<Vec<(usize, u32)>> { Ok(self.inner.products.clone().into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>()) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Reaction", "Reaction", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// One record from a trajectory.
///
/// Rust: `statistical_mechanics::md::MdSample`
#[pyclass(name = "MdSample", module = "rust_physics_engine.statistical_mechanics.md", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyMdSample { pub inner: rust_physics_engine::statistical_mechanics::md::MdSample }
#[pymethods]
impl PyMdSample {
    /// Builds a `MdSample` from its fields.
    #[new]
    #[pyo3(signature = (time, kinetic, potential, total, temperature, pressure))]
    fn __new__(time: f64, kinetic: f64, potential: f64, total: f64, temperature: f64, pressure: f64) -> Self {

        Self { inner: rust_physics_engine::statistical_mechanics::md::MdSample { time: time, kinetic: kinetic, potential: potential, total: total, temperature: temperature, pressure: pressure } }
    }

    #[getter]
    #[pyo3(name = "time")]
    fn py_get_time(&self) -> PyResult<f64> { Ok(self.inner.time) }

    #[setter]
    #[pyo3(name = "time")]
    fn py_set_time(&mut self, v: f64) { self.inner.time = v; }

    #[getter]
    #[pyo3(name = "kinetic")]
    fn py_get_kinetic(&self) -> PyResult<f64> { Ok(self.inner.kinetic) }

    #[setter]
    #[pyo3(name = "kinetic")]
    fn py_set_kinetic(&mut self, v: f64) { self.inner.kinetic = v; }

    #[getter]
    #[pyo3(name = "potential")]
    fn py_get_potential(&self) -> PyResult<f64> { Ok(self.inner.potential) }

    #[setter]
    #[pyo3(name = "potential")]
    fn py_set_potential(&mut self, v: f64) { self.inner.potential = v; }

    #[getter]
    #[pyo3(name = "total")]
    fn py_get_total(&self) -> PyResult<f64> { Ok(self.inner.total) }

    #[setter]
    #[pyo3(name = "total")]
    fn py_set_total(&mut self, v: f64) { self.inner.total = v; }

    #[getter]
    #[pyo3(name = "temperature")]
    fn py_get_temperature(&self) -> PyResult<f64> { Ok(self.inner.temperature) }

    #[setter]
    #[pyo3(name = "temperature")]
    fn py_set_temperature(&mut self, v: f64) { self.inner.temperature = v; }

    #[getter]
    #[pyo3(name = "pressure")]
    fn py_get_pressure(&self) -> PyResult<f64> { Ok(self.inner.pressure) }

    #[setter]
    #[pyo3(name = "pressure")]
    fn py_set_pressure(&mut self, v: f64) { self.inner.pressure = v; }

    fn __repr__(&self) -> String { format!("MdSample(time={:?}, kinetic={:?}, potential={:?}, total={:?}, temperature={:?}, pressure={:?})", self.inner.time, self.inner.kinetic, self.inner.potential, self.inner.total, self.inner.temperature, self.inner.pressure) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A `MdSample` argument, or anything that can stand in for one.
pub struct PyMdSampleArg(pub rust_physics_engine::statistical_mechanics::md::MdSample);

impl<'a, 'py> pyo3::FromPyObject<'a, 'py> for PyMdSampleArg {
    type Error = pyo3::PyErr;

    fn extract(obj: pyo3::Borrowed<'a, 'py, pyo3::PyAny>) -> Result<Self, pyo3::PyErr> {
        if let Ok(__w) = obj.extract::<PyMdSample>() {
            return Ok(PyMdSampleArg(__w.inner));
        }
        let __v = crate::runtime::coerce::floats_exact(obj, 6, "MdSample")?;
        Ok(PyMdSampleArg(rust_physics_engine::statistical_mechanics::md::MdSample { time: __v[0], kinetic: __v[1], potential: __v[2], total: __v[3], temperature: __v[4], pressure: __v[5] }))
    }
}


/// A box of particles interacting through one pair potential.
///
/// Rust: `statistical_mechanics::md::MdSystem`
#[pyclass(name = "MdSystem", module = "rust_physics_engine.statistical_mechanics.md", from_py_object)]
#[derive(Clone)]
pub struct PyMdSystem { pub inner: rust_physics_engine::statistical_mechanics::md::MdSystem }
#[pymethods]
impl PyMdSystem {
    /// A system from explicit state.
    ///
    /// Errors:
    /// Returns an error for mismatched lengths, a non-positive mass, box or
    /// cutoff, or a cutoff more than half the shortest box edge, which the
    /// minimum-image convention cannot represent.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::new`
    #[pyo3(name = "new")]
    #[staticmethod]
    #[pyo3(signature = (pos, vel, mass, box_size, periodic, potential, cutoff))]
    fn new(pos: Vec<crate::generated::types::PyVec3Arg>, vel: Vec<crate::generated::types::PyVec3Arg>, mass: Vec<f64>, box_size: crate::generated::types::PyVec3Arg, periodic: bool, potential: crate::generated::types::PyPotential, cutoff: f64) -> PyResult<crate::generated::types::PyMdSystem> {
        let pos = pos.into_iter().map(|__e| __e.0).collect::<Vec<_>>();
        let vel = vel.into_iter().map(|__e| __e.0).collect::<Vec<_>>();
        let box_size = box_size.0;
        let potential = potential.inner;
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::md::MdSystem::new(pos, vel, mass, box_size, periodic, potential, cutoff));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyMdSystem { inner: __v })
    }

    /// A face-centred cubic lattice of `cells^3` unit cells at a given
    /// number density, with velocities drawn at the requested temperature.
    ///
    /// FCC rather than simple cubic because it is the Lennard-Jones ground
    /// state: starting from a simple cubic lattice at liquid density puts
    /// the system on a mechanically unstable configuration, and it melts
    /// into a shock rather than into equilibrium.
    ///
    /// Errors:
    /// Returns an error for no cells, a non-positive density or a negative
    /// temperature.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::lattice_fcc`
    #[pyo3(name = "lattice_fcc")]
    #[staticmethod]
    #[pyo3(signature = (cells, density, temperature, eps, sigma, rng))]
    fn lattice_fcc(cells: usize, density: f64, temperature: f64, eps: f64, sigma: f64, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<crate::generated::types::PyMdSystem> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::statistical_mechanics::md::MdSystem::lattice_fcc(cells, density, temperature, eps, sigma, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyMdSystem { inner: __v })
    }

    /// The number of particles.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::len`
    #[pyo3(name = "len")]
    #[pyo3(signature = ())]
    fn len(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.len());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Whether the box is empty. Never true for a constructed system.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::is_empty`
    #[pyo3(name = "is_empty")]
    #[pyo3(signature = ())]
    fn is_empty(&self) -> PyResult<bool> {
        let __r = crate::runtime::guard(|| self.inner.is_empty());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The box volume.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::volume`
    #[pyo3(name = "volume")]
    #[pyo3(signature = ())]
    fn volume(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.volume());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Wraps a position into the primary box.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::wrap`
    #[pyo3(name = "wrap")]
    #[pyo3(signature = (p))]
    fn wrap(&self, p: crate::generated::types::PyVec3Arg) -> PyResult<crate::generated::types::PyVec3> {
        let p = p.0;
        let __r = crate::runtime::guard(|| self.inner.wrap(p));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyVec3 { inner: __v })
    }

    /// The shortest displacement between two points under the periodic
    /// boundary: the *minimum image*.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::minimum_image`
    #[pyo3(name = "minimum_image")]
    #[pyo3(signature = (d))]
    fn minimum_image(&self, d: crate::generated::types::PyVec3Arg) -> PyResult<crate::generated::types::PyVec3> {
        let d = d.0;
        let __r = crate::runtime::guard(|| self.inner.minimum_image(d));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyVec3 { inner: __v })
    }

    /// The velocity-of-the-centre-of-mass subtracted from every particle.
    ///
    /// The total momentum is a constant of the motion, so a non-zero value
    /// never decays: it sits in the kinetic energy for the whole run and
    /// inflates every temperature reading by a fixed amount.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::remove_drift`
    #[pyo3(name = "remove_drift")]
    #[pyo3(signature = ())]
    fn remove_drift(&mut self) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.remove_drift());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// Scales every velocity so the instantaneous temperature is `target`.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::rescale_to_temperature`
    #[pyo3(name = "rescale_to_temperature")]
    #[pyo3(signature = (target))]
    fn rescale_to_temperature(&mut self, target: f64) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.rescale_to_temperature(target));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// The force on every particle, computed afresh.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::forces`
    #[pyo3(name = "forces")]
    #[pyo3(signature = ())]
    fn forces(&self) -> PyResult<Vec<crate::generated::types::PyVec3>> {
        let __r = crate::runtime::guard(|| self.inner.forces());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v.into_iter().map(|__x| crate::generated::types::PyVec3 { inner: __x }).collect::<Vec<_>>())
    }

    /// Recomputes the cached forces the integrator steps with.
    ///
    /// `MdSystem::step_velocity_verlet` reuses the force it computed at the
    /// end of the previous step, which is what makes velocity Verlet one
    /// force evaluation per step rather than two. Every method here that
    /// changes a position keeps that cache current, but `pos`, `box_size`,
    /// `charge`, `potential` and `cutoff` are public: **after writing to any
    /// of them directly, call this before stepping.** Otherwise the next
    /// step integrates the previous configuration's forces, and the symptom
    /// is subtle rather than loud -- energy that almost conserves, and a
    /// trajectory that is no longer reversible.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::refresh_forces`
    #[pyo3(name = "refresh_forces")]
    #[pyo3(signature = ())]
    fn refresh_forces(&mut self) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.refresh_forces());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// The total potential energy.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::potential_energy`
    #[pyo3(name = "potential_energy")]
    #[pyo3(signature = ())]
    fn potential_energy(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.potential_energy());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The total kinetic energy.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::kinetic_energy`
    #[pyo3(name = "kinetic_energy")]
    #[pyo3(signature = ())]
    fn kinetic_energy(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.kinetic_energy());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The momentum of the whole box.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::total_momentum`
    #[pyo3(name = "total_momentum")]
    #[pyo3(signature = ())]
    fn total_momentum(&self) -> PyResult<crate::generated::types::PyVec3> {
        let __r = crate::runtime::guard(|| self.inner.total_momentum());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyVec3 { inner: __v })
    }

    /// The count of translational degrees of freedom.
    ///
    /// Three fewer than `3 N` on a periodic box, because the total momentum
    /// is conserved and carries no thermal energy. Dividing by `3 N`
    /// instead reports a temperature low by a factor `1 - 1/N`, which is
    /// invisible at ten thousand particles and a two per cent error at a
    /// hundred.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::degrees_of_freedom`
    #[pyo3(name = "degrees_of_freedom")]
    #[pyo3(signature = ())]
    fn degrees_of_freedom(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.degrees_of_freedom());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The instantaneous temperature from equipartition.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::temperature`
    #[pyo3(name = "temperature")]
    #[pyo3(signature = ())]
    fn temperature(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.temperature());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The pressure from the virial theorem.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::pressure_virial`
    #[pyo3(name = "pressure_virial")]
    #[pyo3(signature = ())]
    fn pressure_virial(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.pressure_virial());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// A snapshot of the thermodynamic state.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::sample`
    #[pyo3(name = "sample")]
    #[pyo3(signature = ())]
    fn sample(&self) -> PyResult<crate::generated::types::PyMdSample> {
        let __r = crate::runtime::guard(|| self.inner.sample());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMdSample { inner: __v })
    }

    /// One velocity-Verlet step.
    ///
    /// Symplectic, so the energy error stays bounded rather than
    /// accumulating: the integrator conserves a shadow Hamiltonian close to
    /// the true one, and the true energy oscillates around its initial value
    /// forever instead of drifting away from it. That is the whole reason to
    /// prefer it over a higher-order but non-symplectic scheme here, and
    /// `energy_drift` is written to measure the distinction.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::step_velocity_verlet`
    #[pyo3(name = "step_velocity_verlet")]
    #[pyo3(signature = (dt))]
    fn step_velocity_verlet(&mut self, dt: f64) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.step_velocity_verlet(dt));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// Berendsen velocity rescaling toward `t_target`.
    ///
    /// It reaches the right mean temperature and samples the wrong
    /// ensemble: the fluctuations are suppressed, so a heat capacity taken
    /// from a Berendsen run is too small. Use it to equilibrate and switch
    /// to Nose-Hoover or Langevin before measuring anything that depends on
    /// a fluctuation.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::thermostat_berendsen`
    #[pyo3(name = "thermostat_berendsen")]
    #[pyo3(signature = (t_target, tau, dt))]
    fn thermostat_berendsen(&mut self, t_target: f64, tau: f64, dt: f64) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.thermostat_berendsen(t_target, tau, dt));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// One Nose-Hoover step on the friction coordinate and the velocities.
    ///
    /// Unlike Berendsen this is derived from an extended Hamiltonian, so it
    /// samples the canonical ensemble including the fluctuations -- the
    /// friction is a dynamical variable with its own inertia `q`, and it
    /// oscillates rather than clamping.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::thermostat_nose_hoover`
    #[pyo3(name = "thermostat_nose_hoover")]
    #[pyo3(signature = (t_target, q, dt))]
    fn thermostat_nose_hoover(&mut self, t_target: f64, q: f64, dt: f64) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.thermostat_nose_hoover(t_target, q, dt));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// One Langevin step: friction plus the matching noise.
    ///
    /// The two are not independent. The fluctuation-dissipation theorem
    /// fixes the noise amplitude from the friction and the target
    /// temperature, and any other amplitude thermostats to a different
    /// temperature than the one requested.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::thermostat_langevin`
    #[pyo3(name = "thermostat_langevin")]
    #[pyo3(signature = (t_target, gamma, dt, rng))]
    fn thermostat_langevin(&mut self, t_target: f64, gamma: f64, dt: f64, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<()> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.thermostat_langevin(t_target, gamma, dt, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// Berendsen barostat: the box and every position scaled toward
    /// `p_target`.
    ///
    /// Errors:
    /// Returns an error for a non-positive time constant or compressibility,
    /// or if the rescaling would shrink the box below twice the cutoff.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::barostat_berendsen`
    #[pyo3(name = "barostat_berendsen")]
    #[pyo3(signature = (p_target, compressibility, tau, dt))]
    fn barostat_berendsen(&mut self, p_target: f64, compressibility: f64, tau: f64, dt: f64) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.barostat_berendsen(p_target, compressibility, tau, dt));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(())
    }

    /// Thermalises the system with a Langevin thermostat and returns the
    /// drift-free result.
    ///
    /// Errors:
    /// Returns an error for a non-positive step or a negative temperature.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::equilibrate`
    #[pyo3(name = "equilibrate")]
    #[pyo3(signature = (steps, dt, t_target, rng))]
    fn equilibrate(&mut self, steps: usize, dt: f64, t_target: f64, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<()> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.equilibrate(steps, dt, t_target, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(())
    }

    /// A constant-energy run, sampled every step.
    ///
    /// Errors:
    /// Returns an error for a non-positive step or no steps.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::run_nve`
    #[pyo3(name = "run_nve")]
    #[pyo3(signature = (steps, dt))]
    fn run_nve(&mut self, steps: usize, dt: f64) -> PyResult<Vec<crate::generated::types::PyMdSample>> {
        let __r = crate::runtime::guard(|| self.inner.run_nve(steps, dt));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v.into_iter().map(|__x| crate::generated::types::PyMdSample { inner: __x }).collect::<Vec<_>>())
    }

    /// A constant-energy run recording positions and velocities every
    /// `stride` steps, for the transport measurements.
    ///
    /// Errors:
    /// Returns an error for a non-positive step, no steps, or a zero stride.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::run_trajectory`
    #[pyo3(name = "run_trajectory")]
    #[pyo3(signature = (steps, dt, stride))]
    fn run_trajectory(&mut self, steps: usize, dt: f64, stride: usize) -> PyResult<(Vec<Vec<crate::generated::types::PyVec3>>, Vec<Vec<crate::generated::types::PyVec3>>)> {
        let __r = crate::runtime::guard(|| self.inner.run_trajectory(steps, dt, stride));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok((__v.0.into_iter().map(|__x| __x.into_iter().map(|__x| crate::generated::types::PyVec3 { inner: __x }).collect::<Vec<_>>()).collect::<Vec<_>>(), __v.1.into_iter().map(|__x| __x.into_iter().map(|__x| crate::generated::types::PyVec3 { inner: __x }).collect::<Vec<_>>()).collect::<Vec<_>>()))
    }

    /// The radial distribution function `g(r)` in `bins` shells out to
    /// `r_max`.
    ///
    /// Normalised by the *ideal gas* count in each shell, so `g(r) = 1`
    /// means "no correlation at this separation" rather than "no
    /// neighbours". A histogram normalised by the shell volume alone rises
    /// as `r^2` and says nothing.
    ///
    /// Errors:
    /// Returns an error for no bins, a non-positive range, or a range
    /// exceeding half the shortest box edge on a periodic box.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::rdf`
    #[pyo3(name = "rdf")]
    #[pyo3(signature = (bins, r_max))]
    fn rdf<'py>(&self, py: Python<'py>, bins: usize, r_max: f64) -> PyResult<Vec<f64>> {
        let _ = py;
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.rdf(bins, r_max)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The static structure factor at each scalar wavenumber, by the Debye
    /// formula `S(k) = 1 + (2/N) sum_{i<j} sin(k r) / (k r)`.
    ///
    /// Exact for the sample in hand, and it tends to one at large `k`
    /// whatever the configuration -- which is the check that the
    /// normalisation is right.
    ///
    /// Below `2 pi / L` it is not a structural measurement at all. Every
    /// `sin(k r) / (k r)` tends to one as `k` falls, so the sum tends to
    /// the particle count: what that peak measures is the extent of the
    /// sample, not any correlation inside it. Read this only at
    /// wavenumbers above the smallest reciprocal box vector.
    ///
    /// Errors:
    /// Returns an error for a non-positive wavenumber.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::structure_factor`
    #[pyo3(name = "structure_factor")]
    #[pyo3(signature = (k_values))]
    fn structure_factor<'py>(&self, py: Python<'py>, k_values: Vec<f64>) -> PyResult<Vec<f64>> {
        let _ = py;
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.structure_factor(&k_values)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// A Kolmogorov-Smirnov test of the speeds against the Maxwell-Boltzmann
    /// distribution at the system's own temperature.
    ///
    /// The check is worth making because equipartition alone does not pin
    /// the distribution: a system with every particle at the same speed has
    /// exactly the right temperature and entirely the wrong statistics, and
    /// that is precisely the state a freshly rescaled lattice is in.
    ///
    /// Errors:
    /// Returns an error for a zero temperature or fewer than five
    /// particles, and for masses that are not all equal -- the speeds then
    /// come from a mixture of distributions and a single-sample test does
    /// not apply.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::maxwell_boltzmann_check`
    #[pyo3(name = "maxwell_boltzmann_check")]
    #[pyo3(signature = ())]
    fn maxwell_boltzmann_check(&self) -> PyResult<crate::generated::types::PyTestResult> {
        let __r = crate::runtime::guard(|| self.inner.maxwell_boltzmann_check());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyTestResult { inner: __v })
    }

    /// The Lindemann ratio: the root-mean-square displacement of each
    /// particle from its own mean position, divided by the nearest-neighbour
    /// distance.
    ///
    /// Above about 0.15 a crystal has melted. The ratio is taken about each
    /// particle's *own* time-averaged site rather than about a lattice, so
    /// it does not need to know the crystal structure -- but for the same
    /// reason it only means something for a trajectory long enough for that
    /// average to settle.
    ///
    /// Errors:
    /// Returns an error for fewer than two frames or a frame of the wrong
    /// length.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::melting_indicator_lindemann`
    #[pyo3(name = "melting_indicator_lindemann")]
    #[pyo3(signature = (traj))]
    fn melting_indicator_lindemann<'py>(&self, py: Python<'py>, traj: Vec<Vec<crate::generated::types::PyVec3Arg>>) -> PyResult<f64> {
        let _ = py;
        let traj = traj.into_iter().map(|__e| __e.into_iter().map(|__e| __e.0).collect::<Vec<_>>()).collect::<Vec<_>>();
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.melting_indicator_lindemann(&traj)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The mean squared displacement against lag, averaged over particles
    /// and over every time origin.
    ///
    /// The trajectory must be *unwrapped*: a position folded back into the
    /// box turns a steady drift into a sawtooth, and the resulting MSD
    /// saturates at the box size and reports no diffusion at all. Use the
    /// positions from `MdSystem::run_trajectory`, which are unwrapped for
    /// this reason.
    ///
    /// Errors:
    /// Returns an error for fewer than two frames or ragged frames.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::msd`
    #[pyo3(name = "msd")]
    #[staticmethod]
    #[pyo3(signature = (traj))]
    fn msd<'py>(py: Python<'py>, traj: Vec<Vec<crate::generated::types::PyVec3Arg>>) -> PyResult<Vec<f64>> {
        let _ = py;
        let traj = traj.into_iter().map(|__e| __e.into_iter().map(|__e| __e.0).collect::<Vec<_>>()).collect::<Vec<_>>();
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::statistical_mechanics::md::MdSystem::msd(&traj)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The diffusion coefficient from the Einstein relation `<r^2> = 6 D t`.
    ///
    /// Fitted over the middle half of the record. The two ends are excluded
    /// deliberately: the short-lag part is ballistic rather than diffusive,
    /// and the long-lag part is averaged over so few time origins that it is
    /// mostly noise. Fitting the whole curve mixes both in.
    ///
    /// Errors:
    /// Returns an error for fewer than eight lags or a non-positive step.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::diffusion_coefficient`
    #[pyo3(name = "diffusion_coefficient")]
    #[staticmethod]
    #[pyo3(signature = (msd, dt))]
    fn diffusion_coefficient<'py>(py: Python<'py>, msd: Vec<f64>, dt: f64) -> PyResult<f64> {
        let _ = py;
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::statistical_mechanics::md::MdSystem::diffusion_coefficient(&msd, dt)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The normalised velocity autocorrelation function.
    ///
    /// Errors:
    /// Returns an error for fewer than two frames, ragged frames, or a
    /// trajectory with no motion in it.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::vacf`
    #[pyo3(name = "vacf")]
    #[staticmethod]
    #[pyo3(signature = (traj_vel))]
    fn vacf<'py>(py: Python<'py>, traj_vel: Vec<Vec<crate::generated::types::PyVec3Arg>>) -> PyResult<Vec<f64>> {
        let _ = py;
        let traj_vel = traj_vel.into_iter().map(|__e| __e.into_iter().map(|__e| __e.0).collect::<Vec<_>>()).collect::<Vec<_>>();
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::statistical_mechanics::md::MdSystem::vacf(&traj_vel)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The vibrational density of states: the cosine transform of the
    /// velocity autocorrelation.
    ///
    /// Returned on the frequency grid `omega_k = pi k / (N dt)`, so the
    /// caller can label the axis without guessing.
    ///
    /// Errors:
    /// Returns an error for fewer than two points or a non-positive step.
    ///
    /// Rust: `statistical_mechanics::md::MdSystem::vdos_from_vacf`
    #[pyo3(name = "vdos_from_vacf")]
    #[staticmethod]
    #[pyo3(signature = (vacf, dt))]
    fn vdos_from_vacf<'py>(py: Python<'py>, vacf: Vec<f64>, dt: f64) -> PyResult<Vec<f64>> {
        let _ = py;
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::statistical_mechanics::md::MdSystem::vdos_from_vacf(&vacf, dt)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "pos")]
    fn py_get_pos(&self) -> PyResult<Vec<crate::generated::types::PyVec3>> { Ok(self.inner.pos.clone().into_iter().map(|__x| crate::generated::types::PyVec3 { inner: __x }).collect::<Vec<_>>()) }

    #[getter]
    #[pyo3(name = "unwrapped")]
    fn py_get_unwrapped(&self) -> PyResult<Vec<crate::generated::types::PyVec3>> { Ok(self.inner.unwrapped.clone().into_iter().map(|__x| crate::generated::types::PyVec3 { inner: __x }).collect::<Vec<_>>()) }

    #[getter]
    #[pyo3(name = "vel")]
    fn py_get_vel(&self) -> PyResult<Vec<crate::generated::types::PyVec3>> { Ok(self.inner.vel.clone().into_iter().map(|__x| crate::generated::types::PyVec3 { inner: __x }).collect::<Vec<_>>()) }

    #[getter]
    #[pyo3(name = "mass")]
    fn py_get_mass(&self) -> PyResult<Vec<f64>> { Ok(self.inner.mass.clone()) }

    #[getter]
    #[pyo3(name = "charge")]
    fn py_get_charge(&self) -> PyResult<Vec<f64>> { Ok(self.inner.charge.clone()) }

    #[getter]
    #[pyo3(name = "box_size")]
    fn py_get_box_size(&self) -> PyResult<crate::generated::types::PyVec3> { Ok(crate::generated::types::PyVec3 { inner: self.inner.box_size.clone() }) }

    #[getter]
    #[pyo3(name = "periodic")]
    fn py_get_periodic(&self) -> PyResult<bool> { Ok(self.inner.periodic) }

    #[setter]
    #[pyo3(name = "periodic")]
    fn py_set_periodic(&mut self, v: bool) { self.inner.periodic = v; }

    #[getter]
    #[pyo3(name = "potential")]
    fn py_get_potential(&self) -> PyResult<crate::generated::types::PyPotential> { Ok(crate::generated::types::PyPotential { inner: self.inner.potential.clone() }) }

    #[getter]
    #[pyo3(name = "cutoff")]
    fn py_get_cutoff(&self) -> PyResult<f64> { Ok(self.inner.cutoff) }

    #[setter]
    #[pyo3(name = "cutoff")]
    fn py_set_cutoff(&mut self, v: f64) { self.inner.cutoff = v; }

    #[getter]
    #[pyo3(name = "time")]
    fn py_get_time(&self) -> PyResult<f64> { Ok(self.inner.time) }

    #[setter]
    #[pyo3(name = "time")]
    fn py_set_time(&mut self, v: f64) { self.inner.time = v; }

    #[getter]
    #[pyo3(name = "nose_hoover_zeta")]
    fn py_get_nose_hoover_zeta(&self) -> PyResult<f64> { Ok(self.inner.nose_hoover_zeta) }

    #[setter]
    #[pyo3(name = "nose_hoover_zeta")]
    fn py_set_nose_hoover_zeta(&mut self, v: f64) { self.inner.nose_hoover_zeta = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("MdSystem", "MdSystem", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A pair potential, as a function of the separation alone.
///
/// Each variant supplies both the energy and the force so that they cannot
/// drift apart: a force that is not the negative gradient of the energy in
/// use will conserve nothing, and the failure looks exactly like an
/// integrator bug.
///
/// Rust: `statistical_mechanics::md::Potential`
#[pyclass(name = "Potential", module = "rust_physics_engine.statistical_mechanics.md", from_py_object)]
#[derive(Clone)]
pub struct PyPotential { pub inner: rust_physics_engine::statistical_mechanics::md::Potential }
#[pymethods]
impl PyPotential {
    /// The energy and the radial force `-du/dr` at separation `r` between
    /// charges `qi` and `qj`.
    ///
    /// The force is returned rather than derived numerically so that the two
    /// are guaranteed consistent; the tests check each variant's force
    /// against a finite difference of its own energy.
    ///
    /// Rust: `statistical_mechanics::md::Potential::evaluate`
    #[pyo3(name = "evaluate")]
    #[pyo3(signature = (r, qi, qj))]
    fn evaluate(&self, r: f64, qi: f64, qj: f64) -> PyResult<(f64, f64)> {
        let __r = crate::runtime::guard(|| self.inner.evaluate(r, qi, qj));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    /// Whether the potential carries a charge term, and so cannot be
    /// truncated at a cutoff without an Ewald correction.
    ///
    /// Rust: `statistical_mechanics::md::Potential::is_charged`
    #[pyo3(name = "is_charged")]
    #[pyo3(signature = ())]
    fn is_charged(&self) -> PyResult<bool> {
        let __r = crate::runtime::guard(|| self.inner.is_charged());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    fn __repr__(&self) -> String { "<Potential>".to_string() }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}
