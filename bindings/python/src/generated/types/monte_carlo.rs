// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;


/// Linear congruential pseudo-random number generator.
///
/// Rust: `monte_carlo::Rng`
#[pyclass(name = "Rng", module = "numeria.monte_carlo")]
pub struct PyRng { pub inner: rust_physics_engine::monte_carlo::Rng }
#[pymethods]
impl PyRng {
    /// Creates a new RNG seeded with the given value.
    ///
    /// Rust: `monte_carlo::Rng::new`
    #[new]
    #[pyo3(signature = (seed))]
    fn __new__(seed: u64) -> PyResult<crate::generated::types::PyRng> {
        let __r = crate::runtime::guard(|| rust_physics_engine::monte_carlo::Rng::new(seed));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyRng { inner: __v })
    }

    /// Advances the LCG state and returns the next pseudo-random u64.
    ///
    /// The low bits are not random:
    ///
    /// This is a plain linear congruential generator returning its raw
    /// state, and for such a generator bit `k` has period at most
    /// `2^(k+1)`. The bottom bit therefore alternates, the bottom two
    /// cycle with period four, and so on. Taking `next_u64() % m` for a
    /// **power of two** `m` reads exactly those bits and produces a
    /// fixed repeating cycle -- `% 2` gives `0, 1, 0, 1, ...` and `% 4`
    /// gives `0, 3, 2, 1, ...` for ever. Two such sequences drawn one
    /// after another are perfectly correlated, which is not a subtle
    /// statistical defect but a complete absence of randomness.
    ///
    /// A modulus with an odd factor mixes in higher bits and is fine.
    /// Rather than remember which is which, use `Rng::below`, which
    /// takes its answer from the top of the word.
    ///
    /// Rust: `monte_carlo::Rng::next_u64`
    #[pyo3(name = "next_u64")]
    #[pyo3(signature = ())]
    fn next_u64(&mut self) -> PyResult<u64> {
        let __r = crate::runtime::guard(|| self.inner.next_u64());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Returns a uniform random f64 in [0, 1) by extracting the top 53 mantissa bits.
    ///
    /// Rust: `monte_carlo::Rng::next_f64`
    #[pyo3(name = "next_f64")]
    #[pyo3(signature = ())]
    fn next_f64(&mut self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.next_f64());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// A uniform integer in `0..n`, taken from the high bits.
    ///
    /// Use this rather than `next_u64() % n` whenever `n` might be a
    /// power of two -- see the note on `Rng::next_u64` for why that
    /// combination returns a short repeating cycle instead of a random
    /// value. Returns zero for `n == 0`, there being no such range.
    ///
    /// Exact for `n` up to `2^53`, which is where the mantissa the
    /// scaling goes through runs out.
    ///
    /// Rust: `monte_carlo::Rng::below`
    #[pyo3(name = "below")]
    #[pyo3(signature = (n))]
    fn below(&mut self, n: u64) -> PyResult<u64> {
        let __r = crate::runtime::guard(|| self.inner.below(n));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Returns a standard normal variate via the Box-Muller transform.
    ///
    /// Rust: `monte_carlo::Rng::next_gaussian`
    #[pyo3(name = "next_gaussian")]
    #[pyo3(signature = ())]
    fn next_gaussian(&mut self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.next_gaussian());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    fn __repr__(&self) -> String { "<Rng>".to_string() }
}

/// Halton low-discrepancy sequence: dimension d uses the radical
/// inverse in the d-th prime.
///
/// Rust: `monte_carlo::quasi::Halton`
#[pyclass(name = "Halton", module = "numeria.monte_carlo.quasi")]
pub struct PyHalton { pub inner: rust_physics_engine::monte_carlo::quasi::Halton }
#[pymethods]
impl PyHalton {
    /// Panics:
    /// Panics unless 1 ≤ dim ≤ 25.
    ///
    /// Rust: `monte_carlo::quasi::Halton::new`
    #[new]
    #[pyo3(signature = (dim))]
    fn __new__(dim: usize) -> PyResult<crate::generated::types::PyHalton> {
        let __r = crate::runtime::guard(|| rust_physics_engine::monte_carlo::quasi::Halton::new(dim));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyHalton { inner: __v })
    }

    /// The next point in the sequence (index starts at 1).
    ///
    /// Inherent rather than `Iterator` for the same reason as the Sobol
    /// sequence above.
    ///
    /// Rust: `monte_carlo::quasi::Halton::next`
    #[pyo3(name = "next")]
    #[pyo3(signature = ())]
    fn next<'py>(&mut self, py: Python<'py>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.next()));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    fn __repr__(&self) -> String { "<Halton>".to_string() }
}

/// Sobol low-discrepancy sequence in `dim` dimensions (dim ≤ 21).
///
/// Successive calls to `Sobol::next` return points x₁, x₂, … in
/// [0, 1)^dim (the origin point x₀ = 0 is skipped).
///
/// Rust: `monte_carlo::quasi::Sobol`
#[pyclass(name = "Sobol", module = "numeria.monte_carlo.quasi")]
pub struct PySobol { pub inner: rust_physics_engine::monte_carlo::quasi::Sobol }
#[pymethods]
impl PySobol {
    /// Panics:
    /// Panics unless 1 ≤ dim ≤ 21.
    ///
    /// Rust: `monte_carlo::quasi::Sobol::new`
    #[new]
    #[pyo3(signature = (dim))]
    fn __new__(dim: usize) -> PyResult<crate::generated::types::PySobol> {
        let __r = crate::runtime::guard(|| rust_physics_engine::monte_carlo::quasi::Sobol::new(dim));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PySobol { inner: __v })
    }

    /// The next point in the sequence.
    ///
    /// Named `next` to match the sequence vocabulary; it is deliberately an
    /// inherent method rather than `Iterator`, which cannot borrow `self`
    /// mutably and yield a fresh `Vec` without allocating an adapter.
    ///
    /// Rust: `monte_carlo::quasi::Sobol::next`
    #[pyo3(name = "next")]
    #[pyo3(signature = ())]
    fn next<'py>(&mut self, py: Python<'py>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.next()));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Skips the next n points.
    ///
    /// Rust: `monte_carlo::quasi::Sobol::skip`
    #[pyo3(name = "skip")]
    #[pyo3(signature = (n))]
    fn skip(&mut self, n: u64) -> PyResult<()> {
        let __r = crate::runtime::guard(|| self.inner.skip(n));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// Dimensionality of the sequence.
    ///
    /// Rust: `monte_carlo::quasi::Sobol::dim`
    #[pyo3(name = "dim")]
    #[pyo3(signature = ())]
    fn dim(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.dim());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    fn __repr__(&self) -> String { "<Sobol>".to_string() }
}
