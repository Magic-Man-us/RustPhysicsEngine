// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;


/// The Archimedean and elliptical families supported here.
///
/// Rust: `stochastic::extreme::CopulaFamily`
#[pyclass(name = "CopulaFamily", module = "numeria.stochastic.extreme", from_py_object, eq, eq_int)]
#[derive(Clone, Copy, PartialEq)]
pub enum PyCopulaFamily {
    Gaussian,
    Clayton,
    Gumbel,
    Frank,
}
impl PyCopulaFamily {
    pub fn to_rust(&self) -> rust_physics_engine::stochastic::extreme::CopulaFamily { match self {
        Self::Gaussian => rust_physics_engine::stochastic::extreme::CopulaFamily::Gaussian,
        Self::Clayton => rust_physics_engine::stochastic::extreme::CopulaFamily::Clayton,
        Self::Gumbel => rust_physics_engine::stochastic::extreme::CopulaFamily::Gumbel,
        Self::Frank => rust_physics_engine::stochastic::extreme::CopulaFamily::Frank,
    } }
    pub fn from_rust(v: &rust_physics_engine::stochastic::extreme::CopulaFamily) -> Self { match v {
        rust_physics_engine::stochastic::extreme::CopulaFamily::Gaussian => Self::Gaussian,
        rust_physics_engine::stochastic::extreme::CopulaFamily::Clayton => Self::Clayton,
        rust_physics_engine::stochastic::extreme::CopulaFamily::Gumbel => Self::Gumbel,
        rust_physics_engine::stochastic::extreme::CopulaFamily::Frank => Self::Frank,
    } }
}
#[pymethods]
impl PyCopulaFamily {
    fn __repr__(&self) -> &'static str {
        match self {
            Self::Gaussian => "CopulaFamily.Gaussian",
            Self::Clayton => "CopulaFamily.Clayton",
            Self::Gumbel => "CopulaFamily.Gumbel",
            Self::Frank => "CopulaFamily.Frank",
        }
    }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// One step of a Kalman filter's output: the state estimate and its
/// covariance, before and after the measurement.
///
/// Rust: `stochastic::hmm::FilterStep`
#[pyclass(name = "FilterStep", module = "numeria.stochastic.hmm", from_py_object)]
#[derive(Clone)]
pub struct PyFilterStep { pub inner: rust_physics_engine::stochastic::hmm::FilterStep }
#[pymethods]
impl PyFilterStep {
    /// Builds a `FilterStep` from its fields.
    #[new]
    #[pyo3(signature = (predicted, predicted_cov, filtered, filtered_cov))]
    fn __new__(predicted: Vec<f64>, predicted_cov: crate::generated::types::PyMatrixArg, filtered: Vec<f64>, filtered_cov: crate::generated::types::PyMatrixArg) -> Self {
        let predicted_cov = predicted_cov.0;
        let filtered_cov = filtered_cov.0;
        Self { inner: rust_physics_engine::stochastic::hmm::FilterStep { predicted: predicted, predicted_cov: predicted_cov, filtered: filtered, filtered_cov: filtered_cov } }
    }

    #[getter]
    #[pyo3(name = "predicted")]
    fn py_get_predicted(&self) -> PyResult<Vec<f64>> { Ok(self.inner.predicted.clone()) }

    #[getter]
    #[pyo3(name = "predicted_cov")]
    fn py_get_predicted_cov(&self) -> PyResult<crate::generated::types::PyMatrix> { Ok(crate::generated::types::PyMatrix { inner: self.inner.predicted_cov.clone() }) }

    #[getter]
    #[pyo3(name = "filtered")]
    fn py_get_filtered(&self) -> PyResult<Vec<f64>> { Ok(self.inner.filtered.clone()) }

    #[getter]
    #[pyo3(name = "filtered_cov")]
    fn py_get_filtered_cov(&self) -> PyResult<crate::generated::types::PyMatrix> { Ok(crate::generated::types::PyMatrix { inner: self.inner.filtered_cov.clone() }) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("FilterStep", "FilterStep", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A hidden Markov model whose emissions are one-dimensional Gaussians.
///
/// Rust: `stochastic::hmm::GaussianHmm`
#[pyclass(name = "GaussianHmm", module = "numeria.stochastic.hmm", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyGaussianHmm { pub inner: rust_physics_engine::stochastic::hmm::GaussianHmm }
#[pymethods]
impl PyGaussianHmm {
    /// The model with the given parameters.
    ///
    /// Errors:
    /// Returns an error unless the shapes agree, the variances are positive,
    /// and the rows are distributions.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::new`
    #[new]
    #[pyo3(signature = (a, means, vars, pi))]
    fn __new__(a: crate::generated::types::PyMatrixArg, means: Vec<f64>, vars: Vec<f64>, pi: Vec<f64>) -> PyResult<crate::generated::types::PyGaussianHmm> {
        let a = a.0;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::hmm::GaussianHmm::new(a, means, vars, pi));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyGaussianHmm { inner: __v })
    }

    /// The number of hidden states.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::n_states`
    #[pyo3(name = "n_states")]
    #[pyo3(signature = ())]
    fn n_states(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.n_states());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The emission density of state `i` at `x`.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::emission`
    #[pyo3(name = "emission")]
    #[pyo3(signature = (i, x))]
    fn emission(&self, i: usize, x: f64) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.emission(i, x));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The forward recursion, scaled, with the log-likelihood.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::forward`
    #[pyo3(name = "forward")]
    #[pyo3(signature = (obs))]
    fn forward(&self, obs: Vec<f64>) -> PyResult<(f64, crate::generated::types::PyMatrix)> {
        let __r = crate::runtime::guard(|| self.inner.forward(&obs));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, crate::generated::types::PyMatrix { inner: __v.1 }))
    }

    /// The scaled backward recursion.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::backward`
    #[pyo3(name = "backward")]
    #[pyo3(signature = (obs))]
    fn backward(&self, obs: Vec<f64>) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.backward(&obs));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    /// The most likely state path and its log probability.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::viterbi`
    #[pyo3(name = "viterbi")]
    #[pyo3(signature = (obs))]
    fn viterbi<'py>(&self, py: Python<'py>, obs: Vec<f64>) -> PyResult<(f64, Vec<usize>)> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.viterbi(&obs)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    /// Baum-Welch for Gaussian emissions, returning the final
    /// log-likelihood.
    ///
    /// The maximisation step is the posterior-weighted mean and variance of
    /// the observations, which is the same closed form a Gaussian mixture
    /// uses -- the only difference is where the weights come from.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::baum_welch`
    #[pyo3(name = "baum_welch")]
    #[pyo3(signature = (obs, iters, tol))]
    fn baum_welch<'py>(&mut self, py: Python<'py>, obs: Vec<f64>, iters: usize, tol: f64) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.baum_welch(&obs, iters, tol)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Draws a state path and the observations it produces.
    ///
    /// Rust: `stochastic::hmm::GaussianHmm::simulate`
    #[pyo3(name = "simulate")]
    #[pyo3(signature = (n, rng))]
    fn simulate(&self, n: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<(Vec<usize>, Vec<f64>)> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.simulate(n, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    #[getter]
    #[pyo3(name = "a")]
    fn py_get_a(&self) -> PyResult<crate::generated::types::PyMatrix> { Ok(crate::generated::types::PyMatrix { inner: self.inner.a.clone() }) }

    #[getter]
    #[pyo3(name = "means")]
    fn py_get_means(&self) -> PyResult<Vec<f64>> { Ok(self.inner.means.clone()) }

    #[getter]
    #[pyo3(name = "vars")]
    fn py_get_vars(&self) -> PyResult<Vec<f64>> { Ok(self.inner.vars.clone()) }

    #[getter]
    #[pyo3(name = "pi")]
    fn py_get_pi(&self) -> PyResult<Vec<f64>> { Ok(self.inner.pi.clone()) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("GaussianHmm", "GaussianHmm", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A hidden Markov model with discrete emissions.
///
/// Rust: `stochastic::hmm::Hmm`
#[pyclass(name = "Hmm", module = "numeria.stochastic.hmm", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyHmm { pub inner: rust_physics_engine::stochastic::hmm::Hmm }
#[pymethods]
impl PyHmm {
    /// The model with the given parameters.
    ///
    /// Errors:
    /// Returns an error unless the shapes agree and every row of each matrix,
    /// and the initial distribution, sums to one over non-negative entries.
    ///
    /// Rust: `stochastic::hmm::Hmm::new`
    #[new]
    #[pyo3(signature = (a, b, pi))]
    fn __new__(a: crate::generated::types::PyMatrixArg, b: crate::generated::types::PyMatrixArg, pi: Vec<f64>) -> PyResult<crate::generated::types::PyHmm> {
        let a = a.0;
        let b = b.0;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::hmm::Hmm::new(a, b, pi));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyHmm { inner: __v })
    }

    /// The number of hidden states.
    ///
    /// Rust: `stochastic::hmm::Hmm::n_states`
    #[pyo3(name = "n_states")]
    #[pyo3(signature = ())]
    fn n_states(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.n_states());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The number of observable symbols.
    ///
    /// Rust: `stochastic::hmm::Hmm::n_symbols`
    #[pyo3(name = "n_symbols")]
    #[pyo3(signature = ())]
    fn n_symbols(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.n_symbols());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// A model with random parameters, for Baum-Welch to start from.
    ///
    /// Panics:
    /// Panics if either dimension is zero.
    ///
    /// Rust: `stochastic::hmm::Hmm::random_init`
    #[pyo3(name = "random_init")]
    #[staticmethod]
    #[pyo3(signature = (n_states, n_symbols, rng))]
    fn random_init(n_states: usize, n_symbols: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<crate::generated::types::PyHmm> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::hmm::Hmm::random_init(n_states, n_symbols, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyHmm { inner: __v })
    }

    /// The forward recursion, returning the log-likelihood and the scaled
    /// forward probabilities.
    ///
    /// `alpha[t][i]` is the probability of state `i` at time `t` given the
    /// observations up to `t`, rescaled to sum to one at each step. Scaling
    /// is not an optimisation: without it the raw forward variables shrink by
    /// roughly the observation's probability at every step and underflow a
    /// double within a few hundred symbols. The scale factors, summed as
    /// logs, are exactly the log-likelihood.
    ///
    /// Panics:
    /// Panics if an observation is outside the alphabet.
    ///
    /// Rust: `stochastic::hmm::Hmm::forward`
    #[pyo3(name = "forward")]
    #[pyo3(signature = (obs))]
    fn forward(&self, obs: Vec<usize>) -> PyResult<(f64, crate::generated::types::PyMatrix)> {
        let __r = crate::runtime::guard(|| self.inner.forward(&obs));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, crate::generated::types::PyMatrix { inner: __v.1 }))
    }

    /// The backward recursion, scaled to match `forward`.
    ///
    /// `beta[t][i]` is proportional to the probability of the observations
    /// after `t` given state `i` at `t`, under the same per-step scaling.
    ///
    /// Panics:
    /// Panics if an observation is outside the alphabet.
    ///
    /// Rust: `stochastic::hmm::Hmm::backward`
    #[pyo3(name = "backward")]
    #[pyo3(signature = (obs))]
    fn backward(&self, obs: Vec<usize>) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.backward(&obs));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    /// The log-likelihood of an observation sequence.
    ///
    /// Panics:
    /// Panics if an observation is outside the alphabet.
    ///
    /// Rust: `stochastic::hmm::Hmm::log_likelihood`
    #[pyo3(name = "log_likelihood")]
    #[pyo3(signature = (obs))]
    fn log_likelihood<'py>(&self, py: Python<'py>, obs: Vec<usize>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.log_likelihood(&obs)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The single most likely state path, and its log probability.
    ///
    /// The forward recursion with the sum replaced by a maximum, in logs. It
    /// answers a different question from decoding each state separately: the
    /// best path need not contain any individual state's most likely value,
    /// and unlike posterior decoding its answer is always a path the model
    /// can actually produce.
    ///
    /// Panics:
    /// Panics if an observation is outside the alphabet.
    ///
    /// Rust: `stochastic::hmm::Hmm::viterbi`
    #[pyo3(name = "viterbi")]
    #[pyo3(signature = (obs))]
    fn viterbi<'py>(&self, py: Python<'py>, obs: Vec<usize>) -> PyResult<(f64, Vec<usize>)> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.viterbi(&obs)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    /// The state posteriors: `gamma[t][i]` is the probability of state `i` at
    /// time `t` given the whole sequence.
    ///
    /// Panics:
    /// Panics if an observation is outside the alphabet.
    ///
    /// Rust: `stochastic::hmm::Hmm::posteriors`
    #[pyo3(name = "posteriors")]
    #[pyo3(signature = (obs))]
    fn posteriors(&self, obs: Vec<usize>) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.posteriors(&obs));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    /// The most likely state at each time, taken separately.
    ///
    /// Maximises the expected number of correct states, which is a different
    /// objective from Viterbi's. The path it returns can have probability
    /// zero -- if two consecutive states are each individually likeliest but
    /// the transition between them is impossible, this will happily report
    /// both.
    ///
    /// Panics:
    /// Panics if an observation is outside the alphabet.
    ///
    /// Rust: `stochastic::hmm::Hmm::posterior_decode`
    #[pyo3(name = "posterior_decode")]
    #[pyo3(signature = (obs))]
    fn posterior_decode<'py>(&self, py: Python<'py>, obs: Vec<usize>) -> PyResult<Vec<usize>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.posterior_decode(&obs)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Baum-Welch training, returning the final total log-likelihood.
    ///
    /// Expectation-maximisation: compute the expected transition and emission
    /// counts under the current parameters, then set the parameters to their
    /// maximum-likelihood values given those counts. Each round is guaranteed
    /// not to lower the likelihood, which is what makes it safe to run
    /// without a line search -- and also all it guarantees, since it climbs
    /// to a local optimum that depends entirely on where it started.
    ///
    /// Stops early when the improvement falls below `tol`.
    ///
    /// Panics:
    /// Panics if a sequence contains an observation outside the alphabet.
    ///
    /// Rust: `stochastic::hmm::Hmm::baum_welch`
    #[pyo3(name = "baum_welch")]
    #[pyo3(signature = (sequences, iters, tol))]
    fn baum_welch<'py>(&mut self, py: Python<'py>, sequences: Vec<Vec<usize>>, iters: usize, tol: f64) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.baum_welch(&sequences, iters, tol)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Draws a state path and the observations it produces.
    ///
    /// Rust: `stochastic::hmm::Hmm::simulate`
    #[pyo3(name = "simulate")]
    #[pyo3(signature = (n, rng))]
    fn simulate(&self, n: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<(Vec<usize>, Vec<usize>)> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.simulate(n, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    #[getter]
    #[pyo3(name = "a")]
    fn py_get_a(&self) -> PyResult<crate::generated::types::PyMatrix> { Ok(crate::generated::types::PyMatrix { inner: self.inner.a.clone() }) }

    #[getter]
    #[pyo3(name = "b")]
    fn py_get_b(&self) -> PyResult<crate::generated::types::PyMatrix> { Ok(crate::generated::types::PyMatrix { inner: self.inner.b.clone() }) }

    #[getter]
    #[pyo3(name = "pi")]
    fn py_get_pi(&self) -> PyResult<Vec<f64>> { Ok(self.inner.pi.clone()) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Hmm", "Hmm", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A bootstrap particle filter: a cloud of weighted samples standing in for
/// the state distribution.
///
/// Where the Kalman filter propagates a mean and a covariance -- which is
/// exactly right if everything is linear and Gaussian and wrong otherwise --
/// this propagates samples, so it can represent any shape at all. The price
/// is variance, and the need to resample: without it the weight concentrates
/// on one particle and the rest of the cloud stops contributing.
///
/// Rust: `stochastic::hmm::ParticleFilter`
#[pyclass(name = "ParticleFilter", module = "numeria.stochastic.hmm", from_py_object)]
#[derive(Clone)]
pub struct PyParticleFilter { pub inner: rust_physics_engine::stochastic::hmm::ParticleFilter }
#[pymethods]
impl PyParticleFilter {
    /// Reweights the particles by how well each explains a measurement.
    ///
    /// The weights are multiplied by the likelihood and renormalised. If
    /// every particle is impossible the cloud is reset to uniform weights,
    /// since the alternative is dividing by zero.
    ///
    /// Rust: `stochastic::hmm::ParticleFilter::update`
    #[pyo3(name = "update")]
    #[pyo3(signature = (likelihood))]
    fn update(&mut self, likelihood: pyo3::Py<pyo3::PyAny>) -> PyResult<()> {
        let __cb_likelihood = std::rc::Rc::new(crate::runtime::Callback::new(likelihood));
        let likelihood = { let __cb = __cb_likelihood.clone(); move |__a0: &[f64]| -> f64 { __cb.call::<_, f64>((__a0.to_vec(),), f64::NAN) } };
        let __r = crate::runtime::guard(|| self.inner.update(&likelihood));
        crate::runtime::callback::check(&[&__cb_likelihood], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// Systematic resampling: draw a single uniform and take evenly spaced
    /// points from the cumulative weights.
    ///
    /// One random number for the whole cloud rather than one per particle,
    /// which gives lower variance than independent draws and guarantees that
    /// a particle with weight `w` is copied either `floor(nw)` or
    /// `ceil(nw)` times -- never zero when it deserves several.
    ///
    /// Rust: `stochastic::hmm::ParticleFilter::resample_systematic`
    #[pyo3(name = "resample_systematic")]
    #[pyo3(signature = (rng))]
    fn resample_systematic(&mut self, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<()> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.resample_systematic(&mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(())
    }

    /// The weighted mean of the cloud.
    ///
    /// Panics:
    /// Panics if the cloud is empty.
    ///
    /// Rust: `stochastic::hmm::ParticleFilter::estimate`
    #[pyo3(name = "estimate")]
    #[pyo3(signature = ())]
    fn estimate<'py>(&self, py: Python<'py>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.estimate()));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The effective number of particles: the reciprocal of the sum of
    /// squared weights.
    ///
    /// Equal to the particle count when the weights are uniform and one when
    /// a single particle holds everything. Falling below about half the count
    /// is the usual signal to resample.
    ///
    /// Rust: `stochastic::hmm::ParticleFilter::effective_n`
    #[pyo3(name = "effective_n")]
    #[pyo3(signature = ())]
    fn effective_n(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.effective_n());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "particles")]
    fn py_get_particles(&self) -> PyResult<Vec<Vec<f64>>> { Ok(self.inner.particles.clone()) }

    #[getter]
    #[pyo3(name = "weights")]
    fn py_get_weights(&self) -> PyResult<Vec<f64>> { Ok(self.inner.weights.clone()) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("ParticleFilter", "ParticleFilter", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A finite Markov chain, held as its row-stochastic transition matrix.
///
/// Rust: `stochastic::markov::MarkovChain`
#[pyclass(name = "MarkovChain", module = "numeria.stochastic.markov", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyMarkovChain { pub inner: rust_physics_engine::stochastic::markov::MarkovChain }
#[pymethods]
impl PyMarkovChain {
    /// The chain with the given transition matrix.
    ///
    /// Errors:
    /// Returns an error unless the matrix is square, non-empty, has no
    /// negative entries, and every row sums to one.
    ///
    /// Rust: `stochastic::markov::MarkovChain::new`
    #[new]
    #[pyo3(signature = (p))]
    fn __new__(p: crate::generated::types::PyMatrixArg) -> PyResult<crate::generated::types::PyMarkovChain> {
        let p = p.0;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::MarkovChain::new(p));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyMarkovChain { inner: __v })
    }

    /// The number of states.
    ///
    /// Rust: `stochastic::markov::MarkovChain::n`
    #[pyo3(name = "n")]
    #[pyo3(signature = ())]
    fn n(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.n());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// A chain estimated from a matrix of observed transition counts.
    ///
    /// Each row is normalised by its total, which is the maximum likelihood
    /// estimate. A row with no observations is made absorbing, since the data
    /// says nothing about where that state goes and any other choice would be
    /// an invention.
    ///
    /// Errors:
    /// Returns an error unless the counts form a non-empty square matrix with
    /// no negative entries.
    ///
    /// Rust: `stochastic::markov::MarkovChain::from_counts`
    #[pyo3(name = "from_counts")]
    #[staticmethod]
    #[pyo3(signature = (transitions))]
    fn from_counts(transitions: crate::generated::types::PyMatrixArg) -> PyResult<crate::generated::types::PyMarkovChain> {
        let transitions = transitions.0;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::MarkovChain::from_counts(&transitions));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyMarkovChain { inner: __v })
    }

    /// A chain estimated from one observed sequence of states.
    ///
    /// Errors:
    /// Returns an error if `n_states` is zero or a state is out of range.
    ///
    /// Rust: `stochastic::markov::MarkovChain::from_sequence`
    #[pyo3(name = "from_sequence")]
    #[staticmethod]
    #[pyo3(signature = (states, n_states))]
    fn from_sequence(states: Vec<usize>, n_states: usize) -> PyResult<crate::generated::types::PyMarkovChain> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::MarkovChain::from_sequence(&states, n_states));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyMarkovChain { inner: __v })
    }

    /// The distribution one step on from `dist`.
    ///
    /// Panics:
    /// Panics unless `dist` has one entry per state.
    ///
    /// Rust: `stochastic::markov::MarkovChain::step_dist`
    #[pyo3(name = "step_dist")]
    #[pyo3(signature = (dist))]
    fn step_dist<'py>(&self, py: Python<'py>, dist: Vec<f64>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.step_dist(&dist)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The `n`-step transition matrix, by repeated squaring.
    ///
    /// Rust: `stochastic::markov::MarkovChain::n_step`
    #[pyo3(name = "n_step")]
    #[pyo3(signature = (n))]
    fn n_step(&self, n: usize) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.n_step(n));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    /// A stationary distribution: a row vector left fixed by the matrix.
    ///
    /// Solved as a linear system rather than found by iteration, so a
    /// periodic chain -- where the powers of the matrix never converge --
    /// still gives its stationary distribution. The system is `pi (P - I) =
    /// 0` with the normalisation `sum pi = 1` substituted for one of the
    /// redundant equations.
    ///
    /// Panics:
    /// Panics if the linear system is singular, which happens only when the
    /// matrix is not stochastic.
    ///
    /// Rust: `stochastic::markov::MarkovChain::stationary`
    #[pyo3(name = "stationary")]
    #[pyo3(signature = ())]
    fn stationary<'py>(&self, py: Python<'py>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.stationary()));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Runs the chain, returning the states visited including the start.
    ///
    /// Panics:
    /// Panics unless `start` is a valid state.
    ///
    /// Rust: `stochastic::markov::MarkovChain::simulate`
    #[pyo3(name = "simulate")]
    #[pyo3(signature = (start, steps, rng))]
    fn simulate(&self, start: usize, steps: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<usize>> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.simulate(start, steps, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Whether every state can reach every other.
    ///
    /// Rust: `stochastic::markov::MarkovChain::is_irreducible`
    #[pyo3(name = "is_irreducible")]
    #[pyo3(signature = ())]
    fn is_irreducible(&self) -> PyResult<bool> {
        let __r = crate::runtime::guard(|| self.inner.is_irreducible());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The period of a state: the greatest common divisor of the lengths of
    /// the loops through it.
    ///
    /// One means aperiodic. A chain with a period above one cycles through
    /// classes of states and its matrix powers never settle, which is why
    /// aperiodicity is a hypothesis of every convergence theorem here.
    ///
    /// Panics:
    /// Panics unless `state` is valid.
    ///
    /// Rust: `stochastic::markov::MarkovChain::period`
    #[pyo3(name = "period")]
    #[pyo3(signature = (state))]
    fn period(&self, state: usize) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.period(state));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Whether every state has period one.
    ///
    /// Rust: `stochastic::markov::MarkovChain::is_aperiodic`
    #[pyo3(name = "is_aperiodic")]
    #[pyo3(signature = ())]
    fn is_aperiodic(&self) -> PyResult<bool> {
        let __r = crate::runtime::guard(|| self.inner.is_aperiodic());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Each state's long-run behaviour.
    ///
    /// A state is recurrent when everything it can reach can reach it back,
    /// and transient otherwise; it is absorbing when it goes nowhere else.
    ///
    /// Rust: `stochastic::markov::MarkovChain::classify_states`
    #[pyo3(name = "classify_states")]
    #[pyo3(signature = ())]
    fn classify_states(&self) -> PyResult<Vec<crate::generated::types::PyStateClass>> {
        let __r = crate::runtime::guard(|| self.inner.classify_states());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v.into_iter().map(|__x| crate::generated::types::PyStateClass::from_rust(&__x)).collect::<Vec<_>>())
    }

    /// The probability of ending in each absorbing state, one row per
    /// transient state.
    ///
    /// The fundamental matrix `N = (I - Q)^-1` counts expected visits to each
    /// transient state before absorption -- its `(i, j)` entry is the sum
    /// over path lengths of the chance of being at `j` at that step -- and
    /// `N R` then routes those visits into the absorbing states. Columns
    /// follow the order the absorbing states appear in.
    ///
    /// Panics:
    /// Panics if the chain has no absorbing state, or if `I - Q` is singular,
    /// which means some transient state cannot reach absorption.
    ///
    /// Rust: `stochastic::markov::MarkovChain::absorbing_probabilities`
    #[pyo3(name = "absorbing_probabilities")]
    #[pyo3(signature = ())]
    fn absorbing_probabilities(&self) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.absorbing_probabilities());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    /// The fundamental matrix `N = (I - Q)^-1` over the transient states.
    ///
    /// Panics:
    /// Panics if `I - Q` is singular.
    ///
    /// Rust: `stochastic::markov::MarkovChain::fundamental_matrix`
    #[pyo3(name = "fundamental_matrix")]
    #[pyo3(signature = ())]
    fn fundamental_matrix(&self) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.fundamental_matrix());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    /// Expected steps to absorption from each state, zero for the absorbing
    /// ones.
    ///
    /// The row sums of the fundamental matrix: total expected visits to all
    /// transient states is total expected time before leaving them.
    ///
    /// Panics:
    /// Panics if the chain has no absorbing state.
    ///
    /// Rust: `stochastic::markov::MarkovChain::expected_steps_to_absorption`
    #[pyo3(name = "expected_steps_to_absorption")]
    #[pyo3(signature = ())]
    fn expected_steps_to_absorption<'py>(&self, py: Python<'py>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.expected_steps_to_absorption()));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The expected number of steps to first reach any state in `target`.
    ///
    /// Infinite when the target cannot be reached. Solved as the linear
    /// system `h_i = 1 + sum_j p_ij h_j` over the states outside the target,
    /// which is the first-step decomposition written down.
    ///
    /// Panics:
    /// Panics unless the states are valid.
    ///
    /// Rust: `stochastic::markov::MarkovChain::hitting_time`
    #[pyo3(name = "hitting_time")]
    #[pyo3(signature = (from_, target))]
    fn hitting_time<'py>(&self, py: Python<'py>, from_: usize, target: Vec<usize>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.hitting_time(from_, &target)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The probability of ever reaching `target` from `from`.
    ///
    /// Panics:
    /// Panics unless the states are valid.
    ///
    /// Rust: `stochastic::markov::MarkovChain::hitting_probability`
    #[pyo3(name = "hitting_probability")]
    #[pyo3(signature = (from_, target))]
    fn hitting_probability<'py>(&self, py: Python<'py>, from_: usize, target: Vec<usize>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.hitting_probability(from_, &target)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The expected number of steps to return to a state, starting from it.
    ///
    /// Kac's formula: the reciprocal of that state's stationary probability.
    /// It is one of the most useful facts about a chain -- the long-run share
    /// of time spent somewhere and the average wait between visits are
    /// reciprocals of each other, with no further hypothesis than
    /// irreducibility.
    ///
    /// Panics:
    /// Panics unless `state` is valid.
    ///
    /// Rust: `stochastic::markov::MarkovChain::return_time`
    #[pyo3(name = "return_time")]
    #[pyo3(signature = (state))]
    fn return_time(&self, state: usize) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.return_time(state));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The mean first passage time from every state to every other.
    ///
    /// The diagonal holds the return times.
    ///
    /// Panics:
    /// Panics if the chain has fewer than one state.
    ///
    /// Rust: `stochastic::markov::MarkovChain::mfpt_matrix`
    #[pyo3(name = "mfpt_matrix")]
    #[pyo3(signature = ())]
    fn mfpt_matrix(&self) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.mfpt_matrix());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    /// Total variation distance between two distributions: half the sum of
    /// the absolute differences.
    ///
    /// The largest difference in probability the two assign to any event,
    /// which is why it is the metric convergence is measured in.
    ///
    /// Panics:
    /// Panics unless the two have the same length.
    ///
    /// Rust: `stochastic::markov::MarkovChain::total_variation_distance`
    #[pyo3(name = "total_variation_distance")]
    #[staticmethod]
    #[pyo3(signature = (a, b))]
    fn total_variation_distance<'py>(py: Python<'py>, a: Vec<f64>, b: Vec<f64>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::stochastic::markov::MarkovChain::total_variation_distance(&a, &b)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The number of steps until every start is within `eps` of stationary in
    /// total variation.
    ///
    /// Infinite for a chain that does not converge -- one that is reducible or
    /// periodic.
    ///
    /// Panics:
    /// Panics unless `eps` is in `(0, 1)`.
    ///
    /// Rust: `stochastic::markov::MarkovChain::mixing_time`
    #[pyo3(name = "mixing_time")]
    #[pyo3(signature = (eps))]
    fn mixing_time(&self, eps: f64) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.mixing_time(eps));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The spectral gap: one minus the second-largest eigenvalue modulus.
    ///
    /// What sets the rate of convergence, since the distance to stationary
    /// falls like the second eigenvalue's magnitude raised to the step count.
    /// Zero for a chain that does not converge. Computed here through the
    /// symmetrised chain, so it is exact for reversible chains and a
    /// reasonable proxy otherwise.
    ///
    /// Rust: `stochastic::markov::MarkovChain::spectral_gap`
    #[pyo3(name = "spectral_gap")]
    #[pyo3(signature = ())]
    fn spectral_gap(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.spectral_gap());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Whether the chain satisfies detailed balance against `pi`.
    ///
    /// `pi_i p_ij = pi_j p_ji` for every pair: the flow between any two
    /// states is the same in both directions. It is much stronger than
    /// stationarity, which needs only that the total flow into each state
    /// balances the total flow out, and it is what every Metropolis-Hastings
    /// sampler arranges because it is far easier to arrange.
    ///
    /// Panics:
    /// Panics unless `pi` has one entry per state.
    ///
    /// Rust: `stochastic::markov::MarkovChain::reversible_check`
    #[pyo3(name = "reversible_check")]
    #[pyo3(signature = (pi, tol))]
    fn reversible_check<'py>(&self, py: Python<'py>, pi: Vec<f64>, tol: f64) -> PyResult<bool> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.reversible_check(&pi, tol)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The entropy rate: the average uncertainty per step in the long run, in
    /// bits.
    ///
    /// The stationary-weighted average of each row's entropy. It is the
    /// compression limit for a stream generated by the chain, and it is what
    /// separates a chain from a memoryless source with the same marginal:
    /// the marginal entropy is an upper bound and the difference is what the
    /// dependence saves.
    ///
    /// Rust: `stochastic::markov::MarkovChain::entropy_rate`
    #[pyo3(name = "entropy_rate")]
    #[pyo3(signature = ())]
    fn entropy_rate(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.entropy_rate());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// An exact sample from the stationary distribution, by coupling from the
    /// past.
    ///
    /// Ordinary simulation gives a sample that is only approximately
    /// stationary, with no way to tell how close. Propp and Wilson's
    /// construction instead runs every possible start from further and
    /// further back until they all coalesce by time zero; the common value is
    /// then *exactly* stationary, because whatever the chain was doing
    /// infinitely far back, it would have ended up there too.
    ///
    /// Panics:
    /// Panics if coalescence does not occur, which for an irreducible
    /// aperiodic chain means only that the bound was too small.
    ///
    /// Rust: `stochastic::markov::MarkovChain::coupling_from_the_past_small`
    #[pyo3(name = "coupling_from_the_past_small")]
    #[pyo3(signature = (rng))]
    fn coupling_from_the_past_small(&self, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<usize> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.coupling_from_the_past_small(&mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The PageRank chain of a graph: follow a random out-edge with
    /// probability `damping`, and teleport to a uniform vertex otherwise.
    ///
    /// The teleportation is what makes the chain irreducible and aperiodic
    /// whatever the graph looks like, so a stationary distribution exists and
    /// is unique. A vertex with no out-edges teleports always, which spreads
    /// its mass rather than letting it vanish.
    ///
    /// Panics:
    /// Panics unless the graph is non-empty and `damping` is in `[0, 1]`.
    ///
    /// Rust: `stochastic::markov::MarkovChain::pagerank_chain`
    #[pyo3(name = "pagerank_chain")]
    #[staticmethod]
    #[pyo3(signature = (g, damping))]
    fn pagerank_chain(g: crate::generated::types::PyGraph, damping: f64) -> PyResult<crate::generated::types::PyMarkovChain> {
        let g = g.inner;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::MarkovChain::pagerank_chain(&g, damping));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyMarkovChain { inner: __v })
    }

    #[getter]
    #[pyo3(name = "p")]
    fn py_get_p(&self) -> PyResult<crate::generated::types::PyMatrix> { Ok(crate::generated::types::PyMatrix { inner: self.inner.p.clone() }) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("MarkovChain", "MarkovChain", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// Samplers that build a chain whose stationary distribution is a target you
/// can evaluate but not sample from directly.
///
/// Every method here takes the *log* of the target, unnormalised. Logs
/// because the density of anything interesting underflows; unnormalised
/// because the normalising constant is exactly the thing that is usually
/// impossible to compute, and none of these methods needs it -- they see the
/// target only through ratios, in which it cancels.
///
/// Rust: `stochastic::markov::Mcmc`
#[pyclass(name = "Mcmc", module = "numeria.stochastic.markov", from_py_object)]
#[derive(Clone)]
pub struct PyMcmc { pub inner: rust_physics_engine::stochastic::markov::Mcmc }
#[pymethods]
impl PyMcmc {
    /// Metropolis-Hastings with a symmetric Gaussian proposal.
    ///
    /// Propose a move, accept it outright if it goes uphill, and accept it
    /// with probability equal to the density ratio if it goes down. That rule
    /// makes detailed balance hold against the target, so the target is
    /// stationary; the downhill moves are not a concession but the mechanism,
    /// since a sampler that only climbed would sit at the mode forever.
    ///
    /// Returns the chain after discarding `burn` samples.
    ///
    /// Panics:
    /// Panics on an empty start, a non-positive proposal width, or a burn-in
    /// at or beyond the requested length.
    ///
    /// Rust: `stochastic::markov::Mcmc::metropolis_hastings`
    #[pyo3(name = "metropolis_hastings")]
    #[staticmethod]
    #[pyo3(signature = (log_target, x0, proposal_std, n, burn, rng))]
    fn metropolis_hastings(log_target: pyo3::Py<pyo3::PyAny>, x0: Vec<f64>, proposal_std: f64, n: usize, burn: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<Vec<f64>>> {
        let __cb_log_target = std::rc::Rc::new(crate::runtime::Callback::new(log_target));
        let log_target = { let __cb = __cb_log_target.clone(); move |__a0: &[f64]| -> f64 { __cb.call::<_, f64>((__a0.to_vec(),), f64::NAN) } };
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::Mcmc::metropolis_hastings(&log_target, &x0, proposal_std, n, burn, &mut rng.inner));
        crate::runtime::callback::check(&[&__cb_log_target], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Metropolis-Hastings that tunes its own proposal width towards an
    /// acceptance rate of about a quarter.
    ///
    /// Too wide a proposal is rejected constantly and the chain stands still;
    /// too narrow a one is always accepted and the chain crawls. The optimum
    /// for a high-dimensional Gaussian target is famously near 0.234, and
    /// adapting towards it costs nothing. Adaptation stops at the end of
    /// burn-in, because a proposal that keeps changing breaks the Markov
    /// property and the chain is no longer guaranteed to have the right
    /// stationary distribution.
    ///
    /// Panics:
    /// Panics under the same conditions as
    /// `metropolis_hastings`.
    ///
    /// Rust: `stochastic::markov::Mcmc::adaptive_metropolis`
    #[pyo3(name = "adaptive_metropolis")]
    #[staticmethod]
    #[pyo3(signature = (log_target, x0, proposal_std, n, burn, rng))]
    fn adaptive_metropolis(log_target: pyo3::Py<pyo3::PyAny>, x0: Vec<f64>, proposal_std: f64, n: usize, burn: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<Vec<f64>>> {
        let __cb_log_target = std::rc::Rc::new(crate::runtime::Callback::new(log_target));
        let log_target = { let __cb = __cb_log_target.clone(); move |__a0: &[f64]| -> f64 { __cb.call::<_, f64>((__a0.to_vec(),), f64::NAN) } };
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::Mcmc::adaptive_metropolis(&log_target, &x0, proposal_std, n, burn, &mut rng.inner));
        crate::runtime::callback::check(&[&__cb_log_target], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Hamiltonian Monte Carlo: give the point a momentum and follow the
    /// resulting trajectory.
    ///
    /// Treat the negative log density as a potential energy, draw a random
    /// momentum, and integrate the equations of motion. The trajectory
    /// conserves energy, so a proposal at the far end is accepted with
    /// probability near one however far it has travelled -- which is what
    /// lets the chain cross the whole distribution in one move instead of
    /// diffusing across it. The leapfrog integrator is used because it is
    /// *symplectic*: its error does not accumulate, so energy stays nearly
    /// conserved over long trajectories, and it is reversible, which the
    /// acceptance rule requires.
    ///
    /// Panics:
    /// Panics on an empty start, a non-positive step, no leapfrog steps, or a
    /// burn-in at or beyond the run.
    ///
    /// Rust: `stochastic::markov::Mcmc::hamiltonian_mc`
    #[pyo3(name = "hamiltonian_mc")]
    #[staticmethod]
    #[pyo3(signature = (log_target, grad, x0, step, n_leapfrog, n, burn, rng))]
    fn hamiltonian_mc(log_target: pyo3::Py<pyo3::PyAny>, grad: pyo3::Py<pyo3::PyAny>, x0: Vec<f64>, step: f64, n_leapfrog: usize, n: usize, burn: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<Vec<f64>>> {
        let __cb_log_target = std::rc::Rc::new(crate::runtime::Callback::new(log_target));
        let log_target = { let __cb = __cb_log_target.clone(); move |__a0: &[f64]| -> f64 { __cb.call::<_, f64>((__a0.to_vec(),), f64::NAN) } };
        let __cb_grad = std::rc::Rc::new(crate::runtime::Callback::new(grad));
        let grad = { let __cb = __cb_grad.clone(); move |__a0: &[f64]| -> Vec<f64> { __cb.call::<_, Vec<f64>>((__a0.to_vec(),), Vec::new()) } };
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::Mcmc::hamiltonian_mc(&log_target, &grad, &x0, step, n_leapfrog, n, burn, &mut rng.inner));
        crate::runtime::callback::check(&[&__cb_log_target, &__cb_grad], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The no-U-turn sampler: Hamiltonian trajectories whose length the
    /// algorithm chooses by watching for the path to double back.
    ///
    /// Hoffman and Gelman's naive scheme. The trajectory is grown by
    /// repeated doubling, forwards or backwards at random, and stops when the
    /// two ends of *any* sub-trajectory start approaching each other; the
    /// next state is drawn uniformly from the states the slice variable
    /// admits. The doubling and the sub-tree stopping check are not
    /// decoration -- simply running until the path turns and taking the last
    /// point is not reversible, and gives the wrong stationary distribution.
    /// Getting that right removes trajectory length from the list of things a
    /// user must tune, which was the practical obstacle to Hamiltonian
    /// methods.
    ///
    /// Panics:
    /// Panics on an empty start, a non-positive step, a zero depth, or a
    /// burn-in at or beyond the run.
    ///
    /// Rust: `stochastic::markov::Mcmc::nuts_lite`
    #[pyo3(name = "nuts_lite")]
    #[staticmethod]
    #[pyo3(signature = (log_target, grad, x0, step, max_depth, n, burn, rng))]
    fn nuts_lite(log_target: pyo3::Py<pyo3::PyAny>, grad: pyo3::Py<pyo3::PyAny>, x0: Vec<f64>, step: f64, max_depth: usize, n: usize, burn: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<Vec<f64>>> {
        let __cb_log_target = std::rc::Rc::new(crate::runtime::Callback::new(log_target));
        let log_target = { let __cb = __cb_log_target.clone(); move |__a0: &[f64]| -> f64 { __cb.call::<_, f64>((__a0.to_vec(),), f64::NAN) } };
        let __cb_grad = std::rc::Rc::new(crate::runtime::Callback::new(grad));
        let grad = { let __cb = __cb_grad.clone(); move |__a0: &[f64]| -> Vec<f64> { __cb.call::<_, Vec<f64>>((__a0.to_vec(),), Vec::new()) } };
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::Mcmc::nuts_lite(&log_target, &grad, &x0, step, max_depth, n, burn, &mut rng.inner));
        crate::runtime::callback::check(&[&__cb_log_target, &__cb_grad], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Slice sampling in one dimension.
    ///
    /// Draw a height uniformly below the density, then draw a point uniformly
    /// from the slice at that height. Every move is accepted and there is no
    /// proposal width to tune -- the stepping-out procedure finds the slice's
    /// extent on its own, so `w` only affects speed and not correctness.
    ///
    /// Panics:
    /// Panics on a non-positive width.
    ///
    /// Rust: `stochastic::markov::Mcmc::slice_sampler`
    #[pyo3(name = "slice_sampler")]
    #[staticmethod]
    #[pyo3(signature = (log_target_1d, x0, w, n, rng))]
    fn slice_sampler(log_target_1d: pyo3::Py<pyo3::PyAny>, x0: f64, w: f64, n: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<f64>> {
        let __cb_log_target_1d = std::rc::Rc::new(crate::runtime::Callback::new(log_target_1d));
        let log_target_1d = { let __cb = __cb_log_target_1d.clone(); move |__a0: f64| -> f64 { __cb.call::<_, f64>((__a0,), f64::NAN) } };
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::Mcmc::slice_sampler(&log_target_1d, x0, w, n, &mut rng.inner));
        crate::runtime::callback::check(&[&__cb_log_target_1d], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Parallel tempering: run several chains at different temperatures and
    /// let them swap.
    ///
    /// A hot chain sees a flattened version of the target and crosses between
    /// modes easily; a cold chain samples the target itself but can be
    /// trapped. Swapping states between neighbouring temperatures, with an
    /// acceptance rule that preserves each chain's own stationary
    /// distribution, lets the cold chain inherit the hot one's mobility.
    /// Returns the samples from the coldest chain.
    ///
    /// Panics:
    /// Panics unless the temperatures are positive with the first equal to
    /// one, and the burn-in is shorter than the run.
    ///
    /// Rust: `stochastic::markov::Mcmc::parallel_tempering`
    #[pyo3(name = "parallel_tempering")]
    #[staticmethod]
    #[pyo3(signature = (log_target, temps, x0, proposal_std, n, burn, rng))]
    fn parallel_tempering(log_target: pyo3::Py<pyo3::PyAny>, temps: Vec<f64>, x0: Vec<f64>, proposal_std: f64, n: usize, burn: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<Vec<f64>>> {
        let __cb_log_target = std::rc::Rc::new(crate::runtime::Callback::new(log_target));
        let log_target = { let __cb = __cb_log_target.clone(); move |__a0: &[f64]| -> f64 { __cb.call::<_, f64>((__a0.to_vec(),), f64::NAN) } };
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::Mcmc::parallel_tempering(&log_target, &temps, &x0, proposal_std, n, burn, &mut rng.inner));
        crate::runtime::callback::check(&[&__cb_log_target], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The autocorrelation time of a chain: one plus twice the sum of the
    /// autocorrelations, truncated where they first turn negative.
    ///
    /// How many steps the chain takes to forget where it was. The truncation
    /// is Geyer's initial positive sequence rule: past that point the
    /// estimates are dominated by noise, and summing them adds variance
    /// rather than information.
    ///
    /// Rust: `stochastic::markov::Mcmc::autocorrelation_time`
    #[pyo3(name = "autocorrelation_time")]
    #[staticmethod]
    #[pyo3(signature = (chain))]
    fn autocorrelation_time<'py>(py: Python<'py>, chain: Vec<f64>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::stochastic::markov::Mcmc::autocorrelation_time(&chain)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The effective sample size: the number of independent draws a
    /// correlated chain is worth.
    ///
    /// The run length divided by the autocorrelation time. Always at most the
    /// run length, and usually far less -- a Metropolis chain with a
    /// well-tuned proposal might be worth a tenth of its length, which is the
    /// honest denominator for any Monte Carlo error estimate.
    ///
    /// Rust: `stochastic::markov::Mcmc::effective_sample_size`
    #[pyo3(name = "effective_sample_size")]
    #[staticmethod]
    #[pyo3(signature = (chain))]
    fn effective_sample_size<'py>(py: Python<'py>, chain: Vec<f64>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::stochastic::markov::Mcmc::effective_sample_size(&chain)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The Gelman-Rubin statistic: the ratio of the pooled variance estimate
    /// to the within-chain one.
    ///
    /// Several chains from different starts should, once converged, look like
    /// draws from the same distribution -- so the spread between chains
    /// should match the spread within them and the ratio should approach one.
    /// A value well above one is the clearest evidence available that a run
    /// has not converged. It cannot prove that one has.
    ///
    /// Panics:
    /// Panics unless there are at least two chains of at least two samples
    /// each, all the same length.
    ///
    /// Rust: `stochastic::markov::Mcmc::gelman_rubin`
    #[pyo3(name = "gelman_rubin")]
    #[staticmethod]
    #[pyo3(signature = (chains))]
    fn gelman_rubin<'py>(py: Python<'py>, chains: Vec<Vec<f64>>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::stochastic::markov::Mcmc::gelman_rubin(&chains)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Simulated annealing: Metropolis on an energy, with the temperature
    /// falling on a schedule.
    ///
    /// At a high temperature almost every move is accepted and the search
    /// wanders; as the temperature falls it becomes a hill descent. Returns
    /// the best point found and its energy -- the best, not the last, because
    /// the walk can and does step away from an optimum it has found.
    ///
    /// Panics:
    /// Panics on an empty start.
    ///
    /// Rust: `stochastic::markov::Mcmc::simulated_annealing`
    #[pyo3(name = "simulated_annealing")]
    #[staticmethod]
    #[pyo3(signature = (energy, x0, schedule, n, rng))]
    fn simulated_annealing(energy: pyo3::Py<pyo3::PyAny>, x0: Vec<f64>, schedule: pyo3::Py<pyo3::PyAny>, n: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<(Vec<f64>, f64)> {
        let __cb_energy = std::rc::Rc::new(crate::runtime::Callback::new(energy));
        let energy = { let __cb = __cb_energy.clone(); move |__a0: &[f64]| -> f64 { __cb.call::<_, f64>((__a0.to_vec(),), f64::NAN) } };
        let __cb_schedule = std::rc::Rc::new(crate::runtime::Callback::new(schedule));
        let schedule = { let __cb = __cb_schedule.clone(); move |__a0: usize| -> f64 { __cb.call::<_, f64>((__a0,), f64::NAN) } };
        let mut rng = rng;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::markov::Mcmc::simulated_annealing(&energy, &x0, &schedule, n, &mut rng.inner));
        crate::runtime::callback::check(&[&__cb_energy, &__cb_schedule], ())?;
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Mcmc", "Mcmc", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// How a state behaves in the long run.
///
/// Rust: `stochastic::markov::StateClass`
#[pyclass(name = "StateClass", module = "numeria.stochastic.markov", from_py_object, eq, eq_int)]
#[derive(Clone, Copy, PartialEq)]
pub enum PyStateClass {
    Transient,
    Recurrent,
    Absorbing,
}
impl PyStateClass {
    pub fn to_rust(&self) -> rust_physics_engine::stochastic::markov::StateClass { match self {
        Self::Transient => rust_physics_engine::stochastic::markov::StateClass::Transient,
        Self::Recurrent => rust_physics_engine::stochastic::markov::StateClass::Recurrent,
        Self::Absorbing => rust_physics_engine::stochastic::markov::StateClass::Absorbing,
    } }
    pub fn from_rust(v: &rust_physics_engine::stochastic::markov::StateClass) -> Self { match v {
        rust_physics_engine::stochastic::markov::StateClass::Transient => Self::Transient,
        rust_physics_engine::stochastic::markov::StateClass::Recurrent => Self::Recurrent,
        rust_physics_engine::stochastic::markov::StateClass::Absorbing => Self::Absorbing,
    } }
}
#[pymethods]
impl PyStateClass {
    fn __repr__(&self) -> &'static str {
        match self {
            Self::Transient => "StateClass.Transient",
            Self::Recurrent => "StateClass.Recurrent",
            Self::Absorbing => "StateClass.Absorbing",
        }
    }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A continuous-time Markov chain, held as its generator matrix.
///
/// Rows of `q` sum to zero: the off-diagonal entries are transition rates and
/// the diagonal is minus their total, so `-q_ii` is the rate of leaving state
/// `i`. Where a discrete chain asks "what is the next state", a generator
/// asks "how long until something happens, and what".
///
/// Rust: `stochastic::queueing::Ctmc`
#[pyclass(name = "Ctmc", module = "numeria.stochastic.queueing", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyCtmc { pub inner: rust_physics_engine::stochastic::queueing::Ctmc }
#[pymethods]
impl PyCtmc {
    /// Wraps a generator after checking its shape.
    ///
    /// Errors:
    /// Returns `GeomError::InvalidArgument` if the matrix is not square,
    /// has a negative off-diagonal rate, or has a row that does not sum to
    /// zero within `1e-9`.
    ///
    /// Rust: `stochastic::queueing::Ctmc::new`
    #[new]
    #[pyo3(signature = (q))]
    fn __new__(q: crate::generated::types::PyMatrixArg) -> PyResult<crate::generated::types::PyCtmc> {
        let q = q.0;
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::queueing::Ctmc::new(q));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyCtmc { inner: __v })
    }

    /// Number of states.
    ///
    /// Rust: `stochastic::queueing::Ctmc::n`
    #[pyo3(name = "n")]
    #[pyo3(signature = ())]
    fn n(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.n());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Mean time spent in each state per visit, `1 / (-q_ii)`.
    ///
    /// Infinite for an absorbing state, which is never left.
    ///
    /// Rust: `stochastic::queueing::Ctmc::mean_holding_times`
    #[pyo3(name = "mean_holding_times")]
    #[pyo3(signature = ())]
    fn mean_holding_times<'py>(&self, py: Python<'py>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.mean_holding_times()));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The jump chain: where the process goes, ignoring how long it waits.
    ///
    /// `P_ij = q_ij / (-q_ii)`. An absorbing state becomes a self-loop so the
    /// result is a valid stochastic matrix.
    ///
    /// Errors:
    /// Returns an error if the resulting matrix is rejected as a chain.
    ///
    /// Rust: `stochastic::queueing::Ctmc::embedded_chain`
    #[pyo3(name = "embedded_chain")]
    #[pyo3(signature = ())]
    fn embedded_chain(&self) -> PyResult<crate::generated::types::PyMarkovChain> {
        let __r = crate::runtime::guard(|| self.inner.embedded_chain());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyMarkovChain { inner: __v })
    }

    /// The stationary distribution, solving `pi Q = 0` with `sum pi = 1`.
    ///
    /// Solved as a linear system rather than by iterating, so periodicity in
    /// the jump chain is irrelevant -- a continuous-time chain has no period
    /// to speak of, and the linear solve reflects that.
    ///
    /// Errors:
    /// Returns `GeomError::Degenerate` if the balance equations are
    /// singular, which happens when the chain is reducible.
    ///
    /// Rust: `stochastic::queueing::Ctmc::stationary`
    #[pyo3(name = "stationary")]
    #[pyo3(signature = ())]
    fn stationary<'py>(&self, py: Python<'py>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.stationary()));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// Simulates one trajectory by the Gillespie construction: hold in the
    /// current state for an exponential time set by its exit rate, then jump
    /// according to the embedded chain.
    ///
    /// Returns `(time of entry, state)` pairs, beginning at `(0, start)`.
    /// Stops early at an absorbing state.
    ///
    /// Panics:
    /// Panics if `start` is out of range or `t_end` is not positive.
    ///
    /// Rust: `stochastic::queueing::Ctmc::simulate`
    #[pyo3(name = "simulate")]
    #[pyo3(signature = (start, t_end, rng))]
    fn simulate(&self, start: usize, t_end: f64, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<(f64, usize)>> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.simulate(start, t_end, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
    }

    /// Mean time to reach `to` starting from `from`.
    ///
    /// Solves `m_i = 1/(-q_ii) + sum_{j != to} P_ij m_j` over the jump chain,
    /// which is the continuous-time analogue of a first-step decomposition:
    /// wait out the holding time, then start again from wherever you land.
    ///
    /// Errors:
    /// Returns an error if the system is singular, which means `to` is not
    /// reachable from every transient state.
    ///
    /// Rust: `stochastic::queueing::Ctmc::first_passage`
    #[pyo3(name = "first_passage")]
    #[pyo3(signature = (from_, to))]
    fn first_passage(&self, from_: usize, to: usize) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.first_passage(from_, to));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "q")]
    fn py_get_q(&self) -> PyResult<crate::generated::types::PyMatrix> { Ok(crate::generated::types::PyMatrix { inner: self.inner.q.clone() }) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Ctmc", "Ctmc", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// The standard summary of a queue in steady state.
///
/// `l` and `lq` count customers, `w` and `wq` measure time. The two pairs are
/// linked by Little's law at the *effective* arrival rate, which differs from
/// the offered rate whenever the system turns customers away.
///
/// Rust: `stochastic::queueing::QueueMetrics`
#[pyclass(name = "QueueMetrics", module = "numeria.stochastic.queueing", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyQueueMetrics { pub inner: rust_physics_engine::stochastic::queueing::QueueMetrics }
#[pymethods]
impl PyQueueMetrics {
    /// Builds a `QueueMetrics` from its fields.
    #[new]
    #[pyo3(signature = (rho, l, lq, w, wq, p0, lambda_eff, model))]
    fn __new__(rho: f64, l: f64, lq: f64, w: f64, wq: f64, p0: f64, lambda_eff: f64, model: crate::generated::types::PyQueueModel) -> Self {
        let model = model.inner;
        Self { inner: rust_physics_engine::stochastic::queueing::QueueMetrics { rho: rho, l: l, lq: lq, w: w, wq: wq, p0: p0, lambda_eff: lambda_eff, model: model } }
    }

    /// Stationary probability of exactly `n` customers in the system.
    ///
    /// Returns NaN for `QueueModel::MeanValueOnly`, where only the means
    /// are determined by the inputs.
    ///
    /// Rust: `stochastic::queueing::QueueMetrics::pn`
    #[pyo3(name = "pn")]
    #[pyo3(signature = (n))]
    fn pn(&self, n: usize) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.pn(n));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "rho")]
    fn py_get_rho(&self) -> PyResult<f64> { Ok(self.inner.rho) }

    #[setter]
    #[pyo3(name = "rho")]
    fn py_set_rho(&mut self, v: f64) { self.inner.rho = v; }

    #[getter]
    #[pyo3(name = "l")]
    fn py_get_l(&self) -> PyResult<f64> { Ok(self.inner.l) }

    #[setter]
    #[pyo3(name = "l")]
    fn py_set_l(&mut self, v: f64) { self.inner.l = v; }

    #[getter]
    #[pyo3(name = "lq")]
    fn py_get_lq(&self) -> PyResult<f64> { Ok(self.inner.lq) }

    #[setter]
    #[pyo3(name = "lq")]
    fn py_set_lq(&mut self, v: f64) { self.inner.lq = v; }

    #[getter]
    #[pyo3(name = "w")]
    fn py_get_w(&self) -> PyResult<f64> { Ok(self.inner.w) }

    #[setter]
    #[pyo3(name = "w")]
    fn py_set_w(&mut self, v: f64) { self.inner.w = v; }

    #[getter]
    #[pyo3(name = "wq")]
    fn py_get_wq(&self) -> PyResult<f64> { Ok(self.inner.wq) }

    #[setter]
    #[pyo3(name = "wq")]
    fn py_set_wq(&mut self, v: f64) { self.inner.wq = v; }

    #[getter]
    #[pyo3(name = "p0")]
    fn py_get_p0(&self) -> PyResult<f64> { Ok(self.inner.p0) }

    #[setter]
    #[pyo3(name = "p0")]
    fn py_set_p0(&mut self, v: f64) { self.inner.p0 = v; }

    #[getter]
    #[pyo3(name = "lambda_eff")]
    fn py_get_lambda_eff(&self) -> PyResult<f64> { Ok(self.inner.lambda_eff) }

    #[setter]
    #[pyo3(name = "lambda_eff")]
    fn py_set_lambda_eff(&mut self, v: f64) { self.inner.lambda_eff = v; }

    #[getter]
    #[pyo3(name = "model")]
    fn py_get_model(&self) -> PyResult<crate::generated::types::PyQueueModel> { Ok(crate::generated::types::PyQueueModel { inner: self.inner.model.clone() }) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("QueueMetrics", "QueueMetrics", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// Which birth-death chain a set of metrics came from.
///
/// Carried alongside the means so that `QueueMetrics::pn` can report the
/// exact stationary probability of `n` in the system. Models with no
/// product-form state distribution report `QueueModel::MeanValueOnly`.
///
/// Rust: `stochastic::queueing::QueueModel`
#[pyclass(name = "QueueModel", module = "numeria.stochastic.queueing", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyQueueModel { pub inner: rust_physics_engine::stochastic::queueing::QueueModel }
#[pymethods]
impl PyQueueModel {
    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("QueueModel", "QueueModel", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// What an event-driven run measured.
///
/// The time averages come from integrating the sample path over a merged
/// list of arrival and departure events, independently of the customer
/// averages, so `l` and `lambda_eff * w` are two separate measurements of the
/// same quantity rather than one derived from the other.
///
/// Rust: `stochastic::queueing::QueueSimResult`
#[pyclass(name = "QueueSimResult", module = "numeria.stochastic.queueing", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyQueueSimResult { pub inner: rust_physics_engine::stochastic::queueing::QueueSimResult }
#[pymethods]
impl PyQueueSimResult {
    /// Builds a `QueueSimResult` from its fields.
    #[new]
    #[pyo3(signature = (l, lq, w, wq, rho, lambda_eff, served))]
    fn __new__(l: f64, lq: f64, w: f64, wq: f64, rho: f64, lambda_eff: f64, served: usize) -> Self {

        Self { inner: rust_physics_engine::stochastic::queueing::QueueSimResult { l: l, lq: lq, w: w, wq: wq, rho: rho, lambda_eff: lambda_eff, served: served } }
    }

    #[getter]
    #[pyo3(name = "l")]
    fn py_get_l(&self) -> PyResult<f64> { Ok(self.inner.l) }

    #[setter]
    #[pyo3(name = "l")]
    fn py_set_l(&mut self, v: f64) { self.inner.l = v; }

    #[getter]
    #[pyo3(name = "lq")]
    fn py_get_lq(&self) -> PyResult<f64> { Ok(self.inner.lq) }

    #[setter]
    #[pyo3(name = "lq")]
    fn py_set_lq(&mut self, v: f64) { self.inner.lq = v; }

    #[getter]
    #[pyo3(name = "w")]
    fn py_get_w(&self) -> PyResult<f64> { Ok(self.inner.w) }

    #[setter]
    #[pyo3(name = "w")]
    fn py_set_w(&mut self, v: f64) { self.inner.w = v; }

    #[getter]
    #[pyo3(name = "wq")]
    fn py_get_wq(&self) -> PyResult<f64> { Ok(self.inner.wq) }

    #[setter]
    #[pyo3(name = "wq")]
    fn py_set_wq(&mut self, v: f64) { self.inner.wq = v; }

    #[getter]
    #[pyo3(name = "rho")]
    fn py_get_rho(&self) -> PyResult<f64> { Ok(self.inner.rho) }

    #[setter]
    #[pyo3(name = "rho")]
    fn py_set_rho(&mut self, v: f64) { self.inner.rho = v; }

    #[getter]
    #[pyo3(name = "lambda_eff")]
    fn py_get_lambda_eff(&self) -> PyResult<f64> { Ok(self.inner.lambda_eff) }

    #[setter]
    #[pyo3(name = "lambda_eff")]
    fn py_set_lambda_eff(&mut self, v: f64) { self.inner.lambda_eff = v; }

    #[getter]
    #[pyo3(name = "served")]
    fn py_get_served(&self) -> PyResult<usize> { Ok(self.inner.served) }

    #[setter]
    #[pyo3(name = "served")]
    fn py_set_served(&mut self, v: usize) { self.inner.served = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("QueueSimResult", "QueueSimResult", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// Parameters of the Heston stochastic volatility model.
///
/// Rust: `stochastic::sde::HestonParams`
#[pyclass(name = "HestonParams", module = "numeria.stochastic.sde", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyHestonParams { pub inner: rust_physics_engine::stochastic::sde::HestonParams }
#[pymethods]
impl PyHestonParams {
    /// Builds a `HestonParams` from its fields.
    #[new]
    #[pyo3(signature = (mu, kappa, theta, xi, rho))]
    fn __new__(mu: f64, kappa: f64, theta: f64, xi: f64, rho: f64) -> Self {

        Self { inner: rust_physics_engine::stochastic::sde::HestonParams { mu: mu, kappa: kappa, theta: theta, xi: xi, rho: rho } }
    }

    #[getter]
    #[pyo3(name = "mu")]
    fn py_get_mu(&self) -> PyResult<f64> { Ok(self.inner.mu) }

    #[setter]
    #[pyo3(name = "mu")]
    fn py_set_mu(&mut self, v: f64) { self.inner.mu = v; }

    #[getter]
    #[pyo3(name = "kappa")]
    fn py_get_kappa(&self) -> PyResult<f64> { Ok(self.inner.kappa) }

    #[setter]
    #[pyo3(name = "kappa")]
    fn py_set_kappa(&mut self, v: f64) { self.inner.kappa = v; }

    #[getter]
    #[pyo3(name = "theta")]
    fn py_get_theta(&self) -> PyResult<f64> { Ok(self.inner.theta) }

    #[setter]
    #[pyo3(name = "theta")]
    fn py_set_theta(&mut self, v: f64) { self.inner.theta = v; }

    #[getter]
    #[pyo3(name = "xi")]
    fn py_get_xi(&self) -> PyResult<f64> { Ok(self.inner.xi) }

    #[setter]
    #[pyo3(name = "xi")]
    fn py_set_xi(&mut self, v: f64) { self.inner.xi = v; }

    #[getter]
    #[pyo3(name = "rho")]
    fn py_get_rho(&self) -> PyResult<f64> { Ok(self.inner.rho) }

    #[setter]
    #[pyo3(name = "rho")]
    fn py_set_rho(&mut self, v: f64) { self.inner.rho = v; }

    fn __repr__(&self) -> String { format!("HestonParams(mu={:?}, kappa={:?}, theta={:?}, xi={:?}, rho={:?})", self.inner.mu, self.inner.kappa, self.inner.theta, self.inner.xi, self.inner.rho) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A `HestonParams` argument, or anything that can stand in for one.
pub struct PyHestonParamsArg(pub rust_physics_engine::stochastic::sde::HestonParams);

impl<'a, 'py> pyo3::FromPyObject<'a, 'py> for PyHestonParamsArg {
    type Error = pyo3::PyErr;

    fn extract(obj: pyo3::Borrowed<'a, 'py, pyo3::PyAny>) -> Result<Self, pyo3::PyErr> {
        if let Ok(__w) = obj.extract::<PyHestonParams>() {
            return Ok(PyHestonParamsArg(__w.inner));
        }
        let __v = crate::runtime::coerce::floats_exact(obj, 5, "HestonParams")?;
        Ok(PyHestonParamsArg(rust_physics_engine::stochastic::sde::HestonParams { mu: __v[0], kappa: __v[1], theta: __v[2], xi: __v[3], rho: __v[4] }))
    }
}


/// An ARIMA model: an `Arma` fitted to the `d`-th difference.
///
/// Rust: `stochastic::timeseries::Arima`
#[pyclass(name = "Arima", module = "numeria.stochastic.timeseries", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyArima { pub inner: rust_physics_engine::stochastic::timeseries::Arima }
#[pymethods]
impl PyArima {
    /// Differences `d` times, then fits an ARMA(`p`, `q`) by conditional sum
    /// of squares.
    ///
    /// Errors:
    /// Returns an error if the series is too short or the ARMA fit fails.
    ///
    /// Rust: `stochastic::timeseries::Arima::fit`
    #[pyo3(name = "fit")]
    #[staticmethod]
    #[pyo3(signature = (x, p, d, q))]
    fn fit(x: Vec<f64>, p: usize, d: usize, q: usize) -> PyResult<crate::generated::types::PyArima> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::timeseries::Arima::fit(&x, p, d, q));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyArima { inner: __v })
    }

    /// `h`-step forecasts on the original scale, with standard errors.
    ///
    /// The ARMA part forecasts the differenced series; integrating those
    /// forecasts back up is a cumulative sum, so the errors accumulate too --
    /// the standard error of the `h`-step forecast of an integrated series is
    /// the norm of the *partial sums* of the psi weights, not of the weights
    /// themselves. That is why an ARIMA forecast interval keeps widening
    /// without bound while a stationary ARMA one levels off.
    ///
    /// Panics:
    /// Panics if `h` is zero.
    ///
    /// Rust: `stochastic::timeseries::Arima::forecast`
    #[pyo3(name = "forecast")]
    #[pyo3(signature = (h))]
    fn forecast(&self, h: usize) -> PyResult<(Vec<f64>, Vec<f64>)> {
        let __r = crate::runtime::guard(|| self.inner.forecast(h));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    #[getter]
    #[pyo3(name = "d")]
    fn py_get_d(&self) -> PyResult<usize> { Ok(self.inner.d) }

    #[setter]
    #[pyo3(name = "d")]
    fn py_set_d(&mut self, v: usize) { self.inner.d = v; }

    #[getter]
    #[pyo3(name = "arma")]
    fn py_get_arma(&self) -> PyResult<crate::generated::types::PyArma> { Ok(crate::generated::types::PyArma { inner: self.inner.arma.clone() }) }

    #[getter]
    #[pyo3(name = "initial")]
    fn py_get_initial(&self) -> PyResult<Vec<f64>> { Ok(self.inner.initial.clone()) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Arima", "Arima", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// An autoregressive moving-average model.
///
/// The process is written around its mean:
/// `(x_t - mu) = sum_i phi_i (x_{t-i} - mu) + e_t + sum_j theta_j e_{t-j}`,
/// with `e_t` white noise of variance `sigma2`. The sign convention on the
/// moving-average side is the additive one, matching the Box-Jenkins form.
///
/// Rust: `stochastic::timeseries::Arma`
#[pyclass(name = "Arma", module = "numeria.stochastic.timeseries", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyArma { pub inner: rust_physics_engine::stochastic::timeseries::Arma }
#[pymethods]
impl PyArma {
    /// A model with the given coefficients.
    ///
    /// Rust: `stochastic::timeseries::Arma::new`
    #[new]
    #[pyo3(signature = (ar, ma, sigma2, mean))]
    fn __new__(ar: Vec<f64>, ma: Vec<f64>, sigma2: f64, mean: f64) -> PyResult<crate::generated::types::PyArma> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::timeseries::Arma::new(ar, ma, sigma2, mean));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(crate::generated::types::PyArma { inner: __v })
    }

    /// Autoregressive order.
    ///
    /// Rust: `stochastic::timeseries::Arma::p`
    #[pyo3(name = "p")]
    #[pyo3(signature = ())]
    fn p(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.p());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Moving-average order.
    ///
    /// Rust: `stochastic::timeseries::Arma::q`
    #[pyo3(name = "q")]
    #[pyo3(signature = ())]
    fn q(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.q());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The conditional innovations implied by the model and the data.
    ///
    /// Pre-sample observations are replaced by the mean and pre-sample
    /// innovations by zero, which is the "conditional" in conditional sum of
    /// squares. The first few residuals therefore carry that assumption, and
    /// its influence dies out at the rate the moving-average part is
    /// invertible.
    ///
    /// Rust: `stochastic::timeseries::Arma::residuals`
    #[pyo3(name = "residuals")]
    #[pyo3(signature = (x))]
    fn residuals<'py>(&self, py: Python<'py>, x: Vec<f64>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.residuals(&x)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Fits by conditional sum of squares: choose the mean and the
    /// coefficients that make the conditional innovations as small as
    /// possible in the least-squares sense.
    ///
    /// The innovations *are* the residual vector, so this is a plain
    /// nonlinear least-squares problem and Levenberg-Marquardt solves it
    /// directly. `sigma2` is then the residual mean square on `n - k` degrees
    /// of freedom.
    ///
    /// Errors:
    /// Returns an error if the series is too short or the optimiser fails to
    /// converge.
    ///
    /// Rust: `stochastic::timeseries::Arma::fit_css`
    #[pyo3(name = "fit_css")]
    #[staticmethod]
    #[pyo3(signature = (x, p, q))]
    fn fit_css(x: Vec<f64>, p: usize, q: usize) -> PyResult<crate::generated::types::PyArma> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::timeseries::Arma::fit_css(&x, p, q));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyArma { inner: __v })
    }

    /// Fits by the Hannan-Rissanen two-stage procedure.
    ///
    /// A long autoregression approximates the innovations, and those
    /// estimated innovations then enter a second regression as if they were
    /// observed, turning a nonlinear problem into two linear ones. It costs
    /// some efficiency against `Arma::fit_css` but needs no starting values
    /// and no iteration, which makes it a good source of starting values.
    ///
    /// Errors:
    /// Returns an error if the series is too short or either regression is
    /// rank deficient.
    ///
    /// Rust: `stochastic::timeseries::Arma::fit_hannan_rissanen`
    #[pyo3(name = "fit_hannan_rissanen")]
    #[staticmethod]
    #[pyo3(signature = (x, p, q))]
    fn fit_hannan_rissanen(x: Vec<f64>, p: usize, q: usize) -> PyResult<crate::generated::types::PyArma> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::timeseries::Arma::fit_hannan_rissanen(&x, p, q));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyArma { inner: __v })
    }

    /// Generates `n` observations, discarding a burn-in long enough for the
    /// transient from the zero start to decay.
    ///
    /// Panics:
    /// Panics if `n` is zero or `sigma2` is negative.
    ///
    /// Rust: `stochastic::timeseries::Arma::simulate`
    #[pyo3(name = "simulate")]
    #[pyo3(signature = (n, rng))]
    fn simulate(&self, n: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<f64>> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.simulate(n, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The impulse-response (psi) weights: the coefficients of the model's
    /// infinite moving-average representation.
    ///
    /// `psi_0 = 1` and `psi_j = theta_j + sum_i phi_i psi_{j-i}`. These are
    /// the single most useful derived quantity in the module -- forecast
    /// error variances, the autocovariances, and the spectral density are all
    /// expressible in them.
    ///
    /// Returns `n` weights, `psi_0` first.
    ///
    /// Panics:
    /// Panics if `n` is zero.
    ///
    /// Rust: `stochastic::timeseries::Arma::impulse_response`
    #[pyo3(name = "impulse_response")]
    #[pyo3(signature = (n))]
    fn impulse_response<'py>(&self, py: Python<'py>, n: usize) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.impulse_response(n)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The spectral density at each supplied angular frequency.
    ///
    /// `f(w) = (sigma2 / 2 pi) |theta(e^{-iw})|^2 / |phi(e^{-iw})|^2`. It
    /// integrates over `[-pi, pi]` to the process variance, which is the
    /// frequency-domain statement of the same second-order structure the
    /// autocovariances describe.
    ///
    /// Rust: `stochastic::timeseries::Arma::spectral_density`
    #[pyo3(name = "spectral_density")]
    #[pyo3(signature = (freqs))]
    fn spectral_density<'py>(&self, py: Python<'py>, freqs: Vec<f64>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.spectral_density(&freqs)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// `(stationary, invertible)`.
    ///
    /// Stationarity asks that every root of `1 - phi_1 z - ... - phi_p z^p`
    /// lie outside the unit circle; invertibility asks the same of
    /// `1 + theta_1 z + ... + theta_q z^q`. An empty side is trivially both.
    ///
    /// A root exactly on the circle counts as failing, since the boundary is
    /// where stationarity breaks down.
    ///
    /// Rust: `stochastic::timeseries::Arma::roots_check`
    #[pyo3(name = "roots_check")]
    #[pyo3(signature = ())]
    fn roots_check(&self) -> PyResult<(bool, bool)> {
        let __r = crate::runtime::guard(|| self.inner.roots_check());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    /// The conditional (Gaussian) log-likelihood of `x` under this model.
    ///
    /// Conditional because the pre-sample values are fixed rather than
    /// integrated out; it is the quantity `Arma::fit_css` maximises, and the
    /// one `Arma::aic` and `Arma::bic` penalise.
    ///
    /// Rust: `stochastic::timeseries::Arma::log_likelihood`
    #[pyo3(name = "log_likelihood")]
    #[pyo3(signature = (x))]
    fn log_likelihood<'py>(&self, py: Python<'py>, x: Vec<f64>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.log_likelihood(&x)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Number of free parameters: the coefficients, the mean, and the
    /// innovation variance.
    ///
    /// Rust: `stochastic::timeseries::Arma::n_params`
    #[pyo3(name = "n_params")]
    #[pyo3(signature = ())]
    fn n_params(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.n_params());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Akaike's information criterion, `-2 ln L + 2k`. Lower is better.
    ///
    /// Rust: `stochastic::timeseries::Arma::aic`
    #[pyo3(name = "aic")]
    #[pyo3(signature = (x))]
    fn aic<'py>(&self, py: Python<'py>, x: Vec<f64>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.aic(&x)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The Bayesian information criterion, `-2 ln L + k ln n`. Penalises
    /// extra parameters harder than `Arma::aic` for any sample past
    /// `n = e^2`, so it selects more parsimonious models.
    ///
    /// Rust: `stochastic::timeseries::Arma::bic`
    #[pyo3(name = "bic")]
    #[pyo3(signature = (x))]
    fn bic<'py>(&self, py: Python<'py>, x: Vec<f64>) -> PyResult<f64> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.bic(&x)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// `h`-step-ahead forecasts and their standard errors.
    ///
    /// Point forecasts run the model recursion forward with future
    /// innovations set to their expectation of zero. The standard errors are
    /// `sigma sqrt(sum_{j<h} psi_j^2)`, which grows with the horizon and, for
    /// a stationary model, converges to the process standard deviation --
    /// beyond the memory of the process the best forecast is the mean and the
    /// uncertainty is the unconditional one.
    ///
    /// Panics:
    /// Panics if `h` is zero or `x` is empty.
    ///
    /// Rust: `stochastic::timeseries::Arma::forecast`
    #[pyo3(name = "forecast")]
    #[pyo3(signature = (x, h))]
    fn forecast<'py>(&self, py: Python<'py>, x: Vec<f64>, h: usize) -> PyResult<(Vec<f64>, Vec<f64>)> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.forecast(&x, h)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok((__v.0, __v.1))
    }

    #[getter]
    #[pyo3(name = "ar")]
    fn py_get_ar(&self) -> PyResult<Vec<f64>> { Ok(self.inner.ar.clone()) }

    #[getter]
    #[pyo3(name = "ma")]
    fn py_get_ma(&self) -> PyResult<Vec<f64>> { Ok(self.inner.ma.clone()) }

    #[getter]
    #[pyo3(name = "sigma2")]
    fn py_get_sigma2(&self) -> PyResult<f64> { Ok(self.inner.sigma2) }

    #[setter]
    #[pyo3(name = "sigma2")]
    fn py_set_sigma2(&mut self, v: f64) { self.inner.sigma2 = v; }

    #[getter]
    #[pyo3(name = "mean")]
    fn py_get_mean(&self) -> PyResult<f64> { Ok(self.inner.mean) }

    #[setter]
    #[pyo3(name = "mean")]
    fn py_set_mean(&mut self, v: f64) { self.inner.mean = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Arma", "Arma", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A GARCH(1,1) volatility model:
/// `sigma_t^2 = omega + alpha r_{t-1}^2 + beta sigma_{t-1}^2`.
///
/// The single most used model in the family, because two parameters are
/// enough to reproduce the two features that matter: volatility clusters, and
/// it mean-reverts. `alpha + beta` is the persistence, and the model is
/// stationary only while that sum is below one.
///
/// Rust: `stochastic::timeseries::Garch11`
#[pyclass(name = "Garch11", module = "numeria.stochastic.timeseries", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyGarch11 { pub inner: rust_physics_engine::stochastic::timeseries::Garch11 }
#[pymethods]
impl PyGarch11 {
    /// Builds a `Garch11` from its fields.
    #[new]
    #[pyo3(signature = (omega, alpha, beta))]
    fn __new__(omega: f64, alpha: f64, beta: f64) -> Self {

        Self { inner: rust_physics_engine::stochastic::timeseries::Garch11 { omega: omega, alpha: alpha, beta: beta } }
    }

    /// `alpha + beta`: how much of today's variance shock survives to
    /// tomorrow. At 1 the process has a unit root in variance and no
    /// unconditional variance exists.
    ///
    /// Rust: `stochastic::timeseries::Garch11::persistence`
    #[pyo3(name = "persistence")]
    #[pyo3(signature = ())]
    fn persistence(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.persistence());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// `omega / (1 - alpha - beta)`, the level volatility reverts to.
    ///
    /// Infinite once persistence reaches one.
    ///
    /// Rust: `stochastic::timeseries::Garch11::unconditional_variance`
    #[pyo3(name = "unconditional_variance")]
    #[pyo3(signature = ())]
    fn unconditional_variance(&self) -> PyResult<f64> {
        let __r = crate::runtime::guard(|| self.inner.unconditional_variance());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// The filtered conditional variance for each return, seeded at the
    /// unconditional variance where one exists and at the sample variance
    /// otherwise.
    ///
    /// Panics:
    /// Panics if `returns` is empty.
    ///
    /// Rust: `stochastic::timeseries::Garch11::conditional_variance`
    #[pyo3(name = "conditional_variance")]
    #[pyo3(signature = (returns))]
    fn conditional_variance<'py>(&self, py: Python<'py>, returns: Vec<f64>) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.conditional_variance(&returns)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Fits by maximising the Gaussian quasi-likelihood over a Nelder-Mead
    /// simplex, parameterised so the constraints hold by construction.
    ///
    /// `omega` is optimised on the log scale, keeping it positive, and
    /// `(alpha, beta)` through a softmax-style map onto the simplex
    /// `alpha, beta > 0`, `alpha + beta < 1`, which is where the model is
    /// stationary. An unconstrained fit routinely wanders to a negative
    /// variance, where the likelihood is not merely bad but undefined.
    ///
    /// Errors:
    /// Returns an error for a series too short to identify three parameters.
    ///
    /// Rust: `stochastic::timeseries::Garch11::fit`
    #[pyo3(name = "fit")]
    #[staticmethod]
    #[pyo3(signature = (returns))]
    fn fit(returns: Vec<f64>) -> PyResult<crate::generated::types::PyGarch11> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::timeseries::Garch11::fit(&returns));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyGarch11 { inner: __v })
    }

    /// Simulates `n` returns with Gaussian innovations.
    ///
    /// Panics:
    /// Panics if `n` is zero or the parameters are not non-negative.
    ///
    /// Rust: `stochastic::timeseries::Garch11::simulate`
    #[pyo3(name = "simulate")]
    #[pyo3(signature = (n, rng))]
    fn simulate(&self, n: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<f64>> {
        let mut rng = rng;
        let __r = crate::runtime::guard(|| self.inner.simulate(n, &mut rng.inner));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Variance forecasts `1..=h` steps ahead from the end of `returns`.
    ///
    /// Each step pulls the forecast toward the unconditional variance at rate
    /// `persistence`, so the sequence is monotone and converges there
    /// geometrically.
    ///
    /// Panics:
    /// Panics if `h` is zero or `returns` is empty.
    ///
    /// Rust: `stochastic::timeseries::Garch11::forecast_variance`
    #[pyo3(name = "forecast_variance")]
    #[pyo3(signature = (returns, h))]
    fn forecast_variance<'py>(&self, py: Python<'py>, returns: Vec<f64>, h: usize) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.forecast_variance(&returns, h)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "omega")]
    fn py_get_omega(&self) -> PyResult<f64> { Ok(self.inner.omega) }

    #[setter]
    #[pyo3(name = "omega")]
    fn py_set_omega(&mut self, v: f64) { self.inner.omega = v; }

    #[getter]
    #[pyo3(name = "alpha")]
    fn py_get_alpha(&self) -> PyResult<f64> { Ok(self.inner.alpha) }

    #[setter]
    #[pyo3(name = "alpha")]
    fn py_set_alpha(&mut self, v: f64) { self.inner.alpha = v; }

    #[getter]
    #[pyo3(name = "beta")]
    fn py_get_beta(&self) -> PyResult<f64> { Ok(self.inner.beta) }

    #[setter]
    #[pyo3(name = "beta")]
    fn py_set_beta(&mut self, v: f64) { self.inner.beta = v; }

    fn __repr__(&self) -> String { format!("Garch11(omega={:?}, alpha={:?}, beta={:?})", self.inner.omega, self.inner.alpha, self.inner.beta) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A `Garch11` argument, or anything that can stand in for one.
pub struct PyGarch11Arg(pub rust_physics_engine::stochastic::timeseries::Garch11);

impl<'a, 'py> pyo3::FromPyObject<'a, 'py> for PyGarch11Arg {
    type Error = pyo3::PyErr;

    fn extract(obj: pyo3::Borrowed<'a, 'py, pyo3::PyAny>) -> Result<Self, pyo3::PyErr> {
        if let Ok(__w) = obj.extract::<PyGarch11>() {
            return Ok(PyGarch11Arg(__w.inner));
        }
        let __v = crate::runtime::coerce::floats_exact(obj, 3, "Garch11")?;
        Ok(PyGarch11Arg(rust_physics_engine::stochastic::timeseries::Garch11 { omega: __v[0], alpha: __v[1], beta: __v[2] }))
    }
}


/// The smoothing state left behind by `holt_winters`, enough to continue
/// the recursion or to forecast forward.
///
/// Rust: `stochastic::timeseries::HwState`
#[pyclass(name = "HwState", module = "numeria.stochastic.timeseries", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyHwState { pub inner: rust_physics_engine::stochastic::timeseries::HwState }
#[pymethods]
impl PyHwState {
    /// Builds a `HwState` from its fields.
    #[new]
    #[pyo3(signature = (level, trend, seasonal, multiplicative))]
    fn __new__(level: f64, trend: f64, seasonal: Vec<f64>, multiplicative: bool) -> Self {

        Self { inner: rust_physics_engine::stochastic::timeseries::HwState { level: level, trend: trend, seasonal: seasonal, multiplicative: multiplicative } }
    }

    /// `h`-step forecasts continuing from this state.
    ///
    /// Panics:
    /// Panics if `h` is zero or the seasonal vector is empty.
    ///
    /// Rust: `stochastic::timeseries::HwState::forecast`
    #[pyo3(name = "forecast")]
    #[pyo3(signature = (h))]
    fn forecast<'py>(&self, py: Python<'py>, h: usize) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.forecast(h)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "level")]
    fn py_get_level(&self) -> PyResult<f64> { Ok(self.inner.level) }

    #[setter]
    #[pyo3(name = "level")]
    fn py_set_level(&mut self, v: f64) { self.inner.level = v; }

    #[getter]
    #[pyo3(name = "trend")]
    fn py_get_trend(&self) -> PyResult<f64> { Ok(self.inner.trend) }

    #[setter]
    #[pyo3(name = "trend")]
    fn py_set_trend(&mut self, v: f64) { self.inner.trend = v; }

    #[getter]
    #[pyo3(name = "seasonal")]
    fn py_get_seasonal(&self) -> PyResult<Vec<f64>> { Ok(self.inner.seasonal.clone()) }

    #[getter]
    #[pyo3(name = "multiplicative")]
    fn py_get_multiplicative(&self) -> PyResult<bool> { Ok(self.inner.multiplicative) }

    #[setter]
    #[pyo3(name = "multiplicative")]
    fn py_set_multiplicative(&mut self, v: bool) { self.inner.multiplicative = v; }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("HwState", "HwState", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A seasonal ARIMA model, `(p, d, q) x (P, D, Q)_s`.
///
/// Fitted by applying the seasonal difference `D` times and the ordinary
/// difference `d` times, then estimating the non-seasonal and seasonal
/// polynomials on the doubly differenced series. The seasonal part is modelled
/// as an ARMA in lags that are multiples of `s`.
///
/// Rust: `stochastic::timeseries::Sarima`
#[pyclass(name = "Sarima", module = "numeria.stochastic.timeseries", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PySarima { pub inner: rust_physics_engine::stochastic::timeseries::Sarima }
#[pymethods]
impl PySarima {
    /// Fits a `(p, d, q) x (P, D, Q)_s` model.
    ///
    /// The combined autoregressive polynomial has non-zero coefficients at
    /// lags `1..=p` and at `s, 2s, ...` up to `P s`; the moving-average side
    /// likewise. Cross-product terms of the multiplicative form are omitted,
    /// which makes this the additive rather than the strictly multiplicative
    /// SARIMA -- the difference is second order and the additive form is what
    /// conditional least squares can identify without a much longer series.
    ///
    /// Errors:
    /// Returns an error if the series is too short after differencing or the
    /// fit fails.
    ///
    /// Rust: `stochastic::timeseries::Sarima::fit`
    #[pyo3(name = "fit")]
    #[staticmethod]
    #[pyo3(signature = (x, p, d, q, seasonal_p, seasonal_d, seasonal_q, s))]
    fn fit(x: Vec<f64>, p: usize, d: usize, q: usize, seasonal_p: usize, seasonal_d: usize, seasonal_q: usize, s: usize) -> PyResult<crate::generated::types::PySarima> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::timeseries::Sarima::fit(&x, p, d, q, seasonal_p, seasonal_d, seasonal_q, s));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PySarima { inner: __v })
    }

    /// `h`-step forecasts on the original scale.
    ///
    /// Panics:
    /// Panics if `h` is zero.
    ///
    /// Rust: `stochastic::timeseries::Sarima::forecast`
    #[pyo3(name = "forecast")]
    #[pyo3(signature = (h))]
    fn forecast<'py>(&self, py: Python<'py>, h: usize) -> PyResult<Vec<f64>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.forecast(h)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    #[getter]
    #[pyo3(name = "d")]
    fn py_get_d(&self) -> PyResult<usize> { Ok(self.inner.d) }

    #[setter]
    #[pyo3(name = "d")]
    fn py_set_d(&mut self, v: usize) { self.inner.d = v; }

    #[getter]
    #[pyo3(name = "seasonal_d")]
    fn py_get_seasonal_d(&self) -> PyResult<usize> { Ok(self.inner.seasonal_d) }

    #[setter]
    #[pyo3(name = "seasonal_d")]
    fn py_set_seasonal_d(&mut self, v: usize) { self.inner.seasonal_d = v; }

    #[getter]
    #[pyo3(name = "s")]
    fn py_get_s(&self) -> PyResult<usize> { Ok(self.inner.s) }

    #[setter]
    #[pyo3(name = "s")]
    fn py_set_s(&mut self, v: usize) { self.inner.s = v; }

    #[getter]
    #[pyo3(name = "arma")]
    fn py_get_arma(&self) -> PyResult<crate::generated::types::PyArma> { Ok(crate::generated::types::PyArma { inner: self.inner.arma.clone() }) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Sarima", "Sarima", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}

/// A vector autoregression: each series regressed on `p` lags of every series.
///
/// Rust: `stochastic::timeseries::Var`
#[pyclass(name = "Var", module = "numeria.stochastic.timeseries", from_py_object, eq)]
#[derive(Clone, PartialEq)]
pub struct PyVar { pub inner: rust_physics_engine::stochastic::timeseries::Var }
#[pymethods]
impl PyVar {
    /// Number of series.
    ///
    /// Rust: `stochastic::timeseries::Var::k`
    #[pyo3(name = "k")]
    #[pyo3(signature = ())]
    fn k(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.k());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Lag order.
    ///
    /// Rust: `stochastic::timeseries::Var::p`
    #[pyo3(name = "p")]
    #[pyo3(signature = ())]
    fn p(&self) -> PyResult<usize> {
        let __r = crate::runtime::guard(|| self.inner.p());
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v)
    }

    /// Fits by equation-by-equation least squares.
    ///
    /// Every equation has the same right-hand side, so the seemingly
    /// unrelated regression collapses to ordinary least squares run
    /// separately -- there is nothing to gain from estimating them jointly.
    ///
    /// `data[t]` holds all series at time `t`.
    ///
    /// Errors:
    /// Returns an error if the series are ragged, too short, or the design is
    /// rank deficient.
    ///
    /// Rust: `stochastic::timeseries::Var::fit`
    #[pyo3(name = "fit")]
    #[staticmethod]
    #[pyo3(signature = (data, p))]
    fn fit(data: Vec<Vec<f64>>, p: usize) -> PyResult<crate::generated::types::PyVar> {
        let __r = crate::runtime::guard(|| rust_physics_engine::stochastic::timeseries::Var::fit(&data, p));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyVar { inner: __v })
    }

    /// `h`-step forecasts, each row one time step.
    ///
    /// Errors:
    /// Returns an error if `data` is too short or shaped wrongly.
    ///
    /// Rust: `stochastic::timeseries::Var::forecast`
    #[pyo3(name = "forecast")]
    #[pyo3(signature = (data, h))]
    fn forecast<'py>(&self, py: Python<'py>, data: Vec<Vec<f64>>, h: usize) -> PyResult<Vec<Vec<f64>>> {
        let __r = py.detach(move || crate::runtime::guard(move || self.inner.forecast(&data, h)));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(__v)
    }

    /// The moving-average (impulse-response) matrices `Psi_0 ..= Psi_{h}`.
    ///
    /// `Psi_0` is the identity and `Psi_m = sum_l A_l Psi_{m-l}`. Entry
    /// `(r, c)` of `Psi_m` is the response of series `r` at horizon `m` to a
    /// unit shock in series `c` now.
    ///
    /// Panics:
    /// Panics if the coefficient matrices are not square and conformable.
    ///
    /// Rust: `stochastic::timeseries::Var::impulse_response`
    #[pyo3(name = "impulse_response")]
    #[pyo3(signature = (h))]
    fn impulse_response(&self, h: usize) -> PyResult<Vec<crate::generated::types::PyMatrix>> {
        let __r = crate::runtime::guard(|| self.inner.impulse_response(h));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        Ok(__v.into_iter().map(|__x| crate::generated::types::PyMatrix { inner: __x }).collect::<Vec<_>>())
    }

    /// A matrix of Granger-causality p-values: entry `(i, j)` tests whether
    /// series `j` helps predict series `i` given the rest of the system.
    ///
    /// The diagonal is set to 1: a series always predicts itself, so the
    /// question is not meaningful there.
    ///
    /// Errors:
    /// Returns an error if a restricted regression is degenerate.
    ///
    /// Rust: `stochastic::timeseries::Var::granger_matrix`
    #[pyo3(name = "granger_matrix")]
    #[pyo3(signature = (data))]
    fn granger_matrix(&self, data: Vec<Vec<f64>>) -> PyResult<crate::generated::types::PyMatrix> {
        let __r = crate::runtime::guard(|| self.inner.granger_matrix(&data));
        let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
        let __v = __v.map_err(crate::runtime::map_geom)?;
        Ok(crate::generated::types::PyMatrix { inner: __v })
    }

    #[getter]
    #[pyo3(name = "coeffs")]
    fn py_get_coeffs(&self) -> PyResult<Vec<crate::generated::types::PyMatrix>> { Ok(self.inner.coeffs.clone().into_iter().map(|__x| crate::generated::types::PyMatrix { inner: __x }).collect::<Vec<_>>()) }

    #[getter]
    #[pyo3(name = "intercept")]
    fn py_get_intercept(&self) -> PyResult<Vec<f64>> { Ok(self.inner.intercept.clone()) }

    fn __repr__(&self) -> String { format!("{:?}", self.inner).replacen("Var", "Var", 1) }

    fn __copy__(&self) -> Self { self.clone() }

    #[pyo3(signature = (_memo=None))]
    fn __deepcopy__(&self, _memo: Option<pyo3::Py<pyo3::PyAny>>) -> Self { self.clone() }
}
