// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// Generates an RSA modulus and exponent pair: `(n, e, d)`.
///
/// Two primes of about `bits / 2` each are drawn, `n` is their product, and
/// `d` inverts `e` modulo the Carmichael function of `n` -- the exponent of
/// the multiplicative group, which is the least value that works and so gives
/// the smallest `d`. The public exponent is 65537, whose binary form has two
/// set bits and therefore encrypts in seventeen squarings.
///
/// Panics:
/// Panics unless `bits` is between 16 and 2048. Anything in that range is far
/// too small to protect anything.
///
/// Rust: `codes::crypto_math::rsa_keygen`
#[pyfunction]
#[pyo3(name = "rsa_keygen", signature = (bits, rng))]
pub fn pyfn_rsa_keygen<'py>(py: Python<'py>, bits: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<(pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>)> {
    let mut rng = rng;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::rsa_keygen(bits, &mut rng.inner));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((crate::runtime::coerce::bigint_out(py, &__v.0)?, crate::runtime::coerce::bigint_out(py, &__v.1)?, crate::runtime::coerce::bigint_out(py, &__v.2)?))
}

/// Generates a key and keeps the primes, which the Chinese remainder form of
/// decryption needs.
///
/// Panics:
/// Panics unless `bits` is between 16 and 2048.
///
/// Rust: `codes::crypto_math::rsa_keygen_with_primes`
#[pyfunction]
#[pyo3(name = "rsa_keygen_with_primes", signature = (bits, rng))]
pub fn pyfn_rsa_keygen_with_primes<'py>(py: Python<'py>, bits: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<(pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>)> {
    let mut rng = rng;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::rsa_keygen_with_primes(bits, &mut rng.inner));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((crate::runtime::coerce::bigint_out(py, &__v.0)?, crate::runtime::coerce::bigint_out(py, &__v.1)?, crate::runtime::coerce::bigint_out(py, &__v.2)?, crate::runtime::coerce::bigint_out(py, &__v.3)?, crate::runtime::coerce::bigint_out(py, &__v.4)?))
}

/// Textbook RSA encryption: `m^e` modulo `n`.
///
/// Deterministic, and therefore not a secure encryption scheme on its own --
/// the same message always gives the same ciphertext, so an attacker who can
/// guess the plaintext can confirm the guess. Real use pads the message with
/// randomness first.
///
/// Rust: `codes::crypto_math::rsa_encrypt`
#[pyfunction]
#[pyo3(name = "rsa_encrypt", signature = (m, e, n))]
pub fn pyfn_rsa_encrypt<'py>(py: Python<'py>, m: crate::runtime::coerce::BigIntArg, e: crate::runtime::coerce::BigIntArg, n: crate::runtime::coerce::BigIntArg) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let m = m.0;
    let e = e.0;
    let n = n.0;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::rsa_encrypt(&m, &e, &n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// Textbook RSA decryption: `c^d` modulo `n`.
///
/// Rust: `codes::crypto_math::rsa_decrypt`
#[pyfunction]
#[pyo3(name = "rsa_decrypt", signature = (c, d, n))]
pub fn pyfn_rsa_decrypt<'py>(py: Python<'py>, c: crate::runtime::coerce::BigIntArg, d: crate::runtime::coerce::BigIntArg, n: crate::runtime::coerce::BigIntArg) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let c = c.0;
    let d = d.0;
    let n = n.0;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::rsa_decrypt(&c, &d, &n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// Decryption through the Chinese remainder theorem, given the two primes.
///
/// Working modulo `p` and `q` separately and recombining costs about a
/// quarter of the work, since modular exponentiation is cubic in the operand
/// size and the operands are half as long. Every real implementation does
/// this, which is also why a fault during one of the two halves famously
/// reveals the factorisation.
///
/// Panics:
/// Panics if `p` and `q` are not coprime, so that the recombination has no
/// inverse.
///
/// Rust: `codes::crypto_math::rsa_crt_decrypt`
#[pyfunction]
#[pyo3(name = "rsa_crt_decrypt", signature = (c, d, p, q))]
pub fn pyfn_rsa_crt_decrypt<'py>(py: Python<'py>, c: crate::runtime::coerce::BigIntArg, d: crate::runtime::coerce::BigIntArg, p: crate::runtime::coerce::BigIntArg, q: crate::runtime::coerce::BigIntArg) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let c = c.0;
    let d = d.0;
    let p = p.0;
    let q = q.0;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::rsa_crt_decrypt(&c, &d, &p, &q));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// A Diffie-Hellman exchange in full: both parties' key pairs and the shared
/// secret they arrive at.
///
/// Returns `((a, A), (b, B), s)` where `A = g^a`, `B = g^b` and
/// `s = B^a = A^b`, all modulo `p`. The exchange works because
/// exponentiation commutes; it is secure only if recovering `a` from `g^a` is
/// hard, which needs `p` to be a large safe prime and `g` to generate a large
/// subgroup. Neither is checked here.
///
/// Panics:
/// Panics unless `p` is at least three.
///
/// Rust: `codes::crypto_math::diffie_hellman_demo`
#[pyfunction]
#[pyo3(name = "diffie_hellman_demo", signature = (p, g, rng))]
pub fn pyfn_diffie_hellman_demo<'py>(py: Python<'py>, p: crate::runtime::coerce::BigIntArg, g: crate::runtime::coerce::BigIntArg, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<((pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>), (pyo3::Bound<'py, pyo3::PyAny>, pyo3::Bound<'py, pyo3::PyAny>), pyo3::Bound<'py, pyo3::PyAny>)> {
    let p = p.0;
    let g = g.0;
    let mut rng = rng;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::diffie_hellman_demo(&p, &g, &mut rng.inner));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(((crate::runtime::coerce::bigint_out(py, &__v.0.0)?, crate::runtime::coerce::bigint_out(py, &__v.0.1)?), (crate::runtime::coerce::bigint_out(py, &__v.1.0)?, crate::runtime::coerce::bigint_out(py, &__v.1.1)?), crate::runtime::coerce::bigint_out(py, &__v.2)?))
}

/// An elliptic curve Diffie-Hellman exchange in full.
///
/// Returns `((a, aG), (b, bG), s)`. The same construction as the
/// multiplicative version, in a group where the best known attack is
/// square-root time rather than sub-exponential -- which is why a 256-bit
/// curve stands against a 3072-bit modulus.
///
/// Panics:
/// Panics if the base point is not on the curve.
///
/// Rust: `codes::crypto_math::ecdh_demo`
#[pyfunction]
#[pyo3(name = "ecdh_demo", signature = (curve, g, order, rng))]
pub fn pyfn_ecdh_demo<'py>(py: Python<'py>, curve: crate::generated::types::PyEcCurve, g: crate::generated::types::PyEcPoint, order: crate::runtime::coerce::BigIntArg, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<((pyo3::Bound<'py, pyo3::PyAny>, crate::generated::types::PyEcPoint), (pyo3::Bound<'py, pyo3::PyAny>, crate::generated::types::PyEcPoint), crate::generated::types::PyEcPoint)> {
    let curve = curve.inner;
    let g = g.inner;
    let order = order.0;
    let mut rng = rng;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::ecdh_demo(&curve, &g, &order, &mut rng.inner));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(((crate::runtime::coerce::bigint_out(py, &__v.0.0)?, crate::generated::types::PyEcPoint { inner: __v.0.1 }), (crate::runtime::coerce::bigint_out(py, &__v.1.0)?, crate::generated::types::PyEcPoint { inner: __v.1.1 }), crate::generated::types::PyEcPoint { inner: __v.2 }))
}

/// The number of points on a small curve, including infinity.
///
/// Panics:
/// Panics if the field has more than a million elements.
///
/// Rust: `codes::crypto_math::ec_count_points_small`
#[pyfunction]
#[pyo3(name = "ec_count_points_small", signature = (curve))]
pub fn pyfn_ec_count_points_small(curve: crate::generated::types::PyEcCurve) -> PyResult<u64> {
    let curve = curve.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::ec_count_points_small(&curve));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether a point count satisfies Hasse's theorem.
///
/// The count lies within `2 sqrt(p)` of `p + 1`. That is a remarkably tight
/// bound -- the group is always about as large as the field, never a constant
/// factor away -- and it is what makes a curve's security predictable from
/// its field size alone.
///
/// Rust: `codes::crypto_math::hasse_bound_check`
#[pyfunction]
#[pyo3(name = "hasse_bound_check", signature = (count, p))]
pub fn pyfn_hasse_bound_check(count: u64, p: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::hasse_bound_check(count, p));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Splits a secret into `n` shares of which any `k` suffice.
///
/// The secret is the constant term of a random polynomial of degree `k - 1`
/// over `F_prime`, and a share is that polynomial's value at a non-zero
/// point. Any `k` points determine the polynomial by interpolation, and any
/// `k - 1` leave the constant term uniformly distributed -- so fewer than `k`
/// shares give not merely a hard problem but no information at all. That is
/// what makes the scheme *perfect*, and it is rare.
///
/// Panics:
/// Panics unless `1 <= k <= n`, `n` is below the prime, and the secret is a
/// non-negative residue below it.
///
/// Rust: `codes::crypto_math::shamir_split`
#[pyfunction]
#[pyo3(name = "shamir_split", signature = (secret, k, n, prime, rng))]
pub fn pyfn_shamir_split<'py>(py: Python<'py>, secret: crate::runtime::coerce::BigIntArg, k: usize, n: usize, prime: crate::runtime::coerce::BigIntArg, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<Vec<(u64, pyo3::Bound<'py, pyo3::PyAny>)>> {
    let secret = secret.0;
    let prime = prime.0;
    let mut rng = rng;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::shamir_split(&secret, k, n, &prime, &mut rng.inner));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| -> PyResult<(u64, pyo3::Bound<'py, pyo3::PyAny>)> { Ok((__x.0, crate::runtime::coerce::bigint_out(py, &__x.1)?)) }).collect::<PyResult<Vec<_>>>()?)
}

/// Recovers the secret from any `k` shares by Lagrange interpolation at zero.
///
/// Panics:
/// Panics on an empty share list, on a repeated abscissa, or if the modulus
/// is not prime enough for the required inverses to exist.
///
/// Rust: `codes::crypto_math::shamir_reconstruct`
#[pyfunction]
#[pyo3(name = "shamir_reconstruct", signature = (shares, prime))]
pub fn pyfn_shamir_reconstruct<'py>(py: Python<'py>, shares: Vec<(u64, crate::runtime::coerce::BigIntArg)>, prime: crate::runtime::coerce::BigIntArg) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let shares = shares.into_iter().map(|__e| (__e.0, __e.1.0)).collect::<Vec<_>>();
    let prime = prime.0;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::shamir_reconstruct(&shares, &prime));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// Exclusive-or of the data with a repeating key.
///
/// With a key as long as the message, drawn uniformly and never reused, this
/// is the one cipher with a proof of perfect secrecy: the ciphertext is
/// independent of the plaintext, so an adversary with unlimited computation
/// learns nothing. With a short key repeated, it is a Vigenere cipher and
/// `vigenere_break` undoes it. The gap between those two is entirely the
/// key.
///
/// Panics:
/// Panics on an empty key.
///
/// Rust: `codes::crypto_math::one_time_pad`
#[pyfunction]
#[pyo3(name = "one_time_pad", signature = (data, key))]
pub fn pyfn_one_time_pad<'py>(py: Python<'py>, data: Vec<u8>, key: Vec<u8>) -> PyResult<Vec<u8>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::one_time_pad(&data, &key)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A Fibonacci linear feedback shift register: `n` output bits from a state
/// and a tap mask.
///
/// The new bit is the parity of the tapped positions, and the register shifts
/// right. The output is a linear recurrence over `GF(2)`, which is what makes
/// it fast, and also what makes it hopeless as a cipher on its own:
/// `berlekamp_massey_attack` recovers the whole register from twice its
/// length in output.
///
/// Tap bit zero, or the step map is not reversible and the register cannot
/// reach every state -- see `lfsr_period`.
///
/// Panics:
/// Panics on a zero tap mask.
///
/// Rust: `codes::crypto_math::lfsr`
#[pyfunction]
#[pyo3(name = "lfsr", signature = (taps, state, n))]
pub fn pyfn_lfsr<'py>(py: Python<'py>, taps: u64, state: u64, n: usize) -> PyResult<Vec<bool>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::lfsr(taps, state, n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The period of a shift register of the given width, by running it until it
/// repeats.
///
/// A width-`w` register has at most `2^w - 1` states before it must repeat,
/// and reaches that only for a *primitive* tap polynomial. The all-zero state
/// is absorbing, which is why the maximum is one short of the state count.
///
/// The step map is a bijection only when bit zero is tapped: without it, the
/// outgoing bit does not influence the feedback, two states share an image,
/// and the register runs into a cycle it can never leave and never started
/// on. Returns zero in that case, meaning the register never comes back.
///
/// Rust: `codes::crypto_math::lfsr_period`
#[pyfunction]
#[pyo3(name = "lfsr_period", signature = (taps, width))]
pub fn pyfn_lfsr_period(taps: u64, width: u32) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::lfsr_period(taps, width));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Recovers the shortest linear recurrence a bit stream satisfies, as
/// `(length, taps)`.
///
/// The Berlekamp-Massey algorithm, over `GF(2)`. Given `2L` bits of output
/// from a register of length `L` it returns that register, which is why a
/// bare shift register is not a cipher: the keystream reveals the key
/// generator in time linear in its size.
///
/// Rust: `codes::crypto_math::berlekamp_massey_attack`
#[pyfunction]
#[pyo3(name = "berlekamp_massey_attack", signature = (stream))]
pub fn pyfn_berlekamp_massey_attack<'py>(py: Python<'py>, stream: Vec<bool>) -> PyResult<(u64, u64)> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::berlekamp_massey_attack(&stream)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// How close a hash comes to flipping half its output bits when one input bit
/// changes.
///
/// Returns the mean fraction of output bits that flip. A good hash sits at a
/// half: every output bit should be an unbiased, independent-looking function
/// of every input bit, so that no partial information about the input
/// survives. A value far from a half is a structural weakness a distinguisher
/// can be built from.
///
/// Panics:
/// Panics if `trials` is zero.
///
/// Rust: `codes::crypto_math::hash_avalanche_test`
#[pyfunction]
#[pyo3(name = "hash_avalanche_test", signature = (h, trials, rng))]
pub fn pyfn_hash_avalanche_test(h: pyo3::Py<pyo3::PyAny>, trials: usize, rng: pyo3::PyRefMut<'_, crate::generated::types::PyRng>) -> PyResult<f64> {
    let __cb_h = std::rc::Rc::new(crate::runtime::Callback::new(h));
    let h = { let __cb = __cb_h.clone(); move |__a0: &[u8]| -> u64 { __cb.call::<_, u64>((__a0.to_vec(),), 0) } };
    let mut rng = rng;
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::hash_avalanche_test(&h, trials, &mut rng.inner));
    crate::runtime::callback::check(&[&__cb_h], ())?;
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The number of samples at which a collision becomes likely for an output of
/// `n_bits`.
///
/// About `2^(n/2)`, up to a constant: with `k` samples there are about
/// `k^2 / 2` pairs and each collides with probability `2^-n`, so the count of
/// collisions reaches one near the square root. It is why a 128-bit hash
/// offers 64 bits of collision resistance, not 128.
///
/// Rust: `codes::crypto_math::birthday_bound`
#[pyfunction]
#[pyo3(name = "birthday_bound", signature = (n_bits))]
pub fn pyfn_birthday_bound(n_bits: u32) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::crypto_math::birthday_bound(n_bits));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The frequency of each letter, ignoring everything else, as fractions
/// summing to one.
///
/// Rust: `codes::crypto_math::frequency_analysis`
#[pyfunction]
#[pyo3(name = "frequency_analysis", signature = (text))]
pub fn pyfn_frequency_analysis<'py>(py: Python<'py>, text: Vec<u8>) -> PyResult<Vec<f64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::frequency_analysis(&text)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.to_vec())
}

/// The index of coincidence: the chance that two letters drawn at random from
/// the text are the same.
///
/// About `0.066` for English and `0.038` for a uniform jumble. Because it is
/// unchanged by a substitution -- relabelling the letters does not change how
/// often two match -- it tells a monoalphabetic cipher from a polyalphabetic
/// one without any guess about the key, which is what makes it the first
/// measurement to take.
///
/// Rust: `codes::crypto_math::index_of_coincidence`
#[pyfunction]
#[pyo3(name = "index_of_coincidence", signature = (text))]
pub fn pyfn_index_of_coincidence<'py>(py: Python<'py>, text: Vec<u8>) -> PyResult<f64> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::index_of_coincidence(&text)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Candidate key lengths from repeated trigrams, as Kasiski proposed.
///
/// A trigram repeating in the ciphertext usually means the same plaintext
/// trigram met the same stretch of key, so the gap between the two is a
/// multiple of the key length. Returns the lengths that divide the most gaps,
/// best first.
///
/// Rust: `codes::crypto_math::kasiski_examination`
#[pyfunction]
#[pyo3(name = "kasiski_examination", signature = (text))]
pub fn pyfn_kasiski_examination<'py>(py: Python<'py>, text: Vec<u8>) -> PyResult<Vec<usize>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::kasiski_examination(&text)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Caesar shift that best matches English letter frequencies.
///
/// Scored by the dot product of the observed and expected distributions,
/// which is largest when the two line up -- the same statistic as chi-squared
/// scoring, with the arithmetic the other way up.
///
/// Rust: `codes::crypto_math::caesar_break`
#[pyfunction]
#[pyo3(name = "caesar_break", signature = (text))]
pub fn pyfn_caesar_break<'py>(py: Python<'py>, text: Vec<u8>) -> PyResult<u8> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::caesar_break(&text)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The most likely Vigenere key, searching lengths up to `max_key`.
///
/// The key length is chosen by the average index of coincidence of the
/// columns -- at the true length each column is a Caesar shift of English and
/// so looks like English, and at any other length the columns are jumbled --
/// and each column is then solved as its own Caesar shift.
///
/// Panics:
/// Panics if `max_key` is zero.
///
/// Rust: `codes::crypto_math::vigenere_break`
#[pyfunction]
#[pyo3(name = "vigenere_break", signature = (text, max_key))]
pub fn pyfn_vigenere_break<'py>(py: Python<'py>, text: Vec<u8>, max_key: usize) -> PyResult<String> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::vigenere_break(&text, max_key)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.to_string())
}

/// The permutation a perfect riffle shuffle applies to `n` cards.
///
/// An *out* shuffle keeps the top card on top; an *in* shuffle pushes it to
/// second. Eight out-shuffles restore a 52-card deck and 52 in-shuffles do,
/// which is the standard demonstration that a deterministic shuffle is no
/// shuffle at all.
///
/// Panics:
/// Panics unless `n` is positive and even.
///
/// Rust: `codes::crypto_math::perfect_shuffle_permutation`
#[pyfunction]
#[pyo3(name = "perfect_shuffle_permutation", signature = (n, out))]
pub fn pyfn_perfect_shuffle_permutation<'py>(py: Python<'py>, n: usize, out: bool) -> PyResult<Vec<usize>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::perfect_shuffle_permutation(n, out)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// How many times a permutation must be applied before everything returns
/// home: the least common multiple of its cycle lengths.
///
/// Panics:
/// Panics unless the input is a permutation of `0..n`.
///
/// Rust: `codes::crypto_math::permutation_cipher_period`
#[pyfunction]
#[pyo3(name = "permutation_cipher_period", signature = (perm))]
pub fn pyfn_permutation_cipher_period<'py>(py: Python<'py>, perm: Vec<usize>) -> PyResult<u64> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::crypto_math::permutation_cipher_period(&perm)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_rsa_keygen, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_rsa_keygen_with_primes, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_rsa_encrypt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_rsa_decrypt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_rsa_crt_decrypt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_diffie_hellman_demo, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_ecdh_demo, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_ec_count_points_small, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_hasse_bound_check, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_shamir_split, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_shamir_reconstruct, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_one_time_pad, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lfsr, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lfsr_period, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_berlekamp_massey_attack, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_hash_avalanche_test, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_birthday_bound, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_frequency_analysis, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_index_of_coincidence, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_kasiski_examination, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_caesar_break, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_vigenere_break, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_perfect_shuffle_permutation, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_permutation_cipher_period, m)?)?;
    m.add_class::<crate::generated::types::PyEcCurve>()?;
    m.add_class::<crate::generated::types::PyEcPoint>()?;
    Ok(())
}
