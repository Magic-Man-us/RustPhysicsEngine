// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// The first `n` Taylor coefficients of `f` about the origin, by Cauchy's
/// integral evaluated on a circle of the given radius.
///
/// `a_k = (1 / 2 pi i) * contour integral of f(z) / z^(k+1)`. Sampling the
/// circle at `N` equally spaced points turns that into a discrete Fourier
/// transform, so all `N` coefficients come out of one FFT rather than `n`
/// separate quadratures.
///
/// The radius is the accuracy knob and the caller owns it: it must be inside
/// the disc of convergence, and the error in `a_k` scales like
/// `(radius / R)^N` for the true radius of convergence `R`. A radius near `R`
/// resolves high-order coefficients but amplifies the low-order ones by
/// `radius^-k`; a small radius does the reverse.
///
/// Returns the real parts, so this is for series with real coefficients.
///
/// Panics:
/// Panics if `n` is zero or `radius` is not positive.
///
/// Rust: `discrete::sequences::ogf_coefficients`
#[pyfunction]
#[pyo3(name = "ogf_coefficients", signature = (f, n, radius))]
pub fn pyfn_ogf_coefficients(f: pyo3::Py<pyo3::PyAny>, n: usize, radius: f64) -> PyResult<Vec<f64>> {
    let __cb_f = std::rc::Rc::new(crate::runtime::Callback::new(f));
    let f = { let __cb = __cb_f.clone(); move |__a0: rust_physics_engine::fractals::Complex| -> rust_physics_engine::fractals::Complex { { let __r = __cb.call::<_, crate::runtime::coerce::ComplexArg>((crate::runtime::coerce::Cx(__a0),), crate::runtime::coerce::ComplexArg(rust_physics_engine::fractals::Complex::new(f64::NAN, f64::NAN))); __r.0 } } };
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::ogf_coefficients(&f, n, radius));
    crate::runtime::callback::check(&[&__cb_f], ())?;
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Converts exponential generating function coefficients to ordinary ones by
/// multiplying term `k` by `k!`.
///
/// The factorial overflows `f64` past `k = 170`, so the tail beyond that is
/// infinite rather than silently wrong.
///
/// Rust: `discrete::sequences::egf_to_ogf`
#[pyfunction]
#[pyo3(name = "egf_to_ogf", signature = (coeffs))]
pub fn pyfn_egf_to_ogf<'py>(py: Python<'py>, coeffs: Vec<f64>) -> PyResult<Vec<f64>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::egf_to_ogf(&coeffs)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The `n`-th term of the linear recurrence
/// `a_k = coeffs[0] a_{k-1} + coeffs[1] a_{k-2} + ...`, with `init` giving
/// `a_0 .. a_{order-1}`.
///
/// Panics:
/// Panics unless `init` and `coeffs` have the same non-zero length.
///
/// Rust: `discrete::sequences::linear_recurrence`
#[pyfunction]
#[pyo3(name = "linear_recurrence", signature = (init, coeffs, n))]
pub fn pyfn_linear_recurrence<'py>(py: Python<'py>, init: Vec<i64>, coeffs: Vec<i64>, n: u64) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::linear_recurrence(&init, &coeffs, n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// The `n`-th term of the same recurrence, modulo `m`, by matrix
/// exponentiation.
///
/// Costs `O(order^3 log n)` rather than `O(order * n)`, which is what makes an
/// index like `10^18` reachable.
///
/// Panics:
/// Panics unless `init` and `coeffs` have the same non-zero length, or if `m`
/// is zero.
///
/// Rust: `discrete::sequences::linear_recurrence_mod`
#[pyfunction]
#[pyo3(name = "linear_recurrence_mod", signature = (init, coeffs, n, m))]
pub fn pyfn_linear_recurrence_mod<'py>(py: Python<'py>, init: Vec<i64>, coeffs: Vec<i64>, n: u64, m: u64) -> PyResult<u64> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::linear_recurrence_mod(&init, &coeffs, n, m)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The shortest linear recurrence generating `seq`, by Berlekamp-Massey over
/// the rationals.
///
/// Returns `c` with `a_n = c[0] a_{n-1} + c[1] a_{n-2} + ...`, or `None` when
/// the sequence is too short to determine one. A recurrence of order `L` is
/// only pinned down by `2L` terms, so a candidate found from fewer is a guess;
/// this reports `None` in that case rather than returning it. The empty vector
/// is returned for the all-zero sequence, whose recurrence has order zero.
///
/// Rust: `discrete::sequences::find_linear_recurrence`
#[pyfunction]
#[pyo3(name = "find_linear_recurrence", signature = (seq))]
pub fn pyfn_find_linear_recurrence<'py>(py: Python<'py>, seq: Vec<crate::runtime::coerce::RationalArg>) -> PyResult<Option<Vec<pyo3::Bound<'py, pyo3::PyAny>>>> {
    let seq = seq.into_iter().map(|__e| __e.0).collect::<Vec<_>>();
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::find_linear_recurrence(&seq));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(match __v { Some(__x) => Some(__x.into_iter().map(|__x| -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> { Ok(crate::runtime::coerce::rational_out(py, &__x)?) }).collect::<PyResult<Vec<_>>>()?), None => None })
}

/// The connection polynomial of the shortest linear feedback shift register
/// generating `seq` over GF(2), returned as taps `t` with
/// `a_n = t[0] a_{n-1} XOR t[1] a_{n-2} XOR ...`.
///
/// Same algorithm as `find_linear_recurrence` with the field replaced by
/// GF(2), where every non-zero discrepancy is one and subtraction is XOR, so
/// there is no division to do.
///
/// Rust: `discrete::sequences::berlekamp_massey_gf2`
#[pyfunction]
#[pyo3(name = "berlekamp_massey_gf2", signature = (seq))]
pub fn pyfn_berlekamp_massey_gf2<'py>(py: Python<'py>, seq: Vec<bool>) -> PyResult<Vec<bool>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::berlekamp_massey_gf2(&seq)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// `F(n) mod m`, by fast doubling.
///
/// The identities `F(2k) = F(k) (2 F(k+1) - F(k))` and
/// `F(2k+1) = F(k)^2 + F(k+1)^2` halve the index each step, so this is
/// `O(log n)` multiplications rather than `O(n)` additions.
///
/// Panics:
/// Panics if `m` is zero.
///
/// Rust: `discrete::sequences::fibonacci_mod`
#[pyfunction]
#[pyo3(name = "fibonacci_mod", signature = (n, m))]
pub fn pyfn_fibonacci_mod(n: u64, m: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::fibonacci_mod(n, m));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Pisano period: the period of the Fibonacci sequence modulo `m`.
///
/// Found by advancing until the pair `(0, 1)` recurs, which is the state that
/// starts the sequence, so the first recurrence is the full period.
///
/// Panics:
/// Panics if `m` is zero.
///
/// Rust: `discrete::sequences::pisano_period`
#[pyfunction]
#[pyo3(name = "pisano_period", signature = (m))]
pub fn pyfn_pisano_period(m: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::pisano_period(m));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The `n`-th Lucas number: `L(0) = 2`, `L(1) = 1`, `L(n) = L(n-1) + L(n-2)`.
///
/// Rust: `discrete::sequences::lucas`
#[pyfunction]
#[pyo3(name = "lucas", signature = (n))]
pub fn pyfn_lucas<'py>(py: Python<'py>, n: u64) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::lucas(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// The `n`-th Pell number: `P(0) = 0`, `P(1) = 1`, `P(n) = 2 P(n-1) + P(n-2)`.
///
/// Rust: `discrete::sequences::pell_number`
#[pyfunction]
#[pyo3(name = "pell_number", signature = (n))]
pub fn pyfn_pell_number<'py>(py: Python<'py>, n: u64) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::pell_number(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// The `n`-th Jacobsthal number: `J(0) = 0`, `J(1) = 1`,
/// `J(n) = J(n-1) + 2 J(n-2)`.
///
/// Rust: `discrete::sequences::jacobsthal`
#[pyfunction]
#[pyo3(name = "jacobsthal", signature = (n))]
pub fn pyfn_jacobsthal<'py>(py: Python<'py>, n: u64) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::jacobsthal(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// The `n`-th tribonacci number: `0, 0, 1, 1, 2, 4, 7, 13, ...`.
///
/// Rust: `discrete::sequences::tribonacci`
#[pyfunction]
#[pyo3(name = "tribonacci", signature = (n))]
pub fn pyfn_tribonacci<'py>(py: Python<'py>, n: u64) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::tribonacci(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// The look-and-say sequence: each step reads the previous term aloud.
///
/// `"1"` becomes `"11"` (one 1), which becomes `"21"` (two 1s), and so on.
///
/// Panics:
/// Panics if `seed` is empty or contains a non-digit.
///
/// Rust: `discrete::sequences::look_and_say`
#[pyfunction]
#[pyo3(name = "look_and_say", signature = (seed, iterations))]
pub fn pyfn_look_and_say(seed: String, iterations: usize) -> PyResult<String> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::look_and_say(&seed, iterations));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.to_string())
}

/// Conway's constant, estimated from the growth of look-and-say lengths.
///
/// The true value 1.303577... is the unique real root above one of Conway's
/// degree-71 polynomial. Lengths grow at that rate asymptotically, but the
/// single-step ratio does not settle onto it smoothly: it is still swinging
/// between 1.3137 and 1.3510 at twenty iterations, so reading off one ratio
/// would be worse at twenty steps than at sixteen. The swing has period four,
/// so this takes the geometric mean across a four-step window instead, which
/// cancels most of it and reaches four digits by thirty iterations.
///
/// Fewer than four iterations are run as four, since the window needs them.
///
/// Rust: `discrete::sequences::conway_constant_estimate`
#[pyfunction]
#[pyo3(name = "conway_constant_estimate", signature = (iters))]
pub fn pyfn_conway_constant_estimate(iters: usize) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::conway_constant_estimate(iters));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The `n`-th Thue-Morse bit: the parity of the number of ones in `n`.
///
/// Rust: `discrete::sequences::thue_morse`
#[pyfunction]
#[pyo3(name = "thue_morse", signature = (n))]
pub fn pyfn_thue_morse(n: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::thue_morse(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The first `n` bits of the Thue-Morse sequence.
///
/// Rust: `discrete::sequences::thue_morse_sequence`
#[pyfunction]
#[pyo3(name = "thue_morse_sequence", signature = (n))]
pub fn pyfn_thue_morse_sequence<'py>(py: Python<'py>, n: usize) -> PyResult<Vec<bool>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::thue_morse_sequence(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The first `n` terms of the Kolakoski sequence over `{1, 2}`.
///
/// The sequence is its own run-length encoding: it starts `1, 2, 2, 1, 1, 2`,
/// whose run lengths are `1, 2, 2, 1, 1, 2` again. Generated by reading the
/// sequence back as it is written -- term `k` says how long run `k` is.
///
/// Rust: `discrete::sequences::kolakoski`
#[pyfunction]
#[pyo3(name = "kolakoski", signature = (n))]
pub fn pyfn_kolakoski<'py>(py: Python<'py>, n: usize) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::kolakoski(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The first `n` terms of Recaman's sequence.
///
/// `a(0) = 0`; each step subtracts the index if the result is positive and
/// has not appeared before, and otherwise adds it.
///
/// Rust: `discrete::sequences::recaman`
#[pyfunction]
#[pyo3(name = "recaman", signature = (n))]
pub fn pyfn_recaman<'py>(py: Python<'py>, n: usize) -> PyResult<Vec<i64>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::recaman(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The first `n` terms of the Ulam sequence starting `a, b`.
///
/// After the seeds, each term is the smallest integer larger than the last
/// that is the sum of two distinct earlier terms in exactly one way.
///
/// Panics:
/// Panics unless `0 < a < b`.
///
/// Rust: `discrete::sequences::ulam_sequence`
#[pyfunction]
#[pyo3(name = "ulam_sequence", signature = (a, b, n))]
pub fn pyfn_ulam_sequence<'py>(py: Python<'py>, a: u64, b: u64, n: usize) -> PyResult<Vec<u64>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::ulam_sequence(a, b, n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The aliquot sequence from `n`: repeatedly replace a number by the sum of
/// its proper divisors.
///
/// Stops early at zero, which is terminal, and at a repeat, which means the
/// sequence has entered a cycle (a perfect number, an amicable pair, or a
/// longer sociable chain). The returned vector includes `n` itself and the
/// repeated value, so a cycle is visible in the output.
///
/// Rust: `discrete::sequences::aliquot_sequence`
#[pyfunction]
#[pyo3(name = "aliquot_sequence", signature = (n, max_steps))]
pub fn pyfn_aliquot_sequence<'py>(py: Python<'py>, n: u64, max_steps: usize) -> PyResult<Vec<u64>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::aliquot_sequence(n, max_steps)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Ackermann function, for arguments whose value is representable.
///
/// `A(m, n)` is computed by the closed forms rather than the recursion, which
/// would not terminate in practice: `A(0,n) = n+1`, `A(1,n) = n+2`,
/// `A(2,n) = 2n+3`, `A(3,n) = 2^(n+3) - 3`, and `A(4,n)` is a tower of twos.
/// Returns `None` when the value cannot be built -- `A(4, 2)` already has
/// 19729 digits and `A(5, 0) = A(4, 1)` is the largest value below it that
/// this returns.
///
/// Rust: `discrete::sequences::ackermann_small`
#[pyfunction]
#[pyo3(name = "ackermann_small", signature = (m, n))]
pub fn pyfn_ackermann_small<'py>(py: Python<'py>, m: u64, n: u64) -> PyResult<Option<pyo3::Bound<'py, pyo3::PyAny>>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::sequences::ackermann_small(m, n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(match __v { Some(__x) => Some(crate::runtime::coerce::bigint_out(py, &__x)?), None => None })
}

/// Names of the known sequences whose opening terms match `terms`.
///
/// Every candidate family is generated and compared term by term, so a name is
/// returned only on an exact match of the whole input. A linear recurrence
/// found by `find_linear_recurrence` is reported as well, which covers the
/// families not listed by name.
///
/// The result is a list because short prefixes are genuinely ambiguous:
/// `1, 1, 2` opens the Fibonacci numbers, the Catalan numbers, and the
/// partition counts alike.
///
/// Rust: `discrete::sequences::sequence_identify`
#[pyfunction]
#[pyo3(name = "sequence_identify", signature = (terms))]
pub fn pyfn_sequence_identify<'py>(py: Python<'py>, terms: Vec<i64>) -> PyResult<Vec<String>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::sequences::sequence_identify(&terms)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| __x.to_string()).collect::<Vec<_>>())
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_ogf_coefficients, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_egf_to_ogf, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_linear_recurrence, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_linear_recurrence_mod, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_find_linear_recurrence, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_berlekamp_massey_gf2, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_fibonacci_mod, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_pisano_period, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lucas, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_pell_number, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_jacobsthal, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_tribonacci, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_look_and_say, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_conway_constant_estimate, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_thue_morse, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_thue_morse_sequence, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_kolakoski, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_recaman, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_ulam_sequence, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_aliquot_sequence, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_ackermann_small, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_sequence_identify, m)?)?;
    Ok(())
}
