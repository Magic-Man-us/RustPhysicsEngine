// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// Optimal prefix code lengths and codewords for the given symbol
/// frequencies, one entry per symbol.
///
/// Returns `(codeword, length)` pairs; a symbol of zero frequency gets
/// `(0, 0)` and must not be encoded. The codes are canonical, so a decoder
/// needs only the lengths.
///
/// Huffman's construction repeatedly merges the two least frequent symbols.
/// It is optimal, and the proof is short: in some optimal code the two rarest
/// symbols are siblings at the greatest depth, so merging them and solving
/// the smaller problem loses nothing. Optimal means no prefix code has a
/// smaller expected length -- not that it reaches the entropy, which it
/// cannot when the probabilities are not powers of two.
///
/// Panics:
/// Panics on an empty frequency table.
///
/// Rust: `codes::compression::huffman_build`
#[pyfunction]
#[pyo3(name = "huffman_build", signature = (freqs))]
pub fn pyfn_huffman_build<'py>(py: Python<'py>, freqs: Vec<u64>) -> PyResult<Vec<(u64, u8)>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::huffman_build(&freqs)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
}

/// Canonical codewords for the given code lengths.
///
/// Symbols are ordered by length and then by index, and codewords are
/// assigned in increasing numeric order, doubling at each length increase.
/// Any two prefix codes with the same length multiset compress identically,
/// so a decoder can be handed the lengths alone -- which is why every real
/// format transmits lengths rather than a tree.
///
/// Panics:
/// Panics if the lengths do not satisfy Kraft's inequality, since no prefix
/// code has them.
///
/// Rust: `codes::compression::canonical_huffman`
#[pyfunction]
#[pyo3(name = "canonical_huffman", signature = (lengths))]
pub fn pyfn_canonical_huffman<'py>(py: Python<'py>, lengths: Vec<u8>) -> PyResult<Vec<u64>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::canonical_huffman(&lengths)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Kraft sum of a set of code lengths: `sum 2^-l`.
///
/// At most one for any prefix code, and exactly one when the code wastes
/// nothing -- which Huffman's always does, since a tree with an only child
/// could shorten that child by a bit.
///
/// Rust: `codes::compression::kraft_sum`
#[pyfunction]
#[pyo3(name = "kraft_sum", signature = (lengths))]
pub fn pyfn_kraft_sum<'py>(py: Python<'py>, lengths: Vec<u8>) -> PyResult<f64> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::kraft_sum(&lengths)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Huffman-codes a byte string, returning the packed bits, the code table,
/// and the number of bits that matter.
///
/// Rust: `codes::compression::huffman_encode`
#[pyfunction]
#[pyo3(name = "huffman_encode", signature = (data))]
pub fn pyfn_huffman_encode<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<(Vec<u8>, Vec<(u64, u8)>, usize)> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::huffman_encode(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>(), __v.2))
}

/// Decodes `n` symbols from a Huffman-coded bit string.
///
/// Panics:
/// Panics if the bits do not spell out `n` valid codewords.
///
/// Rust: `codes::compression::huffman_decode`
#[pyfunction]
#[pyo3(name = "huffman_decode", signature = (bits, table, n))]
pub fn pyfn_huffman_decode<'py>(py: Python<'py>, bits: Vec<u8>, table: Vec<(u64, u8)>, n: usize) -> PyResult<Vec<u8>> {
    let table = table.into_iter().map(|__e| (__e.0, __e.1)).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::huffman_decode(&bits, &table, n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Shannon-Fano coding: split the frequency-sorted symbols into two halves of
/// as nearly equal weight as possible, and recurse.
///
/// The older construction, and never better than Huffman: it decides the top
/// of the tree first and cannot revise, while Huffman builds from the leaves
/// and so is optimal. The gap is usually small and occasionally a whole bit
/// per symbol.
///
/// Panics:
/// Panics on an empty frequency table.
///
/// Rust: `codes::compression::shannon_fano`
#[pyfunction]
#[pyo3(name = "shannon_fano", signature = (freqs))]
pub fn pyfn_shannon_fano<'py>(py: Python<'py>, freqs: Vec<u64>) -> PyResult<Vec<(u64, u8)>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::shannon_fano(&freqs)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
}

/// The average code length of a prefix code against the given frequencies, in
/// bits per symbol.
///
/// Rust: `codes::compression::average_code_length`
#[pyfunction]
#[pyo3(name = "average_code_length", signature = (table, freqs))]
pub fn pyfn_average_code_length<'py>(py: Python<'py>, table: Vec<(u64, u8)>, freqs: Vec<u64>) -> PyResult<f64> {
    let table = table.into_iter().map(|__e| (__e.0, __e.1)).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::average_code_length(&table, &freqs)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Arithmetic coding against a fixed model of symbol frequencies.
///
/// Where a prefix code must spend a whole number of bits on every symbol,
/// arithmetic coding narrows a single interval by a factor of each symbol's
/// probability and writes out one number identifying it. The cost of a
/// message is therefore `-log2` of its probability to within two bits *in
/// total*, not per symbol, which is what makes it beat Huffman whenever some
/// symbol is much more likely than a half.
///
/// Panics:
/// Panics unless the model has one non-negative count per symbol value, the
/// total is between one and 65536, and every byte that occurs has a positive
/// count.
///
/// Rust: `codes::compression::arithmetic_encode`
#[pyfunction]
#[pyo3(name = "arithmetic_encode", signature = (data, model))]
pub fn pyfn_arithmetic_encode<'py>(py: Python<'py>, data: Vec<u8>, model: Vec<u64>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::arithmetic_encode(&data, &model)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Decodes `n` symbols from an arithmetic-coded stream.
///
/// Panics:
/// Panics under the same conditions as `arithmetic_encode`.
///
/// Rust: `codes::compression::arithmetic_decode`
#[pyfunction]
#[pyo3(name = "arithmetic_decode", signature = (bits, model, n))]
pub fn pyfn_arithmetic_decode<'py>(py: Python<'py>, bits: Vec<u8>, model: Vec<u64>, n: usize) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::arithmetic_decode(&bits, &model, n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// LZ77: replace repeats with references to earlier text.
///
/// The window bounds how far back a reference may point and the lookahead how
/// long a match may be. A match is allowed to run past its own start -- an
/// offset of one with length twenty is a run of twenty identical bytes -- and
/// the decompressor copying one byte at a time handles that for free, which
/// is why run-length encoding falls out of LZ77 rather than needing to be
/// added to it.
///
/// Panics:
/// Panics if the window or lookahead is zero.
///
/// Rust: `codes::compression::lz77_compress`
#[pyfunction]
#[pyo3(name = "lz77_compress", signature = (data, window, lookahead))]
pub fn pyfn_lz77_compress(data: Vec<u8>, window: usize, lookahead: usize) -> PyResult<Vec<crate::generated::types::PyLz77Token>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::codes::compression::lz77_compress(&data, window, lookahead));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| crate::generated::types::PyLz77Token { inner: __x }).collect::<Vec<_>>())
}

/// Rebuilds the original from LZ77 tokens.
///
/// Panics:
/// Panics if a token points further back than the output so far.
///
/// Rust: `codes::compression::lz77_decompress`
#[pyfunction]
#[pyo3(name = "lz77_decompress", signature = (tokens))]
pub fn pyfn_lz77_decompress<'py>(py: Python<'py>, tokens: Vec<crate::generated::types::PyLz77Token>) -> PyResult<Vec<u8>> {
    let tokens = tokens.into_iter().map(|__e| __e.inner).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::lz77_decompress(&tokens)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// LZW: build a dictionary of every phrase seen plus one byte, and emit
/// dictionary indices.
///
/// The decoder rebuilds the same dictionary from the same output, so nothing
/// has to be transmitted with the data -- which is what made it practical for
/// modems and printers with no memory to spare.
///
/// Rust: `codes::compression::lzw_compress`
#[pyfunction]
#[pyo3(name = "lzw_compress", signature = (data))]
pub fn pyfn_lzw_compress<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<u16>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::lzw_compress(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Rebuilds the original from LZW codes.
///
/// Panics:
/// Panics on a code the dictionary cannot yet contain.
///
/// Rust: `codes::compression::lzw_decompress`
#[pyfunction]
#[pyo3(name = "lzw_decompress", signature = (codes))]
pub fn pyfn_lzw_decompress<'py>(py: Python<'py>, codes: Vec<u16>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::lzw_decompress(&codes)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Run-length encoding in the PackBits scheme.
///
/// A control byte below 128 means "the next `n + 1` bytes are literal"; one
/// at or above means "repeat the next byte `257 - n` times". Incompressible
/// data grows by one byte in every 128, which is the price of never needing
/// an escape character.
///
/// Rust: `codes::compression::rle_compress`
#[pyfunction]
#[pyo3(name = "rle_compress", signature = (data))]
pub fn pyfn_rle_compress<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::rle_compress(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Rebuilds the original from PackBits run-length encoding.
///
/// Panics:
/// Panics if the stream is truncated part way through a run or literal.
///
/// Rust: `codes::compression::rle_decompress`
#[pyfunction]
#[pyo3(name = "rle_decompress", signature = (data))]
pub fn pyfn_rle_decompress<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::rle_decompress(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The suffix array: the starting positions of the suffixes, in the order
/// those suffixes sort.
///
/// Built by prefix doubling. After round `k` the suffixes are sorted by their
/// first `2^k` characters, and the next round sorts by pairs of the ranks
/// already computed -- so each round doubles the prefix length examined and
/// `log n` rounds settle it. Not the linear-time construction, but the
/// simplest one whose correctness is visible.
///
/// Rust: `codes::compression::suffix_array`
#[pyfunction]
#[pyo3(name = "suffix_array", signature = (data))]
pub fn pyfn_suffix_array<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<usize>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::suffix_array(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The longest common prefix of each adjacent pair in the suffix array, by
/// Kasai's algorithm.
///
/// `lcp[i]` is the overlap between the suffixes at `sa[i - 1]` and `sa[i]`,
/// with `lcp[0]` zero. Kasai's insight is that walking the suffixes in
/// *text* order lets the previous answer be reused: dropping the first
/// character of a suffix shortens its overlap with its neighbour by at most
/// one, so the total work is linear rather than quadratic.
///
/// Panics:
/// Panics unless the suffix array matches the data's length.
///
/// Rust: `codes::compression::lcp_array`
#[pyfunction]
#[pyo3(name = "lcp_array", signature = (data, sa))]
pub fn pyfn_lcp_array<'py>(py: Python<'py>, data: Vec<u8>, sa: Vec<usize>) -> PyResult<Vec<usize>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::lcp_array(&data, &sa)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The longest substring that occurs at least twice, as `(start, length)`.
///
/// The largest entry of the longest-common-prefix array, because two
/// occurrences of the same substring are two suffixes sharing that prefix,
/// and suffixes sharing a long prefix are adjacent in the suffix array.
/// Length zero when nothing repeats.
///
/// Rust: `codes::compression::longest_repeated_substring`
#[pyfunction]
#[pyo3(name = "longest_repeated_substring", signature = (data))]
pub fn pyfn_longest_repeated_substring<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<(usize, usize)> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::longest_repeated_substring(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// The Burrows-Wheeler transform: the last column of the sorted rotations,
/// and which row the original occupies.
///
/// The transform is reversible and sorts nothing about the data itself -- it
/// is a permutation of the bytes. What it does is bring together the bytes
/// that precede similar contexts, so English text comes out in long runs of
/// the same letter, and a run-length or move-to-front stage that could do
/// nothing with the original then has plenty to work with.
///
/// Rust: `codes::compression::bwt`
#[pyfunction]
#[pyo3(name = "bwt", signature = (data))]
pub fn pyfn_bwt<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<(Vec<u8>, usize)> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::bwt(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// Inverts the Burrows-Wheeler transform.
///
/// The last column plus the row index is enough, because sorting the last
/// column gives the first, and the `i`-th occurrence of a byte in the last
/// column is the `i`-th in the first -- rotations sharing a first byte stay
/// in the same relative order. That correspondence is the whole inverse.
///
/// Panics:
/// Panics if the index is outside the data.
///
/// Rust: `codes::compression::ibwt`
#[pyfunction]
#[pyo3(name = "ibwt", signature = (data, idx))]
pub fn pyfn_ibwt<'py>(py: Python<'py>, data: Vec<u8>, idx: usize) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::ibwt(&data, idx)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Move-to-front coding: emit each byte's position in a list, then move it to
/// the front.
///
/// It turns locality into small numbers. A stretch using only a few distinct
/// bytes -- which is what the Burrows-Wheeler transform produces -- becomes a
/// stretch of values near zero, and a stretch of one repeated byte becomes a
/// run of zeros, which an entropy coder or a run-length stage can then
/// exploit.
///
/// Rust: `codes::compression::mtf_encode`
#[pyfunction]
#[pyo3(name = "mtf_encode", signature = (data))]
pub fn pyfn_mtf_encode<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::mtf_encode(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Inverts move-to-front coding.
///
/// Rust: `codes::compression::mtf_decode`
#[pyfunction]
#[pyo3(name = "mtf_decode", signature = (data))]
pub fn pyfn_mtf_decode<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::mtf_decode(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Differences between consecutive bytes, modulo 256, with the first byte
/// kept as it is.
///
/// Worth doing when the data is a slowly varying signal: a smooth ramp has
/// high byte entropy and near-zero difference entropy.
///
/// Rust: `codes::compression::delta_encode`
#[pyfunction]
#[pyo3(name = "delta_encode", signature = (data))]
pub fn pyfn_delta_encode<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::delta_encode(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Inverts delta coding.
///
/// Rust: `codes::compression::delta_decode`
#[pyfunction]
#[pyo3(name = "delta_decode", signature = (data))]
pub fn pyfn_delta_decode<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<Vec<u8>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::delta_decode(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Shannon entropy of the byte histogram, in bits per byte.
///
/// The floor for any coder that treats the bytes as independent draws.
/// Between zero, for a constant stream, and eight, for a uniform one. It is
/// not a floor for compression in general: a stream of a million alternating
/// bytes has an entropy of one bit per byte and compresses to nothing, since
/// the bytes are not independent.
///
/// Rust: `codes::compression::entropy_bytes`
#[pyfunction]
#[pyo3(name = "entropy_bytes", signature = (data))]
pub fn pyfn_entropy_bytes<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<f64> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::entropy_bytes(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The size in bytes that the byte entropy allows, which no memoryless coder
/// can beat.
///
/// Rust: `codes::compression::compression_bound`
#[pyfunction]
#[pyo3(name = "compression_bound", signature = (data))]
pub fn pyfn_compression_bound<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<f64> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::compression_bound(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The size the module's own best pipeline achieves, as a stand-in for the
/// incompressible content of the data.
///
/// Kolmogorov complexity is not computable, and this is not an approximation
/// to it in any rigorous sense -- it is an upper bound that happens to behave
/// sensibly, which is what the practical literature uses it for. The pipeline
/// is Burrows-Wheeler, then move-to-front, then run lengths, then Huffman:
/// each stage exposes structure the next can spend.
///
/// Rust: `codes::compression::kolmogorov_estimate_by_compressors`
#[pyfunction]
#[pyo3(name = "kolmogorov_estimate_by_compressors", signature = (data))]
pub fn pyfn_kolmogorov_estimate_by_compressors<'py>(py: Python<'py>, data: Vec<u8>) -> PyResult<f64> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::kolmogorov_estimate_by_compressors(&data)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The normalized compression distance between two byte strings.
///
/// `(C(ab) - min(C(a), C(b))) / max(C(a), C(b))`: if knowing `a` makes `b`
/// cheap to describe, they are close. Near zero for identical inputs and near
/// one for unrelated ones, and it needs no notion of what the data means,
/// which is why it gets used on genomes and on music alike.
///
/// Rust: `codes::compression::normalized_compression_distance`
#[pyfunction]
#[pyo3(name = "normalized_compression_distance", signature = (a, b))]
pub fn pyfn_normalized_compression_distance<'py>(py: Python<'py>, a: Vec<u8>, b: Vec<u8>) -> PyResult<f64> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::codes::compression::normalized_compression_distance(&a, &b)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_huffman_build, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_canonical_huffman, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_kraft_sum, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_huffman_encode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_huffman_decode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_shannon_fano, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_average_code_length, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_arithmetic_encode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_arithmetic_decode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lz77_compress, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lz77_decompress, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lzw_compress, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lzw_decompress, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_rle_compress, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_rle_decompress, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_suffix_array, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lcp_array, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_longest_repeated_substring, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_bwt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_ibwt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_mtf_encode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_mtf_decode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_delta_encode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_delta_decode, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_entropy_bytes, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_compression_bound, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_kolmogorov_estimate_by_compressors, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_normalized_compression_distance, m)?)?;
    m.add_class::<crate::generated::types::PyBitWriter>()?;
    m.add_class::<crate::generated::types::PyLz77Token>()?;
    Ok(())
}
