// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// Greedy colouring in the given vertex order.
///
/// Returns one colour per vertex, numbered from zero. Every order yields a
/// proper colouring; the count of colours is what varies, and
/// `Order::SmallestLast` and `Order::Dsatur` carry the guarantees worth
/// having. Self-loops are ignored, since no colouring can respect one.
///
/// Rust: `graph::coloring::greedy_coloring`
#[pyfunction]
#[pyo3(name = "greedy_coloring", signature = (g, order))]
pub fn pyfn_greedy_coloring<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, order: crate::generated::types::PyOrder) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let order = order.to_rust();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::greedy_coloring(&g, order)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The number of distinct colours a colouring uses.
///
/// Rust: `graph::coloring::color_count`
#[pyfunction]
#[pyo3(name = "color_count", signature = (coloring))]
pub fn pyfn_color_count<'py>(py: Python<'py>, coloring: Vec<usize>) -> PyResult<usize> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::color_count(&coloring)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether a colouring gives no edge two ends of the same colour.
///
/// A self-loop always fails, which is the correct answer: a graph with one
/// has no proper colouring at all.
///
/// Rust: `graph::coloring::is_proper_coloring`
#[pyfunction]
#[pyo3(name = "is_proper_coloring", signature = (g, coloring))]
pub fn pyfn_is_proper_coloring<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, coloring: Vec<usize>) -> PyResult<bool> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::is_proper_coloring(&g, &coloring)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Welsh-Powell colouring: sort by descending degree, then fill one colour
/// class at a time by sweeping the list.
///
/// This is the same colouring `greedy_coloring` produces under
/// `Order::LargestFirst`, and for the same reason: a vertex takes colour
/// `c` in the sweep exactly when every earlier class held a neighbour of it,
/// which is the greedy rule stated the other way round. The procedure is
/// kept in its own form because the bound it is quoted with --
/// `max_i min(d_i + 1, i)` over the sorted degrees -- is a statement about
/// the sweep.
///
/// Rust: `graph::coloring::welsh_powell`
#[pyfunction]
#[pyo3(name = "welsh_powell", signature = (g))]
pub fn pyfn_welsh_powell<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::welsh_powell(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Welsh-Powell bound on the number of colours: `max_i min(d_i + 1, i)`
/// over the degrees sorted descending, indexed from one.
///
/// Rust: `graph::coloring::welsh_powell_bound`
#[pyfunction]
#[pyo3(name = "welsh_powell_bound", signature = (g))]
pub fn pyfn_welsh_powell_bound(g: crate::generated::types::PyGraph) -> PyResult<usize> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::coloring::welsh_powell_bound(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The chromatic number, by exhaustive search. Intended for `n <= 20`.
///
/// Bracketed first: a greedy clique gives a lower bound, since a clique of
/// size `q` needs `q` colours, and DSATUR gives an upper bound. Then each `k`
/// in between is decided exactly. On a graph the bracket already pins -- and
/// it often does -- no search runs at all.
///
/// Panics:
/// Panics on a self-loop, which admits no proper colouring.
///
/// Rust: `graph::coloring::chromatic_number_exact_small`
#[pyfunction]
#[pyo3(name = "chromatic_number_exact_small", signature = (g))]
pub fn pyfn_chromatic_number_exact_small(g: crate::generated::types::PyGraph) -> PyResult<usize> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::coloring::chromatic_number_exact_small(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The chromatic polynomial, exactly, by deletion-contraction. For `n <= 12`.
///
/// `P(G, x)` counts the proper colourings of `G` with `x` colours, and the
/// recursion is `P(G) = P(G - e) - P(G / e)`: colourings of `G - e` either
/// give `e`'s ends different colours, which is a colouring of `G`, or the
/// same colour, which is a colouring of the contraction. Both branches
/// shrink the graph -- deletion loses an edge, contraction loses a vertex --
/// so the recursion terminates on the edgeless graph, whose polynomial is
/// `x^n`. Memoised on the canonical edge set, which is what makes it
/// tractable at all: the two branches meet again constantly.
///
/// Panics:
/// Panics on a self-loop. Contraction can create one only from a parallel
/// edge, which is collapsed first.
///
/// Rust: `graph::coloring::chromatic_polynomial_small`
#[pyfunction]
#[pyo3(name = "chromatic_polynomial_small", signature = (g))]
pub fn pyfn_chromatic_polynomial_small(g: crate::generated::types::PyGraph) -> PyResult<crate::generated::types::PyPolyQ> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::coloring::chromatic_polynomial_small(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::generated::types::PyPolyQ { inner: __v })
}

/// Vizing edge colouring by the Misra-Gries construction: one colour per
/// edge, no two edges sharing a vertex alike, in at most `Delta + 1` colours.
///
/// Vizing's theorem says every simple graph needs `Delta` or `Delta + 1`, and
/// this reaches the upper end constructively. Each edge is coloured by
/// building a *fan* around one endpoint -- a run of neighbours where each
/// one's edge colour is free at the previous, so the whole run can shift
/// down by one -- then either rotating the fan to slide a free colour into
/// place, or first inverting a two-colour alternating path to make one free.
/// The alternating path is the part that makes the bound work: it repairs
/// the one obstruction rotation alone cannot, and it does so without
/// disturbing any other vertex, since every interior vertex of the path
/// simply exchanges its `c` for its `d`.
///
/// Returns one colour per entry of `g.edges()`, in that order.
///
/// Panics:
/// Panics if the graph is directed, or is not simple. A self-loop cannot be
/// coloured at all, and a parallel edge takes the bound outside Vizing's
/// theorem into Shannon's `Delta + mu`.
///
/// Rust: `graph::coloring::edge_coloring_vizing`
#[pyfunction]
#[pyo3(name = "edge_coloring_vizing", signature = (g))]
pub fn pyfn_edge_coloring_vizing<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::edge_coloring_vizing(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether an edge colouring gives no two edges sharing a vertex the same
/// colour.
///
/// Rust: `graph::coloring::is_proper_edge_coloring`
#[pyfunction]
#[pyo3(name = "is_proper_edge_coloring", signature = (g, coloring))]
pub fn pyfn_is_proper_edge_coloring<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, coloring: Vec<usize>) -> PyResult<bool> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::is_proper_edge_coloring(&g, &coloring)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Optimal colouring of an interval graph, given the intervals themselves.
///
/// Two intervals conflict when they overlap, and the greedy sweep in order of
/// left endpoint is optimal here -- unlike on a general graph -- because at
/// the moment an interval opens, every interval it will ever conflict with
/// that came earlier is still open. So the colours in use are exactly the
/// current overlap, and the total is the maximum overlap, which is a lower
/// bound for any colouring. Half-open intervals: touching at an endpoint is
/// not an overlap, and an interval whose ends coincide is empty, meets
/// nothing, and shares the first colour.
///
/// Returns one colour per interval, in the input order.
///
/// Panics:
/// Panics if an interval has its end before its start, or is not finite.
///
/// Rust: `graph::coloring::interval_graph_coloring`
#[pyfunction]
#[pyo3(name = "interval_graph_coloring", signature = (intervals))]
pub fn pyfn_interval_graph_coloring<'py>(py: Python<'py>, intervals: Vec<(f64, f64)>) -> PyResult<Vec<usize>> {
    let intervals = intervals.into_iter().map(|__e| (__e.0, __e.1)).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::interval_graph_coloring(&intervals)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A proper `k`-colouring of a graph given by adjacency lists, or `None`.
///
/// The classic map-colouring formulation: regions and the regions they
/// border. Straight chronological backtracking with forward checking, which
/// is what the four-colour problem was posed as long before it was a theorem.
///
/// Panics:
/// Panics if an adjacency list names a region outside the range.
///
/// Rust: `graph::coloring::map_coloring_backtrack`
#[pyfunction]
#[pyo3(name = "map_coloring_backtrack", signature = (adjacency, k))]
pub fn pyfn_map_coloring_backtrack<'py>(py: Python<'py>, adjacency: Vec<Vec<usize>>, k: usize) -> PyResult<Option<Vec<usize>>> {
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::map_coloring_backtrack(&adjacency, k)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// Every maximal clique, by Bron-Kerbosch with pivoting.
///
/// A clique is maximal when no vertex can be added; the algorithm grows one
/// while maintaining the candidates that could still join (`p`) and those
/// already ruled out (`x`), and reports when both are empty. The pivot is the
/// speedup: choosing a vertex `q` from `p | x` with the most neighbours in
/// `p`, and branching only on `p` minus `q`'s neighbourhood, skips the
/// branches that could only ever rediscover a clique through `q`.
///
/// Panics:
/// Panics above 64 vertices. The output can be exponential in the input --
/// a graph on `3j` vertices can have `3^j` maximal cliques -- so this is for
/// small graphs by construction.
///
/// Rust: `graph::coloring::all_maximal_cliques`
#[pyfunction]
#[pyo3(name = "all_maximal_cliques", signature = (g))]
pub fn pyfn_all_maximal_cliques<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<Vec<usize>>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::all_maximal_cliques(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A maximum clique: the largest set of mutually adjacent vertices.
///
/// Every maximum clique is maximal, so enumerating the maximal ones and
/// taking the largest is exact. Ties go to the lexicographically first.
///
/// Panics:
/// Panics above 64 vertices.
///
/// Rust: `graph::coloring::max_clique_bron_kerbosch`
#[pyfunction]
#[pyo3(name = "max_clique_bron_kerbosch", signature = (g))]
pub fn pyfn_max_clique_bron_kerbosch<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::max_clique_bron_kerbosch(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A maximal independent set, greedily: repeatedly take a vertex of minimum
/// remaining degree and discard its neighbours.
///
/// Minimum degree first is the right greed here: taking the vertex that
/// eliminates the fewest others leaves the most room for the rest. The
/// result is guaranteed maximal -- nothing can be added -- and at least
/// `sum_v 1/(d_v + 1)` in size by the Caro-Wei bound, but not maximum.
///
/// Rust: `graph::coloring::independent_set_greedy`
#[pyfunction]
#[pyo3(name = "independent_set_greedy", signature = (g))]
pub fn pyfn_independent_set_greedy<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::independent_set_greedy(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A maximum independent set, exactly, via the complement.
///
/// An independent set in `G` is a clique in the complement of `G` and the
/// other way round, so this is the clique enumeration with the edges flipped.
///
/// Panics:
/// Panics above 64 vertices.
///
/// Rust: `graph::coloring::max_independent_set_small`
#[pyfunction]
#[pyo3(name = "max_independent_set_small", signature = (g))]
pub fn pyfn_max_independent_set_small<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::max_independent_set_small(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A vertex cover within a factor of two of the smallest, by taking both ends
/// of a maximal matching.
///
/// The matching's edges are disjoint, so any cover must contain at least one
/// end of each, giving `opt >= |M|`; taking both ends gives `2|M| <= 2 opt`.
/// The bound comes free with the construction and holds on every graph, which
/// is more than the best known algorithm can say about doing better.
///
/// Rust: `graph::coloring::vertex_cover_2approx`
#[pyfunction]
#[pyo3(name = "vertex_cover_2approx", signature = (g))]
pub fn pyfn_vertex_cover_2approx<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::vertex_cover_2approx(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A minimum vertex cover, exactly, as the complement of a maximum
/// independent set.
///
/// Gallai's identity: a set covers every edge exactly when its complement
/// spans none, so the two problems are the same problem read twice, and
/// `tau + alpha = n`.
///
/// Panics:
/// Panics above 64 vertices, or on a self-loop, whose vertex every cover must
/// contain and which the complement identity does not account for.
///
/// Rust: `graph::coloring::vertex_cover_exact_small`
#[pyfunction]
#[pyo3(name = "vertex_cover_exact_small", signature = (g))]
pub fn pyfn_vertex_cover_exact_small<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::vertex_cover_exact_small(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A dominating set, greedily: every vertex is in it or next to it.
///
/// Set cover in disguise, with each vertex offering its closed neighbourhood,
/// so the greedy choice of whichever vertex newly dominates the most inherits
/// set cover's `ln(n) + 1` guarantee -- and its hardness, since matching that
/// factor in polynomial time would collapse the same complexity assumption.
///
/// Rust: `graph::coloring::dominating_set_greedy`
#[pyfunction]
#[pyo3(name = "dominating_set_greedy", signature = (g))]
pub fn pyfn_dominating_set_greedy<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<usize>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::dominating_set_greedy(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A feedback arc set: arcs whose removal leaves a directed acyclic graph.
///
/// By the Eades-Lin-Smyth ordering. It builds a linear order by repeatedly
/// taking sinks from the right, sources from the left, and otherwise the
/// vertex whose out-degree most exceeds its in-degree; the arcs pointing
/// backwards in that order are the answer. Removing them must leave a DAG,
/// since a linear order that every remaining arc respects is a topological
/// order. The count is within `m/2 - n/6` of the total, which is the bound
/// the heuristic is quoted for.
///
/// Self-loops are always returned: no ordering can place a vertex before
/// itself.
///
/// Panics:
/// Panics if the graph is undirected.
///
/// Rust: `graph::coloring::feedback_arc_set_greedy`
#[pyfunction]
#[pyo3(name = "feedback_arc_set_greedy", signature = (g))]
pub fn pyfn_feedback_arc_set_greedy<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<(usize, usize)>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::coloring::feedback_arc_set_greedy(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_greedy_coloring, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_color_count, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_is_proper_coloring, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_welsh_powell, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_welsh_powell_bound, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_chromatic_number_exact_small, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_chromatic_polynomial_small, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_edge_coloring_vizing, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_is_proper_edge_coloring, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_interval_graph_coloring, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_map_coloring_backtrack, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_all_maximal_cliques, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_max_clique_bron_kerbosch, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_independent_set_greedy, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_max_independent_set_small, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_vertex_cover_2approx, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_vertex_cover_exact_small, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_dominating_set_greedy, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_feedback_arc_set_greedy, m)?)?;
    m.add_class::<crate::generated::types::PyOrder>()?;
    Ok(())
}
