// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// The physicists' Hermite polynomial `H_n(x)`.
///
/// Evaluated by the upward recurrence `H_{n+1} = 2x H_n - 2n H_{n-1}` rather
/// than from the explicit sum, whose alternating terms cancel catastrophically:
/// at `n = 20` and moderate `x` the largest term exceeds the answer by many
/// orders of magnitude, and a direct sum loses every significant digit.
///
/// Rust: `quantum::wavefunction::hermite_polynomial`
#[pyfunction]
#[pyo3(name = "hermite_polynomial", signature = (n, x))]
pub fn pyfn_hermite_polynomial(n: usize, x: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::hermite_polynomial(n, x));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The associated Laguerre polynomial `L_n^k(x)`.
///
/// Also by recurrence, and for the same reason.
///
/// Rust: `quantum::wavefunction::laguerre_associated`
#[pyfunction]
#[pyo3(name = "laguerre_associated", signature = (n, k, x))]
pub fn pyfn_laguerre_associated(n: usize, k: f64, x: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::laguerre_associated(n, k, x));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The `n`-th harmonic oscillator eigenstate, normalised on the whole line.
///
/// The normalisation `(m omega / pi hbar)^(1/4) / sqrt(2^n n!)` is folded in
/// through logarithms, since `2^n n!` overflows a double at `n = 170` while
/// the state itself stays perfectly ordinary.
///
/// Panics:
/// Panics unless the mass, frequency and `hbar` are positive.
///
/// Rust: `quantum::wavefunction::harmonic_oscillator_eigenstate`
#[pyfunction]
#[pyo3(name = "harmonic_oscillator_eigenstate", signature = (n, x, mass, omega, hbar))]
pub fn pyfn_harmonic_oscillator_eigenstate(n: usize, x: f64, mass: f64, omega: f64, hbar: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::harmonic_oscillator_eigenstate(n, x, mass, omega, hbar));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The energy of the `n`-th harmonic oscillator level: `(n + 1/2) hbar omega`.
///
/// The half is the zero-point energy, and it is not a convention: the ground
/// state cannot sit at the bottom of the well without violating the
/// uncertainty relation, and `hbar omega / 2` is exactly what the relation
/// costs.
///
/// Panics:
/// Panics unless `omega` and `hbar` are positive.
///
/// Rust: `quantum::wavefunction::harmonic_oscillator_energy`
#[pyfunction]
#[pyo3(name = "harmonic_oscillator_energy", signature = (n, omega, hbar))]
pub fn pyfn_harmonic_oscillator_energy(n: usize, omega: f64, hbar: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::harmonic_oscillator_energy(n, omega, hbar));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The `n`-th eigenstate of an infinite square well of width `l`, indexed
/// from one, and zero outside the well.
///
/// Panics:
/// Panics unless `n >= 1` and the width is positive.
///
/// Rust: `quantum::wavefunction::infinite_well_eigenstate`
#[pyfunction]
#[pyo3(name = "infinite_well_eigenstate", signature = (n, x, l))]
pub fn pyfn_infinite_well_eigenstate(n: usize, x: f64, l: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::infinite_well_eigenstate(n, x, l));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The energy of the `n`-th infinite-well level.
///
/// Panics:
/// Panics unless `n >= 1` and the width, mass and `hbar` are positive.
///
/// Rust: `quantum::wavefunction::infinite_well_energy`
#[pyfunction]
#[pyo3(name = "infinite_well_energy", signature = (n, l, mass, hbar))]
pub fn pyfn_infinite_well_energy(n: usize, l: f64, mass: f64, hbar: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::infinite_well_energy(n, l, mass, hbar));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The hydrogen radial wavefunction `R_{n,l}(r)` in units of the Bohr radius
/// `a0`.
///
/// Panics:
/// Panics unless `n >= 1`, `l < n` and `a0` is positive.
///
/// Rust: `quantum::wavefunction::hydrogen_radial`
#[pyfunction]
#[pyo3(name = "hydrogen_radial", signature = (n, l, r, a0))]
pub fn pyfn_hydrogen_radial(n: usize, l: usize, r: f64, a0: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::hydrogen_radial(n, l, r, a0));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The hydrogen energy level in electronvolts: `-13.6 / n^2`.
///
/// Panics:
/// Panics unless `n >= 1`.
///
/// Rust: `quantum::wavefunction::hydrogen_energy`
#[pyfunction]
#[pyo3(name = "hydrogen_energy", signature = (n))]
pub fn pyfn_hydrogen_energy(n: usize) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::hydrogen_energy(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The probability density of a real hydrogen orbital at a point in spherical
/// coordinates.
///
/// Uses the real spherical harmonics, so `m` selects the real combinations
/// -- the `p_x`, `p_y`, `p_z` shapes rather than the complex `m` eigenstates.
/// The two bases span the same space and give the same total density in a
/// shell; they differ in the angular shape of an individual orbital, which is
/// exactly what chemistry draws.
///
/// Panics:
/// Panics unless `n >= 1`, `l < n`, `|m| <= l` and `a0` is positive.
///
/// Rust: `quantum::wavefunction::hydrogen_orbital_density`
#[pyfunction]
#[pyo3(name = "hydrogen_orbital_density", signature = (n, l, m, r, theta, phi, a0))]
pub fn pyfn_hydrogen_orbital_density(n: usize, l: usize, m: i32, r: f64, theta: f64, phi: f64, a0: f64) -> PyResult<f64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::hydrogen_orbital_density(n, l, m, r, theta, phi, a0));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Fock coefficients of a coherent state `|alpha>`, truncated at
/// `n_max` photons.
///
/// A Poisson distribution over photon number with mean `|alpha|^2`. Coherent
/// states are the eigenstates of the annihilation operator, which is why
/// removing a photon from a laser beam leaves it unchanged, and why the
/// photon statistics of a laser are Poissonian rather than thermal.
///
/// Errors:
/// Returns an error for an empty truncation.
///
/// Rust: `quantum::wavefunction::coherent_state`
#[pyfunction]
#[pyo3(name = "coherent_state", signature = (alpha, n_max))]
pub fn pyfn_coherent_state<'py>(py: Python<'py>, alpha: crate::runtime::coerce::ComplexArg, n_max: usize) -> PyResult<Vec<pyo3::Bound<'py, pyo3::types::PyComplex>>> {
    let alpha = alpha.0;
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::coherent_state(alpha, n_max));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    let __v = __v.map_err(crate::runtime::map_geom)?;
    Ok(__v.into_iter().map(|__x| crate::runtime::coerce::complex_out(py, __x)).collect::<Vec<_>>())
}

/// The Fock coefficients of a squeezed vacuum state, truncated at `n_max`.
///
/// Only the even photon numbers are populated, because the squeezing operator
/// creates photons in pairs. That parity is the state's signature and is what
/// makes it useful: the noise removed from one quadrature has to go somewhere,
/// and it goes into the other.
///
/// Errors:
/// Returns an error for an empty truncation.
///
/// Rust: `quantum::wavefunction::squeezed_state`
#[pyfunction]
#[pyo3(name = "squeezed_state", signature = (r, phi, n_max))]
pub fn pyfn_squeezed_state<'py>(py: Python<'py>, r: f64, phi: f64, n_max: usize) -> PyResult<Vec<pyo3::Bound<'py, pyo3::types::PyComplex>>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::quantum::wavefunction::squeezed_state(r, phi, n_max));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    let __v = __v.map_err(crate::runtime::map_geom)?;
    Ok(__v.into_iter().map(|__x| crate::runtime::coerce::complex_out(py, __x)).collect::<Vec<_>>())
}

/// The Wigner function of a wavefunction at a point of phase space.
///
/// `W(x, p) = (1 / pi hbar) integral psi*(x + y) psi(x - y) e^{2 i p y / hbar} dy`.
///
/// The nearest thing quantum mechanics has to a phase-space probability
/// density: its marginals are the true position and momentum distributions.
/// It is not a probability density, because it takes negative values -- and
/// where it does is exactly where the state has no classical description, so
/// the negativity is the useful part rather than a defect of the definition.
///
/// Errors:
/// Returns an error for a non-positive spacing or `hbar`.
///
/// Rust: `quantum::wavefunction::wigner_function`
#[pyfunction]
#[pyo3(name = "wigner_function", signature = (psi, dx, x0, x, p, hbar))]
pub fn pyfn_wigner_function<'py>(py: Python<'py>, psi: Vec<crate::runtime::coerce::ComplexArg>, dx: f64, x0: f64, x: f64, p: f64, hbar: f64) -> PyResult<f64> {
    let psi = psi.into_iter().map(|__e| __e.0).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::quantum::wavefunction::wigner_function(&psi, dx, x0, x, p, hbar)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    let __v = __v.map_err(crate::runtime::map_geom)?;
    Ok(__v)
}

/// The Husimi Q function: the Wigner function smoothed by a coherent state of
/// width `sigma`.
///
/// Smoothing over a phase-space cell of the minimum allowed area is exactly
/// enough to remove the negativity, so `Q` is a genuine probability density.
/// What it buys in interpretability it loses in resolution: the interference
/// fringes that make the Wigner function negative are precisely what the
/// smoothing erases.
///
/// Errors:
/// Returns an error for a non-positive spacing, width, or `hbar`.
///
/// Rust: `quantum::wavefunction::husimi_q`
#[pyfunction]
#[pyo3(name = "husimi_q", signature = (psi, dx, x0, x, p, sigma, hbar))]
pub fn pyfn_husimi_q<'py>(py: Python<'py>, psi: Vec<crate::runtime::coerce::ComplexArg>, dx: f64, x0: f64, x: f64, p: f64, sigma: f64, hbar: f64) -> PyResult<f64> {
    let psi = psi.into_iter().map(|__e| __e.0).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::quantum::wavefunction::husimi_q(&psi, dx, x0, x, p, sigma, hbar)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    let __v = __v.map_err(crate::runtime::map_geom)?;
    Ok(__v)
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_hermite_polynomial, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_laguerre_associated, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_harmonic_oscillator_eigenstate, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_harmonic_oscillator_energy, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_infinite_well_eigenstate, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_infinite_well_energy, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_hydrogen_radial, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_hydrogen_energy, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_hydrogen_orbital_density, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_coherent_state, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_squeezed_state, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_wigner_function, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_husimi_q, m)?)?;
    m.add_class::<crate::generated::types::PyWavefunction1D>()?;
    Ok(())
}
