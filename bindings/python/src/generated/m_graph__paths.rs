// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// Single-source shortest paths with non-negative weights, by Dijkstra.
///
/// Returns the distances and the predecessor array. Unreached vertices have
/// distance `f64::INFINITY` and no predecessor.
///
/// Panics:
/// Panics if any weight is negative, where the algorithm is simply wrong
/// rather than merely slow -- use `bellman_ford` instead.
///
/// Rust: `graph::paths::dijkstra`
#[pyfunction]
#[pyo3(name = "dijkstra", signature = (g, s))]
pub fn pyfn_dijkstra(g: crate::generated::types::PyGraph, s: usize) -> PyResult<(Vec<f64>, Vec<Option<usize>>)> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::dijkstra(&g, s));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1.into_iter().map(|__x| __x.map(|__x| __x)).collect::<Vec<_>>()))
}

/// The shortest path from `s` to `t` and its length, or `None` if `t` is
/// unreachable.
///
/// Rust: `graph::paths::dijkstra_target`
#[pyfunction]
#[pyo3(name = "dijkstra_target", signature = (g, s, t))]
pub fn pyfn_dijkstra_target(g: crate::generated::types::PyGraph, s: usize, t: usize) -> PyResult<Option<(f64, Vec<usize>)>> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::dijkstra_target(&g, s, t));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// Single-source shortest paths allowing negative weights, by Bellman-Ford.
///
/// Errors:
/// Returns `NegativeCycle` when a cycle of negative total weight is
/// reachable from `s`, which is detected by one relaxation pass beyond the
/// `n - 1` that suffice when none exists.
///
/// Rust: `graph::paths::bellman_ford`
#[pyfunction]
#[pyo3(name = "bellman_ford", signature = (g, s))]
pub fn pyfn_bellman_ford(g: crate::generated::types::PyGraph, s: usize) -> PyResult<(Vec<f64>, Vec<Option<usize>>)> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::bellman_ford(&g, s));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    let __v = __v.map_err(crate::runtime::errors::map_display)?;
    Ok((__v.0, __v.1.into_iter().map(|__x| __x.map(|__x| __x)).collect::<Vec<_>>()))
}

/// All-pairs shortest paths by Floyd-Warshall, `O(n^3)`.
///
/// Entry `(i, j)` is the distance, `f64::INFINITY` when unreachable. Negative
/// cycles are not detected here; a negative diagonal entry in the result is
/// the sign of one.
///
/// Rust: `graph::paths::floyd_warshall`
#[pyfunction]
#[pyo3(name = "floyd_warshall", signature = (g))]
pub fn pyfn_floyd_warshall(g: crate::generated::types::PyGraph) -> PyResult<crate::generated::types::PyMatrix> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::floyd_warshall(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::generated::types::PyMatrix { inner: __v })
}

/// All-pairs shortest paths by Johnson's algorithm: a Bellman-Ford pass from a
/// virtual source supplies potentials that make every weight non-negative,
/// then one Dijkstra per vertex.
///
/// Faster than Floyd-Warshall on sparse graphs, and unlike plain Dijkstra it
/// tolerates negative weights.
///
/// Errors:
/// Returns `NegativeCycle` if the graph contains one.
///
/// Rust: `graph::paths::johnson`
#[pyfunction]
#[pyo3(name = "johnson", signature = (g))]
pub fn pyfn_johnson(g: crate::generated::types::PyGraph) -> PyResult<crate::generated::types::PyMatrix> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::johnson(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    let __v = __v.map_err(crate::runtime::errors::map_display)?;
    Ok(crate::generated::types::PyMatrix { inner: __v })
}

/// A* search with the heuristic `h`.
///
/// Returns the path and its true length, or `None` if `t` is unreachable. The
/// result is optimal exactly when `h` is admissible -- never overestimating
/// the remaining distance -- and the search is efficient when `h` is also
/// consistent. An inadmissible heuristic still terminates but may return a
/// suboptimal path, which is the caller's trade to make.
///
/// Panics:
/// Panics if any weight is negative.
///
/// Rust: `graph::paths::a_star`
#[pyfunction]
#[pyo3(name = "a_star", signature = (g, s, t, h))]
pub fn pyfn_a_star(g: crate::generated::types::PyGraph, s: usize, t: usize, h: pyo3::Py<pyo3::PyAny>) -> PyResult<Option<(f64, Vec<usize>)>> {
    let g = g.inner;
    let __cb_h = std::rc::Rc::new(crate::runtime::Callback::new(h));
    let h = { let __cb = __cb_h.clone(); move |__a0: usize| -> f64 { __cb.call::<_, f64>((__a0,), f64::NAN) } };
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::a_star(&g, s, t, &h));
    crate::runtime::callback::check(&[&__cb_h], ())?;
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// Dijkstra from both ends at once, alternating between them.
///
/// Both searches settle vertices; the answer is the best path through any
/// vertex either has reached, and the search stops once the two settled
/// radii sum to at least the best path found. On a graph where the reachable
/// set grows with the radius, this settles roughly the square root of the
/// vertices a one-sided search would.
///
/// Panics:
/// Panics if any weight is negative.
///
/// Rust: `graph::paths::bidirectional_dijkstra`
#[pyfunction]
#[pyo3(name = "bidirectional_dijkstra", signature = (g, s, t))]
pub fn pyfn_bidirectional_dijkstra(g: crate::generated::types::PyGraph, s: usize, t: usize) -> PyResult<Option<(f64, Vec<usize>)>> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::bidirectional_dijkstra(&g, s, t));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// The `k` shortest loopless paths from `s` to `t`, by Yen's algorithm.
///
/// Returns them in increasing length, and may return fewer than `k` when
/// fewer exist. Each candidate is found by forcing a shared prefix with an
/// already-accepted path and forbidding the arc it took next, which is what
/// keeps the results distinct and loopless.
///
/// Rust: `graph::paths::k_shortest_paths_yen`
#[pyfunction]
#[pyo3(name = "k_shortest_paths_yen", signature = (g, s, t, k))]
pub fn pyfn_k_shortest_paths_yen<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, s: usize, t: usize, k: usize) -> PyResult<Vec<(f64, Vec<usize>)>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::k_shortest_paths_yen(&g, s, t, k)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
}

/// The widest path: the one whose narrowest edge is as wide as possible.
///
/// Also called the bottleneck shortest path or the maximum capacity path.
/// Dijkstra with `min` in place of `+` and `max` in place of `min`, which is
/// valid because `min` is monotone in the same way `+` is.
///
/// Rust: `graph::paths::widest_path`
#[pyfunction]
#[pyo3(name = "widest_path", signature = (g, s, t))]
pub fn pyfn_widest_path(g: crate::generated::types::PyGraph, s: usize, t: usize) -> PyResult<Option<(f64, Vec<usize>)>> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::widest_path(&g, s, t));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// The minimax path: the one whose widest edge is as narrow as possible.
///
/// The dual of `widest_path`, and the path a minimum spanning tree gives
/// between any two vertices.
///
/// Rust: `graph::paths::minimax_path`
#[pyfunction]
#[pyo3(name = "minimax_path", signature = (g, s, t))]
pub fn pyfn_minimax_path(g: crate::generated::types::PyGraph, s: usize, t: usize) -> PyResult<Option<(f64, Vec<usize>)>> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::minimax_path(&g, s, t));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// Shortest distances from `s` in a DAG, by relaxing in topological order.
///
/// Linear time and correct with negative weights, neither of which Dijkstra
/// manages.
///
/// Panics:
/// Panics if the graph is not a DAG.
///
/// Rust: `graph::paths::dag_shortest`
#[pyfunction]
#[pyo3(name = "dag_shortest", signature = (g, s))]
pub fn pyfn_dag_shortest<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, s: usize) -> PyResult<Vec<f64>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::dag_shortest(&g, s)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Longest distances from `s` in a DAG.
///
/// Longest path is NP-hard in general but linear on a DAG, since the
/// topological order removes any need to revisit.
///
/// Panics:
/// Panics if the graph is not a DAG.
///
/// Rust: `graph::paths::dag_longest`
#[pyfunction]
#[pyo3(name = "dag_longest", signature = (g, s))]
pub fn pyfn_dag_longest<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, s: usize) -> PyResult<Vec<f64>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::dag_longest(&g, s)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The number of distinct directed paths from `s` to `t` in a DAG.
///
/// Exact, because the count grows exponentially: a grid DAG of side `n` has
/// `C(2n, n)` paths, past `u64` before `n = 34`.
///
/// Panics:
/// Panics if the graph is not a DAG.
///
/// Rust: `graph::paths::count_paths_dag`
#[pyfunction]
#[pyo3(name = "count_paths_dag", signature = (g, s, t))]
pub fn pyfn_count_paths_dag<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, s: usize, t: usize) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::count_paths_dag(&g, s, t));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::bigint_out(py, &__v)?)
}

/// The reachability matrix: `[i][j]` is true when `j` is reachable from `i`.
///
/// Every vertex reaches itself.
///
/// Rust: `graph::paths::transitive_closure`
#[pyfunction]
#[pyo3(name = "transitive_closure", signature = (g))]
pub fn pyfn_transitive_closure<'py>(py: Python<'py>, g: crate::generated::types::PyGraph) -> PyResult<Vec<Vec<bool>>> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::transitive_closure(&g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A minimum spanning forest by Kruskal's algorithm: sort the edges, accept
/// each one that joins two different components.
///
/// Returns the total weight and the edges, each with `u < v`. On a
/// disconnected graph this is a spanning forest, and the edge count is
/// `n - components` rather than `n - 1`.
///
/// Rust: `graph::paths::minimum_spanning_tree_kruskal`
#[pyfunction]
#[pyo3(name = "minimum_spanning_tree_kruskal", signature = (g))]
pub fn pyfn_minimum_spanning_tree_kruskal(g: crate::generated::types::PyGraph) -> PyResult<(f64, Vec<(usize, usize)>)> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::minimum_spanning_tree_kruskal(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>()))
}

/// A minimum spanning forest by Prim's algorithm: grow a tree from each
/// unvisited vertex, always taking the cheapest edge leaving it.
///
/// Returns the same weight as Kruskal on any graph, though possibly a
/// different tree when weights tie.
///
/// Rust: `graph::paths::minimum_spanning_tree_prim`
#[pyfunction]
#[pyo3(name = "minimum_spanning_tree_prim", signature = (g))]
pub fn pyfn_minimum_spanning_tree_prim(g: crate::generated::types::PyGraph) -> PyResult<(f64, Vec<(usize, usize)>)> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::minimum_spanning_tree_prim(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>()))
}

/// A minimum spanning forest by Boruvka's algorithm: every component picks its
/// own cheapest outgoing edge, and all of them are added at once.
///
/// Halves the component count per round, so `O(log n)` rounds suffice. Ties
/// are broken by edge index, which is what stops two components from each
/// picking the other's edge and forming a cycle.
///
/// Rust: `graph::paths::minimum_spanning_tree_boruvka`
#[pyfunction]
#[pyo3(name = "minimum_spanning_tree_boruvka", signature = (g))]
pub fn pyfn_minimum_spanning_tree_boruvka(g: crate::generated::types::PyGraph) -> PyResult<(f64, Vec<(usize, usize)>)> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::minimum_spanning_tree_boruvka(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>()))
}

/// The second-best spanning tree: the cheapest spanning tree that differs from
/// the minimum one in at least one edge.
///
/// Found by swapping: for each non-tree edge, adding it creates one cycle, and
/// removing the heaviest tree edge on that cycle gives the cheapest tree
/// containing it. The best such swap is the answer.
///
/// Returns `None` when the graph is disconnected or has no non-tree edge, so
/// no second tree exists.
///
/// Rust: `graph::paths::second_best_mst`
#[pyfunction]
#[pyo3(name = "second_best_mst", signature = (g))]
pub fn pyfn_second_best_mst(g: crate::generated::types::PyGraph) -> PyResult<Option<(f64, Vec<(usize, usize)>)>> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::second_best_mst(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())))
}

/// A minimum Steiner tree spanning the given terminals, by Dreyfus-Wagner.
///
/// Returns the weight and the edges. The tree may use non-terminal vertices,
/// which is what separates the problem from a spanning tree. Costs
/// `O(3^t n + 2^t n^2)` for `t` terminals, so the terminal count is what has
/// to stay small, not the graph.
///
/// Panics:
/// Panics if there are more than 12 terminals, or a terminal is out of range.
///
/// Rust: `graph::paths::steiner_tree_small`
#[pyfunction]
#[pyo3(name = "steiner_tree_small", signature = (g, terminals))]
pub fn pyfn_steiner_tree_small<'py>(py: Python<'py>, g: crate::generated::types::PyGraph, terminals: Vec<usize>) -> PyResult<(f64, Vec<(usize, usize)>)> {
    let g = g.inner;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::steiner_tree_small(&g, &terminals)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>()))
}

/// The exact optimal travelling salesman tour, by Held-Karp.
///
/// Returns the tour length and the tour as a vertex sequence starting and
/// ending at 0, with the final return implied rather than repeated. Costs
/// `O(2^n n^2)` time and `O(2^n n)` memory.
///
/// Panics:
/// Panics if `dist` is not square, or has more than 20 rows.
///
/// Rust: `graph::paths::traveling_salesman_exact`
#[pyfunction]
#[pyo3(name = "traveling_salesman_exact", signature = (dist))]
pub fn pyfn_traveling_salesman_exact<'py>(py: Python<'py>, dist: crate::generated::types::PyMatrixArg) -> PyResult<(f64, Vec<usize>)> {
    let dist = dist.0;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::traveling_salesman_exact(&dist)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// A nearest-neighbour tour: repeatedly walk to the closest unvisited city.
///
/// Fast and usually poor: on a metric instance it can be a logarithmic factor
/// worse than optimal, so it is a starting point for `tsp_2opt` rather than
/// an answer.
///
/// Panics:
/// Panics if `dist` is not square.
///
/// Rust: `graph::paths::tsp_nearest_neighbor`
#[pyfunction]
#[pyo3(name = "tsp_nearest_neighbor", signature = (dist))]
pub fn pyfn_tsp_nearest_neighbor<'py>(py: Python<'py>, dist: crate::generated::types::PyMatrixArg) -> PyResult<(f64, Vec<usize>)> {
    let dist = dist.0;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::tsp_nearest_neighbor(&dist)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// The length of a closed tour under `dist`.
///
/// Rust: `graph::paths::tour_length`
#[pyfunction]
#[pyo3(name = "tour_length", signature = (dist, tour))]
pub fn pyfn_tour_length<'py>(py: Python<'py>, dist: crate::generated::types::PyMatrixArg, tour: Vec<usize>) -> PyResult<f64> {
    let dist = dist.0;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::tour_length(&dist, &tour)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// 2-opt local search: repeatedly reverse a tour segment when that shortens
/// the tour, until no single reversal helps.
///
/// The result is 2-optimal, not optimal. On a symmetric instance a reversal
/// changes only the two edges at its ends, which is what makes each move an
/// `O(1)` decision.
///
/// Panics:
/// Panics if `dist` is not square, or `tour` is not a permutation of its rows.
///
/// Rust: `graph::paths::tsp_2opt`
#[pyfunction]
#[pyo3(name = "tsp_2opt", signature = (dist, tour))]
pub fn pyfn_tsp_2opt<'py>(py: Python<'py>, dist: crate::generated::types::PyMatrixArg, tour: Vec<usize>) -> PyResult<(f64, Vec<usize>)> {
    let dist = dist.0;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::tsp_2opt(&dist, &tour)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// Or-opt local search: relocate a run of one, two or three consecutive cities
/// elsewhere in the tour, in either orientation, while that shortens it.
///
/// Complements 2-opt, which can only reverse: a run that belongs elsewhere
/// entirely is a move 2-opt cannot make in one step.
///
/// Panics:
/// Panics if `dist` is not square, or `tour` is not a permutation of its rows.
///
/// Rust: `graph::paths::tsp_or_opt`
#[pyfunction]
#[pyo3(name = "tsp_or_opt", signature = (dist, tour))]
pub fn pyfn_tsp_or_opt<'py>(py: Python<'py>, dist: crate::generated::types::PyMatrixArg, tour: Vec<usize>) -> PyResult<(f64, Vec<usize>)> {
    let dist = dist.0;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::tsp_or_opt(&dist, &tour)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1))
}

/// Christofides' tour, which is within a factor of 1.5 of optimal on a metric
/// instance.
///
/// Takes a minimum spanning tree, adds a minimum-weight perfect matching on
/// the odd-degree vertices to make every degree even, walks the resulting
/// Eulerian circuit, and shortcuts repeats. The matching here is exact by
/// brute force over pairings, which is affordable because a tree has few
/// odd-degree vertices on the instances this is used for, and is refused
/// beyond sixteen of them rather than silently degrading to a greedy one.
///
/// Returns `None` when the odd set is too large for the exact matching.
///
/// Panics:
/// Panics if `dist` is not square or is not symmetric, since the guarantee
/// needs a metric.
///
/// Rust: `graph::paths::tsp_christofides`
#[pyfunction]
#[pyo3(name = "tsp_christofides", signature = (dist))]
pub fn pyfn_tsp_christofides<'py>(py: Python<'py>, dist: crate::generated::types::PyMatrixArg) -> PyResult<Option<(f64, Vec<usize>)>> {
    let dist = dist.0;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::graph::paths::tsp_christofides(&dist)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// A shortest closed walk crossing every edge at least once: the Chinese
/// postman problem.
///
/// Returns the walk's total weight and the vertex sequence. When every degree
/// is already even the answer is an Eulerian circuit and costs exactly the
/// total edge weight; otherwise the odd-degree vertices are paired up by a
/// minimum-weight perfect matching over shortest paths, and those paths are
/// duplicated. Returns `None` when the edges span more than one component, so
/// that no single closed walk can cross them all, or when the odd set is too
/// large for the exact matching. An edgeless graph has nothing to cross, so it
/// returns the empty route rather than failing on being disconnected.
///
/// Panics:
/// Panics if the graph is directed, where the construction differs.
///
/// Rust: `graph::paths::chinese_postman`
#[pyfunction]
#[pyo3(name = "chinese_postman", signature = (g))]
pub fn pyfn_chinese_postman(g: crate::generated::types::PyGraph) -> PyResult<Option<(f64, Vec<usize>)>> {
    let g = g.inner;
    let __r = crate::runtime::guard(|| rust_physics_engine::graph::paths::chinese_postman(&g));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_dijkstra, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_dijkstra_target, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_bellman_ford, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_floyd_warshall, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_johnson, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_a_star, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_bidirectional_dijkstra, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_k_shortest_paths_yen, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_widest_path, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_minimax_path, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_dag_shortest, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_dag_longest, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_count_paths_dag, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_transitive_closure, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_minimum_spanning_tree_kruskal, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_minimum_spanning_tree_prim, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_minimum_spanning_tree_boruvka, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_second_best_mst, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_steiner_tree_small, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_traveling_salesman_exact, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_tsp_nearest_neighbor, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_tour_length, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_tsp_2opt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_tsp_or_opt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_tsp_christofides, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_chinese_postman, m)?)?;
    Ok(())
}
