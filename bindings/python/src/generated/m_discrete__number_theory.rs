// @generated by bindings/python/generate.py -- do not edit.
//
// Regenerate with:  python3 bindings/python/generate.py


#![allow(clippy::all)]
#![allow(deprecated)]
#![allow(rustdoc::all)]
#![allow(unused_imports)]
#![allow(non_snake_case)]


use pyo3::prelude::*;
use pyo3::types::PyModule;


/// Greatest common divisor, by the binary (Stein) algorithm.
///
/// `gcd(0, n) == n`, so `gcd(0, 0) == 0`.
///
/// Rust: `discrete::number_theory::gcd_u64`
#[pyfunction]
#[pyo3(name = "gcd_u64", signature = (a, b))]
pub fn pyfn_gcd_u64(a: u64, b: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::gcd_u64(a, b));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Least common multiple; zero whenever either argument is zero.
///
/// Panics:
/// Panics if the least common multiple does not fit in a `u64`.
///
/// Rust: `discrete::number_theory::lcm_u64`
#[pyfunction]
#[pyo3(name = "lcm_u64", signature = (a, b))]
pub fn pyfn_lcm_u64(a: u64, b: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::lcm_u64(a, b));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Extended Euclidean algorithm: `(g, x, y)` with `a*x + b*y == g` and
/// `g == gcd(|a|, |b|) >= 0`.
///
/// Panics:
/// Panics on `a == i64::MIN` or `b == i64::MIN`, whose negation is not
/// representable.
///
/// Rust: `discrete::number_theory::extended_gcd_i64`
#[pyfunction]
#[pyo3(name = "extended_gcd_i64", signature = (a, b))]
pub fn pyfn_extended_gcd_i64(a: i64, b: i64) -> PyResult<(i64, i64, i64)> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::extended_gcd_i64(a, b));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1, __v.2))
}

/// Modular exponentiation `base^exp mod m`.
///
/// Shares the implementation in `discrete::primes::mod_pow_u64`.
///
/// Rust: `discrete::number_theory::mod_pow_u64`
#[pyfunction]
#[pyo3(name = "mod_pow_u64", signature = (base, exp, m))]
pub fn pyfn_mod_pow_u64(base: u64, exp: u64, m: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::mod_pow_u64(base, exp, m));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The inverse of `a` modulo `m`, or `None` when `gcd(a, m) != 1`.
///
/// The residue is returned in `[0, m)`; the modulus `0` has no residues
/// and yields `None`, while modulus `1` yields `0`.
///
/// Rust: `discrete::number_theory::mod_inverse_u64`
#[pyfunction]
#[pyo3(name = "mod_inverse_u64", signature = (a, m))]
pub fn pyfn_mod_inverse_u64(a: u64, m: u64) -> PyResult<Option<u64>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::mod_inverse_u64(a, m));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// Chinese remainder theorem for general (not necessarily coprime)
/// moduli.
///
/// Takes `(remainder, modulus)` pairs and returns the unique class
/// `(r, m)` with `m == lcm` of the moduli and `r` in `[0, m)` satisfying
/// every congruence. Returns `None` when the system is inconsistent,
/// when any modulus is zero, or when the combined modulus overflows a
/// `u64`. An empty system is solved by `(0, 1)`.
///
/// Rust: `discrete::number_theory::crt`
#[pyfunction]
#[pyo3(name = "crt", signature = (residues))]
pub fn pyfn_crt<'py>(py: Python<'py>, residues: Vec<(u64, u64)>) -> PyResult<Option<(u64, u64)>> {
    let _ = py;
    let residues = residues.into_iter().map(|__e| (__e.0, __e.1)).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::crt(&residues)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// Euler's totient: the count of integers in `[1, n]` coprime to `n`.
///
/// `euler_phi(0)` is defined as `0`.
///
/// Rust: `discrete::number_theory::euler_phi`
#[pyfunction]
#[pyo3(name = "euler_phi", signature = (n))]
pub fn pyfn_euler_phi(n: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::euler_phi(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// `euler_phi` for every index up to `n`, by a sieve.
///
/// Entry `i` of the returned vector is `euler_phi(i)`, so its length is
/// `n + 1`.
///
/// Rust: `discrete::number_theory::phi_sieve`
#[pyfunction]
#[pyo3(name = "phi_sieve", signature = (n))]
pub fn pyfn_phi_sieve<'py>(py: Python<'py>, n: usize) -> PyResult<Vec<u64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::phi_sieve(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Moebius function: `0` when `n` is not squarefree, otherwise
/// `(-1)^k` for `k` distinct prime factors.
///
/// `mobius(0)` is defined as `0` and `mobius(1) == 1`.
///
/// Rust: `discrete::number_theory::mobius`
#[pyfunction]
#[pyo3(name = "mobius", signature = (n))]
pub fn pyfn_mobius(n: u64) -> PyResult<i8> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::mobius(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// `mobius` for every index up to `n`, by a linear sieve.
///
/// Entry `i` of the returned vector is `mobius(i)`, so its length is
/// `n + 1`.
///
/// Rust: `discrete::number_theory::mobius_sieve`
#[pyfunction]
#[pyo3(name = "mobius_sieve", signature = (n))]
pub fn pyfn_mobius_sieve<'py>(py: Python<'py>, n: usize) -> PyResult<Vec<i8>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::mobius_sieve(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Every divisor of `n`, ascending. Empty for `n == 0`.
///
/// Rust: `discrete::number_theory::divisors`
#[pyfunction]
#[pyo3(name = "divisors", signature = (n))]
pub fn pyfn_divisors<'py>(py: Python<'py>, n: u64) -> PyResult<Vec<u64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::divisors(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The number of divisors, `sigma_0(n)`. Zero for `n == 0`.
///
/// Rust: `discrete::number_theory::divisor_count`
#[pyfunction]
#[pyo3(name = "divisor_count", signature = (n))]
pub fn pyfn_divisor_count(n: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::divisor_count(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The sum of divisors, `sigma_1(n)`. Zero for `n == 0`.
///
/// Panics:
/// Panics if the sum does not fit in a `u64`.
///
/// Rust: `discrete::number_theory::divisor_sum`
#[pyfunction]
#[pyo3(name = "divisor_sum", signature = (n))]
pub fn pyfn_divisor_sum(n: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::divisor_sum(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The divisor power sum `sigma_k(n) = sum_{d | n} d^k`.
///
/// `k == 0` counts divisors. Zero for `n == 0`.
///
/// Panics:
/// Panics if the sum does not fit in a `u64`.
///
/// Rust: `discrete::number_theory::sigma_k`
#[pyfunction]
#[pyo3(name = "sigma_k", signature = (n, k))]
pub fn pyfn_sigma_k(n: u64, k: u32) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::sigma_k(n, k));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether `n` equals the sum of its proper divisors.
///
/// Panics:
/// Panics if the divisor sum does not fit in a `u64`.
///
/// Rust: `discrete::number_theory::is_perfect`
#[pyfunction]
#[pyo3(name = "is_perfect", signature = (n))]
pub fn pyfn_is_perfect(n: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::is_perfect(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether the proper divisors of `n` sum to more than `n`.
///
/// Panics:
/// Panics if the divisor sum does not fit in a `u64`.
///
/// Rust: `discrete::number_theory::is_abundant`
#[pyfunction]
#[pyo3(name = "is_abundant", signature = (n))]
pub fn pyfn_is_abundant(n: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::is_abundant(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether the proper divisors of `n` sum to less than `n`.
///
/// Panics:
/// Panics if the divisor sum does not fit in a `u64`.
///
/// Rust: `discrete::number_theory::is_deficient`
#[pyfunction]
#[pyo3(name = "is_deficient", signature = (n))]
pub fn pyfn_is_deficient(n: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::is_deficient(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// All amicable pairs `(a, b)` with `a < b <= limit`.
///
/// A pair is amicable when each number is the sum of the other's proper
/// divisors. Aliquot sums are built by one `O(limit log limit)` sieve.
///
/// Rust: `discrete::number_theory::amicable_pairs`
#[pyfunction]
#[pyo3(name = "amicable_pairs", signature = (limit))]
pub fn pyfn_amicable_pairs<'py>(py: Python<'py>, limit: u64) -> PyResult<Vec<(u64, u64)>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::amicable_pairs(limit)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
}

/// The least `k > 0` with `a^k == 1 (mod n)`, or `None` when `a` and `n`
/// are not coprime.
///
/// The trivial group modulo `1` gives `Some(1)`.
///
/// Rust: `discrete::number_theory::multiplicative_order`
#[pyfunction]
#[pyo3(name = "multiplicative_order", signature = (a, n))]
pub fn pyfn_multiplicative_order(a: u64, n: u64) -> PyResult<Option<u64>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::multiplicative_order(a, n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// The least primitive root modulo the prime `p`, or `None` when `p` is
/// not prime.
///
/// A primitive root generates the whole multiplicative group, so its
/// order is `p - 1`.
///
/// Rust: `discrete::number_theory::primitive_root`
#[pyfunction]
#[pyo3(name = "primitive_root", signature = (p))]
pub fn pyfn_primitive_root(p: u64) -> PyResult<Option<u64>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::primitive_root(p));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// Every primitive root modulo the prime `p`, ascending.
///
/// There are `euler_phi(p - 1)` of them; the list is empty when `p` is
/// not prime.
///
/// Rust: `discrete::number_theory::all_primitive_roots`
#[pyfunction]
#[pyo3(name = "all_primitive_roots", signature = (p))]
pub fn pyfn_all_primitive_roots<'py>(py: Python<'py>, p: u64) -> PyResult<Vec<u64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::all_primitive_roots(p)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Discrete logarithm by baby-step giant-step: the least `x >= 0` with
/// `base^x == target (mod modulus)`, or `None` when none exists.
///
/// The modulus is arbitrary — a leading reduction strips the common
/// factors of `base` and `modulus` before the classical coprime search,
/// so `base` need not be invertible. Time and memory are both
/// `O(sqrt(modulus))`.
///
/// Rust: `discrete::number_theory::discrete_log_bsgs`
#[pyfunction]
#[pyo3(name = "discrete_log_bsgs", signature = (base, target, modulus))]
pub fn pyfn_discrete_log_bsgs(base: u64, target: u64, modulus: u64) -> PyResult<Option<u64>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::discrete_log_bsgs(base, target, modulus));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// Discrete logarithm modulo a prime by the Pohlig-Hellman reduction.
///
/// `factorization` is the factorization of the order of `base` — for a
/// primitive root, that of `p - 1`, as produced by
/// `discrete::primes::factorize`. The logarithm is recovered in
/// each prime-power subgroup and glued by the CRT, which costs
/// `O(sum e_i (log n + sqrt(q_i)))` instead of `O(sqrt(p))`.
///
/// Returns `None` when `p` is not an odd prime, when the factorization
/// does not describe the order of `base`, or when no logarithm exists.
///
/// Rust: `discrete::number_theory::discrete_log_pohlig_hellman`
#[pyfunction]
#[pyo3(name = "discrete_log_pohlig_hellman", signature = (base, target, p, factorization))]
pub fn pyfn_discrete_log_pohlig_hellman<'py>(py: Python<'py>, base: u64, target: u64, p: u64, factorization: Vec<(u64, u32)>) -> PyResult<Option<u64>> {
    let _ = py;
    let factorization = factorization.into_iter().map(|__e| (__e.0, __e.1)).collect::<Vec<_>>();
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::discrete_log_pohlig_hellman(base, target, p, &factorization)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// The Legendre symbol `(a/p)`: `0` when `p` divides `a`, `1` when `a`
/// is a nonzero quadratic residue, `-1` otherwise.
///
/// Panics:
/// Panics unless `p` is an odd prime.
///
/// Rust: `discrete::number_theory::legendre_symbol`
#[pyfunction]
#[pyo3(name = "legendre_symbol", signature = (a, p))]
pub fn pyfn_legendre_symbol(a: i64, p: u64) -> PyResult<i8> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::legendre_symbol(a, p));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Jacobi symbol `(a/n)` for odd `n > 0`, by reciprocity.
///
/// Equal to the Legendre symbol when `n` is prime. A value of `1` for
/// composite `n` does not imply that `a` is a residue.
///
/// Panics:
/// Panics if `n` is even or zero.
///
/// Rust: `discrete::number_theory::jacobi_symbol`
#[pyfunction]
#[pyo3(name = "jacobi_symbol", signature = (a, n))]
pub fn pyfn_jacobi_symbol(a: i64, n: u64) -> PyResult<i8> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::jacobi_symbol(a, n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A square root of `a` modulo the prime `p` by Tonelli-Shanks, or
/// `None` when `a` is a non-residue.
///
/// The smaller of the two roots is returned, so the result is always in
/// `[0, p/2]`.
///
/// Panics:
/// Panics unless `p` is prime.
///
/// Rust: `discrete::number_theory::tonelli_shanks`
#[pyfunction]
#[pyo3(name = "tonelli_shanks", signature = (a, p))]
pub fn pyfn_tonelli_shanks(a: u64, p: u64) -> PyResult<Option<u64>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::tonelli_shanks(a, p));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// The nonzero quadratic residues modulo the odd prime `p`, ascending.
///
/// There are exactly `(p - 1) / 2` of them. The list is empty when `p`
/// is not an odd prime.
///
/// Rust: `discrete::number_theory::quadratic_residues`
#[pyfunction]
#[pyo3(name = "quadratic_residues", signature = (p))]
pub fn pyfn_quadratic_residues<'py>(py: Python<'py>, p: u64) -> PyResult<Vec<u64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::quadratic_residues(p)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Carmichael's `lambda(n)`: the exponent of the group of units modulo
/// `n`, that is the least `k` with `a^k == 1 (mod n)` for every `a`
/// coprime to `n`.
///
/// Always a divisor of `euler_phi(n)`. `lambda(0)` is defined as `0`.
///
/// Rust: `discrete::number_theory::carmichael_lambda`
#[pyfunction]
#[pyo3(name = "carmichael_lambda", signature = (n))]
pub fn pyfn_carmichael_lambda(n: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::carmichael_lambda(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether `n` is a Carmichael number: composite, yet `a^(n-1) == 1
/// (mod n)` for every `a` coprime to `n`.
///
/// Decided by Korselt's criterion — `n` odd, squarefree, and `p - 1`
/// divides `n - 1` for every prime `p` dividing `n`.
///
/// Rust: `discrete::number_theory::is_carmichael`
#[pyfunction]
#[pyo3(name = "is_carmichael", signature = (n))]
pub fn pyfn_is_carmichael(n: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::is_carmichael(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The sum of the digits of `n` written in `base`.
///
/// Panics:
/// Panics if `base < 2`.
///
/// Rust: `discrete::number_theory::digit_sum`
#[pyfunction]
#[pyo3(name = "digit_sum", signature = (n, base))]
pub fn pyfn_digit_sum(n: u64, base: u32) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::digit_sum(n, base));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The digital root: repeated digit sums until a single digit remains.
///
/// Equal to `1 + (n - 1) mod (base - 1)` for positive `n`, which is the
/// closed form used here.
///
/// Panics:
/// Panics if `base < 2`.
///
/// Rust: `discrete::number_theory::digital_root`
#[pyfunction]
#[pyo3(name = "digital_root", signature = (n, base))]
pub fn pyfn_digital_root(n: u64, base: u32) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::digital_root(n, base));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether the digits of `n` in `base` read the same both ways.
///
/// Panics:
/// Panics if `base < 2`.
///
/// Rust: `discrete::number_theory::is_palindrome`
#[pyfunction]
#[pyo3(name = "is_palindrome", signature = (n, base))]
pub fn pyfn_is_palindrome(n: u64, base: u32) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::is_palindrome(n, base));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// `n` with its digits in `base` reversed.
///
/// Panics:
/// Panics if `base < 2`, or if the reversed value overflows a `u64`.
///
/// Rust: `discrete::number_theory::reverse_digits`
#[pyfunction]
#[pyo3(name = "reverse_digits", signature = (n, base))]
pub fn pyfn_reverse_digits(n: u64, base: u32) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::reverse_digits(n, base));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Whether iterating the sum of squared decimal digits reaches `1`.
///
/// Cycle detection is by Floyd's algorithm; `0` is not happy.
///
/// Rust: `discrete::number_theory::happy_number`
#[pyfunction]
#[pyo3(name = "happy_number", signature = (n))]
pub fn pyfn_happy_number(n: u64) -> PyResult<bool> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::happy_number(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Collatz trajectory of `n`, from `n` down to the terminal `1`.
///
/// Empty for `n == 0`.
///
/// Panics:
/// Panics if some `3x + 1` step overflows a `u64`.
///
/// Rust: `discrete::number_theory::collatz_trajectory`
#[pyfunction]
#[pyo3(name = "collatz_trajectory", signature = (n))]
pub fn pyfn_collatz_trajectory<'py>(py: Python<'py>, n: u64) -> PyResult<Vec<u64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::collatz_trajectory(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The total stopping time: the number of Collatz steps from `n` to `1`.
///
/// Zero for `n == 0` and `n == 1`.
///
/// Panics:
/// Panics if some `3x + 1` step overflows a `u64`.
///
/// Rust: `discrete::number_theory::collatz_stopping_time`
#[pyfunction]
#[pyo3(name = "collatz_stopping_time", signature = (n))]
pub fn pyfn_collatz_stopping_time(n: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::collatz_stopping_time(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// A representation `n = a^2 + b^2` with `a <= b`, or `None` when none
/// exists.
///
/// By Fermat's two-square theorem a representation exists exactly when
/// every prime `p == 3 (mod 4)` divides `n` to an even power; that test
/// runs first, so non-representable inputs cost only a factorization.
///
/// Rust: `discrete::number_theory::sum_of_two_squares`
#[pyfunction]
#[pyo3(name = "sum_of_two_squares", signature = (n))]
pub fn pyfn_sum_of_two_squares(n: u64) -> PyResult<Option<(u64, u64)>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::sum_of_two_squares(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1)))
}

/// A representation `n = a^2 + b^2 + c^2 + d^2` with the parts
/// ascending.
///
/// Lagrange's four-square theorem guarantees one exists for every `n`.
/// The search fixes the largest part first, which leaves a small
/// remainder for the inner two-square search.
///
/// Panics:
/// Panics if no representation is found, which would contradict
/// Lagrange's theorem.
///
/// Rust: `discrete::number_theory::sum_of_four_squares`
#[pyfunction]
#[pyo3(name = "sum_of_four_squares", signature = (n))]
pub fn pyfn_sum_of_four_squares(n: u64) -> PyResult<(u64, u64, u64, u64)> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::sum_of_four_squares(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok((__v.0, __v.1, __v.2, __v.3))
}

/// Every primitive Pythagorean triple `(a, b, c)` with `a < b < c` and
/// hypotenuse `c <= limit`, ascending.
///
/// Generated by the Berggren ternary tree rooted at `(3, 4, 5)`: every
/// primitive triple is reached exactly once, so no gcd filtering or
/// deduplication is needed.
///
/// Rust: `discrete::number_theory::pythagorean_triples_primitive`
#[pyfunction]
#[pyo3(name = "pythagorean_triples_primitive", signature = (limit))]
pub fn pyfn_pythagorean_triples_primitive<'py>(py: Python<'py>, limit: u64) -> PyResult<Vec<(u64, u64, u64)>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::pythagorean_triples_primitive(limit)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1, __x.2)).collect::<Vec<_>>())
}

/// Factor a Gaussian integer into Gaussian primes.
///
/// The product of the returned list reproduces the input exactly: a
/// leading unit (`-1`, `i` or `-i`) is included whenever one is needed,
/// and the empty list is returned for the input `1` and for `0`. Rational
/// primes `p == 3 (mod 4)` stay inert and appear as `(p, 0)`; `2` splits
/// as powers of `1 + i`; primes `p == 1 (mod 4)` split into the conjugate
/// pair coming from `p = a^2 + b^2`.
///
/// Panics:
/// Panics if the norm `re^2 + im^2` does not fit in a `u64`.
///
/// Rust: `discrete::number_theory::gaussian_integer_factor`
#[pyfunction]
#[pyo3(name = "gaussian_integer_factor", signature = (re, im))]
pub fn pyfn_gaussian_integer_factor<'py>(py: Python<'py>, re: i64, im: i64) -> PyResult<Vec<(i64, i64)>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::gaussian_integer_factor(re, im)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
}

/// The Frobenius number of a coin system: the largest amount that cannot
/// be paid exactly.
///
/// `None` when the coins share a common factor (infinitely many amounts
/// are then unreachable) or when the list holds no positive coin. A coin
/// of value `1` makes every non-negative amount payable and reports `0`.
/// Two coprime coins use the closed form `ab - a - b`; more coins use a
/// Dijkstra search over the residues of the smallest coin, so memory is
/// `O(min(coins))`.
///
/// Rust: `discrete::number_theory::frobenius_number`
#[pyfunction]
#[pyo3(name = "frobenius_number", signature = (coins))]
pub fn pyfn_frobenius_number<'py>(py: Python<'py>, coins: Vec<u64>) -> PyResult<Option<u64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::frobenius_number(&coins)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| __x))
}

/// The greedy (Fibonacci-Sylvester) Egyptian-fraction expansion of a
/// positive rational: denominators `d` with `sum 1/d == r`.
///
/// Each step subtracts the largest unit fraction not exceeding the
/// remainder, which strictly reduces the numerator and therefore
/// terminates. An empty list is returned for `r <= 0`.
///
/// Rust: `discrete::number_theory::egyptian_fractions_greedy`
#[pyfunction]
#[pyo3(name = "egyptian_fractions_greedy", signature = (r))]
pub fn pyfn_egyptian_fractions_greedy<'py>(py: Python<'py>, r: crate::runtime::coerce::RationalArg) -> PyResult<Vec<pyo3::Bound<'py, pyo3::PyAny>>> {
    let r = r.0;
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::egyptian_fractions_greedy(&r));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> { Ok(crate::runtime::coerce::bigint_out(py, &__x)?) }).collect::<PyResult<Vec<_>>>()?)
}

/// The Zeckendorf representation of `n`: the unique set of
/// non-consecutive Fibonacci numbers summing to `n`, ascending.
///
/// Uses the Fibonacci numbers `1, 2, 3, 5, 8, ...`, each at most once.
/// Empty for `n == 0`.
///
/// Rust: `discrete::number_theory::zeckendorf`
#[pyfunction]
#[pyo3(name = "zeckendorf", signature = (n))]
pub fn pyfn_zeckendorf<'py>(py: Python<'py>, n: u64) -> PyResult<Vec<u64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::zeckendorf(n)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// The Lucas sequence `U_n(P, Q) mod m`, where `U_0 = 0`, `U_1 = 1` and
/// `U_n = P*U_{n-1} - Q*U_{n-2}`.
///
/// `U_n(1, -1)` is the Fibonacci sequence. Evaluated by the recurrence,
/// so the cost is linear in `n`. Returns `0` for `m <= 1`.
///
/// Rust: `discrete::number_theory::lucas_sequence_u`
#[pyfunction]
#[pyo3(name = "lucas_sequence_u", signature = (p, q, n, m))]
pub fn pyfn_lucas_sequence_u(p: i64, q: i64, n: u64, m: u64) -> PyResult<u64> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::lucas_sequence_u(p, q, n, m));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Every integer solution of `a*x^2 + b*y^2 == c`, ascending.
///
/// Only the definite case is enumerable: with `a > 0`, `b > 0` and
/// `c >= 0` the solution set is finite and is returned in full. An
/// indefinite form (a Pell-type equation) has infinitely many solutions,
/// so an empty list is returned there instead.
///
/// Rust: `discrete::number_theory::quadratic_diophantine_solve`
#[pyfunction]
#[pyo3(name = "quadratic_diophantine_solve", signature = (a, b, c))]
pub fn pyfn_quadratic_diophantine_solve<'py>(py: Python<'py>, a: i64, b: i64, c: i64) -> PyResult<Vec<(i64, i64)>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::quadratic_diophantine_solve(a, b, c)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.into_iter().map(|__x| (__x.0, __x.1)).collect::<Vec<_>>())
}

/// Solve `a*x + b*y == c` over the integers.
///
/// Returns `(x0, y0, dx, dy)`: a particular solution together with the
/// homogeneous step, so that `(x0 + t*dx, y0 + t*dy)` is a solution for
/// every integer `t` and every solution has this form. `None` when
/// `gcd(a, b)` does not divide `c`, when both coefficients are zero, or
/// when the particular solution overflows an `i64`.
///
/// Rust: `discrete::number_theory::linear_diophantine`
#[pyfunction]
#[pyo3(name = "linear_diophantine", signature = (a, b, c))]
pub fn pyfn_linear_diophantine(a: i64, b: i64, c: i64) -> PyResult<Option<(i64, i64, i64, i64)>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::linear_diophantine(a, b, c));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v.map(|__x| (__x.0, __x.1, __x.2, __x.3)))
}

/// The `n`-th positive rational in breadth-first order on the
/// Stern-Brocot tree, counting the root `1/1` as `n == 1`.
///
/// The bits of `n` below its leading bit spell the descent: `0` goes
/// left, `1` goes right, and each node is the mediant of its bounding
/// ancestors. Every positive rational appears exactly once, already in
/// lowest terms.
///
/// Panics:
/// Panics if `n == 0`.
///
/// Rust: `discrete::number_theory::stern_brocot_nth`
#[pyfunction]
#[pyo3(name = "stern_brocot_nth", signature = (n))]
pub fn pyfn_stern_brocot_nth<'py>(py: Python<'py>, n: u64) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::stern_brocot_nth(n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::rational_out(py, &__v)?)
}

/// The next fraction after `a` in the Farey sequence of order `n`.
///
/// The successor `r/s` is the unique fraction with `s <= n` and
/// `r*q - p*s == 1` for `a = p/q`, found by solving `p*s == -1 (mod q)`
/// and taking the largest admissible `s`.
///
/// Panics:
/// Panics if `n == 0`, if `a` does not fit in `i64`, or if the
/// denominator of `a` exceeds `n`.
///
/// Rust: `discrete::number_theory::farey_next`
#[pyfunction]
#[pyo3(name = "farey_next", signature = (a, n))]
pub fn pyfn_farey_next<'py>(py: Python<'py>, a: crate::runtime::coerce::RationalArg, n: u64) -> PyResult<pyo3::Bound<'py, pyo3::PyAny>> {
    let a = a.0;
    let __r = crate::runtime::guard(|| rust_physics_engine::discrete::number_theory::farey_next(&a, n));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(crate::runtime::coerce::rational_out(py, &__v)?)
}

/// The Dirichlet convolution `(f * g)(n) = sum_{d | n} f(d) g(n/d)`.
///
/// Both slices are indexed by the argument, so element `i` holds the
/// value at `i` and element `0` is unused (it is zero on output). The
/// result has the length of the shorter input.
///
/// Rust: `discrete::number_theory::dirichlet_convolution`
#[pyfunction]
#[pyo3(name = "dirichlet_convolution", signature = (f, g))]
pub fn pyfn_dirichlet_convolution<'py>(py: Python<'py>, f: Vec<i64>, g: Vec<i64>) -> PyResult<Vec<i64>> {
    let _ = py;
    let __r = py.detach(move || crate::runtime::guard(move || rust_physics_engine::discrete::number_theory::dirichlet_convolution(&f, &g)));
    let __v = __r.map_err(crate::runtime::errors::InvalidArgumentError::new_err)?;
    Ok(__v)
}

/// Registers this module's contents.
pub fn register(py: Python<'_>, m: &Bound<'_, PyModule>) -> PyResult<()> {
    let _ = (py, m);
    m.add_function(wrap_pyfunction!(pyfn_gcd_u64, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lcm_u64, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_extended_gcd_i64, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_mod_pow_u64, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_mod_inverse_u64, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_crt, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_euler_phi, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_phi_sieve, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_mobius, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_mobius_sieve, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_divisors, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_divisor_count, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_divisor_sum, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_sigma_k, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_is_perfect, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_is_abundant, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_is_deficient, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_amicable_pairs, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_multiplicative_order, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_primitive_root, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_all_primitive_roots, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_discrete_log_bsgs, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_discrete_log_pohlig_hellman, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_legendre_symbol, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_jacobi_symbol, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_tonelli_shanks, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_quadratic_residues, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_carmichael_lambda, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_is_carmichael, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_digit_sum, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_digital_root, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_is_palindrome, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_reverse_digits, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_happy_number, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_collatz_trajectory, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_collatz_stopping_time, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_sum_of_two_squares, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_sum_of_four_squares, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_pythagorean_triples_primitive, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_gaussian_integer_factor, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_frobenius_number, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_egyptian_fractions_greedy, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_zeckendorf, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_lucas_sequence_u, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_quadratic_diophantine_solve, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_linear_diophantine, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_stern_brocot_nth, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_farey_next, m)?)?;
    m.add_function(wrap_pyfunction!(pyfn_dirichlet_convolution, m)?)?;
    Ok(())
}
