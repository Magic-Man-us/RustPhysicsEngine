"""
Quasi-random (low-discrepancy) sequences: Sobol and Halton.  Sobol points use the Gray-code construction of Bratley & Fox with the Joe-Kuo (new-joe-kuo-6) primitive polynomials and initial direction numbers, embedded here for dimensions up to 21. Halton points use the radical inverse in the first `dim` primes.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class Halton:
    """
Halton low-discrepancy sequence: dimension d uses the radical
inverse in the d-th prime.

Rust: `monte_carlo::quasi::Halton`
    """
    def __init__(self, dim: int) -> None: ...
    def next(self) -> list[float]: ...

class Sobol:
    """
Sobol low-discrepancy sequence in `dim` dimensions (dim ≤ 21).

Successive calls to `Sobol::next` return points x₁, x₂, … in
[0, 1)^dim (the origin point x₀ = 0 is skipped).

Rust: `monte_carlo::quasi::Sobol`
    """
    def __init__(self, dim: int) -> None: ...
    def next(self) -> list[float]: ...
    def skip(self, n: int) -> None: ...
    def dim(self) -> int: ...

def mc_integrate_sobol(f: Callable[[list[float]], float], dim: int, n: int) -> float:
    """
Quasi-Monte Carlo integration of f over [0, 1]^dim with the first n
Sobol points.

Panics:
Panics unless n > 0 and dim is Sobol-supported.

Rust: `monte_carlo::quasi::mc_integrate_sobol`
    """
    ...
