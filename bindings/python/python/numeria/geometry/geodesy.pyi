"""
Geodesy on a reference ellipsoid.  Vincenty's inverse and direct formulae (Vincenty, "Direct and inverse solutions of geodesics on the ellipsoid", Survey Review 1975) plus geodetic ↔ ECEF ↔ ENU coordinate conversions. Angles are radians; distances and heights are meters.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec3

class Ellipsoid:
    """
Reference ellipsoid: semi-major axis a (m) and flattening f.

Rust: `geometry::geodesy::Ellipsoid`
    """
    def __init__(self, a: float, f: float) -> None: ...
    def b(self) -> float: ...
    def e_sq(self) -> float: ...
    @property
    def a(self) -> float: ...
    @property
    def f(self) -> float: ...

def vincenty_inverse(lat1: float, lon1: float, lat2: float, lon2: float, e: Ellipsoid | Sequence[float]) -> tuple[float, float, float]:
    """
Vincenty inverse problem: geodesic distance (m) and forward/reverse
azimuths (rad) between two geodetic points.

Returns `NoConvergence` for the nearly antipodal cases where
Vincenty's lambda iteration fails, and distance 0 with azimuth 0
for coincident points.

Rust: `geometry::geodesy::vincenty_inverse`
    """
    ...

def vincenty_direct(lat1: float, lon1: float, az1: float, dist: float, e: Ellipsoid | Sequence[float]) -> tuple[float, float, float]:
    """
Vincenty direct problem: destination (lat2, lon2) and final azimuth
after traveling `dist` meters from (lat1, lon1) on initial azimuth
`az1`.

Rust: `geometry::geodesy::vincenty_direct`
    """
    ...

def geodetic_to_ecef(lat: float, lon: float, h: float, e: Ellipsoid | Sequence[float]) -> Vec3:
    """
Geodetic (lat, lon, height) → Earth-centered Earth-fixed Cartesian.

Rust: `geometry::geodesy::geodetic_to_ecef`
    """
    ...

def ecef_to_geodetic(p: Vec3 | Sequence[float], e: Ellipsoid | Sequence[float]) -> tuple[float, float, float]:
    """
ECEF Cartesian → geodetic (lat, lon, height) by fixed-point
iteration on the latitude (converges to sub-millimeter in a few
steps).

Rust: `geometry::geodesy::ecef_to_geodetic`
    """
    ...

def ecef_to_enu(p: Vec3 | Sequence[float], ref_lat: float, ref_lon: float, ref_h: float, e: Ellipsoid | Sequence[float]) -> Vec3:
    """
ECEF point → local East-North-Up coordinates relative to the given
geodetic reference.

Rust: `geometry::geodesy::ecef_to_enu`
    """
    ...
