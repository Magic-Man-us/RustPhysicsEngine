"""
Areas, volumes and surface areas of the standard shapes.  Plane figures (circle, ellipse, triangle by base-height and by Heron's formula, regular polygon, sector, annulus) and solids (sphere, cylinder, cone, ellipsoid, torus, frustum, capsule), with perimeters and surface areas alongside.  Closed-form mensuration only. For triangle solving see `trigonometry`, for curves and conics see `curves`, for polygon algorithms such as triangulation and offsetting see `patterns::polygon_ops`, and for meshes see `mesh`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import delaunay, geodesy, hull, mesh
from numeria.geometry.geodesy import Ellipsoid as Ellipsoid
from numeria.mesh import Mesh as Mesh
from numeria.geometry.mesh import RayHit as RayHit
from numeria.geometry.delaunay import circumcircle as circumcircle
from numeria.geometry.hull import convex_hull_2d as convex_hull_2d
from numeria.geometry.hull import convex_hull_3d as convex_hull_3d
from numeria.geometry.delaunay import delaunay_2d as delaunay_2d
from numeria.geometry.geodesy import ecef_to_enu as ecef_to_enu
from numeria.geometry.geodesy import ecef_to_geodetic as ecef_to_geodetic
from numeria.geometry.geodesy import geodetic_to_ecef as geodetic_to_ecef
from numeria.geometry.hull import point_in_polygon as point_in_polygon
from numeria.geometry.hull import polygon_area_signed as polygon_area_signed
from numeria.geometry.geodesy import vincenty_direct as vincenty_direct
from numeria.geometry.geodesy import vincenty_inverse as vincenty_inverse
from numeria.geometry.delaunay import voronoi_cells_2d as voronoi_cells_2d

def area_circle(radius: float) -> float:
    """
Area of a circle: A = πr²

Rust: `geometry::area_circle`
    """
    ...

def area_ellipse(semi_major: float, semi_minor: float) -> float:
    """
Area of an ellipse: A = πab

Rust: `geometry::area_ellipse`
    """
    ...

def area_triangle(base: float, height: float) -> float:
    """
Area of a triangle: A = bh/2

Rust: `geometry::area_triangle`
    """
    ...

def area_triangle_heron(a: float, b: float, c: float) -> float:
    """
Area of a triangle via Heron's formula: A = √(s(s-a)(s-b)(s-c)) where s = (a+b+c)/2

Rust: `geometry::area_triangle_heron`
    """
    ...

def area_regular_polygon(n_sides: int, side_length: float) -> float:
    """
Area of a regular polygon: A = n·s²/(4·tan(π/n))

Rust: `geometry::area_regular_polygon`
    """
    ...

def area_sector(radius: float, angle: float) -> float:
    """
Area of a circular sector: A = r²θ/2

Rust: `geometry::area_sector`
    """
    ...

def area_annulus(outer_r: float, inner_r: float) -> float:
    """
Area of an annulus: A = π(R² - r²)

Rust: `geometry::area_annulus`
    """
    ...

def volume_sphere(radius: float) -> float:
    """
Volume of a sphere: V = 4πr³/3

Rust: `geometry::volume_sphere`
    """
    ...

def volume_cylinder(radius: float, height: float) -> float:
    """
Volume of a cylinder: V = πr²h

Rust: `geometry::volume_cylinder`
    """
    ...

def volume_cone(radius: float, height: float) -> float:
    """
Volume of a cone: V = πr²h/3

Rust: `geometry::volume_cone`
    """
    ...

def volume_ellipsoid(a: float, b: float, c: float) -> float:
    """
Volume of an ellipsoid: V = 4πabc/3

Rust: `geometry::volume_ellipsoid`
    """
    ...

def volume_torus(major_r: float, minor_r: float) -> float:
    """
Volume of a torus: V = 2π²Rr²

Rust: `geometry::volume_torus`
    """
    ...

def volume_frustum(r1: float, r2: float, height: float) -> float:
    """
Volume of a frustum: V = πh(r₁² + r₁r₂ + r₂²)/3

Rust: `geometry::volume_frustum`
    """
    ...

def volume_capsule(radius: float, cylinder_height: float) -> float:
    """
Volume of a capsule (cylinder + sphere): V = πr²h + 4πr³/3

Rust: `geometry::volume_capsule`
    """
    ...

def surface_sphere(radius: float) -> float:
    """
Surface area of a sphere: A = 4πr²

Rust: `geometry::surface_sphere`
    """
    ...

def surface_cylinder_total(radius: float, height: float) -> float:
    """
Total surface area of a cylinder (lateral + both caps): A = 2πr(r + h)

Rust: `geometry::surface_cylinder_total`
    """
    ...

def surface_cylinder_lateral(radius: float, height: float) -> float:
    """
Lateral surface area of a cylinder: A = 2πrh

Rust: `geometry::surface_cylinder_lateral`
    """
    ...

def surface_cone_lateral(radius: float, slant_height: float) -> float:
    """
Lateral surface area of a cone: A = πrl

Rust: `geometry::surface_cone_lateral`
    """
    ...

def surface_torus(major_r: float, minor_r: float) -> float:
    """
Surface area of a torus: A = 4π²Rr

Rust: `geometry::surface_torus`
    """
    ...

def solid_angle_cone(half_angle: float) -> float:
    """
Solid angle subtended by a cone: Ω = 2π(1 - cos(θ))

Rust: `geometry::solid_angle_cone`
    """
    ...

def solid_angle_full_sphere() -> float:
    """
Solid angle of a full sphere: Ω = 4π steradians

Rust: `geometry::solid_angle_full_sphere`
    """
    ...

def great_circle_distance(r: float, lat1: float, lon1: float, lat2: float, lon2: float) -> float:
    """
Great-circle distance on a sphere: d = r·arccos(sin(φ₁)sin(φ₂) + cos(φ₁)cos(φ₂)cos(Δλ))

Rust: `geometry::great_circle_distance`
    """
    ...

def spherical_excess(a: float, b: float, c: float) -> float:
    """
Spherical excess of a spherical triangle: E = A + B + C - π

Rust: `geometry::spherical_excess`
    """
    ...

def moi_solid_sphere(mass: float, radius: float) -> float:
    """
Moment of inertia of a solid sphere: I = 2mr²/5

Rust: `geometry::moi_solid_sphere`
    """
    ...

def moi_hollow_sphere(mass: float, radius: float) -> float:
    """
Moment of inertia of a hollow sphere (thin shell): I = 2mr²/3

Rust: `geometry::moi_hollow_sphere`
    """
    ...

def moi_solid_cylinder(mass: float, radius: float) -> float:
    """
Moment of inertia of a solid cylinder about its axis: I = mr²/2

Rust: `geometry::moi_solid_cylinder`
    """
    ...

def moi_thin_rod_center(mass: float, length: float) -> float:
    """
Moment of inertia of a thin rod about its center: I = mL²/12

Rust: `geometry::moi_thin_rod_center`
    """
    ...

def moi_thin_rod_end(mass: float, length: float) -> float:
    """
Moment of inertia of a thin rod about one end: I = mL²/3

Rust: `geometry::moi_thin_rod_end`
    """
    ...

def moi_rectangular_plate(mass: float, width: float, height: float) -> float:
    """
Moment of inertia of a rectangular plate about its center: I = m(w² + h²)/12

Rust: `geometry::moi_rectangular_plate`
    """
    ...

def parallel_axis(i_cm: float, mass: float, distance: float) -> float:
    """
Parallel axis theorem: I = I_cm + md²

Rust: `geometry::parallel_axis`
    """
    ...
