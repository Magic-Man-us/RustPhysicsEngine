"""
Convex hulls and polygon predicates.  2-D hull: Andrew's monotone chain (O(n log n)), CCW output. 3-D hull: incremental visible-face (quickhull-style) algorithm returning triangle index triples with outward-facing orientation.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec3

def convex_hull_2d(points: list[tuple[float, float]]) -> list[tuple[float, float]]:
    """
Convex hull of 2-D points by monotone chain, returned in
counter-clockwise order without repetition of the first vertex.
Collinear boundary points are dropped. Fewer than 3 distinct points
return the distinct points themselves.

Rust: `geometry::hull::convex_hull_2d`
    """
    ...

def polygon_area_signed(poly: list[tuple[float, float]]) -> float:
    """
Shoelace formula: positive for counter-clockwise vertex order.

Panics:
Panics if the polygon has fewer than 3 vertices.

Rust: `geometry::hull::polygon_area_signed`
    """
    ...

def point_in_polygon(p: tuple[float, float], poly: list[tuple[float, float]]) -> bool:
    """
Even-odd (ray casting) point-in-polygon test; boundary points count
as inside up to floating-point tolerance.

Panics:
Panics if the polygon has fewer than 3 vertices.

Rust: `geometry::hull::point_in_polygon`
    """
    ...

def convex_hull_3d(points: list[Vec3 | Sequence[float]]) -> list[list[int]]:
    """
Convex hull of 3-D points as outward-oriented triangle index
triples, by the incremental visible-face (quickhull-style)
algorithm.

Panics:
Panics with fewer than 4 points or fully degenerate (coplanar)
input.

Rust: `geometry::hull::convex_hull_3d`
    """
    ...
