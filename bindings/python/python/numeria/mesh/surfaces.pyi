"""
Parametric surfaces: Bézier/B-spline/NURBS patches, classic surface constructions, differential geometry via fundamental forms, and a catalogue of named surfaces.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec2
from numeria.math import Vec3

class BSplineSurface:
    """
Tensor-product B-spline surface. Parameters range over
`[knots[degree], knots[len - degree - 1]]` in each direction.

Rust: `mesh::surfaces::BSplineSurface`
    """
    def __init__(self, degree_u: int, degree_v: int, knots_u: list[float], knots_v: list[float], control: list[list[Vec3 | Sequence[float]]]) -> None: ...
    @staticmethod
    def uniform(degree_u: int, degree_v: int, control: list[list[Vec3 | Sequence[float]]]) -> BSplineSurface: ...
    def eval(self, u: float, v: float) -> Vec3: ...
    def normal(self, u: float, v: float) -> Vec3: ...
    def to_mesh(self, nu: int, nv: int) -> Mesh: ...
    @property
    def degree_u(self) -> int: ...
    @property
    def degree_v(self) -> int: ...
    @property
    def knots_u(self) -> list[float]: ...
    @property
    def knots_v(self) -> list[float]: ...
    @property
    def control(self) -> list[list[Vec3]]: ...

class BezierPatch:
    """
Bicubic Bézier patch; `control[i][j]` weights the Bernstein product
B_i(u) B_j(v).

Rust: `mesh::surfaces::BezierPatch`
    """
    def eval(self, u: float, v: float) -> Vec3: ...
    def du(self, u: float, v: float) -> Vec3: ...
    def dv(self, u: float, v: float) -> Vec3: ...
    def normal(self, u: float, v: float) -> Vec3: ...
    def to_mesh(self, nu: int, nv: int) -> Mesh: ...
    def subdivide(self) -> list[BezierPatch]: ...
    @property
    def control(self) -> list[list[Vec3]]: ...

class FundamentalForms:
    """
First (E, F, G) and second (L, M, N) fundamental form coefficients
of a parametric surface.

Rust: `mesh::surfaces::FundamentalForms`
    """
    def __init__(self, e: float, f: float, g: float, l: float, m: float, n: float) -> None: ...
    @property
    def e(self) -> float: ...
    @property
    def f(self) -> float: ...
    @property
    def g(self) -> float: ...
    @property
    def l(self) -> float: ...
    @property
    def m(self) -> float: ...
    @property
    def n(self) -> float: ...

class NurbsSurface:
    """
Tensor-product NURBS surface (rational B-spline): projective
weights allow exact conics.

Rust: `mesh::surfaces::NurbsSurface`
    """
    def __init__(self, degree_u: int, degree_v: int, knots_u: list[float], knots_v: list[float], control: list[list[Vec3 | Sequence[float]]], weights: list[list[float]]) -> None: ...
    def eval(self, u: float, v: float) -> Vec3: ...
    def to_mesh(self, nu: int, nv: int) -> Mesh: ...
    @staticmethod
    def sphere(r: float) -> NurbsSurface: ...
    @staticmethod
    def torus(big_r: float, r: float) -> NurbsSurface: ...
    @staticmethod
    def cylinder(r: float, h: float) -> NurbsSurface: ...
    @property
    def degree_u(self) -> int: ...
    @property
    def degree_v(self) -> int: ...
    @property
    def knots_u(self) -> list[float]: ...
    @property
    def knots_v(self) -> list[float]: ...
    @property
    def control(self) -> list[list[Vec3]]: ...
    @property
    def weights(self) -> list[list[float]]: ...

def surface_of_revolution(profile: Callable[[float], Vec2 | Sequence[float]], t: float, theta: float) -> Vec3:
    """
Point of the surface of revolution of a planar profile
`t ↦ (radius, height)` rotated by `theta` around the y axis.

Rust: `mesh::surfaces::surface_of_revolution`
    """
    ...

def ruled_surface(c1: Callable[[float], Vec3 | Sequence[float]], c2: Callable[[float], Vec3 | Sequence[float]], u: float, v: float) -> Vec3:
    """
Ruled surface: linear blend between two curves,
`(1 − v) c1(u) + v c2(u)`.

Rust: `mesh::surfaces::ruled_surface`
    """
    ...

def coons_patch(c0: Callable[[float], Vec3 | Sequence[float]], c1: Callable[[float], Vec3 | Sequence[float]], d0: Callable[[float], Vec3 | Sequence[float]], d1: Callable[[float], Vec3 | Sequence[float]], u: float, v: float) -> Vec3:
    """
Bilinearly blended Coons patch interpolating four boundary curves:
`c0` (v = 0), `c1` (v = 1) over u, and `d0` (u = 0), `d1` (u = 1)
over v. The curves must agree at the corners.

Rust: `mesh::surfaces::coons_patch`
    """
    ...

def fundamental_forms(f: Callable[[float, float], Vec3 | Sequence[float]], u: float, v: float, h: float) -> FundamentalForms:
    """
Fundamental forms by central finite differences with step `h`.

Panics:
Panics unless `h > 0` and the surface is regular (nonzero
`f_u × f_v`) at `(u, v)`.

Rust: `mesh::surfaces::fundamental_forms`
    """
    ...

def gaussian_curvature(forms: FundamentalForms | Sequence[float]) -> float:
    """
Gaussian curvature K = (LN − M²) / (EG − F²).

Rust: `mesh::surfaces::gaussian_curvature`
    """
    ...

def mean_curvature(forms: FundamentalForms | Sequence[float]) -> float:
    """
Mean curvature H = (EN − 2FM + GL) / (2 (EG − F²)).

Rust: `mesh::surfaces::mean_curvature`
    """
    ...

def principal_curvatures(forms: FundamentalForms | Sequence[float]) -> tuple[float, float]:
    """
Principal curvatures `(κ₁, κ₂)` with κ₁ ≥ κ₂:
H ± sqrt(H² − K).

Rust: `mesh::surfaces::principal_curvatures`
    """
    ...

def surface_area_parametric(f: Callable[[float, float], Vec3 | Sequence[float]], u_range: tuple[float, float], v_range: tuple[float, float], nu: int, nv: int) -> float:
    """
Surface area of a parametric patch by the midpoint rule on
`nu` x `nv` cells: Σ |f_u × f_v| du dv, partials by central
differences at each cell midpoint.

Panics:
Panics unless `nu >= 1` and `nv >= 1`.

Rust: `mesh::surfaces::surface_area_parametric`
    """
    ...

def mobius_strip(u: float, v: float, r: float, w: float) -> Vec3:
    """
Möbius strip of center radius `r` and half-width `w`:
u ∈ [0, 2π) around, v ∈ [−1, 1] across.

Rust: `mesh::surfaces::mobius_strip`
    """
    ...

def klein_bottle(u: float, v: float) -> Vec3:
    """
Figure-8 immersion of the Klein bottle, u, v ∈ [0, 2π).

Rust: `mesh::surfaces::klein_bottle`
    """
    ...

def enneper(u: float, v: float) -> Vec3:
    """
Enneper's minimal surface.

Rust: `mesh::surfaces::enneper`
    """
    ...

def catenoid(u: float, v: float, c: float) -> Vec3:
    """
Catenoid (minimal): u around, v along the axis, waist radius `c`.

Rust: `mesh::surfaces::catenoid`
    """
    ...

def helicoid(u: float, v: float, c: float) -> Vec3:
    """
Helicoid (minimal): pitch parameter `c`.

Rust: `mesh::surfaces::helicoid`
    """
    ...

def monkey_saddle(u: float, v: float) -> Vec3:
    """
Monkey saddle z = u³ − 3uv².

Rust: `mesh::surfaces::monkey_saddle`
    """
    ...

def dini(u: float, v: float, a: float, b: float) -> Vec3:
    """
Dini's surface (constant negative curvature −1/(a² + b²)):
a twisted pseudosphere. v ∈ (0, π).

Rust: `mesh::surfaces::dini`
    """
    ...

def boy_surface(u: float, v: float) -> Vec3:
    """
Boy's surface (Apéry parametrization of the real projective
plane), u ∈ [−π/2, π/2], v ∈ [0, π/2].

Rust: `mesh::surfaces::boy_surface`
    """
    ...

def superellipsoid(u: float, v: float, a: Vec3 | Sequence[float], e1: float, e2: float) -> Vec3:
    """
Superellipsoid with semi-axes `a` and exponents `e1` (latitude),
`e2` (longitude): u = longitude ∈ [−π, π], v = latitude ∈
[−π/2, π/2].

Rust: `mesh::surfaces::superellipsoid`
    """
    ...

def supershape_2d(theta: float, m: float, n1: float, n2: float, n3: float, a: float, b: float) -> Vec2:
    """
Gielis superformula in the plane:
r(θ) = (|cos(mθ/4)/a|^n2 + |sin(mθ/4)/b|^n3)^(−1/n1).

Panics:
Panics unless `a, b > 0` and `n1 != 0`.

Rust: `mesh::surfaces::supershape_2d`
    """
    ...

def supershape_3d(theta: float, phi: float, params: list[float]) -> Vec3:
    """
3-D supershape: the spherical product of two superformulas,
`params = [m1, n11, n12, n13, a1, b1, m2, n21, n22, n23, a2, b2]`
with θ = longitude ∈ [−π, π], φ = latitude ∈ [−π/2, π/2].

Rust: `mesh::surfaces::supershape_3d`
    """
    ...
