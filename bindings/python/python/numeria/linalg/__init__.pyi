"""
Dense and sparse linear algebra.  `Matrix` is the dense row-major `f64` type everything here operates on. The factorizations are chosen by what the matrix is: `lu` with partial pivoting for a general square solve, `cholesky` for symmetric positive-definite (half the work, and it fails cleanly if the matrix is not), `qr` by Householder reflections for least squares, `svd` by one-sided Jacobi for rank and pseudo-inverse, and `tridiagonal` for the Thomas algorithm in O(n).  `eigen` provides the symmetric eigenproblem and general eigenvalues. `sparse` provides CSR storage with conjugate gradient and a Jacobi-preconditioned variant, for the large systems that the PDE solvers in `fem` produce.  Note that `pcg_jacobi`'s tolerance is relative to the norm of the right-hand side, not absolute.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import cholesky, eigen, lu, matrix, qr, sparse, svd, tridiagonal
from numeria.math import Vec3
from numeria.linalg.sparse import CsrMatrix as CsrMatrix
from numeria.linalg.lu import Lu as Lu
from numeria.linalg.matrix import Matrix as Matrix
from numeria.linalg.qr import Qr as Qr
from numeria.linalg.svd import Svd as Svd
from numeria.linalg.eigen import SymEigen as SymEigen
from numeria.linalg.cholesky import cholesky_solve as cholesky_solve
from numeria.linalg.sparse import conjugate_gradient as conjugate_gradient
from numeria.linalg.eigen import eigen_symmetric as eigen_symmetric
from numeria.linalg.tridiagonal import eigen_symmetric_tridiagonal as eigen_symmetric_tridiagonal
from numeria.linalg.eigen import eigenvalues_general as eigenvalues_general
from numeria.linalg.svd import kabsch as kabsch
from numeria.linalg.qr import least_squares as least_squares
from numeria.linalg.lu import lu_decompose as lu_decompose
from numeria.linalg.sparse import pcg_jacobi as pcg_jacobi
from numeria.linalg.svd import pseudoinverse as pseudoinverse
from numeria.linalg.qr import qr_householder as qr_householder
from numeria.linalg.svd import rank as rank
from numeria.linalg.lu import solve as solve
from numeria.linalg.tridiagonal import thomas_solve as thomas_solve

class Mat3:
    """

Rust: `linalg::Mat3`
    """
    @staticmethod
    def zero() -> Mat3: ...
    @staticmethod
    def identity() -> Mat3: ...
    @staticmethod
    def from_rows(r0: list[float], r1: list[float], r2: list[float]) -> Mat3: ...
    def determinant(self) -> float: ...
    def transpose(self) -> Mat3: ...
    def inverse(self) -> Optional[Mat3]: ...
    def trace(self) -> float: ...
    def mul_vec(self, v: Vec3 | Sequence[float]) -> Vec3: ...
    def mul_mat(self, other: Mat3) -> Mat3: ...
    @staticmethod
    def scale(s: float) -> Mat3: ...
    def principal_axes(self) -> tuple[list[float], Mat3]: ...
    def principal_axes_3x3(self) -> tuple[list[float], Mat3]: ...
    def mul_scalar(self, s: float) -> Mat3: ...
    @property
    def data(self) -> list[list[float]]: ...

class Mat4:
    """
A dense 4x4 matrix with fixed-size storage.

Rust: `linalg::Mat4`
    """
    @staticmethod
    def zero() -> Mat4: ...
    @staticmethod
    def identity() -> Mat4: ...
    @staticmethod
    def from_rows(r0: list[float], r1: list[float], r2: list[float], r3: list[float]) -> Mat4: ...
    def mul_mat(self, other: Mat4) -> Mat4: ...
    def mul_vec4(self, v: list[float]) -> list[float]: ...
    def transpose(self) -> Mat4: ...
    def trace(self) -> float: ...
    def determinant(self) -> float: ...
    def inverse(self) -> Optional[Mat4]: ...
    def to_matrix(self) -> Matrix: ...
    @staticmethod
    def from_matrix(m: Matrix | Sequence[Sequence[float]]) -> Mat4: ...
    @property
    def data(self) -> list[list[float]]: ...

def rotation_x(angle: float) -> Mat3:
    """
Rotation matrix about the x-axis by the given angle in radians.

Rust: `linalg::rotation_x`
    """
    ...

def rotation_y(angle: float) -> Mat3:
    """
Rotation matrix about the y-axis by the given angle in radians.

Rust: `linalg::rotation_y`
    """
    ...

def rotation_z(angle: float) -> Mat3:
    """
Rotation matrix about the z-axis by the given angle in radians.

Rust: `linalg::rotation_z`
    """
    ...

def rotation_axis_angle(axis: Vec3 | Sequence[float], angle: float) -> Mat3:
    """
Rodrigues' rotation formula: rotate by `angle` radians about `axis`.
The axis is normalized internally.

Rust: `linalg::rotation_axis_angle`
    """
    ...

def cartesian_to_spherical(x: float, y: float, z: float) -> tuple[float, float, float]:
    """
Returns (r, theta, phi) where theta is the polar angle from +z and phi is the azimuthal angle from +x.

Rust: `linalg::cartesian_to_spherical`
    """
    ...

def spherical_to_cartesian(r: float, theta: float, phi: float) -> tuple[float, float, float]:
    """
Converts spherical coordinates (r, theta, phi) to Cartesian (x, y, z).

Rust: `linalg::spherical_to_cartesian`
    """
    ...

def cartesian_to_cylindrical(x: float, y: float, z: float) -> tuple[float, float, float]:
    """
Returns (rho, phi, z) where rho is the radial distance in the xy-plane and phi is the azimuthal angle from +x.

Rust: `linalg::cartesian_to_cylindrical`
    """
    ...

def cylindrical_to_cartesian(rho: float, phi: float, z: float) -> tuple[float, float, float]:
    """
Converts cylindrical coordinates (rho, phi, z) to Cartesian (x, y, z).

Rust: `linalg::cylindrical_to_cartesian`
    """
    ...

def polar_to_cartesian(r: float, theta: float) -> tuple[float, float]:
    """
Converts 2D polar coordinates (r, theta) to Cartesian (x, y).

Rust: `linalg::polar_to_cartesian`
    """
    ...

def cartesian_to_polar(x: float, y: float) -> tuple[float, float]:
    """
Returns (r, theta) where theta is the angle from +x.

Rust: `linalg::cartesian_to_polar`
    """
    ...
