"""
QR decomposition by Householder reflections and least-squares solve.  Reference: Golub & Van Loan, *Matrix Computations*, §5.2: A = Q·R with Q orthogonal (m×m) and R upper trapezoidal (m×n).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.linalg.matrix import Matrix

class Qr:
    """
QR factorization A = Q·R.

Rust: `linalg::qr::Qr`
    """
    def __init__(self, q: Matrix | Sequence[Sequence[float]], r: Matrix | Sequence[Sequence[float]]) -> None: ...
    @property
    def q(self) -> Matrix: ...
    @property
    def r(self) -> Matrix: ...

def qr_householder(a: Matrix | Sequence[Sequence[float]]) -> Qr:
    """
Factors A (m×n) as Q·R using Householder reflections.

Q is m×m orthogonal; R is m×n with zeros below the diagonal.

Rust: `linalg::qr::qr_householder`
    """
    ...

def least_squares(a: Matrix | Sequence[Sequence[float]], b: list[float]) -> list[float]:
    """
Solves the least-squares problem min ‖A·x − b‖₂ via QR.

Requires m ≥ n and full column rank; returns `Singular` when R has a
negligible diagonal entry, `DimensionMismatch` when `b.len() != m`,
and `InvalidArgument` when the system is underdetermined (m < n).

Rust: `linalg::qr::least_squares`
    """
    ...
