"""
Eigenvalue solvers.  Symmetric matrices use the cyclic Jacobi rotation method (Golub & Van Loan §8.5), which is unconditionally convergent. General real matrices are reduced to upper Hessenberg form by Gaussian similarity transformations and their eigenvalues extracted with the Francis-shift QR iteration (Wilkinson, *The Algebraic Eigenvalue Problem*; the classic `hqr` algorithm).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.linalg.matrix import Matrix

class SymEigen:
    """
Eigen-decomposition of a symmetric matrix: A·vᵢ = λᵢ·vᵢ.

`values[i]` pairs with column i of `vectors`; entries are sorted in
descending eigenvalue order and the vectors are orthonormal.

Rust: `linalg::eigen::SymEigen`
    """
    def __init__(self, values: list[float], vectors: Matrix | Sequence[Sequence[float]]) -> None: ...
    @property
    def values(self) -> list[float]: ...
    @property
    def vectors(self) -> Matrix: ...

def eigen_symmetric(a: Matrix | Sequence[Sequence[float]], tol: float, max_sweeps: int) -> SymEigen:
    """
Cyclic Jacobi eigen-decomposition of a symmetric matrix.

Sweeps Givens rotations over all off-diagonal pairs until the
off-diagonal Frobenius norm falls below `tol` (relative to ‖A‖) or
`max_sweeps` is exhausted, in which case `NoConvergence` is returned.

Rust: `linalg::eigen::eigen_symmetric`
    """
    ...

def eigenvalues_general(a: Matrix | Sequence[Sequence[float]], max_iter: int) -> list[complex]:
    """
All eigenvalues (possibly complex) of a general real square matrix,
by Hessenberg reduction followed by Francis-shift QR iteration.

`max_iter` bounds the QR iterations spent per eigenvalue (30 is the
classical choice).

Rust: `linalg::eigen::eigenvalues_general`
    """
    ...
