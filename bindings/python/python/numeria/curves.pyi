"""
Plane curves: conics, Bézier curves, and parametric families.  The conic sections with their eccentricities, foci and the discriminant that classifies a general quadratic; quadratic and cubic Bézier curves in 2-D and 3-D; and parametric circles, ellipses, spirals, Lissajous figures, cycloids and helices.  Arc length and signed curvature close the module. For subdivision surfaces and B-spline or NURBS patches see `mesh::surfaces`; for space curves with Frenet frames see `patterns::knots`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.patterns.tilings import Archimedean
from numeria.spatial.primitives import Circle
from numeria.math import Vec3

def circle_area(radius: float) -> float:
    """
Area of a circle: A = πr²

Rust: `curves::circle_area`
    """
    ...

def circle_circumference(radius: float) -> float:
    """
Circle circumference: C = 2πr

Rust: `curves::circle_circumference`
    """
    ...

def circle_equation(x: float, y: float, cx: float, cy: float, r: float) -> float:
    """
Returns (x-cx)^2 + (y-cy)^2 - r^2. Zero means the point lies on the circle.

Rust: `curves::circle_equation`
    """
    ...

def ellipse_circumference_approx(a: float, b: float) -> float:
    """
Ramanujan approximation: pi * (3(a+b) - sqrt((3a+b)(a+3b)))

Rust: `curves::ellipse_circumference_approx`
    """
    ...

def ellipse_equation(x: float, y: float, a: float, b: float) -> float:
    """
Returns x^2/a^2 + y^2/b^2 - 1. Zero means the point lies on the ellipse.

Rust: `curves::ellipse_equation`
    """
    ...

def ellipse_eccentricity(a: float, b: float) -> float:
    """
Eccentricity e = sqrt(1 - b^2/a^2) for a > b.

Rust: `curves::ellipse_eccentricity`
    """
    ...

def parabola_focus(a: float) -> float:
    """
Focus distance f = 1/(4a) for parabola y = ax^2.

Rust: `curves::parabola_focus`
    """
    ...

def parabola_equation(x: float, a: float) -> float:
    """
Parabola equation: y = ax²

Rust: `curves::parabola_equation`
    """
    ...

def hyperbola_eccentricity(a: float, b: float) -> float:
    """
Eccentricity e = sqrt(1 + b^2/a^2).

Rust: `curves::hyperbola_eccentricity`
    """
    ...

def hyperbola_asymptote_slope(a: float, b: float) -> float:
    """
Asymptote slope of a hyperbola: m = b/a

Rust: `curves::hyperbola_asymptote_slope`
    """
    ...

def conic_discriminant(a: float, b: float, c: float) -> float:
    """
Discriminant B^2 - 4AC for general conic Ax^2 + Bxy + Cy^2 + ...
Negative => ellipse, zero => parabola, positive => hyperbola.

Rust: `curves::conic_discriminant`
    """
    ...

def bezier_quadratic(t: float, p0: tuple[float, float], p1: tuple[float, float], p2: tuple[float, float]) -> tuple[float, float]:
    """
Quadratic Bezier curve point: B(t) = (1-t)²P₀ + 2(1-t)tP₁ + t²P₂

Rust: `curves::bezier_quadratic`
    """
    ...

def bezier_cubic(t: float, p0: tuple[float, float], p1: tuple[float, float], p2: tuple[float, float], p3: tuple[float, float]) -> tuple[float, float]:
    """
Cubic Bezier curve point: B(t) = (1-t)³P₀ + 3(1-t)²tP₁ + 3(1-t)t²P₂ + t³P₃

Rust: `curves::bezier_cubic`
    """
    ...

def bezier_quadratic_3d(t: float, p0: Vec3 | Sequence[float], p1: Vec3 | Sequence[float], p2: Vec3 | Sequence[float]) -> Vec3:
    """
Quadratic Bezier curve in 3D: B(t) = (1-t)²P₀ + 2(1-t)tP₁ + t²P₂

Rust: `curves::bezier_quadratic_3d`
    """
    ...

def bezier_cubic_3d(t: float, p0: Vec3 | Sequence[float], p1: Vec3 | Sequence[float], p2: Vec3 | Sequence[float], p3: Vec3 | Sequence[float]) -> Vec3:
    """
Cubic Bezier curve in 3D: B(t) = (1-t)³P₀ + 3(1-t)²tP₁ + 3(1-t)t²P₂ + t³P₃

Rust: `curves::bezier_cubic_3d`
    """
    ...

def bezier_sample(p0: tuple[float, float], p1: tuple[float, float], p2: tuple[float, float], p3: tuple[float, float], n: int) -> list[tuple[float, float]]:
    """
Sample n+1 evenly spaced points along a cubic Bezier curve (t from 0 to 1).

Rust: `curves::bezier_sample`
    """
    ...

def parametric_circle(t: float, r: float) -> tuple[float, float]:
    """
Parametric circle: (x, y) = (r·cos(t), r·sin(t))

Rust: `curves::parametric_circle`
    """
    ...

def parametric_ellipse(t: float, a: float, b: float) -> tuple[float, float]:
    """
Parametric ellipse: (x, y) = (a·cos(t), b·sin(t))

Rust: `curves::parametric_ellipse`
    """
    ...

def parametric_spiral(t: float, a: float, b: float) -> tuple[float, float]:
    """
Archimedean spiral: r = a + b*t.

Rust: `curves::parametric_spiral`
    """
    ...

def parametric_lissajous(t: float, a: float, b: float, delta: float) -> tuple[float, float]:
    """
Lissajous figure: (sin(a*t + delta), sin(b*t)).

Rust: `curves::parametric_lissajous`
    """
    ...

def parametric_cycloid(t: float, r: float) -> tuple[float, float]:
    """
Cycloid: (r(t - sin(t)), r(1 - cos(t))).

Rust: `curves::parametric_cycloid`
    """
    ...

def parametric_helix(t: float, radius: float, pitch: float) -> tuple[float, float, float]:
    """
Helix: (r cos(t), r sin(t), pitch * t / (2*pi)).

Rust: `curves::parametric_helix`
    """
    ...

def arc_length_parametric(fx: Callable[[float], float], fy: Callable[[float], float], t0: float, t1: float, n: int) -> float:
    """
Numerical arc length via piecewise linear approximation with n segments.

Rust: `curves::arc_length_parametric`
    """
    ...

def arc_length_circle(radius: float, angle: float) -> float:
    """
Arc length of a circular arc: s = rθ

Rust: `curves::arc_length_circle`
    """
    ...

def curvature_2d(dxdt: float, dydt: float, d2xdt2: float, d2ydt2: float) -> float:
    """
Curvature kappa = |x'y'' - y'x''| / (x'^2 + y'^2)^(3/2).

Rust: `curves::curvature_2d`
    """
    ...
