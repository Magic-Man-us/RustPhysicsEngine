"""
Incompressible potential flow: elementary singularities, complex potentials, Joukowski and Karman-Trefftz airfoils, NACA sections, the Hess-Smith panel method, thin-airfoil and lifting-line theory, a simple vortex lattice, and added-mass results.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec2

class Element:
    """
Elementary potential-flow element.

Rust: `cfd::potential_flow::Element`
    """
    def complex_potential(self, z: complex) -> complex: ...
    def complex_velocity(self, z: complex) -> complex: ...

class PanelMethod:
    """
Hess-Smith source/vortex panel method.

Rust: `cfd::potential_flow::PanelMethod`
    """
    def __init__(self, airfoil: list[Vec2 | Sequence[float]]) -> None: ...
    def solve(self) -> None: ...
    def velocity_at(self, p: Vec2 | Sequence[float]) -> Vec2: ...
    def cp_distribution(self) -> list[tuple[float, float]]: ...
    def cl(self) -> float: ...
    def cm_quarter_chord(self) -> float: ...
    def streamlines(self, seeds: list[Vec2 | Sequence[float]], steps: int, dt: float) -> list[list[Vec2]]: ...
    def pressure_center(self) -> float: ...
    @property
    def alpha(self) -> float: ...
    @property
    def u_inf(self) -> float: ...
    @property
    def sources(self) -> list[float]: ...
    @property
    def gamma(self) -> float: ...

class Plane2:
    """
A wall line for the method of images.

Rust: `cfd::potential_flow::Plane2`
    """
    def __init__(self, point: Vec2 | Sequence[float], normal: Vec2 | Sequence[float]) -> None: ...
    @property
    def point(self) -> Vec2: ...
    @property
    def normal(self) -> Vec2: ...

class PotentialFlow2:
    """
Superposition of potential-flow elements.

Rust: `cfd::potential_flow::PotentialFlow2`
    """
    def __init__(self, elements: list[Element]) -> None: ...
    def velocity(self, p: Vec2 | Sequence[float]) -> Vec2: ...
    def potential(self, p: Vec2 | Sequence[float]) -> float: ...
    def stream_function(self, p: Vec2 | Sequence[float]) -> float: ...
    def complex_potential(self, z: complex) -> complex: ...
    def complex_velocity(self, z: complex) -> complex: ...
    def pressure_coefficient(self, p: Vec2 | Sequence[float], u_inf: float) -> float: ...
    def stagnation_points(self) -> list[Vec2]: ...
    def streamlines(self, seeds: list[Vec2 | Sequence[float]], steps: int, dt: float) -> list[list[Vec2]]: ...
    def lift_kutta_joukowski(self, u_inf: float, rho: float) -> float: ...
    @property
    def elements(self) -> list[Element]: ...

class WingGeometry:
    """
Simple wing planform for the vortex lattice.

Rust: `cfd::potential_flow::WingGeometry`
    """
    def __init__(self, span: float, root_chord: float, tip_chord: float, sweep: float) -> None: ...
    @property
    def span(self) -> float: ...
    @property
    def root_chord(self) -> float: ...
    @property
    def tip_chord(self) -> float: ...
    @property
    def sweep(self) -> float: ...

def cylinder_flow(u_inf: float, r: float, gamma: float) -> PotentialFlow2:
    """
Flow past a circular cylinder with circulation Γ.

Rust: `cfd::potential_flow::cylinder_flow`
    """
    ...

def rankine_oval(u: float, m: float, a: float) -> PotentialFlow2:
    """
Rankine oval: source and sink of strength m at (±a, 0) in a stream.

Rust: `cfd::potential_flow::rankine_oval`
    """
    ...

def cylinder_cp_exact(theta: float, gamma: float, u: float, r: float) -> float:
    """
Exact surface pressure coefficient of the rotating cylinder
(Γ counterclockwise-positive, matching `Element::Vortex`).

Rust: `cfd::potential_flow::cylinder_cp_exact`
    """
    ...

def joukowski_transform(z: complex, c: float) -> complex:
    """
Joukowski map ζ = z + c²/z.

Rust: `cfd::potential_flow::joukowski_transform`
    """
    ...

def inverse_joukowski(zeta: complex, c: float) -> complex:
    """
Inverse Joukowski map (branch with |z| ≥ c).

Rust: `cfd::potential_flow::inverse_joukowski`
    """
    ...

def joukowski_airfoil(center: complex, c: float, n_points: int) -> list[Vec2]:
    """
Joukowski airfoil: image of the circle through (c, 0) centered at
`center`.

Rust: `cfd::potential_flow::joukowski_airfoil`
    """
    ...

def joukowski_airfoil_flow(center: complex, c: float, alpha: float, u_inf: float) -> tuple[list[Vec2], list[float], float]:
    """
Joukowski airfoil with the Kutta condition: returns (surface points,
surface cp, lift coefficient).

Rust: `cfd::potential_flow::joukowski_airfoil_flow`
    """
    ...

def karman_trefftz_airfoil(center: complex, c: float, n_exp: float, n_points: int) -> list[Vec2]:
    """
Karman-Trefftz airfoil (finite trailing-edge angle set by `n_exp`
slightly below 2).

Rust: `cfd::potential_flow::karman_trefftz_airfoil`
    """
    ...

def naca4(code: str, n_points: int, closed_te: bool) -> list[Vec2]:
    """
NACA 4-digit airfoil ("2412" etc.), chord 1, from the trailing edge
over the top and back along the bottom.

Rust: `cfd::potential_flow::naca4`
    """
    ...

def naca5(code: str, n_points: int) -> list[Vec2]:
    """
NACA 5-digit airfoil ("23012" etc.).

Rust: `cfd::potential_flow::naca5`
    """
    ...

def thin_airfoil_cl(alpha: float, camber_slope: Callable[[float], float]) -> float:
    """
Thin-airfoil lift coefficient for a camber-line slope dz/dx given on
x ∈ [0, 1]: cl = 2π(α − α_L0).

Rust: `cfd::potential_flow::thin_airfoil_cl`
    """
    ...

def thin_airfoil_cl_flat(alpha: float) -> float:
    """
Flat-plate thin-airfoil lift 2πα.

Rust: `cfd::potential_flow::thin_airfoil_cl_flat`
    """
    ...

def lifting_line(span: float, chord: Callable[[float], float], alpha: Callable[[float], float], n_terms: int, u_inf: float) -> tuple[float, float, list[float]]:
    """
Prandtl lifting line with a Fourier sine series: returns
(CL, CDi, circulation at the collocation stations).

Rust: `cfd::potential_flow::lifting_line`
    """
    ...

def elliptic_wing_cl(ar: float, alpha: float) -> float:
    """
Elliptic-wing lift slope: CL = 2πα/(1 + 2/AR).

Rust: `cfd::potential_flow::elliptic_wing_cl`
    """
    ...

def induced_drag(cl: float, ar: float, e: float) -> float:
    """
Induced drag CL²/(π e AR).

Rust: `cfd::potential_flow::induced_drag`
    """
    ...

def oswald_efficiency_estimate(ar: float, sweep: float) -> float:
    """
Raymer's straight-wing Oswald efficiency estimate with a sweep
correction.

Rust: `cfd::potential_flow::oswald_efficiency_estimate`
    """
    ...

def vortex_lattice(wing: WingGeometry | Sequence[float], alpha: float, n_span: int, n_chord: int, u_inf: float) -> tuple[float, float, list[float]]:
    """
Single-lattice-row vortex lattice method (horseshoe vortices at the
quarter chord, collocation at 3/4 chord): returns (CL, CDi,
circulation per strip).

Rust: `cfd::potential_flow::vortex_lattice`
    """
    ...

def ground_effect_factor(h_over_b: float) -> float:
    """
McCormick ground-effect induced-drag factor (16 h/b)²/(1 + (16 h/b)²).

Rust: `cfd::potential_flow::ground_effect_factor`
    """
    ...

def conformal_map_flow(map: Callable[[complex], complex], base: PotentialFlow2, z: complex) -> Vec2:
    """
Velocity of a base flow seen through a conformal map at the physical
point z (numerical dW/dζ via the chain rule).

Rust: `cfd::potential_flow::conformal_map_flow`
    """
    ...

def method_of_images_wall(elements: list[Element], wall: Plane2) -> PotentialFlow2:
    """
Mirror every element across a wall (method of images).

Rust: `cfd::potential_flow::method_of_images_wall`
    """
    ...

def added_mass_cylinder(rho: float, r: float) -> float:
    """
Added mass of an accelerating cylinder per unit length ρπr².

Rust: `cfd::potential_flow::added_mass_cylinder`
    """
    ...

def added_mass_sphere(rho: float, r: float) -> float:
    """
Added mass of a sphere (2/3)ρπr³.

Rust: `cfd::potential_flow::added_mass_sphere`
    """
    ...
