"""
Elliptic integrals and physical applications.  Complete integrals use the arithmetic-geometric mean (Abramowitz & Stegun §17.6); incomplete integrals use the Carlson symmetric forms R_F and R_D (Carlson 1979; NR §6.11). The parameter is m = k².
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def elliptic_k(m: float) -> float:
    """
Complete elliptic integral of the first kind K(m), parameter m = k²:
K(m) = ∫₀^{π/2} dθ/√(1 − m·sin²θ) = π / (2·AGM(1, √(1−m))).

Panics:
Panics unless 0 ≤ m < 1.

Rust: `special::elliptic::elliptic_k`
    """
    ...

def elliptic_e(m: float) -> float:
    """
Complete elliptic integral of the second kind E(m), parameter m = k²:
E(m) = ∫₀^{π/2} √(1 − m·sin²θ) dθ, via the AGM with the
c²-correction sum: E = K·(1 − Σ 2^{n−1}·cₙ²).

Panics:
Panics unless 0 ≤ m ≤ 1 (E(1) = 1 exactly).

Rust: `special::elliptic::elliptic_e`
    """
    ...

def elliptic_f(phi: float, m: float) -> float:
    """
Incomplete elliptic integral of the first kind F(φ | m) via Carlson
R_F: F = sinφ·R_F(cos²φ, 1 − m·sin²φ, 1).

Panics:
Panics unless 0 ≤ φ ≤ π/2 and m·sin²φ < 1.

Rust: `special::elliptic::elliptic_f`
    """
    ...

def elliptic_e_inc(phi: float, m: float) -> float:
    """
Incomplete elliptic integral of the second kind E(φ | m) via Carlson
forms: E = sinφ·R_F − (m/3)·sin³φ·R_D.

Panics:
Panics unless 0 ≤ φ ≤ π/2 and m·sin²φ < 1.

Rust: `special::elliptic::elliptic_e_inc`
    """
    ...

def pendulum_period_exact(length: float, g: float, amplitude_rad: float) -> float:
    """
Exact large-amplitude pendulum period:
T = 4·√(L/g)·K(sin²(θ₀/2)).

Reduces to 2π√(L/g) as the amplitude → 0. Fails with
`InvalidArgument` for non-positive length/gravity or amplitude
outside [0, π).

Rust: `special::elliptic::pendulum_period_exact`
    """
    ...

def ellipse_perimeter_exact(a: float, b: float) -> float:
    """
Exact ellipse perimeter: P = 4·a·E(m) with m = 1 − (b/a)² for
a ≥ b (arguments may be given in either order).

Panics:
Panics unless both semi-axes are positive.

Rust: `special::elliptic::ellipse_perimeter_exact`
    """
    ...

def jacobi_elliptic(u: float, m: float) -> tuple[float, float, float]:
    """
Jacobi elliptic functions (sn, cn, dn) of real argument u with
parameter m = k², by the descending Gauss/AGM transformation
(Abramowitz & Stegun §16.4).

Panics:
Panics unless 0 ≤ m ≤ 1.

Rust: `special::elliptic::jacobi_elliptic`
    """
    ...
