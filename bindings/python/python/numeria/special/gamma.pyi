"""
Gamma function family.  `gamma` uses the Lanczos approximation (g = 7, n = 9); `lgamma` is the same approximation carried in log space so it does not overflow up to very large arguments. The regularized incomplete functions P(a,x) (`gamma_p`) and Q(a,x) (`gamma_q`) follow Numerical Recipes ch. 6.2 (series for x < a+1, Lentz continued fraction otherwise).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.statistics.distributions import Gamma

def gamma(z: float) -> float:
    """
Gamma function Γ(z) via the Lanczos approximation, with the
reflection formula for z < 0.5.

Rust: `special::gamma::gamma`
    """
    ...

def lgamma(z: float) -> float:
    """
Natural log of |Γ(z)|, computed in log space so it stays finite up to
z ≈ 1e300 (e.g. `lgamma(1e6)` is exact to ~1e-13 relative).

Panics:
Panics for z ≤ 0 (poles and the reflection region are out of scope
for the real-valued solvers this supports).

Rust: `special::gamma::lgamma`
    """
    ...

def gamma_p(a: float, x: float) -> float:
    """
Regularized lower incomplete gamma P(a,x) = γ(a,x)/Γ(a).

Panics:
Panics unless a > 0 and x ≥ 0.

Rust: `special::gamma::gamma_p`
    """
    ...

def gamma_q(a: float, x: float) -> float:
    """
Regularized upper incomplete gamma Q(a,x) = 1 − P(a,x), computed by
continued fraction for x > a + 1 so large-x values keep precision.

Panics:
Panics unless a > 0 and x ≥ 0.

Rust: `special::gamma::gamma_q`
    """
    ...
