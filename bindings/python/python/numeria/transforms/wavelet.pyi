"""
Discrete and continuous wavelet transforms.  Filter banks, boundary handling, and coefficient lengths follow the PyWavelets conventions (dwt output length ⌊(n + L − 1)/2⌋, idwt output length 2·len − L + 2), so round trips are exact for every wavelet and padding mode. The CWT follows Torrence & Compo (1998).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class Mother:
    """
Mother wavelets for the CWT (Torrence & Compo definitions):
Morlet(ω₀), Mexican hat (DOG order 2), Paul(m), DOG(m).

Rust: `transforms::wavelet::Mother`
    """
    ...

class PadMode:
    """
Signal extension at the boundaries.

Rust: `transforms::wavelet::PadMode`
    """
    ...

class Threshold:
    """
Detail-coefficient thresholding rules for `wavelet_denoise`.

Rust: `transforms::wavelet::Threshold`
    """
    ...

class Wavelet:
    """
Wavelet families: Haar, Daubechies 1–20, symlets 2–20, coiflets 1–5,
and the biorthogonal spline family (bior p.q as in PyWavelets).

Rust: `transforms::wavelet::Wavelet`
    """
    ...

def wavelet_filters(w: Wavelet) -> tuple[list[float], list[float], list[float], list[float]]:
    """
Decomposition and reconstruction filters (dec_lo, dec_hi, rec_lo,
rec_hi), quadrature-mirror related for the orthogonal families.

Panics:
Panics for an unsupported order (Db/Sym > 20, Coif > 5, or a bior
pair outside the standard set).

Rust: `transforms::wavelet::wavelet_filters`
    """
    ...

def dwt(x: list[float], w: Wavelet, mode: PadMode) -> tuple[list[float], list[float]]:
    """
One-level DWT: (approximation, detail), each of length ⌊(n+L−1)/2⌋.

Rust: `transforms::wavelet::dwt`
    """
    ...

def idwt(a: list[float], d: list[float], w: Wavelet, mode: PadMode) -> list[float]:
    """
One-level inverse DWT; output length 2·len − L + 2. `mode` is
accepted for API symmetry (reconstruction itself needs no padding).

Panics:
Panics if the approximation and detail lengths differ.

Rust: `transforms::wavelet::idwt`
    """
    ...

def wavedec(x: list[float], w: Wavelet, levels: int, mode: PadMode) -> list[list[float]]:
    """
Multilevel decomposition: returns \\[a_L, d_L, d_{L−1}, …, d_1\\].

Rust: `transforms::wavelet::wavedec`
    """
    ...

def waverec(coeffs: list[list[float]], w: Wavelet, mode: PadMode) -> list[float]:
    """
Multilevel reconstruction (inverse of `wavedec`).

Rust: `transforms::wavelet::waverec`
    """
    ...

def dwt_2d(img: list[float], w: int, h: int, wavelet: Wavelet) -> tuple[list[float], list[float], list[float], list[float]]:
    """
One-level separable 2D DWT with symmetric extension (rows along x
first, then columns): returns (LL, LH, HL, HH) where the first
letter is the x (row-direction) channel. Sub-band dims are
⌊(w+L−1)/2⌋ × ⌊(h+L−1)/2⌋.

Panics:
Panics unless `img.len() == w * h`.

Rust: `transforms::wavelet::dwt_2d`
    """
    ...

def idwt_2d(ll: list[float], lh: list[float], hl: list[float], hh: list[float], w: int, h: int, wavelet: Wavelet) -> list[float]:
    """
Inverse of `dwt_2d`; `w` and `h` are the original image dimensions.

Rust: `transforms::wavelet::idwt_2d`
    """
    ...

def wavelet_denoise(x: list[float], w: Wavelet, levels: int, t: Threshold) -> list[float]:
    """
Wavelet shrinkage denoising: decompose, threshold the detail bands,
reconstruct (trimmed to the input length).

Rust: `transforms::wavelet::wavelet_denoise`
    """
    ...

def wavelet_compress(x: list[float], w: Wavelet, levels: int, keep_fraction: float) -> list[float]:
    """
Keep the largest `keep_fraction` of all coefficients (approximation
always kept), zero the rest, and reconstruct.

Rust: `transforms::wavelet::wavelet_compress`
    """
    ...

def cwt(x: list[float], scales: list[float], mother: Mother, fs: float) -> list[list[complex]]:
    """
Continuous wavelet transform. `scales` are in samples; row s of the
output holds W(s, t) at every sample. Computed in the Fourier domain
(Torrence & Compo eq. 4) with unit-energy normalization √(2πs).

Rust: `transforms::wavelet::cwt`
    """
    ...

def scalogram(x: list[float], scales: list[float], mother: Mother, fs: float) -> list[list[float]]:
    """
|CWT|² per scale and sample.

Rust: `transforms::wavelet::scalogram`
    """
    ...

def scale_to_frequency(scale: float, mother: Mother, fs: float) -> float:
    """
Equivalent Fourier frequency (Hz) of a CWT scale in samples
(Torrence & Compo table 1).

Rust: `transforms::wavelet::scale_to_frequency`
    """
    ...

def wavelet_packet_decompose(x: list[float], w: Wavelet, levels: int) -> list[list[float]]:
    """
Full wavelet-packet tree at the given depth: 2^levels leaves in
natural (frequency-ordered-by-index) order, symmetric extension.

Rust: `transforms::wavelet::wavelet_packet_decompose`
    """
    ...

def lifting_dwt_53(x: MutableSequence[int]) -> None:
    """
Lossless integer 5/3 (LeGall) lifting DWT, in place: the first half
becomes the approximation, the second half the detail.

Panics:
Panics unless the length is even and ≥ 2.

Rust: `transforms::wavelet::lifting_dwt_53`
    """
    ...

def lifting_idwt_53(x: MutableSequence[int]) -> None:
    """
Exact inverse of `lifting_dwt_53`.

Panics:
Panics unless the length is even and ≥ 2.

Rust: `transforms::wavelet::lifting_idwt_53`
    """
    ...

def multiresolution_analysis(x: list[float], w: Wavelet, levels: int) -> list[list[float]]:
    """
Multiresolution analysis: the input split into levels+1 additive
components (details from coarse to fine, then the approximation
first). Component 0 is the level-L approximation signal; component k
(k ≥ 1) is the detail at level L+1−k. The components sum to x.

Rust: `transforms::wavelet::multiresolution_analysis`
    """
    ...
