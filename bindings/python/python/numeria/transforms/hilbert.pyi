"""
Hilbert transform, analytic signals, modulation, empirical mode decomposition, and causality (Kramers-Kronig) tools.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def analytic_signal(x: list[float]) -> list[complex]:
    """
Analytic signal x + j·H(x) by the FFT method: double the positive
frequencies, zero the negative ones.

Rust: `transforms::hilbert::analytic_signal`
    """
    ...

def hilbert(x: list[float]) -> list[float]:
    """
Hilbert transform: the quadrature (imaginary) part of the analytic
signal — hilbert(cos ωt) = sin ωt.

Rust: `transforms::hilbert::hilbert`
    """
    ...

def envelope(x: list[float]) -> list[float]:
    """
Instantaneous amplitude |x + jH(x)|.

Rust: `transforms::hilbert::envelope`
    """
    ...

def instantaneous_phase(x: list[float]) -> list[float]:
    """
Unwrapped instantaneous phase of the analytic signal.

Rust: `transforms::hilbert::instantaneous_phase`
    """
    ...

def instantaneous_frequency(x: list[float], fs: float) -> list[float]:
    """
Instantaneous frequency in Hz (central difference of the unwrapped
phase).

Rust: `transforms::hilbert::instantaneous_frequency`
    """
    ...

def hilbert_fir(n_taps: int) -> list[float]:
    """
FIR Hilbert transformer kernel (odd taps, antisymmetric, windowed).

Panics:
Panics unless `n_taps` is odd.

Rust: `transforms::hilbert::hilbert_fir`
    """
    ...

def ssb_modulate(x: list[float], fc: float, fs: float, upper: bool) -> list[float]:
    """
Single-sideband modulation: upper sideband is x·cos − H(x)·sin,
lower is x·cos + H(x)·sin (carrier fc Hz at sample rate fs).

Rust: `transforms::hilbert::ssb_modulate`
    """
    ...

def am_demodulate(x: list[float]) -> list[float]:
    """
AM envelope demodulation: the analytic-signal envelope (carrier plus
modulation; subtract the mean to recover the AC message).

Rust: `transforms::hilbert::am_demodulate`
    """
    ...

def fm_demodulate(x: list[float], fs: float) -> list[float]:
    """
FM demodulation: instantaneous frequency of the analytic signal (Hz).

Rust: `transforms::hilbert::fm_demodulate`
    """
    ...

def empirical_mode_decomposition(x: list[float], max_imfs: int, sift_tol: float) -> list[list[float]]:
    """
Empirical mode decomposition (Huang sifting): returns the IMFs plus
the final residual as the last entry, so the components sum to x.

Rust: `transforms::hilbert::empirical_mode_decomposition`
    """
    ...

def hilbert_huang_spectrum(x: list[float], fs: float, max_imfs: int) -> list[tuple[list[float], list[float]]]:
    """
Hilbert-Huang spectrum: per IMF (excluding the residual), the
instantaneous frequency track and amplitude envelope.

Rust: `transforms::hilbert::hilbert_huang_spectrum`
    """
    ...

def kramers_kronig(im: list[float], omega: list[float]) -> list[float]:
    """
Kramers-Kronig relation: real part of a causal response from its
imaginary part sampled on `omega` (ω ≥ 0), by principal-value
trapezoid integration of (2/π)∫ ω′·Im(ω′)/(ω′² − ω²) dω′.

Panics:
Panics if the lengths differ.

Rust: `transforms::hilbert::kramers_kronig`
    """
    ...

def minimum_phase_from_magnitude(mag: list[float]) -> list[complex]:
    """
Minimum-phase spectrum with the given magnitude, by the real-cepstrum
method: fold the causal cepstrum and re-exponentiate. `mag` samples
|H| on the full FFT circle (length n).

Rust: `transforms::hilbert::minimum_phase_from_magnitude`
    """
    ...
