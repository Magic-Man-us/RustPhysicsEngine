"""
Fractals: escape-time sets, attractors, automata and noise.  The module root holds the classic escape-time sets computed directly -- Mandelbrot with smooth (continuous) iteration counts, Julia, burning ship, Newton fractals and the Sierpinski gasket.  The submodules generalise each direction: `escape_time` for a generic iteration engine, `attractors` for chaotic flows and maps, `ifs` for iterated function systems and the chaos game, `lsystem` for Lindenmayer rewriting, `automata` for cellular automata and growth, and `noise` for Perlin, OpenSimplex2, Worley and fBm.  For the dynamical-systems view -- Lyapunov exponents and bifurcation -- see `nonlinear`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import attractors, automata, escape_time, ifs, lsystem, noise

def mandelbrot_iterations(c_re: float, c_im: float, max_iter: int) -> int:
    """
Mandelbrot escape-time iteration count: z_{n+1} = z_n² + c, returns iterations to escape |z| > 2.

Rust: `fractals::mandelbrot_iterations`
    """
    ...

def mandelbrot_smooth(c_re: float, c_im: float, max_iter: int) -> float:
    """
Smooth Mandelbrot iteration count using continuous escape-time coloring.

Rust: `fractals::mandelbrot_smooth`
    """
    ...

def mandelbrot_grid(x_min: float, x_max: float, y_min: float, y_max: float, width: int, height: int, max_iter: int) -> list[int]:
    """
Compute Mandelbrot iteration counts for an entire grid of pixels.

Rust: `fractals::mandelbrot_grid`
    """
    ...

def julia_iterations(z_re: float, z_im: float, c_re: float, c_im: float, max_iter: int) -> int:
    """
Julia set escape-time iteration count for a fixed c: z_{n+1} = z_n² + c.

Rust: `fractals::julia_iterations`
    """
    ...

def julia_grid(c_re: float, c_im: float, x_min: float, x_max: float, y_min: float, y_max: float, width: int, height: int, max_iter: int) -> list[int]:
    """
Compute Julia set iteration counts for an entire grid of pixels.

Rust: `fractals::julia_grid`
    """
    ...

def burning_ship_iterations(c_re: float, c_im: float, max_iter: int) -> int:
    """
Burning Ship fractal iteration count: z_{n+1} = (|Re(z_n)| + i|Im(z_n)|)² + c.

Rust: `fractals::burning_ship_iterations`
    """
    ...

def newton_fractal_iterations(z_re: float, z_im: float, max_iter: int, tolerance: float) -> tuple[int, int]:
    """
Newton fractal for f(z) = z³ - 1: returns (iterations, root_index) for convergence.

Rust: `fractals::newton_fractal_iterations`
    """
    ...

def sierpinski_point(x: float, y: float, iterations: int) -> list[tuple[float, float]]:
    """
Generate Sierpinski triangle points via the chaos game (iterated function system).

Rust: `fractals::sierpinski_point`
    """
    ...

def barnsley_fern_point(x: float, y: float, iterations: int) -> list[tuple[float, float]]:
    """
Generate Barnsley fern points via the iterated function system with four affine maps.

Rust: `fractals::barnsley_fern_point`
    """
    ...

def box_count_2d(points: list[tuple[float, float]], grid_size: int, bounds: tuple[float, float, float, float]) -> int:
    """
Count occupied grid cells for box-counting fractal dimension estimation.

Rust: `fractals::box_count_2d`
    """
    ...

def new(re: float, im: float) -> complex:
    """
Create a complex number from real and imaginary parts.

Rust: `fractals::Complex::new`
    """
    ...

def norm_sq(z: complex) -> float:
    """
Squared norm (modulus squared): |z|² = re² + im²

Rust: `fractals::Complex::norm_sq`
    """
    ...

def norm(z: complex) -> float:
    """
Norm (modulus): |z| = √(re² + im²)

Rust: `fractals::Complex::norm`
    """
    ...

def arg(z: complex) -> float:
    """
Argument (phase angle): arg(z) = atan2(im, re)

Rust: `fractals::Complex::arg`
    """
    ...

def conjugate(z: complex) -> complex:
    """
Complex conjugate: z* = re - im·i

Rust: `fractals::Complex::conjugate`
    """
    ...
