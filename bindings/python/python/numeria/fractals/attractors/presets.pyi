"""
Named systems: 3-D flows with customary parameters and time
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.fractals.attractors import Attractor2Map
from numeria.fractals.attractors import Attractor3

def lorenz(sigma: float, rho: float, beta: float) -> Attractor3:
    """
Lorenz 1963: ẋ = σ(y−x), ẏ = x(ρ−z) − y, ż = xy − βz.

Rust: `fractals::attractors::presets::lorenz`
    """
    ...

def rossler(a: float, b: float, c: float) -> Attractor3:
    """
Rössler 1976: ẋ = −y−z, ẏ = x+ay, ż = b + z(x−c).

Rust: `fractals::attractors::presets::rossler`
    """
    ...

def aizawa() -> Attractor3:
    """
Aizawa attractor (a sphere-wrapped scroll).

Rust: `fractals::attractors::presets::aizawa`
    """
    ...

def thomas(b: float) -> Attractor3:
    """
Thomas' cyclically symmetric attractor: ẋ = sin y − bx, ...

Rust: `fractals::attractors::presets::thomas`
    """
    ...

def chen() -> Attractor3:
    """
Chen 1999 (a = 35, b = 3, c = 28).

Rust: `fractals::attractors::presets::chen`
    """
    ...

def lu() -> Attractor3:
    """
Lü 2002 (a = 36, b = 3, c = 20), bridging Lorenz and Chen.

Rust: `fractals::attractors::presets::lu`
    """
    ...

def halvorsen(a: float) -> Attractor3:
    """
Halvorsen's cyclic attractor: ẋ = −ax − 4y − 4z − y².

Rust: `fractals::attractors::presets::halvorsen`
    """
    ...

def sprott_b() -> Attractor3:
    """
Sprott case B: ẋ = yz, ẏ = x − y, ż = 1 − xy.

Rust: `fractals::attractors::presets::sprott_b`
    """
    ...

def dadras() -> Attractor3:
    """
Dadras attractor.

Rust: `fractals::attractors::presets::dadras`
    """
    ...

def rabinovich_fabrikant(a: float, g: float) -> Attractor3:
    """
Rabinovich-Fabrikant: ẋ = y(z − 1 + x²) + γx, ...

Rust: `fractals::attractors::presets::rabinovich_fabrikant`
    """
    ...

def three_scroll() -> Attractor3:
    """
Three-scroll unified chaotic system (TSUCS-1).

Rust: `fractals::attractors::presets::three_scroll`
    """
    ...

def arneodo() -> Attractor3:
    """
Arneodo-Coullet: ẋ = y, ẏ = z, ż = ax − by − z − x³
with (a, b) = (5.5, 3.5).

Rust: `fractals::attractors::presets::arneodo`
    """
    ...

def nose_hoover() -> Attractor3:
    """
Nosé-Hoover oscillator (Sprott A): ẋ = y, ẏ = −x + yz,
ż = 1 − y².

Rust: `fractals::attractors::presets::nose_hoover`
    """
    ...

def four_wing() -> Attractor3:
    """
Four-wing attractor.

Rust: `fractals::attractors::presets::four_wing`
    """
    ...

def chua(alpha: float, beta: float, m0: float, m1: float) -> Attractor3:
    """
Chua's circuit with the piecewise-linear diode
characteristic f(x) = m₁x + (m₀−m₁)(|x+1| − |x−1|)/2.

Rust: `fractals::attractors::presets::chua`
    """
    ...

def rikitake() -> Attractor3:
    """
Rikitake two-disc dynamo (μ = 1, a = 5).

Rust: `fractals::attractors::presets::rikitake`
    """
    ...

def duffing_forced(delta: float, alpha: float, beta: float, gamma: float, omega: float) -> Attractor3:
    """
Forced Duffing oscillator as an autonomous 3-D flow with
z = ωt: ẋ = y, ẏ = −δy − αx − βx³ + γ cos z, ż = ω.

Rust: `fractals::attractors::presets::duffing_forced`
    """
    ...

def clifford(a: float, b: float, c: float, d: float) -> Attractor2Map:
    """
Clifford attractor: x' = sin(ay) + c cos(ax), ...

Rust: `fractals::attractors::presets::clifford`
    """
    ...

def de_jong(a: float, b: float, c: float, d: float) -> Attractor2Map:
    """
Peter de Jong attractor.

Rust: `fractals::attractors::presets::de_jong`
    """
    ...

def ikeda(u: float) -> Attractor2Map:
    """
Ikeda map with t = 0.4 − 6/(1 + x² + y²).

Rust: `fractals::attractors::presets::ikeda`
    """
    ...

def tinkerbell(a: float, b: float, c: float, d: float) -> Attractor2Map:
    """
Tinkerbell map.

Rust: `fractals::attractors::presets::tinkerbell`
    """
    ...

def gingerbreadman() -> Attractor2Map:
    """
Gingerbreadman map: x' = 1 − y + |x|, y' = x.

Rust: `fractals::attractors::presets::gingerbreadman`
    """
    ...

def henon(a: float, b: float) -> Attractor2Map:
    """
Hénon map: x' = 1 − ax² + y, y' = bx.

Rust: `fractals::attractors::presets::henon`
    """
    ...

def duffing_map(a: float, b: float) -> Attractor2Map:
    """
Duffing map: x' = y, y' = −bx + ay − y³.

Rust: `fractals::attractors::presets::duffing_map`
    """
    ...

def bogdanov(eps: float, k: float, mu: float) -> Attractor2Map:
    """
Bogdanov map.

Rust: `fractals::attractors::presets::bogdanov`
    """
    ...

def standard_map(k: float) -> Attractor2Map:
    """
Chirikov standard map on the torus [0, 2π)²:
p' = p + k sin θ, θ' = θ + p'. Area-preserving.

Rust: `fractals::attractors::presets::standard_map`
    """
    ...

def gumowski_mira(a: float, b: float) -> Attractor2Map:
    """
Gumowski-Mira map with g(x) = ax + 2(1−a)x²/(1+x²).

Rust: `fractals::attractors::presets::gumowski_mira`
    """
    ...

def hopalong(a: float, b: float, c: float) -> Attractor2Map:
    """
Barry Martin's hopalong: x' = y − sign(x)√|bx − c|, y' = a − x.

Rust: `fractals::attractors::presets::hopalong`
    """
    ...

def bedhead(a: float, b: float) -> Attractor2Map:
    """
Bedhead attractor.

Rust: `fractals::attractors::presets::bedhead`
    """
    ...

def svensson(a: float, b: float, c: float, d: float) -> Attractor2Map:
    """
Johnny Svensson's attractor.

Rust: `fractals::attractors::presets::svensson`
    """
    ...

def fractal_dream(a: float, b: float, c: float, d: float) -> Attractor2Map:
    """
"Fractal dream" attractor.

Rust: `fractals::attractors::presets::fractal_dream`
    """
    ...

def popcorn(h: float) -> Attractor2Map:
    """
Popcorn map: x' = x − h sin(y + tan 3y), ...

Rust: `fractals::attractors::presets::popcorn`
    """
    ...

def arnold_cat() -> Attractor2Map:
    """
Arnold's cat map on the unit torus.

Rust: `fractals::attractors::presets::arnold_cat`
    """
    ...

def baker() -> Attractor2Map:
    """
Baker's map on the unit square.

Rust: `fractals::attractors::presets::baker`
    """
    ...

def zaslavskii(eps: float, nu: float, r: float) -> Attractor2Map:
    """
Zaslavskii map (ε forcing, ν rotation, r damping).

Rust: `fractals::attractors::presets::zaslavskii`
    """
    ...
