"""
Strange attractors: 3-D chaotic flows and 2-D chaotic maps with trajectory integration, Lyapunov spectra (Benettin renormalization), Kaplan-Yorke dimension, Poincaré sections, bifurcation diagrams, and dimension estimators (Grassberger-Procaccia correlation dimension, box counting, Rosenstein's largest-Lyapunov method, Feigenbaum ratios).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import presets
from numeria.spatial.primitives import Rect
from numeria.math import Vec2
from numeria.math import Vec3

class Attractor2Map:
    """
A discrete 2-D map x ← step(x).

Rust: `fractals::attractors::Attractor2Map`
    """
    def trajectory(self, x0: Vec2 | Sequence[float], n: int, burn_in: int) -> list[Vec2]: ...
    def density_map(self, x0: Vec2 | Sequence[float], n: int, res: tuple[int, int], bounds: Rect) -> list[int]: ...
    def lyapunov(self, x0: Vec2 | Sequence[float], n: int) -> float: ...

class Attractor3:
    """
A 3-D autonomous flow ẋ = f(x) with a preferred time step.

Rust: `fractals::attractors::Attractor3`
    """
    def trajectory(self, x0: Vec3 | Sequence[float], n: int, method: Integrator) -> list[Vec3]: ...
    def lyapunov_spectrum(self, x0: Vec3 | Sequence[float], n: int, dt: float) -> list[float]: ...
    def poincare_section(self, x0: Vec3 | Sequence[float], n: int, plane: Plane) -> list[Vec3]: ...
    def density_map(self, x0: Vec3 | Sequence[float], n: int, res: tuple[int, int], bounds: Rect) -> list[int]: ...
    @property
    def dt(self) -> float: ...

class Integrator:
    """
Fixed-step integration schemes (`Rk45` takes two half steps and
keeps the fifth-order combination, giving adaptive-quality
accuracy at fixed cost).

Rust: `fractals::attractors::Integrator`
    """
    ...

def kaplan_yorke_dimension(spectrum: list[float]) -> float:
    """
Kaplan-Yorke (Lyapunov) dimension of a spectrum: with exponents
sorted descending and k the largest index with Σ₁ᵏ λᵢ ≥ 0,
D = k + Σ₁ᵏ λᵢ / |λ_{k+1}|.

Rust: `fractals::attractors::kaplan_yorke_dimension`
    """
    ...

def correlation_dimension(points: list[Vec3 | Sequence[float]], r_min: float, r_max: float, n_r: int) -> float:
    """
Grassberger-Procaccia correlation dimension: the least-squares
slope of ln C(r) against ln r over `n_r` log-spaced radii, where
C(r) is the fraction of point pairs closer than r.

Panics:
Panics unless there are >= 100 points, `0 < r_min < r_max`, and
`n_r >= 2`.

Rust: `fractals::attractors::correlation_dimension`
    """
    ...

def box_counting_dimension_3d(points: list[Vec3 | Sequence[float]], scales: list[float]) -> float:
    """
Box-counting dimension of a 3-D point set: slope of ln N(s)
versus ln(1/s) over the given box sizes.

Panics:
Panics unless points and at least two positive scales are given.

Rust: `fractals::attractors::box_counting_dimension_3d`
    """
    ...

def delay_embedding(series: list[float], dim: int, delay: int) -> list[list[float]]:
    """
Time-delay embedding: vectors [x(i), x(i+τ), ..., x(i+(m−1)τ)].

Panics:
Panics unless the series is long enough for one vector.

Rust: `fractals::attractors::delay_embedding`
    """
    ...

def recurrence_plot(series: list[float], embed_dim: int, delay: int, eps: float) -> list[bool]:
    """
Recurrence plot: `R[i,j]` is true when embedded states i and j are
within `eps` (row-major over the n embedded points).

Rust: `fractals::attractors::recurrence_plot`
    """
    ...

def largest_lyapunov_rosenstein(series: list[float], embed_dim: int, delay: int, mean_period: int, max_iter: int) -> float:
    """
Largest Lyapunov exponent from a scalar series by Rosenstein's
method: embed, pair each point with its nearest neighbor at
temporal distance > `mean_period`, and fit the slope of the mean
log divergence over `max_iter` steps. Returned per sample step.

Panics:
Panics on a series too short for the embedding and tracking.

Rust: `fractals::attractors::largest_lyapunov_rosenstein`
    """
    ...

def feigenbaum_estimate(bifurcations: list[float]) -> float:
    """
Feigenbaum δ estimate from successive bifurcation parameters:
δₙ = (bₙ₋₁ − bₙ₋₂)/(bₙ − bₙ₋₁) for the last triple.

Panics:
Panics unless at least 3 bifurcation points are given.

Rust: `fractals::attractors::feigenbaum_estimate`
    """
    ...
