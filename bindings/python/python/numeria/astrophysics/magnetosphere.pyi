"""
Planetary dipole fields and the magnetopause.  The magnetic dipole field in vector form, field-line tracing by integration along the field, and the magnetopause standoff distance -- where magnetic pressure balances the solar wind's dynamic pressure, which is what sets the size of a magnetosphere.  Field strength falls as `1/r³`, so the standoff distance depends only weakly (as the sixth root) on the wind pressure.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec3

class CelestialBodyType:
    """

Rust: `astrophysics::magnetosphere::CelestialBodyType`
    """
    ...

def magnetic_moment(body_type: CelestialBodyType, mass: float, temperature: float) -> float:
    """
Estimates a celestial body's magnetic dipole moment based on body type, mass, and temperature.

Rust: `astrophysics::magnetosphere::magnetic_moment`
    """
    ...

def magnetosphere_radius(collision_radius: float, moment: float) -> float:
    """
Computes the magnetosphere standoff radius from the magnetic moment and body radius.

Rust: `astrophysics::magnetosphere::magnetosphere_radius`
    """
    ...

def dipole_field(center: Vec3 | Sequence[float], moment_vec: Vec3 | Sequence[float], point: Vec3 | Sequence[float]) -> Vec3:
    """
Computes the magnetic dipole field at a point: B = (3(m·r̂)r̂ - m) / r³.

Rust: `astrophysics::magnetosphere::dipole_field`
    """
    ...

def total_field(centers: list[Vec3 | Sequence[float]], moments: list[Vec3 | Sequence[float]], point: Vec3 | Sequence[float]) -> Vec3:
    """
Computes the superposition of multiple magnetic dipole fields at a point.

Rust: `astrophysics::magnetosphere::total_field`
    """
    ...

def trace_field_line(centers: list[Vec3 | Sequence[float]], moments: list[Vec3 | Sequence[float]], seed: Vec3 | Sequence[float], forward: bool, step_size: float, max_distance: float, max_points: int, min_field_strength: float, body_radii: list[float]) -> list[tuple[Vec3, float]]:
    """
Traces a magnetic field line from a seed point using adaptive Euler stepping, returning positions and field strengths.

Rust: `astrophysics::magnetosphere::trace_field_line`
    """
    ...

def generate_seed_points(center: Vec3 | Sequence[float], radius: float, num_seeds: int) -> list[Vec3]:
    """
Generates seed points on a sphere using the golden-angle spiral for uniform distribution.

Rust: `astrophysics::magnetosphere::generate_seed_points`
    """
    ...

DEFAULT_MAX_LINES_PER_BODY: int

DEFAULT_POINTS_PER_LINE: int

DEFAULT_MIN_FIELD_STRENGTH: float

SOLAR_TEMPERATURE: float
