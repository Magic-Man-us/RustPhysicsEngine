"""
Quantum mechanics: the elementary relations here, with the wavefunction machinery and the Schrodinger solvers in submodules.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import algorithms, circuit, schrodinger, solid_state, spin, wavefunction
from numeria.transforms.wavelet import Threshold

def de_broglie_wavelength(mass: float, velocity: float) -> float:
    """
de Broglie wavelength: λ = h / p = h / (m * v)

Rust: `quantum::de_broglie_wavelength`
    """
    ...

def de_broglie_wavelength_from_energy(mass: float, kinetic_energy: float) -> float:
    """
de Broglie wavelength from kinetic energy: λ = h / sqrt(2mE)

Rust: `quantum::de_broglie_wavelength_from_energy`
    """
    ...

def photon_momentum(wavelength: float) -> float:
    """
Photon momentum: p = h / λ = h*f / c

Rust: `quantum::photon_momentum`
    """
    ...

def photon_energy(frequency: float) -> float:
    """
Photon energy: E = h * f

Rust: `quantum::photon_energy`
    """
    ...

def photon_energy_from_wavelength(wavelength: float) -> float:
    """
Photon energy from wavelength: E = h * c / λ

Rust: `quantum::photon_energy_from_wavelength`
    """
    ...

def photoelectric_ke(frequency: float, work_function: float) -> float:
    """
Photoelectric effect: KE_max = h*f - φ (work function)
Returns max kinetic energy of emitted electron.

Rust: `quantum::photoelectric_ke`
    """
    ...

def threshold_frequency(work_function: float) -> float:
    """
Threshold frequency: f_0 = φ / h

Rust: `quantum::threshold_frequency`
    """
    ...

def threshold_wavelength(work_function: float) -> float:
    """
Threshold wavelength: λ_0 = h * c / φ

Rust: `quantum::threshold_wavelength`
    """
    ...

def stopping_potential(max_kinetic_energy: float) -> float:
    """
Stopping potential: V_s = KE_max / e

Rust: `quantum::stopping_potential`
    """
    ...

def min_momentum_uncertainty(position_uncertainty: float) -> float:
    """
Heisenberg uncertainty principle (position-momentum): Δx * Δp ≥ ℏ/2
Returns minimum uncertainty in momentum given position uncertainty.

Rust: `quantum::min_momentum_uncertainty`
    """
    ...

def min_position_uncertainty(momentum_uncertainty: float) -> float:
    """
Minimum position uncertainty given momentum uncertainty.

Rust: `quantum::min_position_uncertainty`
    """
    ...

def min_energy_uncertainty(time_uncertainty: float) -> float:
    """
Energy-time uncertainty: ΔE * Δt ≥ ℏ/2

Rust: `quantum::min_energy_uncertainty`
    """
    ...

def min_time_uncertainty(energy_uncertainty: float) -> float:
    """
Minimum time uncertainty given energy uncertainty: Δt ≥ ℏ / (2ΔE)

Rust: `quantum::min_time_uncertainty`
    """
    ...

def bohr_radius() -> float:
    """
Bohr radius: a_0 = ℏ^2 / (m_e * k_e * e^2)

Rust: `quantum::bohr_radius`
    """
    ...

def hydrogen_energy_level(n: int) -> float:
    """
Energy levels of hydrogen atom: E_n = -13.6 eV / n^2
Returns energy in Joules.

Rust: `quantum::hydrogen_energy_level`
    """
    ...

def hydrogen_transition_energy(n_initial: int, n_final: int) -> float:
    """
Energy of photon emitted in hydrogen transition: E = 13.6 eV * (1/n_f^2 - 1/n_i^2)
Returns energy in Joules (positive for emission when n_i > n_f).

Rust: `quantum::hydrogen_transition_energy`
    """
    ...

def hydrogen_transition_wavelength(n_initial: int, n_final: int) -> float:
    """
Wavelength of photon from hydrogen transition (Rydberg formula):
1/λ = R_H * (1/n_f^2 - 1/n_i^2)

Rust: `quantum::hydrogen_transition_wavelength`
    """
    ...

def hydrogen_orbital_radius(n: int) -> float:
    """
Orbital radius of nth level in hydrogen: r_n = n^2 * a_0

Rust: `quantum::hydrogen_orbital_radius`
    """
    ...

def hydrogen_orbital_velocity(n: int) -> float:
    """
Orbital velocity in nth Bohr orbit: v_n = e^2 / (4πε_0 * n * ℏ)

Rust: `quantum::hydrogen_orbital_velocity`
    """
    ...

def tunneling_transmission(mass: float, barrier_height: float, particle_energy: float, barrier_width: float) -> float:
    """
Transmission coefficient for a rectangular barrier (approximate, E < V):
T ≈ e^(-2κL) where κ = sqrt(2m(V-E)) / ℏ

Rust: `quantum::tunneling_transmission`
    """
    ...

def particle_in_box_energy(n: int, mass: float, box_length: float) -> float:
    """
Energy levels of a particle in a 1D infinite potential well:
E_n = n^2 * π^2 * ℏ^2 / (2 * m * L^2)

Rust: `quantum::particle_in_box_energy`
    """
    ...

def zero_point_energy(mass: float, box_length: float) -> float:
    """
Zero-point energy (ground state, n=1):

Rust: `quantum::zero_point_energy`
    """
    ...

def compton_wavelength_shift(scattering_angle_rad: float) -> float:
    """
Compton wavelength shift: Δλ = (h / (m_e * c)) * (1 - cos(θ))

Rust: `quantum::compton_wavelength_shift`
    """
    ...

def compton_wavelength_electron() -> float:
    """
Compton wavelength of the electron: λ_C = h / (m_e * c)

Rust: `quantum::compton_wavelength_electron`
    """
    ...

def wien_peak_wavelength(temperature: float) -> float:
    """
Wien's displacement law: λ_max = b / T where b ≈ 2.898e-3 m·K

Rust: `quantum::wien_peak_wavelength`
    """
    ...

def blackbody_power(area: float, temperature: float) -> float:
    """
Stefan-Boltzmann law (total power): P = σ * A * T^4

Rust: `quantum::blackbody_power`
    """
    ...

def planck_spectral_radiance(wavelength: float, temperature: float) -> float:
    """
Planck's law (spectral radiance): B(λ,T) = (2hc^2/λ^5) / (e^(hc/(λkT)) - 1)

Rust: `quantum::planck_spectral_radiance`
    """
    ...
