"""
Rigorous interval arithmetic with outward rounding.  Every operation returns an interval guaranteed to contain the true real result for all inputs in the operand intervals: computed bounds are widened outward with `f64::next_down`/`next_up` (plus a small ulp margin for transcendental functions whose libm error is ≤ 1 ulp but unproven). Reference: Moore, Kearfott & Cloud, *Introduction to Interval Analysis* (SIAM, 2009).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class Interval:
    """
Closed interval [lo, hi].

Rust: `core::interval::Interval`
    """
    def __init__(self, lo: float, hi: float) -> None: ...
    @staticmethod
    def point(x: float) -> Interval: ...
    def width(self) -> float: ...
    def midpoint(self) -> float: ...
    def contains(self, x: float) -> bool: ...
    def intersect(self, other: Interval | Sequence[float]) -> Optional[Interval]: ...
    def hull(self, other: Interval | Sequence[float]) -> Interval: ...
    def sqrt(self) -> Interval: ...
    def exp(self) -> Interval: ...
    def sin(self) -> Interval: ...
    def cos(self) -> Interval: ...
    def powi(self, n: int) -> Interval: ...
    def __add__(self, rhs: Interval | Sequence[float]) -> Interval: ...
    def __sub__(self, rhs: Interval | Sequence[float]) -> Interval: ...
    def __mul__(self, rhs: Interval | Sequence[float]) -> Interval: ...
    def __truediv__(self, rhs: Interval | Sequence[float]) -> Interval: ...
    def __neg__(self) -> Interval: ...
    @property
    def lo(self) -> float: ...
    @property
    def hi(self) -> float: ...

def interval_newton(f: Callable[[Interval | Sequence[float]], Interval | Sequence[float]], df: Callable[[Interval | Sequence[float]], Interval | Sequence[float]], x0: Interval | Sequence[float], tol: float, max_iter: int) -> list[Interval]:
    """
Rigorous interval Newton method: encloses every root of f in `x0`.

`f` and `df` must be interval extensions of the function and its
derivative. Boxes where 0 ∉ f(X) are discarded; where f'(X)
excludes 0 the Newton contraction N(X) = m − f(m)/f'(X) ∩ X is
applied; otherwise the box is bisected. Boxes narrower than `tol`
that still satisfy 0 ∈ f(X) are reported (overlapping neighbors
merged). Every real root in `x0` is contained in some returned
interval; spurious near-root boxes may also appear at width ~tol.

Panics:
Panics unless tol > 0.

Rust: `core::interval::interval_newton`
    """
    ...
