"""
The damped harmonic oscillator m·x″ + c·x′ + k·x = F(t): closed-form responses in every damping regime, frequency-domain descriptions, and resonance measurement (Lorentzian fits, Q extraction).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class DampedOscillator:
    """
Mass-damper-spring oscillator m·x″ + c·x′ + k·x = F.

Rust: `resonance::oscillator::DampedOscillator`
    """
    def __init__(self, m: float, c: float, k: float) -> None: ...
    def natural_frequency(self) -> float: ...
    def damping_ratio(self) -> float: ...
    def q_factor(self) -> float: ...
    def damped_frequency(self) -> Optional[float]: ...
    def regime(self) -> Damping: ...
    def free_response(self, x0: float, v0: float, t: float) -> float: ...
    def steady_state_amplitude(self, f0: float, omega: float) -> float: ...
    def steady_state_phase(self, omega: float) -> float: ...
    def transfer_function(self, s: complex) -> complex: ...
    def frequency_response(self, omega: float) -> complex: ...
    def resonant_frequency(self) -> Optional[float]: ...
    def bandwidth(self) -> float: ...
    def impulse_response(self, t: float) -> float: ...
    def step_response(self, t: float) -> float: ...
    def forced_response_numeric(self, force: Callable[[float], float], x0: float, v0: float, t_end: float, dt: float) -> list[tuple[float, float, float]]: ...
    def energy(self, x: float, v: float) -> float: ...
    def decay_time(self, fraction: float) -> float: ...
    def logarithmic_decrement(self) -> float: ...
    @staticmethod
    def from_q(omega0: float, q: float, m: float) -> DampedOscillator: ...
    @property
    def m(self) -> float: ...
    @property
    def c(self) -> float: ...
    @property
    def k(self) -> float: ...

class Damping:
    """
Damping regime classification.

Rust: `resonance::oscillator::Damping`
    """
    ...

def bode_plot(tf: Callable[[complex], complex], omega: list[float]) -> tuple[list[float], list[float]]:
    """
Magnitude (dB) and phase (degrees) of a transfer function over a
frequency grid.

Rust: `resonance::oscillator::bode_plot`
    """
    ...

def nyquist_plot(tf: Callable[[complex], complex], omega: list[float]) -> list[complex]:
    """
Nyquist locus H(jω) over a frequency grid.

Rust: `resonance::oscillator::nyquist_plot`
    """
    ...

def lorentzian(omega: float, omega0: float, gamma: float) -> float:
    """
Area-normalized Lorentzian lineshape
(1/π)·(γ/2)/((ω−ω₀)² + (γ/2)²).

Rust: `resonance::oscillator::lorentzian`
    """
    ...

def lorentzian_fit(omega: list[float], y: list[float]) -> tuple[float, float, float]:
    """
Fit y(ω) ≈ A·(γ/2)²/((ω−ω₀)² + (γ/2)²) (peak-amplitude Lorentzian)
by Levenberg-Marquardt; returns (ω₀, γ, A).

Panics:
Panics if the fit fails to converge or fewer than 4 points are given.

Rust: `resonance::oscillator::lorentzian_fit`
    """
    ...

def q_from_ringdown(x: list[float], fs: float) -> tuple[float, float]:
    """
Estimate (f₀ Hz, Q) from a free ring-down record: frequency from
interpolated zero crossings, decay rate from a log-linear fit to the
rectified peaks.

Panics:
Panics if the record has fewer than 4 zero crossings.

Rust: `resonance::oscillator::q_from_ringdown`
    """
    ...

def q_from_spectrum(f: list[float], psd: list[float]) -> tuple[float, float]:
    """
Estimate (f₀, Q) from a power spectrum by the −3 dB method with
linear interpolation of the half-power crossings.

Panics:
Panics on an empty spectrum.

Rust: `resonance::oscillator::q_from_spectrum`
    """
    ...

def resonance_curve(osc: DampedOscillator | Sequence[float], omega: list[float]) -> list[float]:
    """
Steady-state amplitude of the oscillator (unit force) at each ω.

Rust: `resonance::oscillator::resonance_curve`
    """
    ...

def transmissibility(omega_ratio: float, zeta: float) -> float:
    """
Base-excitation transmissibility at frequency ratio r = ω/ω₀:
√((1+(2ζr)²)/((1−r²)² + (2ζr)²)).

Rust: `resonance::oscillator::transmissibility`
    """
    ...

def quality_factor_combined(qs: list[float]) -> float:
    """
Combined quality factor of independent loss channels:
1/Q = Σ 1/Qᵢ.

Rust: `resonance::oscillator::quality_factor_combined`
    """
    ...
