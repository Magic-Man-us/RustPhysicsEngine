"""
Resonant cavities and structures: RLC circuits, Helmholtz resonators, strings, air columns, membranes, plates, beams, rooms, optical etalons, and microwave cavities.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.audio.tuning import Mode

class BeamBc:
    """
Beam boundary conditions.

Rust: `resonance::cavity::BeamBc`
    """
    ...

class PlateBc:
    """
Plate boundary conditions for `rectangular_plate_modes`.

Rust: `resonance::cavity::PlateBc`
    """
    ...

class Rlc:
    """
Series/parallel RLC resonator.

Rust: `resonance::cavity::Rlc`
    """
    def __init__(self, r: float, l: float, c: float) -> None: ...
    def series_impedance(self, omega: float) -> complex: ...
    def parallel_impedance(self, omega: float) -> complex: ...
    def resonant_frequency(self) -> float: ...
    def q_series(self) -> float: ...
    def q_parallel(self) -> float: ...
    def bandwidth(self) -> float: ...
    def damping_ratio(self) -> float: ...
    def transfer_lowpass(self, omega: float) -> complex: ...
    def transfer_bandpass(self, omega: float) -> complex: ...
    def transfer_highpass(self, omega: float) -> complex: ...
    def transfer_notch(self, omega: float) -> complex: ...
    def step_response(self, t: float, v: float) -> float: ...
    def energy(self, i: float, v: float) -> float: ...
    @property
    def r(self) -> float: ...
    @property
    def l(self) -> float: ...
    @property
    def c(self) -> float: ...

def helmholtz_resonator(volume: float, neck_area: float, neck_length: float, c: float) -> float:
    """
Helmholtz resonance frequency (Hz) with a flanged-end correction of
1.7·r added to the neck length.

Rust: `resonance::cavity::helmholtz_resonator`
    """
    ...

def helmholtz_q(volume: float, neck_area: float, neck_length: float, c: float) -> float:
    """
Radiation-limited quality factor of a Helmholtz resonator (flanged
baffle radiation resistance): Q = 2π·√(V·L_eff³/A³).

Rust: `resonance::cavity::helmholtz_q`
    """
    ...

def string_modes(length: float, tension: float, mu: float, n: int) -> list[float]:
    """
Ideal string mode frequencies i·√(T/μ)/(2L), i = 1..=n.

Rust: `resonance::cavity::string_modes`
    """
    ...

def string_mode_shape(length: float, n: int, x: float) -> float:
    """
Mode shape sin(nπx/L).

Rust: `resonance::cavity::string_mode_shape`
    """
    ...

def stiff_string_modes(length: float, tension: float, mu: float, young: float, radius: float, n: int) -> list[float]:
    """
Stiff-string partials fₙ = n·f₁·√(1 + B·n²) with the piano
inharmonicity coefficient B (radius-based).

Rust: `resonance::cavity::stiff_string_modes`
    """
    ...

def inharmonicity_coefficient(young: float, radius: float, tension: float, length: float) -> float:
    """
Piano-string inharmonicity B = π³·E·r⁴/(4·T·L²).

Rust: `resonance::cavity::inharmonicity_coefficient`
    """
    ...

def tube_modes(length: float, c: float, open_open: bool, n: int) -> list[float]:
    """
Air-column modes: open-open i·c/(2L); open-closed odd harmonics
(2i−1)·c/(4L). Pass the end-corrected length.

Rust: `resonance::cavity::tube_modes`
    """
    ...

def tube_end_correction(radius: float, flanged: bool) -> float:
    """
End correction of an open tube end: 0.85·r flanged, 0.61·r unflanged.

Rust: `resonance::cavity::tube_end_correction`
    """
    ...

def conical_tube_modes(length: float, c: float, n: int) -> list[float]:
    """
Complete-cone modes: like an open-open pipe, i·c/(2L).

Rust: `resonance::cavity::conical_tube_modes`
    """
    ...

def rectangular_membrane_modes(a: float, b: float, tension: float, sigma: float, max_m: int, max_n: int) -> list[tuple[int, int, float]]:
    """
Rectangular membrane modes (m, n, f) sorted by frequency:
f = (c/2)·√((m/a)² + (n/b)²), c = √(T/σ).

Rust: `resonance::cavity::rectangular_membrane_modes`
    """
    ...

def circular_membrane_modes(radius: float, tension: float, sigma: float, max_m: int, max_n: int) -> list[tuple[int, int, float]]:
    """
Circular membrane modes (m angular, n radial, f) sorted by
frequency: f = α_mn·c/(2πR) with α_mn the n-th zero of J_m.

Rust: `resonance::cavity::circular_membrane_modes`
    """
    ...

def circular_membrane_shape(radius: float, m: int, n: int, r: float, theta: float) -> float:
    """
Circular membrane mode shape J_m(α_mn·r/R)·cos(mθ).

Rust: `resonance::cavity::circular_membrane_shape`
    """
    ...

def rectangular_plate_modes(a: float, b: float, h: float, young: float, nu: float, rho: float, bc: PlateBc, max_m: int, max_n: int) -> list[tuple[int, int, float]]:
    """
Thin rectangular plate modes (m, n, f Hz) sorted ascending.
Simply supported edges are exact; clamped edges use the separable
beam-function Rayleigh estimate (upper bound, a few % high).

Rust: `resonance::cavity::rectangular_plate_modes`
    """
    ...

def chladni_pattern(a: float, b: float, m: int, n: int, res: int) -> ScalarField2:
    """
Chladni figure of a square-symmetric plate mode: the field
φ_mn + φ_nm with φ_mn = cos(mπx/a)cos(nπy/b) on a res×res grid
(nodal lines are the zero set).

Rust: `resonance::cavity::chladni_pattern`
    """
    ...

def chladni_pattern_mixed(a: float, b: float, modes: list[tuple[int, int, float]], res: int) -> ScalarField2:
    """
General superposition Σ cᵢ·cos(mᵢπx/a)cos(nᵢπy/b).

Rust: `resonance::cavity::chladni_pattern_mixed`
    """
    ...

def beam_modes(length: float, young: float, i_area: float, rho: float, area: float, bc: BeamBc, n: int) -> list[float]:
    """
Euler-Bernoulli beam natural frequencies (Hz):
f_i = λ_i²/(2πL²)·√(EI/(ρA)).

Rust: `resonance::cavity::beam_modes`
    """
    ...

def beam_mode_shape(length: float, bc: BeamBc, n: int, x: float) -> float:
    """
Euler-Bernoulli beam mode shape at position x ∈ \\[0, L\\]
(unnormalized; standard clamped/simply-supported/free functions).

Rust: `resonance::cavity::beam_mode_shape`
    """
    ...

def tuning_fork_frequency(length: float, thickness: float, young: float, rho: float) -> float:
    """
Tuning fork prong frequency: cantilever first mode of a rectangular
prong, f = (1.875²/2π)·(t/L²)·√(E/(12ρ)).

Rust: `resonance::cavity::tuning_fork_frequency`
    """
    ...

def bell_modes_approx(radius: float, thickness: float, young: float, rho: float, nu: float, n: int) -> list[float]:
    """
Bell/ring flexural modes (thin-ring approximation), n = 2..:
f_n = n(n²−1)/√(n²+1) · (t/(2πR²))·√(E/(12ρ(1−ν²))).

Rust: `resonance::cavity::bell_modes_approx`
    """
    ...

def room_modes(lx: float, ly: float, lz: float, c: float, max_n: int) -> list[tuple[int, int, int, float]]:
    """
All room modes (nx, ny, nz, f Hz) with indices up to max_n, sorted:
f = (c/2)·√((nx/lx)² + (ny/ly)² + (nz/lz)²).

Rust: `resonance::cavity::room_modes`
    """
    ...

def room_mode_density(lx: float, ly: float, lz: float, c: float, f: float) -> float:
    """
Asymptotic modal density dN/df = 4πVf²/c³ + πSf/(2c²) + L/(8c).

Rust: `resonance::cavity::room_mode_density`
    """
    ...

def schroeder_frequency(rt60: float, volume: float) -> float:
    """
Schroeder crossover frequency 2000·√(RT60/V) (Hz).

Rust: `resonance::cavity::schroeder_frequency`
    """
    ...

def fabry_perot_transmission(wavelength: float, length: float, r: float, n_index: float) -> float:
    """
Fabry-Perot (Airy) intensity transmission for mirror reflectance r:
T = (1−r)²/((1−r)² + 4r·sin²(δ/2)), δ = 4πnL/λ.

Rust: `resonance::cavity::fabry_perot_transmission`
    """
    ...

def fabry_perot_fsr(length: float, n_index: float, c: float) -> float:
    """
Free spectral range c/(2nL) (Hz).

Rust: `resonance::cavity::fabry_perot_fsr`
    """
    ...

def fabry_perot_finesse(r: float) -> float:
    """
Finesse π√r/(1−r).

Rust: `resonance::cavity::fabry_perot_finesse`
    """
    ...

def cavity_q(frequency: float, fwhm: float) -> float:
    """
Quality factor f/Δf.

Rust: `resonance::cavity::cavity_q`
    """
    ...

def cavity_photon_lifetime(q: float, frequency: float) -> float:
    """
Photon lifetime Q/(2πf) (s).

Rust: `resonance::cavity::cavity_photon_lifetime`
    """
    ...

def microwave_cavity_modes_rect(a: float, b: float, d: float, c: float, max_n: int) -> list[tuple[str, float]]:
    """
Rectangular microwave cavity modes (label, f Hz) up to index max_n,
sorted: f = (c/2)·√((m/a)² + (n/b)² + (p/d)²) with the standard TE
(p ≥ 1, m+n ≥ 1) and TM (m, n ≥ 1, p ≥ 0) index rules.

Rust: `resonance::cavity::microwave_cavity_modes_rect`
    """
    ...

def cylindrical_cavity_modes(radius: float, height: float, c: float, max_n: int) -> list[tuple[str, float]]:
    """
Cylindrical cavity modes (label, f Hz), sorted:
TM_mnp uses J_m zeros (p ≥ 0), TE_mnp uses J′_m zeros (p ≥ 1);
f = (c/2π)·√((x/R)² + (pπ/H)²).

Rust: `resonance::cavity::cylindrical_cavity_modes`
    """
    ...

def quarter_wave_resonator(length: float, c: float) -> float:
    """
Quarter-wave resonator fundamental c/(4L).

Rust: `resonance::cavity::quarter_wave_resonator`
    """
    ...

def coupled_cavity_splitting(f0: float, coupling: float) -> tuple[float, float]:
    """
Mode splitting of two identical coupled cavities with coupling
coefficient κ: (f₀√(1−κ), f₀√(1+κ)).

Rust: `resonance::cavity::coupled_cavity_splitting`
    """
    ...

def resonance_overlap(f1: float, q1: float, f2: float, q2: float) -> float:
    """
Lorentzian overlap of two resonances (1 when co-tuned, → 0 when far
apart relative to their combined half-widths).

Rust: `resonance::cavity::resonance_overlap`
    """
    ...
