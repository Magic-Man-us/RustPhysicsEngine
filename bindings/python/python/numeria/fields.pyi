"""
Uniform-grid scalar fields.  Minimal backfill of the Part 2 `ScalarField2`/`ScalarField3` types that later roadmap phases build on: row-major storage with grid spacing and bilinear/trilinear sampling.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class ScalarField2:
    """
Uniform-grid scalar fields.

Minimal backfill of the Part 2 `ScalarField2`/`ScalarField3` types
that later roadmap phases build on: row-major storage with grid
spacing and bilinear/trilinear sampling.
2D scalar field on an nx×ny uniform grid with spacing dx
(row-major: index = y·nx + x; physical position of node (i, j) is
(i·dx, j·dx)).

Rust: `fields::ScalarField2`
    """
    def __init__(self, nx: int, ny: int, dx: float) -> None: ...
    @staticmethod
    def from_fn(nx: int, ny: int, dx: float, f: Callable[[float, float], float]) -> ScalarField2: ...
    def get(self, i: int, j: int) -> float: ...
    def set(self, i: int, j: int, v: float) -> None: ...
    def sample(self, x: float, y: float) -> float: ...
    def min_max(self) -> tuple[float, float]: ...
    @property
    def nx(self) -> int: ...
    @property
    def ny(self) -> int: ...
    @property
    def dx(self) -> float: ...
    @property
    def data(self) -> list[float]: ...

class ScalarField3:
    """
3D scalar field on an nx×ny×nz uniform grid with spacing dx
(index = (k·ny + j)·nx + i).

Rust: `fields::ScalarField3`
    """
    def __init__(self, nx: int, ny: int, nz: int, dx: float) -> None: ...
    def get(self, i: int, j: int, k: int) -> float: ...
    def set(self, i: int, j: int, k: int, v: float) -> None: ...
    def sample(self, x: float, y: float, z: float) -> float: ...
    @property
    def nx(self) -> int: ...
    @property
    def ny(self) -> int: ...
    @property
    def nz(self) -> int: ...
    @property
    def dx(self) -> float: ...
    @property
    def data(self) -> list[float]: ...
