"""
Vectors and the crate's table of physical constants.  `Vec2` and `Vec3` with the usual algebra -- addition, scaling, dot and cross products, norms, normalization, projection, reflection, rotation and interpolation.  `constants` is the single table the rest of the crate refers back to, and it is deliberately one table: duplicate definitions elsewhere are re-exports of it, and a test enforces that they agree. The values fixed by the 2019 SI redefinition -- `C`, `H`, `HBAR`, `E_CHARGE`, `K_B`, `N_A` -- are exact by definition rather than measured. Constants that are products of others, such as `FARADAY = N_A · E_CHARGE`, are computed from their factors rather than transcribed, so they cannot disagree with them.  For the 2022 CODATA set with units attached see `units::quantity::constants_codata`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import constants

class Vec2:
    """
2D vector (fluid grids, planar geometry).

Rust: `math::Vec2`
    """
    def __init__(self, x: float, y: float) -> None: ...
    def magnitude(self) -> float: ...
    def magnitude_squared(self) -> float: ...
    def normalized(self) -> Vec2: ...
    def dot(self, other: Vec2 | Sequence[float]) -> float: ...
    def cross(self, other: Vec2 | Sequence[float]) -> float: ...
    def perp(self) -> Vec2: ...
    def lerp(self, other: Vec2 | Sequence[float], t: float) -> Vec2: ...
    def distance_to(self, other: Vec2 | Sequence[float]) -> float: ...
    def angle_between(self, other: Vec2 | Sequence[float]) -> float: ...
    def rotate(self, angle: float) -> Vec2: ...
    def to_vec3(self) -> Vec3: ...
    def __add__(self, rhs: Vec2 | Sequence[float]) -> Vec2: ...
    def __sub__(self, rhs: Vec2 | Sequence[float]) -> Vec2: ...
    def __mul__(self, rhs: float) -> Vec2: ...
    def __neg__(self) -> Vec2: ...
    @property
    def x(self) -> float: ...
    @property
    def y(self) -> float: ...

class Vec3:
    """
3D vector used throughout the physics engine.

Rust: `math::Vec3`
    """
    def __init__(self, x: float, y: float, z: float) -> None: ...
    def magnitude(self) -> float: ...
    def magnitude_squared(self) -> float: ...
    def normalized(self) -> Vec3: ...
    def dot(self, other: Vec3 | Sequence[float]) -> float: ...
    def cross(self, other: Vec3 | Sequence[float]) -> Vec3: ...
    def distance_to(self, other: Vec3 | Sequence[float]) -> float: ...
    def angle_between(self, other: Vec3 | Sequence[float]) -> float: ...
    def lerp(self, other: Vec3 | Sequence[float], t: float) -> Vec3: ...
    def project_onto(self, other: Vec3 | Sequence[float]) -> Vec3: ...
    def reflect(self, normal: Vec3 | Sequence[float]) -> Vec3: ...
    def __add__(self, rhs: Vec3 | Sequence[float]) -> Vec3: ...
    def __sub__(self, rhs: Vec3 | Sequence[float]) -> Vec3: ...
    def __mul__(self, rhs: float) -> Vec3: ...
    def __neg__(self) -> Vec3: ...
    @property
    def x(self) -> float: ...
    @property
    def y(self) -> float: ...
    @property
    def z(self) -> float: ...
