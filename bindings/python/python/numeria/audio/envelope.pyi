"""
Envelopes, LFOs, followers, fades, and glides.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.statistics.distributions import Exponential
from numeria.audio.oscillators import Oscillator

class Adsr:
    """
Linear (optionally exponential-curved) ADSR envelope; times in
seconds, sustain as a level in \\[0, 1\\].

Rust: `audio::envelope::Adsr`
    """
    def __init__(self, attack: float, decay: float, sustain: float, release: float, fs: float) -> None: ...
    def gate_on(self) -> None: ...
    def gate_off(self) -> None: ...
    def set_curve(self, exp: bool) -> None: ...
    def next(self) -> float: ...
    def is_active(self) -> bool: ...
    @property
    def attack(self) -> float: ...
    @property
    def decay(self) -> float: ...
    @property
    def sustain(self) -> float: ...
    @property
    def release(self) -> float: ...

class AdsrExp:
    """
Exponential ADSR driven by RC time constants (τ per segment).

Rust: `audio::envelope::AdsrExp`
    """
    def __init__(self, attack_tau: float, decay_tau: float, sustain: float, release_tau: float, fs: float) -> None: ...
    def gate_on(self) -> None: ...
    def gate_off(self) -> None: ...
    def next(self) -> float: ...
    def is_active(self) -> bool: ...
    @property
    def attack_tau(self) -> float: ...
    @property
    def decay_tau(self) -> float: ...
    @property
    def sustain(self) -> float: ...
    @property
    def release_tau(self) -> float: ...

class Ar:
    """
Simple linear attack-release envelope (a one-shot AD when the gate
is released immediately).

Rust: `audio::envelope::Ar`
    """
    def __init__(self, attack: float, release: float, fs: float) -> None: ...
    def gate_on(self) -> None: ...
    def gate_off(self) -> None: ...
    def next(self) -> float: ...
    def is_active(self) -> bool: ...
    @property
    def attack(self) -> float: ...
    @property
    def release(self) -> float: ...

class FadeShape:
    """
Fade curve shapes.

Rust: `audio::envelope::FadeShape`
    """
    ...

class Lfo:
    """
Low-frequency oscillator: scaled/offset wrapper over `Oscillator`.

Rust: `audio::envelope::Lfo`
    """
    def next(self) -> float: ...
    def sync(self) -> None: ...
    @property
    def depth(self) -> float: ...
    @property
    def offset(self) -> float: ...

def envelope_follower(x: list[float], attack_ms: float, release_ms: float, fs: float) -> list[float]:
    """
Peak envelope follower with attack/release time constants (ms).

Rust: `audio::envelope::envelope_follower`
    """
    ...

def peak_envelope(x: list[float], window: int) -> list[float]:
    """
Sliding-window peak magnitude (centered).

Rust: `audio::envelope::peak_envelope`
    """
    ...

def rms_envelope(x: list[float], window: int) -> list[float]:
    """
Sliding-window RMS (centered).

Rust: `audio::envelope::rms_envelope`
    """
    ...

def exponential_decay_envelope(n: int, tau: float, fs: float) -> list[float]:
    """
e^(−t/τ) sampled for n samples.

Rust: `audio::envelope::exponential_decay_envelope`
    """
    ...

def apply_envelope(x: MutableSequence[float], env: list[float]) -> None:
    """
Multiply a signal by an envelope in place.

Rust: `audio::envelope::apply_envelope`
    """
    ...

def fade_in(x: MutableSequence[float], n: int, shape: FadeShape) -> None:
    """
Fade in the first n samples in place.

Rust: `audio::envelope::fade_in`
    """
    ...

def fade_out(x: MutableSequence[float], n: int, shape: FadeShape) -> None:
    """
Fade out the last n samples in place.

Rust: `audio::envelope::fade_out`
    """
    ...

def crossfade(a: list[float], b: list[float], shape: FadeShape) -> list[float]:
    """
Full-length crossfade from a to b.

Panics:
Panics if the inputs differ in length.

Rust: `audio::envelope::crossfade`
    """
    ...

def portamento(from_hz: float, to_hz: float, n: int, fs: float, exponential: bool) -> list[float]:
    """
Pitch glide trajectory (Hz per sample): linear or exponential
(constant cents/second) from one frequency to another.

Rust: `audio::envelope::portamento`
    """
    ...
