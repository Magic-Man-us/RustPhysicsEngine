"""
Musical tuning: temperaments, interval math, Scala parsing, consonance models, stretch tuning, and pitch-class utilities.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class ChordQuality:
    """
Chord qualities.

Rust: `audio::tuning::ChordQuality`
    """
    ...

class Mode:
    """
Diatonic modes and common scales.

Rust: `audio::tuning::Mode`
    """
    ...

def equal_temperament(n_divisions: int, base_hz: float, base_midi: int) -> list[float]:
    """
MIDI frequencies (128 entries) for an equal temperament with
`n_divisions` steps per octave anchored at (`base_midi`, `base_hz`).

Rust: `audio::tuning::equal_temperament`
    """
    ...

def just_intonation_5limit() -> list[float]:
    """
5-limit just intonation ratios from the tonic.

Rust: `audio::tuning::just_intonation_5limit`
    """
    ...

def pythagorean() -> list[float]:
    """
Pythagorean (3-limit) chromatic scale ratios.

Rust: `audio::tuning::pythagorean`
    """
    ...

def meantone_quarter_comma() -> list[float]:
    """
Quarter-comma meantone: fifths flattened so major thirds are pure 5/4.

Rust: `audio::tuning::meantone_quarter_comma`
    """
    ...

def werckmeister_iii() -> list[float]:
    """
Werckmeister III well temperament (1691), as ratios from C.

Rust: `audio::tuning::werckmeister_iii`
    """
    ...

def kirnberger_iii() -> list[float]:
    """
Kirnberger III well temperament, as ratios from C.

Rust: `audio::tuning::kirnberger_iii`
    """
    ...

def young() -> list[float]:
    """
Thomas Young's 1799 well temperament (Young II), as ratios from C.

Rust: `audio::tuning::young`
    """
    ...

def bohlen_pierce() -> list[float]:
    """
Bohlen-Pierce scale: 13 equal divisions of the tritave (3:1); returns
the 14 ratios including both endpoints.

Rust: `audio::tuning::bohlen_pierce`
    """
    ...

def harmonic_series_scale(n: int) -> list[float]:
    """
Harmonic-series scale: partials n..2n reduced to ratios from 1 to 2.

Rust: `audio::tuning::harmonic_series_scale`
    """
    ...

def scala_parse(scl: str) -> list[float]:
    """
Parse a Scala `.scl` file body into cents values (one per scale
degree, ending with the octave entry). Ratios like `3/2` and cents
like `701.955` are both accepted.

Rust: `audio::tuning::scala_parse`
    """
    ...

def cents_between(f1: float, f2: float) -> float:
    """
Signed interval from `f1` to `f2` in cents.

Rust: `audio::tuning::cents_between`
    """
    ...

def ratio_to_cents(r: float) -> float:
    """
Frequency ratio to cents.

Rust: `audio::tuning::ratio_to_cents`
    """
    ...

def cents_to_ratio(c: float) -> float:
    """
Cents to frequency ratio.

Rust: `audio::tuning::cents_to_ratio`
    """
    ...

def nearest_note(freq: float, a4: float) -> tuple[int, float]:
    """
Nearest 12-TET MIDI note to `freq` for the given A4: returns
(midi, cents deviation from that note).

Rust: `audio::tuning::nearest_note`
    """
    ...

def interval_name(ratio: float) -> str:
    """
Name of the just interval closest to `ratio` (within 6 cents), or
"unknown".

Rust: `audio::tuning::interval_name`
    """
    ...

def consonance_plomp_levelt(f1: float, f2: float) -> float:
    """
Plomp-Levelt consonance of two pure tones: 1 at unison, minimum near
a quarter of a critical band apart.

Rust: `audio::tuning::consonance_plomp_levelt`
    """
    ...

def dissonance_curve(base: float, partials: list[tuple[float, float]], ratio_range: tuple[float, float], n: int) -> list[tuple[float, float]]:
    """
Sethares dissonance curve: total pairwise Plomp-Levelt dissonance of
two copies of a `partials` timbre (`(ratio, amplitude)` relative to
`base` Hz) as the second copy sweeps through `ratio_range`. Returns
`n` points of (interval ratio, dissonance).

Rust: `audio::tuning::dissonance_curve`
    """
    ...

def stretch_tuning_railsback(midi: float, b: float) -> float:
    """
Piano stretch tuning deviation (cents from 12-TET) for a constant
string inharmonicity coefficient `b`: octaves are widened so partial 2
of the lower note matches the fundamental of its octave.

Rust: `audio::tuning::stretch_tuning_railsback`
    """
    ...

def syntonic_comma() -> float:
    """
The syntonic comma 81/80.

Rust: `audio::tuning::syntonic_comma`
    """
    ...

def pythagorean_comma() -> float:
    """
The Pythagorean comma 3¹²/2¹⁹.

Rust: `audio::tuning::pythagorean_comma`
    """
    ...

def schisma() -> float:
    """
The schisma 32805/32768 (Pythagorean comma / syntonic comma).

Rust: `audio::tuning::schisma`
    """
    ...

def midi_to_freq_tuned(midi: int, a4: float, temperament: list[float]) -> float:
    """
Frequency of a MIDI note in a 12-tone `temperament` (ratios from the
tonic C), anchored so that A4 (MIDI 69) sounds at `a4`.

Rust: `audio::tuning::midi_to_freq_tuned`
    """
    ...

def circle_of_fifths(start: int, n: int) -> list[int]:
    """
Pitch classes reached by successive fifths from `start`.

Rust: `audio::tuning::circle_of_fifths`
    """
    ...

def scale_degrees(root: int, mode: Mode) -> list[int]:
    """
Pitch classes of a scale on `root` (semitones 0-11, ascending).

Rust: `audio::tuning::scale_degrees`
    """
    ...

def chord_tones(root: int, quality: ChordQuality) -> list[int]:
    """
Pitch classes of a chord on `root` (semitones 0-11).

Rust: `audio::tuning::chord_tones`
    """
    ...
