"""
Sound synthesis: additive, FM (DX7-style operator routing), Karplus-Strong, subtractive, granular, formant, waveshaping, drums, and note/sequence rendering.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.audio.envelope import Adsr
from numeria.monte_carlo import Rng
from numeria.dsp.iir import Sos
from numeria.audio.oscillators import Waveform

class FmOperator:
    """
One FM operator: frequency ratio, modulation index (as output
amplitude when used as a modulator), envelope, and self-feedback.

Rust: `audio::synthesis::FmOperator`
    """
    @property
    def ratio(self) -> float: ...
    @property
    def index(self) -> float: ...
    @property
    def feedback(self) -> float: ...
    @property
    def phase(self) -> float: ...

class FmSynth:
    """
DX7-style FM synth: `algorithm[i]` lists the operators that modulate
operator i (an empty list means it is a carrier unless someone else
consumes it; operators that appear in no modulation list sum into
the output).

Rust: `audio::synthesis::FmSynth`
    """
    @staticmethod
    def dx7_algorithm(n: int) -> list[list[int]]: ...
    def note_on(self, freq: float) -> None: ...
    def note_off(self) -> None: ...
    def next(self) -> float: ...
    def render(self, freq: float, duration: float) -> list[float]: ...
    @property
    def algorithm(self) -> list[list[int]]: ...
    @property
    def fs(self) -> float: ...

class Voice:
    """
Voice types for `vowel_formants`.

Rust: `audio::synthesis::Voice`
    """
    ...

def additive(harmonics: list[tuple[float, float, float]], freq: float, n: int, fs: float) -> list[float]:
    """
Additive synthesis from (ratio, amplitude, phase) partials of a
fundamental `freq`.

Rust: `audio::synthesis::additive`
    """
    ...

def additive_evolving(harmonics: list[tuple[float, list[float]]], freq: float, n: int, fs: float) -> list[float]:
    """
Additive synthesis with a per-partial amplitude envelope
(each envelope is resampled to n output samples).

Rust: `audio::synthesis::additive_evolving`
    """
    ...

def fm_simple(carrier: float, modulator: float, index: float, n: int, fs: float) -> list[float]:
    """
Two-operator FM: sin(2πf_c·t + I·sin(2πf_m·t)).

Rust: `audio::synthesis::fm_simple`
    """
    ...

def fm_bessel_sidebands(index: float, n_sidebands: int) -> list[float]:
    """
Bessel sideband amplitudes |J_k(I)| for k = 0..n_sidebands.

Rust: `audio::synthesis::fm_bessel_sidebands`
    """
    ...

def pm_simple(carrier: float, modulator: float, index: float, n: int, fs: float) -> list[float]:
    """
Phase modulation (identical spectrum to `fm_simple` for a sine
modulator).

Rust: `audio::synthesis::pm_simple`
    """
    ...

def am(carrier: float, modulator: float, depth: float, n: int, fs: float) -> list[float]:
    """
Amplitude modulation (1 + depth·sin(2πf_m t))·sin(2πf_c t).

Rust: `audio::synthesis::am`
    """
    ...

def ring_mod(a: list[float], b: list[float]) -> list[float]:
    """
Ring modulation a·b.

Rust: `audio::synthesis::ring_mod`
    """
    ...

def karplus_strong(freq: float, duration: float, fs: float, decay: float, blend: float, rng: Rng) -> list[float]:
    """
Karplus-Strong plucked string: noise burst through the averaging
loop. `decay` scales the loop gain, `blend` the averaging strength.

Rust: `audio::synthesis::karplus_strong`
    """
    ...

def karplus_strong_extended(freq: float, duration: float, fs: float, pick_pos: float, pick_width: float, decay: float, dynamics: float) -> list[float]:
    """
Extended Karplus-Strong: pick position comb, pick-direction width
low-pass, and a dynamics low-pass on the excitation.

Rust: `audio::synthesis::karplus_strong_extended`
    """
    ...

def subtractive(source: Waveform, freq: float, filter: Sos, env: Adsr, filter_env_amount: float, n: int) -> list[float]:
    """
Subtractive synthesis: raw oscillator through a filter with an
amplitude envelope (`filter_env_amount` scales a per-sample cutoff
bias applied as post-gain tilt on the filtered signal — a simple
stand-in for a modulated-cutoff filter).

Rust: `audio::synthesis::subtractive`
    """
    ...

def granular(grain_source: list[float], grain_size: float, density: float, pitch_shift: float, position: float, spread: float, n: int, fs: float, rng: Rng) -> list[float]:
    """
Granular synthesis: Hann-windowed grains read from a source buffer
at `position` (0..1, with `spread` jitter), pitch shifted by
resampled playback, `density` grains per second.

Rust: `audio::synthesis::granular`
    """
    ...

def vowel_formants(vowel: str, voice: Voice) -> list[tuple[float, float, float]]:
    """
Classic (Peterson-Barney-style) formant tables for the vowels
a, e, i, o, u: (frequency, bandwidth, amplitude) triples.

Rust: `audio::synthesis::vowel_formants`
    """
    ...

def formant_synth(f0: float, formants: list[tuple[float, float, float]], n: int, fs: float) -> list[float]:
    """
Formant synthesis: a pulse-train glottal source through parallel
resonators (freq, bandwidth, amp).

Rust: `audio::synthesis::formant_synth`
    """
    ...

def pulsar_synthesis(f0: float, formant: float, duty: float, n: int, fs: float) -> list[float]:
    """
Pulsar synthesis: a formant-frequency sinusoid burst repeated at f0
with the given duty cycle.

Rust: `audio::synthesis::pulsar_synthesis`
    """
    ...

def phase_distortion(phase: float, amount: float, kind: int) -> float:
    """
Casio CZ-style phase distortion: warp the phase ramp before the
cosine lookup. `kind` 0 = knee (saw-like), 1 = resonant sweep.

Rust: `audio::synthesis::phase_distortion`
    """
    ...

def waveshaper(x: float, f: Callable[[float], float]) -> float:
    """
Apply an arbitrary waveshaper.

Rust: `audio::synthesis::waveshaper`
    """
    ...

def chebyshev_waveshaper(x: float, harmonic_amps: list[float]) -> float:
    """
Chebyshev waveshaper: Σ a_k·T_k(x) turns a pure cosine at amplitude
1 into exactly the requested harmonic mix.

Rust: `audio::synthesis::chebyshev_waveshaper`
    """
    ...

def hard_sync_osc(master_freq: float, slave_freq: float, n: int, fs: float) -> list[float]:
    """
Hard-synced sawtooth: a slave saw retriggered at the master rate.

Rust: `audio::synthesis::hard_sync_osc`
    """
    ...

def supersaw(freq: float, detune: float, n_voices: int, n: int, fs: float) -> list[float]:
    """
Detuned saw stack (JP-8000 style supersaw): `detune` is the maximum
relative detune of the outer voices.

Rust: `audio::synthesis::supersaw`
    """
    ...

def sample_playback(sample: list[float], rate_ratio: float, loop_start: int, loop_end: int, n: int) -> list[float]:
    """
Sample playback with loop points and linear-interpolated rate
conversion.

Rust: `audio::synthesis::sample_playback`
    """
    ...

def drum_kick(fs: float, pitch_start: float, pitch_end: float, decay: float) -> list[float]:
    """
Kick drum: exponential pitch sweep with an exponential amplitude
decay.

Rust: `audio::synthesis::drum_kick`
    """
    ...

def drum_snare(fs: float) -> list[float]:
    """
Snare: tone plus band-passed noise, both decaying.

Rust: `audio::synthesis::drum_snare`
    """
    ...

def drum_hihat(fs: float) -> list[float]:
    """
Hi-hat: short bright filtered noise burst.

Rust: `audio::synthesis::drum_hihat`
    """
    ...

def drum_clap(fs: float) -> list[float]:
    """
Clap: a few staggered noise bursts.

Rust: `audio::synthesis::drum_clap`
    """
    ...

def drum_tom(fs: float, pitch: float) -> list[float]:
    """
Tom: pitch-swept sine, longer than a kick.

Rust: `audio::synthesis::drum_tom`
    """
    ...

def mix(tracks: list[list[float]], gains: list[float]) -> list[float]:
    """
Mix tracks with per-track gains (output as long as the longest track).

Rust: `audio::synthesis::mix`
    """
    ...
