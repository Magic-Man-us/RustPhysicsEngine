"""
Audio analysis: pitch detection (YIN, autocorrelation, cepstral, HPS, McLeod), onset/tempo/beat tracking, MFCCs, LPC and formants, LSPs, spectral descriptors, chroma/key/chord estimation, psychoacoustic approximations, distortion metrics, room-acoustics measures from impulse responses, DTW, and constellation fingerprinting.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.spatial.frame import Frame
from numeria.spatial.primitives import Segment

class PitchMethod:
    """
Frame-wise pitch detection method selector.

Rust: `audio::analysis::PitchMethod`
    """
    ...

class SpectralFeatures:
    """
One frame of spectral descriptors.

Rust: `audio::analysis::SpectralFeatures`
    """
    def __init__(self, centroid: float, spread: float, skewness: float, kurtosis: float, rolloff85: float, flux: float, flatness: float, crest: float, slope: float, decrease: float, entropy: float) -> None: ...
    @property
    def centroid(self) -> float: ...
    @property
    def spread(self) -> float: ...
    @property
    def skewness(self) -> float: ...
    @property
    def kurtosis(self) -> float: ...
    @property
    def rolloff85(self) -> float: ...
    @property
    def flux(self) -> float: ...
    @property
    def flatness(self) -> float: ...
    @property
    def crest(self) -> float: ...
    @property
    def slope(self) -> float: ...
    @property
    def decrease(self) -> float: ...
    @property
    def entropy(self) -> float: ...

def autocorrelation_fft(x: list[float]) -> list[float]:
    """
Raw (biased, un-normalized) autocorrelation of `x` computed with FFTs;
returns lags 0..x.len().

Rust: `audio::analysis::autocorrelation_fft`
    """
    ...

def pitch_yin(x: list[float], fs: float, f_min: float, f_max: float, threshold: float) -> Optional[tuple[float, float]]:
    """
YIN pitch detector. Returns (frequency, confidence in 0..1) or `None`
when no lag drops below `threshold` (unvoiced).

Rust: `audio::analysis::pitch_yin`
    """
    ...

def pitch_autocorrelation(x: list[float], fs: float, f_min: float, f_max: float) -> Optional[float]:
    """
Autocorrelation pitch: highest normalized-autocorrelation peak in the
lag range; `None` if the peak is weak (< 0.3).

Rust: `audio::analysis::pitch_autocorrelation`
    """
    ...

def pitch_cepstral(x: list[float], fs: float, f_min: float, f_max: float) -> Optional[float]:
    """
Cepstral pitch: peak of the real cepstrum in the expected quefrency
range.

Rust: `audio::analysis::pitch_cepstral`
    """
    ...

def pitch_hps(x: list[float], fs: float, n_harmonics: int) -> Optional[float]:
    """
Harmonic product spectrum pitch estimate.

Rust: `audio::analysis::pitch_hps`
    """
    ...

def pitch_mpm(x: list[float], fs: float) -> Optional[tuple[float, float]]:
    """
McLeod pitch method (NSDF). Returns (frequency, clarity).

Rust: `audio::analysis::pitch_mpm`
    """
    ...

def pitch_track(x: list[float], fs: float, hop: int, method: PitchMethod) -> list[tuple[float, Optional[float]]]:
    """
Frame-wise pitch track: (time s, f0) per hop, 2048-sample frames.

Rust: `audio::analysis::pitch_track`
    """
    ...

def pitch_to_midi_track(track: list[tuple[float, Optional[float]]]) -> list[tuple[float, float, int]]:
    """
Segment a pitch track into notes: (start time, duration, MIDI note).
Runs of at least 3 voiced frames on the same rounded MIDI number
become one note.

Rust: `audio::analysis::pitch_to_midi_track`
    """
    ...

def onset_strength(x: list[float], fs: float, n_fft: int, hop: int) -> list[float]:
    """
Spectral-flux onset strength envelope (one value per STFT frame).

Rust: `audio::analysis::onset_strength`
    """
    ...

def onset_detect(strength: list[float], threshold: float, min_gap: int) -> list[int]:
    """
Pick onsets from a strength envelope: local maxima above `threshold`
separated by at least `min_gap` frames.

Rust: `audio::analysis::onset_detect`
    """
    ...

def onset_hfc(x: list[float], fs: float, n_fft: int, hop: int) -> list[float]:
    """
High-frequency-content onset function: Σ k |X_k|² per frame.

Rust: `audio::analysis::onset_hfc`
    """
    ...

def onset_complex_domain(x: list[float], fs: float, n_fft: int, hop: int) -> list[float]:
    """
Complex-domain onset function: deviation of each frame from the
magnitude/phase prediction of the previous frames.

Rust: `audio::analysis::onset_complex_domain`
    """
    ...

def tempo_estimate(onsets: list[float], fs: float) -> float:
    """
Tempo (BPM) from an onset-strength envelope sampled at `fs` frames/s,
via the autocorrelation peak in the 40-240 BPM range (preferring the
shortest strong lag, i.e. the fastest consistent pulse).

Rust: `audio::analysis::tempo_estimate`
    """
    ...

def beat_track(x: list[float], fs: float) -> list[float]:
    """
Beat tracking by dynamic programming (Ellis 2007): onset envelope,
global tempo, then a penalized best-predecessor recursion. Returns
beat times in seconds.

Rust: `audio::analysis::beat_track`
    """
    ...

def mfcc(x: list[float], fs: float, n_fft: int, hop: int, n_mels: int, n_mfcc: int) -> list[list[float]]:
    """
MFCCs: log-mel spectrogram followed by a DCT-II, keeping `n_mfcc`
coefficients per frame.

Rust: `audio::analysis::mfcc`
    """
    ...

def delta_features(f: list[list[float]], width: int) -> list[list[float]]:
    """
Regression delta features over ±`width` frames.

Rust: `audio::analysis::delta_features`
    """
    ...

def lpc(x: list[float], order: int) -> tuple[list[float], float]:
    """
Linear prediction by the autocorrelation method (Levinson-Durbin).
Returns the coefficients of A(z) = 1 + a₁z⁻¹ + … (length order+1,
leading 1) and the residual gain √E.

Rust: `audio::analysis::lpc`
    """
    ...

def lpc_to_formants(coeffs: list[float], fs: float) -> list[tuple[float, float]]:
    """
Formants (frequency, bandwidth in Hz) from LPC coefficients, via the
roots of A(z).

Rust: `audio::analysis::lpc_to_formants`
    """
    ...

def formant_track(x: list[float], fs: float, order: int, hop: int) -> list[list[tuple[float, float]]]:
    """
Frame-wise formant tracking (25 ms Hamming frames, pre-emphasis).

Rust: `audio::analysis::formant_track`
    """
    ...

def lpc_spectrum(coeffs: list[float], gain: float, n: int, fs: float) -> list[float]:
    """
LPC envelope magnitude spectrum: gain/|A(e^{jω})| at `n` frequencies
from 0 to fs/2.

Rust: `audio::analysis::lpc_spectrum`
    """
    ...

def lpc_to_lsp(coeffs: list[float]) -> list[float]:
    """
Line spectral pairs (radian frequencies in (0, π), sorted) of an LPC
polynomial with leading 1. The order must be even.

Rust: `audio::analysis::lpc_to_lsp`
    """
    ...

def lsp_to_lpc(lsp: list[float]) -> list[float]:
    """
Reconstruct LPC coefficients (leading 1) from line spectral pairs.

Rust: `audio::analysis::lsp_to_lpc`
    """
    ...

def spectral_centroid(mag: list[float], freqs: list[float]) -> float:
    """
Amplitude-weighted mean frequency.

Rust: `audio::analysis::spectral_centroid`
    """
    ...

def spectral_spread(mag: list[float], freqs: list[float]) -> float:
    """
Standard deviation of the spectral distribution.

Rust: `audio::analysis::spectral_spread`
    """
    ...

def spectral_skewness(mag: list[float], freqs: list[float]) -> float:
    """
Third standardized moment of the spectral distribution.

Rust: `audio::analysis::spectral_skewness`
    """
    ...

def spectral_kurtosis(mag: list[float], freqs: list[float]) -> float:
    """
Fourth standardized moment of the spectral distribution.

Rust: `audio::analysis::spectral_kurtosis`
    """
    ...

def spectral_rolloff(mag: list[float], freqs: list[float], pct: float) -> float:
    """
Frequency below which `pct` (0..1) of the spectral energy lies.

Rust: `audio::analysis::spectral_rolloff`
    """
    ...

def spectral_flux(prev: list[float], cur: list[float]) -> float:
    """
Half-wave rectified spectral flux between consecutive magnitude
frames.

Rust: `audio::analysis::spectral_flux`
    """
    ...

def spectral_flatness_mag(mag: list[float]) -> float:
    """
Geometric-to-arithmetic mean ratio (1 = white, 0 = tonal).

Rust: `audio::analysis::spectral_flatness_mag`
    """
    ...

def spectral_crest(mag: list[float]) -> float:
    """
Peak-to-mean spectral ratio.

Rust: `audio::analysis::spectral_crest`
    """
    ...

def spectral_slope(mag: list[float], freqs: list[float]) -> float:
    """
Linear-regression slope of magnitude vs frequency.

Rust: `audio::analysis::spectral_slope`
    """
    ...

def spectral_decrease(mag: list[float]) -> float:
    """
Spectral decrease (perceptual measure of how fast magnitude falls off
with bin index).

Rust: `audio::analysis::spectral_decrease`
    """
    ...

def spectral_entropy_mag(mag: list[float]) -> float:
    """
Normalized Shannon entropy of the magnitude distribution (0..1).

Rust: `audio::analysis::spectral_entropy_mag`
    """
    ...

def spectral_features_track(x: list[float], fs: float, n_fft: int, hop: int) -> list[SpectralFeatures]:
    """
Frame-wise spectral descriptor track.

Rust: `audio::analysis::spectral_features_track`
    """
    ...

def zero_crossing_rate(x: list[float], frame: int, hop: int) -> list[float]:
    """
Frame-wise zero-crossing rate (fraction of adjacent sample pairs that
change sign).

Rust: `audio::analysis::zero_crossing_rate`
    """
    ...

def harmonic_to_noise_ratio(x: list[float], fs: float, f0: float) -> float:
    """
Harmonic-to-noise ratio (dB) from the normalized autocorrelation at
the period of `f0`.

Rust: `audio::analysis::harmonic_to_noise_ratio`
    """
    ...

def inharmonicity_measure(x: list[float], fs: float, f0: float) -> float:
    """
Piano-style inharmonicity coefficient B fitted from the measured
partial frequencies: f_k ≈ k f0 √(1 + B k²).

Rust: `audio::analysis::inharmonicity_measure`
    """
    ...

def chroma(x: list[float], fs: float, n_fft: int, hop: int) -> list[list[float]]:
    """
Frame-wise 12-bin chroma (C, C#, …, B), energy-normalized per frame.

Rust: `audio::analysis::chroma`
    """
    ...

def chord_estimate(chroma_frame: list[float]) -> str:
    """
Template chord match on one chroma frame: returns e.g. "C", "Am",
"Bdim", "Faug".

Rust: `audio::analysis::chord_estimate`
    """
    ...

def loudness_sone(x: list[float], fs: float) -> float:
    """
Approximate loudness in sones (Zwicker-style power law on the overall
level, full scale taken as 94 dB SPL).

Rust: `audio::analysis::loudness_sone`
    """
    ...

def sharpness(x: list[float], fs: float) -> float:
    """
Approximate sharpness in acum (Bark-weighted specific-loudness
centroid, von Bismarck weighting).

Rust: `audio::analysis::sharpness`
    """
    ...

def roughness(x: list[float], fs: float) -> float:
    """
Approximate roughness: fraction of envelope fluctuation energy in the
20-300 Hz modulation range.

Rust: `audio::analysis::roughness`
    """
    ...

def fluctuation_strength(x: list[float], fs: float) -> float:
    """
Approximate fluctuation strength: fraction of envelope fluctuation
energy in the 1-10 Hz modulation range (maximal near 4 Hz).

Rust: `audio::analysis::fluctuation_strength`
    """
    ...

def silence_detect(x: list[float], threshold_db: float, min_len: int) -> list[tuple[int, int]]:
    """
Silent regions as (start, end) sample ranges: block RMS below
`threshold_db` (dBFS) for at least `min_len` samples.

Rust: `audio::analysis::silence_detect`
    """
    ...

def transient_detect(x: list[float], fs: float) -> list[int]:
    """
Transient (attack) sample positions from a high-frequency-content
envelope with an adaptive threshold.

Rust: `audio::analysis::transient_detect`
    """
    ...

def estimate_snr(x: list[float], noise_segment: list[float]) -> float:
    """
SNR (dB) of a signal given a noise-only reference segment.

Rust: `audio::analysis::estimate_snr`
    """
    ...

def thd_n(x: list[float], fs: float, f0: float) -> float:
    """
THD+N as a linear ratio: √((P_total − P_fund)/P_fund).

Rust: `audio::analysis::thd_n`
    """
    ...

def sinad(x: list[float], fs: float, f0: float) -> float:
    """
SINAD in dB: 10 log₁₀(P_fund / (P_total − P_fund)).

Rust: `audio::analysis::sinad`
    """
    ...

def enob(sinad_db: float) -> float:
    """
Effective number of bits from a SINAD measurement (dB).

Rust: `audio::analysis::enob`
    """
    ...

def impulse_response_from_sweep(recorded: list[float], inverse_sweep: list[float]) -> list[float]:
    """
Deconvolve a Farina sweep measurement: convolve the recording with the
inverse sweep and align so the direct impulse response starts at 0.

Rust: `audio::analysis::impulse_response_from_sweep`
    """
    ...

def rt60_from_ir(ir: list[float], fs: float) -> float:
    """
RT60 via Schroeder backward integration, extrapolated from the
−5..−25 dB decay slope.

Rust: `audio::analysis::rt60_from_ir`
    """
    ...

def edt_from_ir(ir: list[float], fs: float) -> float:
    """
Early decay time: the 0..−10 dB slope extrapolated to 60 dB.

Rust: `audio::analysis::edt_from_ir`
    """
    ...

def c50(ir: list[float], fs: float) -> float:
    """
Clarity C50 (dB): early (< 50 ms) to late energy ratio.

Rust: `audio::analysis::c50`
    """
    ...

def c80(ir: list[float], fs: float) -> float:
    """
Clarity C80 (dB): early (< 80 ms) to late energy ratio.

Rust: `audio::analysis::c80`
    """
    ...

def d50(ir: list[float], fs: float) -> float:
    """
Definition D50: fraction of energy arriving within 50 ms.

Rust: `audio::analysis::d50`
    """
    ...

def sti_approx(ir: list[float], fs: float) -> float:
    """
Single-band STI approximation from the impulse response's modulation
transfer function at the 14 standard modulation frequencies.

Rust: `audio::analysis::sti_approx`
    """
    ...

def peak_pick(x: list[float], threshold: float, min_distance: int) -> list[int]:
    """
Local maxima above `threshold`, greedily thinned so accepted peaks are
at least `min_distance` apart (strongest first). Returns sorted
indices.

Rust: `audio::analysis::peak_pick`
    """
    ...

def dynamic_time_warping(a: list[list[float]], b: list[list[float]]) -> tuple[float, list[tuple[int, int]]]:
    """
Dynamic time warping between two feature sequences (Euclidean local
cost). Returns (total cost, warping path from (0,0) to (n-1,m-1)).

Rust: `audio::analysis::dynamic_time_warping`
    """
    ...

def audio_fingerprint(x: list[float], fs: float) -> list[int]:
    """
Shazam-style constellation fingerprint: spectrogram peaks paired into
(f_anchor, f_target, Δt) hashes.

Rust: `audio::analysis::audio_fingerprint`
    """
    ...
