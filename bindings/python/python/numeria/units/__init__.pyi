"""
Unit conversions, dimensional analysis and the CODATA constants.  The flat conversion functions below are the original contents of this module and are unchanged. What sits alongside them now is the typed machinery: `quantity` carries a value together with its seven SI exponents so that adding a length to a time is a compile-time-shaped error rather than a silent number, and `dimensional` does the analysis those exponents make possible -- Buckingham's theorem over exact rationals, the named dimensionless groups, natural units and the Planck scale.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import dimensional, quantity

def meters_to_feet(m: float) -> float:
    """
Convert meters to feet: ft = m × 3.28084

Rust: `units::meters_to_feet`
    """
    ...

def feet_to_meters(ft: float) -> float:
    """
Convert feet to meters: m = ft / 3.28084

Rust: `units::feet_to_meters`
    """
    ...

def meters_to_inches(m: float) -> float:
    """
Convert meters to inches: in = m × 39.3701

Rust: `units::meters_to_inches`
    """
    ...

def inches_to_meters(i: float) -> float:
    """
Convert inches to meters: m = in / 39.3701

Rust: `units::inches_to_meters`
    """
    ...

def km_to_miles(km: float) -> float:
    """
Convert kilometers to miles: mi = km × 0.621371

Rust: `units::km_to_miles`
    """
    ...

def miles_to_km(mi: float) -> float:
    """
Convert miles to kilometers: km = mi / 0.621371

Rust: `units::miles_to_km`
    """
    ...

def meters_to_au(m: float) -> float:
    """
Convert meters to astronomical units: AU = m / 1.496×10¹¹

Rust: `units::meters_to_au`
    """
    ...

def au_to_meters(au: float) -> float:
    """
Convert astronomical units to meters: m = AU × 1.496×10¹¹

Rust: `units::au_to_meters`
    """
    ...

def meters_to_light_years(m: float) -> float:
    """
Convert meters to light-years: ly = m / 9.461×10¹⁵

Rust: `units::meters_to_light_years`
    """
    ...

def light_years_to_meters(ly: float) -> float:
    """
Convert light-years to meters: m = ly × 9.461×10¹⁵

Rust: `units::light_years_to_meters`
    """
    ...

def meters_to_parsec(m: float) -> float:
    """
Convert meters to parsecs: pc = m / 3.086×10¹⁶

Rust: `units::meters_to_parsec`
    """
    ...

def parsec_to_meters(pc: float) -> float:
    """
Convert parsecs to meters: m = pc × 3.086×10¹⁶

Rust: `units::parsec_to_meters`
    """
    ...

def angstrom_to_meters(a: float) -> float:
    """
Convert angstroms to meters: m = Å × 10⁻¹⁰

Rust: `units::angstrom_to_meters`
    """
    ...

def meters_to_angstrom(m: float) -> float:
    """
Convert meters to angstroms: Å = m / 10⁻¹⁰

Rust: `units::meters_to_angstrom`
    """
    ...

def nautical_miles_to_meters(nm: float) -> float:
    """
Convert nautical miles to meters: m = nmi × 1852

Rust: `units::nautical_miles_to_meters`
    """
    ...

def meters_to_nautical_miles(m: float) -> float:
    """
Convert meters to nautical miles: nmi = m / 1852

Rust: `units::meters_to_nautical_miles`
    """
    ...

def kg_to_lbs(kg: float) -> float:
    """
Convert kilograms to pounds: lb = kg × 2.20462

Rust: `units::kg_to_lbs`
    """
    ...

def lbs_to_kg(lbs: float) -> float:
    """
Convert pounds to kilograms: kg = lb / 2.20462

Rust: `units::lbs_to_kg`
    """
    ...

def kg_to_solar_masses(kg: float) -> float:
    """
Convert kilograms to solar masses: M☉ = kg / 1.989×10³⁰

Rust: `units::kg_to_solar_masses`
    """
    ...

def solar_masses_to_kg(sm: float) -> float:
    """
Convert solar masses to kilograms: kg = M☉ × 1.989×10³⁰

Rust: `units::solar_masses_to_kg`
    """
    ...

def amu_to_kg(amu: float) -> float:
    """
Convert atomic mass units to kilograms: kg = amu × 1.66054×10⁻²⁷

Rust: `units::amu_to_kg`
    """
    ...

def kg_to_amu(kg: float) -> float:
    """
Convert kilograms to atomic mass units: amu = kg / 1.66054×10⁻²⁷

Rust: `units::kg_to_amu`
    """
    ...

def joules_to_ev(j: float) -> float:
    """
Convert joules to electron-volts: eV = J / e

Rust: `units::joules_to_ev`
    """
    ...

def ev_to_joules(ev: float) -> float:
    """
Convert electron-volts to joules: J = eV × e

Rust: `units::ev_to_joules`
    """
    ...

def joules_to_calories(j: float) -> float:
    """
Convert joules to calories: cal = J / 4.184

Rust: `units::joules_to_calories`
    """
    ...

def calories_to_joules(cal: float) -> float:
    """
Convert calories to joules: J = cal × 4.184

Rust: `units::calories_to_joules`
    """
    ...

def joules_to_kwh(j: float) -> float:
    """
Convert joules to kilowatt-hours: kWh = J / 3.6×10⁶

Rust: `units::joules_to_kwh`
    """
    ...

def kwh_to_joules(kwh: float) -> float:
    """
Convert kilowatt-hours to joules: J = kWh × 3.6×10⁶

Rust: `units::kwh_to_joules`
    """
    ...

def joules_to_btu(j: float) -> float:
    """
Convert joules to British thermal units: BTU = J / 1055.06

Rust: `units::joules_to_btu`
    """
    ...

def btu_to_joules(btu: float) -> float:
    """
Convert British thermal units to joules: J = BTU × 1055.06

Rust: `units::btu_to_joules`
    """
    ...

def ev_to_wavelength(ev: float) -> float:
    """
Converts photon energy in eV to wavelength in meters via λ = hc/E.

Rust: `units::ev_to_wavelength`
    """
    ...

def wavelength_to_ev(wavelength: float) -> float:
    """
Converts photon wavelength in meters to energy in eV via E = hc/λ.

Rust: `units::wavelength_to_ev`
    """
    ...

def pa_to_atm(pa: float) -> float:
    """
Convert pascals to atmospheres: atm = Pa / 101325

Rust: `units::pa_to_atm`
    """
    ...

def atm_to_pa(atm: float) -> float:
    """
Convert atmospheres to pascals: Pa = atm × 101325

Rust: `units::atm_to_pa`
    """
    ...

def pa_to_bar(pa: float) -> float:
    """
Convert pascals to bar: bar = Pa / 10⁵

Rust: `units::pa_to_bar`
    """
    ...

def bar_to_pa(bar: float) -> float:
    """
Convert bar to pascals: Pa = bar × 10⁵

Rust: `units::bar_to_pa`
    """
    ...

def pa_to_psi(pa: float) -> float:
    """
Convert pascals to pounds per square inch: psi = Pa / 6894.76

Rust: `units::pa_to_psi`
    """
    ...

def psi_to_pa(psi: float) -> float:
    """
Convert pounds per square inch to pascals: Pa = psi × 6894.76

Rust: `units::psi_to_pa`
    """
    ...

def pa_to_mmhg(pa: float) -> float:
    """
Convert pascals to millimeters of mercury: mmHg = Pa / 133.322

Rust: `units::pa_to_mmhg`
    """
    ...

def mmhg_to_pa(mmhg: float) -> float:
    """
Convert millimeters of mercury to pascals: Pa = mmHg × 133.322

Rust: `units::mmhg_to_pa`
    """
    ...

def degrees_to_radians(deg: float) -> float:
    """
Convert degrees to radians: rad = deg × π / 180

Rust: `units::degrees_to_radians`
    """
    ...

def radians_to_degrees(rad: float) -> float:
    """
Convert radians to degrees: deg = rad × 180 / π

Rust: `units::radians_to_degrees`
    """
    ...

def rpm_to_rad_per_sec(rpm: float) -> float:
    """
Convert revolutions per minute to radians per second: ω = rpm × 2π / 60

Rust: `units::rpm_to_rad_per_sec`
    """
    ...

def rad_per_sec_to_rpm(omega: float) -> float:
    """
Convert radians per second to revolutions per minute: rpm = ω × 60 / (2π)

Rust: `units::rad_per_sec_to_rpm`
    """
    ...

def seconds_to_years(s: float) -> float:
    """
Convert seconds to years: yr = s / 3.1557×10⁷

Rust: `units::seconds_to_years`
    """
    ...

def years_to_seconds(yr: float) -> float:
    """
Convert years to seconds: s = yr × 3.1557×10⁷

Rust: `units::years_to_seconds`
    """
    ...

def mps_to_kmh(mps: float) -> float:
    """
Convert meters per second to kilometers per hour: km/h = m/s × 3.6

Rust: `units::mps_to_kmh`
    """
    ...

def kmh_to_mps(kmh: float) -> float:
    """
Convert kilometers per hour to meters per second: m/s = km/h / 3.6

Rust: `units::kmh_to_mps`
    """
    ...

def mps_to_mph(mps: float) -> float:
    """
Convert meters per second to miles per hour: mph = m/s × 2.23694

Rust: `units::mps_to_mph`
    """
    ...

def mph_to_mps(mph: float) -> float:
    """
Convert miles per hour to meters per second: m/s = mph / 2.23694

Rust: `units::mph_to_mps`
    """
    ...

def mps_to_knots(mps: float) -> float:
    """
Convert meters per second to knots: kt = m/s × 1.94384

Rust: `units::mps_to_knots`
    """
    ...

def knots_to_mps(kt: float) -> float:
    """
Convert knots to meters per second: m/s = kt / 1.94384

Rust: `units::knots_to_mps`
    """
    ...

def mps_to_mach(mps: float, speed_of_sound: float) -> float:
    """
Convert meters per second to Mach number: M = v / v_sound

Rust: `units::mps_to_mach`
    """
    ...

def mach_to_mps(mach: float, speed_of_sound: float) -> float:
    """
Convert Mach number to meters per second: v = M × v_sound

Rust: `units::mach_to_mps`
    """
    ...

def watts_to_horsepower(w: float) -> float:
    """
Convert watts to mechanical horsepower: hp = W / 745.7

Rust: `units::watts_to_horsepower`
    """
    ...

def horsepower_to_watts(hp: float) -> float:
    """
Convert mechanical horsepower to watts: W = hp × 745.7

Rust: `units::horsepower_to_watts`
    """
    ...

def watts_to_btu_per_hour(w: float) -> float:
    """
Convert watts to BTU per hour: BTU/h = W / 0.293071

Rust: `units::watts_to_btu_per_hour`
    """
    ...

def btu_per_hour_to_watts(btu_hr: float) -> float:
    """
Convert BTU per hour to watts: W = BTU/h × 0.293071

Rust: `units::btu_per_hour_to_watts`
    """
    ...

def watts_to_tons_refrigeration(w: float) -> float:
    """
Convert watts to tons of refrigeration: TR = W / 3516.85

Rust: `units::watts_to_tons_refrigeration`
    """
    ...

def tons_refrigeration_to_watts(tons: float) -> float:
    """
Convert tons of refrigeration to watts: W = TR × 3516.85

Rust: `units::tons_refrigeration_to_watts`
    """
    ...

def watts_to_kcal_per_hour(w: float) -> float:
    """
Convert watts to kilocalories per hour: kcal/h = W / 1.163

Rust: `units::watts_to_kcal_per_hour`
    """
    ...

def kcal_per_hour_to_watts(kcal_hr: float) -> float:
    """
Convert kilocalories per hour to watts: W = kcal/h × 1.163

Rust: `units::kcal_per_hour_to_watts`
    """
    ...

def kilowatts_to_watts(kw: float) -> float:
    """
Convert kilowatts to watts: W = kW × 10³

Rust: `units::kilowatts_to_watts`
    """
    ...

def watts_to_kilowatts(w: float) -> float:
    """
Convert watts to kilowatts: kW = W × 10⁻³

Rust: `units::watts_to_kilowatts`
    """
    ...

def megawatts_to_watts(mw: float) -> float:
    """
Convert megawatts to watts: W = MW × 10⁶

Rust: `units::megawatts_to_watts`
    """
    ...

def watts_to_megawatts(w: float) -> float:
    """
Convert watts to megawatts: MW = W × 10⁻⁶

Rust: `units::watts_to_megawatts`
    """
    ...

def watt_hours_to_joules(wh: float) -> float:
    """
Convert watt-hours to joules: J = Wh × 3600

Rust: `units::watt_hours_to_joules`
    """
    ...

def joules_to_watt_hours(j: float) -> float:
    """
Convert joules to watt-hours: Wh = J / 3600

Rust: `units::joules_to_watt_hours`
    """
    ...

def amp_hours_to_coulombs(ah: float) -> float:
    """
Convert ampere-hours to coulombs: C = Ah × 3600

Rust: `units::amp_hours_to_coulombs`
    """
    ...

def coulombs_to_amp_hours(c: float) -> float:
    """
Convert coulombs to ampere-hours: Ah = C / 3600

Rust: `units::coulombs_to_amp_hours`
    """
    ...

def kg_tnt_to_joules(kg: float) -> float:
    """
Convert kg of TNT equivalent to joules: J = kg × 4.184×10⁶

Rust: `units::kg_tnt_to_joules`
    """
    ...

def joules_to_kg_tnt(j: float) -> float:
    """
Convert joules to kg of TNT equivalent: kg = J / 4.184×10⁶

Rust: `units::joules_to_kg_tnt`
    """
    ...

def kg_coal_to_joules(kg: float) -> float:
    """
Convert kg of coal equivalent to joules: J = kg × 2.9×10⁷

Rust: `units::kg_coal_to_joules`
    """
    ...

def joules_to_kg_coal(j: float) -> float:
    """
Convert joules to kg of coal equivalent: kg = J / 2.9×10⁷

Rust: `units::joules_to_kg_coal`
    """
    ...

def kg_oil_to_joules(kg: float) -> float:
    """
Convert kg of oil equivalent to joules: J = kg × 4.187×10⁷

Rust: `units::kg_oil_to_joules`
    """
    ...

def joules_to_kg_oil(j: float) -> float:
    """
Convert joules to kg of oil equivalent: kg = J / 4.187×10⁷

Rust: `units::joules_to_kg_oil`
    """
    ...

def kg_hydrogen_to_joules(kg: float) -> float:
    """
Convert kg of hydrogen to joules: J = kg × 1.42×10⁸

Rust: `units::kg_hydrogen_to_joules`
    """
    ...

def joules_to_kg_hydrogen(j: float) -> float:
    """
Convert joules to kg of hydrogen equivalent: kg = J / 1.42×10⁸

Rust: `units::joules_to_kg_hydrogen`
    """
    ...

def liters_gasoline_to_joules(liters: float) -> float:
    """
Convert liters of gasoline to joules: J = L × 3.4×10⁷

Rust: `units::liters_gasoline_to_joules`
    """
    ...

def joules_to_liters_gasoline(j: float) -> float:
    """
Convert joules to liters of gasoline equivalent: L = J / 3.4×10⁷

Rust: `units::joules_to_liters_gasoline`
    """
    ...
