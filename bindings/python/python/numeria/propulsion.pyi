"""
Rocket propulsion and impulsive orbital transfers.  The Tsiolkovsky equation `Δv = v_e ln(m₀/m_f)` and the specific impulse and mass ratio around it, thrust with and without the pressure-thrust term, staged Δv, and the gravity-turn loss that makes the ideal Δv an underestimate for a launch.  Transfers: Hohmann Δv and time, the bi-elliptic alternative (which wins beyond a radius ratio of about 11.94), and plane changes. Nozzle design covers exit velocity, throat area and the area ratio for a given exit Mach number.  For Lambert targeting, J2 effects and orbit propagation see `astrophysics`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def tsiolkovsky_delta_v(exhaust_velocity: float, mass_initial: float, mass_final: float) -> float:
    """
Tsiolkovsky rocket equation: Δv = ve × ln(m0/mf)

Rust: `propulsion::tsiolkovsky_delta_v`
    """
    ...

def mass_ratio(delta_v: float, exhaust_velocity: float) -> float:
    """
Mass ratio from the rocket equation inverted: m0/mf = exp(Δv/ve)

Rust: `propulsion::mass_ratio`
    """
    ...

def specific_impulse(thrust: float, mass_flow_rate: float, g: float) -> float:
    """
Specific impulse: Isp = F / (ṁ × g)

Rust: `propulsion::specific_impulse`
    """
    ...

def exhaust_velocity_from_isp(isp: float, g: float) -> float:
    """
Effective exhaust velocity from specific impulse: ve = Isp × g

Rust: `propulsion::exhaust_velocity_from_isp`
    """
    ...

def thrust(mass_flow_rate: float, exhaust_velocity: float) -> float:
    """
Thrust from momentum: F = ṁ × ve

Rust: `propulsion::thrust`
    """
    ...

def thrust_with_pressure(mass_flow_rate: float, exhaust_velocity: float, exit_pressure: float, ambient_pressure: float, exit_area: float) -> float:
    """
Thrust including pressure term: F = ṁve + (Pe - Pa)Ae

Rust: `propulsion::thrust_with_pressure`
    """
    ...

def delta_v_staged(stages: list[tuple[float, float, float]]) -> float:
    """
Total Δv for a multi-stage rocket. Each element is (exhaust_velocity, mass_full, mass_empty).

Rust: `propulsion::delta_v_staged`
    """
    ...

def hohmann_delta_v(mu: float, r1: float, r2: float) -> tuple[float, float]:
    """
Hohmann transfer Δv values. Returns (Δv1, Δv2) for departure and arrival burns.

Rust: `propulsion::hohmann_delta_v`
    """
    ...

def hohmann_transfer_time(mu: float, r1: float, r2: float) -> float:
    """
Transfer time for a Hohmann orbit: t = π√((r1+r2)³ / (8μ))

Rust: `propulsion::hohmann_transfer_time`
    """
    ...

def gravity_turn_loss(g: float, burn_time: float) -> float:
    """
Gravity drag loss approximation: Δv_loss ≈ g × t

Rust: `propulsion::gravity_turn_loss`
    """
    ...

def delta_v_plane_change(velocity: float, angle: float) -> float:
    """
Plane change Δv: Δv = 2v × sin(θ/2)

Rust: `propulsion::delta_v_plane_change`
    """
    ...

def bi_elliptic_delta_v(mu: float, r1: float, r2: float, r_intermediate: float) -> float:
    """
Bi-elliptic transfer total Δv via three burns through an intermediate radius.

Rust: `propulsion::bi_elliptic_delta_v`
    """
    ...

def nozzle_exit_velocity(chamber_temp: float, molar_mass: float, gamma: float, pressure_ratio: float) -> float:
    """
Nozzle exit velocity from thermodynamic properties:
ve = √( 2γRT / (M(γ-1)) × (1 - (Pe/Pc)^((γ-1)/γ)) )

Rust: `propulsion::nozzle_exit_velocity`
    """
    ...

def throat_area(mass_flow: float, chamber_pressure: float, chamber_temp: float, gamma: float, molar_mass: float) -> float:
    """
Throat area required for a given mass flow:
A* = (ṁ / Pc) × √(R T / (γ M)) / (2/(γ+1))^((γ+1)/(2(γ-1)))

Rust: `propulsion::throat_area`
    """
    ...

def area_ratio_from_mach(mach: float, gamma: float) -> float:
    """
Area ratio Ae/A* as a function of Mach number and heat capacity ratio:
Ae/A* = (1/M) × ((2/(γ+1)) × (1 + (γ-1)/2 × M²))^((γ+1)/(2(γ-1)))

Rust: `propulsion::area_ratio_from_mach`
    """
    ...
