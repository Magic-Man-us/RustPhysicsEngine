"""
Nonlinear least squares: Levenberg-Marquardt.  Reference: Marquardt (1963); Nocedal & Wright, *Numerical Optimization*, §10.3. Minimizes ½‖r(p)‖² by solving (JᵀJ + λ·diag(JᵀJ))·δ = −Jᵀr and adapting λ on accept/reject.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.linalg.matrix import Matrix

class LmResult:
    """
Result of a Levenberg-Marquardt fit.

`residual` is the final sum of squared residuals ‖r(p)‖²;
`covariance` is s²·(JᵀJ)⁻¹ with s² = SSR/(m−n) when m > n and JᵀJ is
invertible, `None` otherwise.

Rust: `optimization::least_squares::LmResult`
    """
    def __init__(self, params: list[float], residual: float, iters: int, covariance: Optional[Matrix | Sequence[Sequence[float]]]) -> None: ...
    @property
    def params(self) -> list[float]: ...
    @property
    def residual(self) -> float: ...
    @property
    def iters(self) -> int: ...
    @property
    def covariance(self) -> Optional[Matrix]: ...

def fit_exponential_decay(t: list[float], y: list[float]) -> tuple[float, float]:
    """
Fits y ≈ A·e^(−k·t), returning (A, k). Initial guess from a
log-linear regression over the positive samples, refined by LM.

Fails with `InvalidArgument` unless there are ≥ 2 samples with
matching lengths and at least two positive y values.

Rust: `optimization::least_squares::fit_exponential_decay`
    """
    ...

def fit_gaussian_peak(x: list[float], y: list[float]) -> tuple[float, float, float]:
    """
Fits y ≈ A·exp(−(x−μ)²/(2σ²)), returning (A, μ, σ). Initial guess
from the sample peak and moment-based width, refined by LM.

Rust: `optimization::least_squares::fit_gaussian_peak`
    """
    ...
