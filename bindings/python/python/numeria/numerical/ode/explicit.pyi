"""
Explicit fixed-step ODE integrators.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def euler_step(f: Callable[[float, float], float], t: float, y: float, dt: float) -> float:
    """
Explicit fixed-step ODE integrators.
Single forward Euler step: y_next = y + dt * f(t, y).

Rust: `numerical::ode::explicit::euler_step`
    """
    ...

def rk4_step(f: Callable[[float, float], float], t: float, y: float, dt: float) -> float:
    """
Single step of the classic 4th-order Runge-Kutta method.

Rust: `numerical::ode::explicit::rk4_step`
    """
    ...

def rk4_solve(f: Callable[[float, float], float], t0: float, y0: float, t_end: float, dt: float) -> list[tuple[float, float]]:
    """
Full RK4 integration of dy/dt = f(t, y) from t0 to t_end, returning (t, y) pairs.

Rust: `numerical::ode::explicit::rk4_solve`
    """
    ...

def rk4_step_vec(f: Callable[[float, list[float]], list[float]], t: float, y: list[float], dt: float) -> list[float]:
    """
Single RK4 step for a system of ODEs (vector state).
`f(t, y)` returns a `Vec<f64>` of derivatives matching the length of `y`.

Rust: `numerical::ode::explicit::rk4_step_vec`
    """
    ...
