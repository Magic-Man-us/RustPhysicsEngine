"""
Symplectic integrators for second-order systems x'' = a(x).  These preserve phase-space volume, so energy errors stay bounded instead of drifting. References: Verlet (1967); Yoshida, "Construction of higher order symplectic integrators", Phys. Lett. A 150 (1990).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def velocity_verlet(acc: Callable[[list[float]], list[float]], x: MutableSequence[float], v: MutableSequence[float], dt: float) -> None:
    """
Symplectic integrators for second-order systems x'' = a(x).

These preserve phase-space volume, so energy errors stay bounded
instead of drifting. References: Verlet (1967); Yoshida, "Construction
of higher order symplectic integrators", Phys. Lett. A 150 (1990).
One velocity-Verlet step (kick-drift-kick):
v½ = v + a(x)·dt/2; x₁ = x + v½·dt; v₁ = v½ + a(x₁)·dt/2.

Panics:
Panics if `x` and `v` differ in length or `acc` returns the wrong
length.

Rust: `numerical::ode::symplectic::velocity_verlet`
    """
    ...

def leapfrog_kick_drift_kick(acc: Callable[[list[float]], list[float]], x: MutableSequence[float], v: MutableSequence[float], dt: float) -> None:
    """
Alias of the kick-drift-kick leapfrog scheme (identical to velocity
Verlet in this synchronized form).

Rust: `numerical::ode::symplectic::leapfrog_kick_drift_kick`
    """
    ...

def yoshida4(acc: Callable[[list[float]], list[float]], x: MutableSequence[float], v: MutableSequence[float], dt: float) -> None:
    """
One 4th-order Yoshida step: composition of three velocity-Verlet
sub-steps with weights w1, w0, w1 where
w1 = 1/(2 − 2^(1/3)), w0 = −2^(1/3)·w1.

Rust: `numerical::ode::symplectic::yoshida4`
    """
    ...
