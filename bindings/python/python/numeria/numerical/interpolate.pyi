"""
Interpolation routines.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec3

class BSpline:
    """
Clamped uniform B-spline curve evaluated by de Boor's algorithm
(de Boor, *A Practical Guide to Splines*).

Rust: `numerical::interpolate::BSpline`
    """
    def __init__(self, degree: int, knots: list[float], control: list[Vec3 | Sequence[float]]) -> None: ...
    @staticmethod
    def uniform(degree: int, control: list[Vec3 | Sequence[float]]) -> BSpline: ...
    def domain(self) -> tuple[float, float]: ...
    def eval(self, u: float) -> Vec3: ...
    def derivative(self, u: float) -> Vec3: ...
    @property
    def degree(self) -> int: ...
    @property
    def knots(self) -> list[float]: ...
    @property
    def control(self) -> list[Vec3]: ...

class CubicSpline:
    """
Piecewise cubic spline S_i(t) = a_i + b_i·Δ + c_i·Δ² + d_i·Δ³ with
Δ = t − x_i on segment i (Burden & Faires, *Numerical Analysis*,
§3.5). Built with the Thomas tridiagonal solve; C² across knots.

Rust: `numerical::interpolate::CubicSpline`
    """
    @staticmethod
    def natural(x: list[float], y: list[float]) -> CubicSpline: ...
    @staticmethod
    def clamped(x: list[float], y: list[float], dy0: float, dyn_: float) -> CubicSpline: ...
    def eval(self, t: float) -> float: ...
    def derivative(self, t: float) -> float: ...
    def integrate(self, a: float, b: float) -> float: ...

def lerp(a: float, b: float, t: float) -> float:
    """
Interpolation routines.
Linear interpolation between a and b: a + t*(b - a).

Rust: `numerical::interpolate::lerp`
    """
    ...

def linear_interp(x_data: list[float], y_data: list[float], x: float) -> float:
    """
Piecewise linear interpolation in sorted (x_data, y_data).
Clamps to the endpoint values if x is outside the data range.
Panics if data slices are empty or mismatched in length.

Rust: `numerical::interpolate::linear_interp`
    """
    ...

def cubic_interp(x_data: list[float], y_data: list[float], x: float) -> float:
    """
Natural cubic spline interpolation for a single query point.
Falls back to linear interpolation if fewer than 4 data points.
Panics if data slices are empty or mismatched in length.

Rust: `numerical::interpolate::cubic_interp`
    """
    ...

def catmull_rom(points: list[Vec3 | Sequence[float]], t: float) -> Vec3:
    """
Uniform Catmull-Rom spline through `points`, parameterized so that
`t = i` lands exactly on `points[i]` (`t ∈ [0, n−1]`; endpoints use
duplicated boundary points).

Panics:
Panics unless there are at least 2 points and t is within range.

Rust: `numerical::interpolate::catmull_rom`
    """
    ...

def catmull_rom_2d(points: list[tuple[float, float]], t: float) -> tuple[float, float]:
    """
2-D uniform Catmull-Rom spline (same parameterization as
`catmull_rom`).

Panics:
Panics unless there are at least 2 points and t is within range.

Rust: `numerical::interpolate::catmull_rom_2d`
    """
    ...

def de_casteljau(control: list[Vec3 | Sequence[float]], t: float) -> Vec3:
    """
Arbitrary-degree Bezier curve point by the de Casteljau algorithm.

Panics:
Panics if `control` is empty or t is outside [0, 1].

Rust: `numerical::interpolate::de_casteljau`
    """
    ...
