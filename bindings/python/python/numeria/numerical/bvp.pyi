"""
Two-point boundary value problems.  `shooting` reduces y'' = f(t, y, y') with y(t0) = y0, y(t1) = y1 to root finding on the initial slope (integrated with Dormand-Prince, slope found with Brent's method). `finite_difference_linear_bvp` discretizes the linear problem y'' + p·y' + q·y = r on a uniform grid and solves the tridiagonal system with the Thomas algorithm (Burden & Faires, *Numerical Analysis*, §11.3).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def shooting(f: Callable[[float, float, float], float], t0: float, t1: float, y0: float, y1_target: float, guess_lo: float, guess_hi: float, tol: float) -> list[tuple[float, float]]:
    """
Solves y'' = f(t, y, y') with y(t0) = y0 and y(t1) = y1_target by
the shooting method: the unknown initial slope is bracketed by
[guess_lo, guess_hi] and found with Brent's method.

Returns the (t, y) trajectory of the converged solution. Fails with
`InvalidArgument` if the bracket does not straddle the target.

Rust: `numerical::bvp::shooting`
    """
    ...

def finite_difference_linear_bvp(p: Callable[[float], float], q: Callable[[float], float], r: Callable[[float], float], a: float, b: float, ya: float, yb: float, n: int) -> list[float]:
    """
Solves the linear BVP y'' + p(x)·y' + q(x)·y = r(x) on [a, b] with
y(a) = ya, y(b) = yb using n interior points and second-order
central differences; the tridiagonal system goes to `thomas_solve`.

Returns the full grid of n + 2 values including both boundaries.

Rust: `numerical::bvp::finite_difference_linear_bvp`
    """
    ...
