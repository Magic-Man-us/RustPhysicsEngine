"""
Chaos in low-dimensional systems.  The logistic map and its period-doubling route to chaos, the Hénon map, and the Lorenz and Rössler flows given as derivative functions to hand to an integrator from `numerical`.  Lyapunov exponents are the quantitative test: a positive exponent means nearby trajectories separate exponentially, which is what makes a system chaotic rather than merely complicated. Dimension estimators -- box counting and the correlation dimension -- measure the attractor that results.  For strange attractors as drawable objects, escape-time fractals and cellular automata see `fractals`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def logistic_map(r: float, x: float) -> float:
    """
Chaos in low-dimensional systems.

The logistic map and its period-doubling route to chaos, the Hénon
map, and the Lorenz and Rössler flows given as derivative functions to
hand to an integrator from `numerical`.

Lyapunov exponents are the quantitative test: a positive exponent means
nearby trajectories separate exponentially, which is what makes a
system chaotic rather than merely complicated. Dimension estimators --
box counting and the correlation dimension -- measure the attractor
that results.

For strange attractors as drawable objects, escape-time fractals and
cellular automata see `fractals`.
Computes one iteration of the logistic map: x_{n+1} = r × x × (1 - x).

Rust: `nonlinear::logistic_map`
    """
    ...

def logistic_map_iterate(r: float, x0: float, n: int) -> list[float]:
    """
Iterates the logistic map n times from x0, returning the full trajectory.

Rust: `nonlinear::logistic_map_iterate`
    """
    ...

def logistic_map_converge(r: float, x0: float, transient: int, samples: int) -> list[float]:
    """
Discards an initial transient, then collects post-transient samples from the logistic map.

Rust: `nonlinear::logistic_map_converge`
    """
    ...

def lyapunov_exponent_logistic(r: float, x0: float, n: int) -> float:
    """
Computes the Lyapunov exponent of the logistic map: λ = (1/N) Σ ln|r(1 - 2x_n)|.

Rust: `nonlinear::lyapunov_exponent_logistic`
    """
    ...

def lyapunov_exponent_1d(f: Callable[[float], float], df: Callable[[float], float], x0: float, n: int) -> float:
    """
Computes the Lyapunov exponent of a general 1D map: λ = (1/N) Σ ln|f'(x_n)|.

Rust: `nonlinear::lyapunov_exponent_1d`
    """
    ...

def lorenz_derivatives(state: list[float], sigma: float, rho: float, beta: float) -> list[float]:
    """
Computes the Lorenz system derivatives: dx/dt = σ(y-x), dy/dt = x(ρ-z)-y, dz/dt = xy-βz.

Rust: `nonlinear::lorenz_derivatives`
    """
    ...

def rossler_derivatives(state: list[float], a: float, b: float, c: float) -> list[float]:
    """
Computes the Roessler system derivatives: dx/dt = -y-z, dy/dt = x+ay, dz/dt = b+z(x-c).

Rust: `nonlinear::rossler_derivatives`
    """
    ...

def henon_map(x: float, y: float, a: float, b: float) -> tuple[float, float]:
    """
Computes one iteration of the Henon map: x_{n+1} = 1 - ax² + y, y_{n+1} = bx.

Rust: `nonlinear::henon_map`
    """
    ...

def henon_iterate(x0: float, y0: float, a: float, b: float, n: int) -> list[tuple[float, float]]:
    """
Iterates the Henon map n times from (x0, y0), returning the full trajectory of (x, y) pairs.

Rust: `nonlinear::henon_iterate`
    """
    ...

def correlation_dimension_estimate(distances: list[float], r: float) -> float:
    """
Estimates the correlation integral C(r) as the fraction of pairwise distances below threshold r.

Rust: `nonlinear::correlation_dimension_estimate`
    """
    ...

def box_counting_dimension(occupied_boxes: list[tuple[int, int]], grid_sizes: list[int]) -> float:
    """
Estimates the box-counting (Minkowski) dimension via least-squares fit of log(N) vs log(1/ε).

Rust: `nonlinear::box_counting_dimension`
    """
    ...

def fixed_point_iterate(f: Callable[[float], float], x0: float, tol: float, max_iter: int) -> Optional[float]:
    """
Finds a fixed point of f by iterating x_{n+1} = f(x_n) until convergence within tolerance.

Rust: `nonlinear::fixed_point_iterate`
    """
    ...

def is_stable_fixed_point(df_at_fixed: float) -> bool:
    """
Returns true if the fixed point is stable, i.e., |f'(x*)| < 1.

Rust: `nonlinear::is_stable_fixed_point`
    """
    ...
