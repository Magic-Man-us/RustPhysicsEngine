"""
Linear control: system response, stability margins and PID tuning.  First- and second-order step and impulse responses in closed form, and the parameters that characterise them -- natural frequency, damping ratio, rise and settling time, percent overshoot, bandwidth.  Stability is assessed through the gain and phase margins, which say how much extra gain or delay the loop tolerates before it oscillates. Steady-state error is given by system type.  PID tuning uses the Ziegler-Nichols rules. They are a starting point rather than an answer: they were derived for a quarter-amplitude decay and typically give an aggressive loop that wants detuning.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import kalman
from numeria.resonance.oscillator import Damping
from numeria.control_systems.kalman import ExtendedKalmanFilter as ExtendedKalmanFilter
from numeria.control_systems.kalman import KalmanFilter as KalmanFilter

class PidController:
    """

Rust: `control_systems::PidController`
    """
    def __init__(self, kp: float, ki: float, kd: float, output_min: float, output_max: float) -> None: ...
    def update(self, setpoint: float, measured: float, dt: float) -> float: ...
    def reset(self) -> None: ...
    @property
    def kp(self) -> float: ...
    @property
    def ki(self) -> float: ...
    @property
    def kd(self) -> float: ...

def first_order_step_response(gain: float, tau: float, t: float) -> float:
    """
First-order step response: y(t) = K(1 - e^(-t/τ))

Rust: `control_systems::first_order_step_response`
    """
    ...

def first_order_impulse_response(gain: float, tau: float, t: float) -> float:
    """
First-order impulse response: h(t) = (K/τ)·e^(-t/τ)

Rust: `control_systems::first_order_impulse_response`
    """
    ...

def second_order_step_response(gain: float, omega_n: float, zeta: float, t: float) -> float:
    """
Second-order step response for underdamped, critically damped, and overdamped systems.

Rust: `control_systems::second_order_step_response`
    """
    ...

def second_order_natural_frequency(k: float, m: float) -> float:
    """
Natural frequency of a second-order system: ωn = √(k/m)

Rust: `control_systems::second_order_natural_frequency`
    """
    ...

def second_order_damping_ratio(c: float, k: float, m: float) -> float:
    """
Damping ratio of a second-order system: ζ = c/(2√(km))

Rust: `control_systems::second_order_damping_ratio`
    """
    ...

def rise_time_first_order(tau: float) -> float:
    """
Rise time of a first-order system (10% to 90%): tr = 2.2τ

Rust: `control_systems::rise_time_first_order`
    """
    ...

def settling_time_first_order(tau: float) -> float:
    """
Settling time of a first-order system (2% criterion): ts = 4τ

Rust: `control_systems::settling_time_first_order`
    """
    ...

def settling_time_second_order(zeta: float, omega_n: float) -> float:
    """
Settling time of a second-order system (2% criterion): ts = 4/(ζωn)

Rust: `control_systems::settling_time_second_order`
    """
    ...

def overshoot_percent(zeta: float) -> float:
    """
Peak overshoot percentage: Mp = 100·exp(-πζ/√(1-ζ²))

Rust: `control_systems::overshoot_percent`
    """
    ...

def bandwidth_first_order(tau: float) -> float:
    """
Bandwidth of a first-order system: ωb = 1/τ

Rust: `control_systems::bandwidth_first_order`
    """
    ...

def gain_margin_db(open_loop_gain_at_phase_crossover: float) -> float:
    """
Gain margin in dB: GM = -20·log₁₀(|G(jω)|) at the phase crossover frequency

Rust: `control_systems::gain_margin_db`
    """
    ...

def phase_margin(phase_at_gain_crossover: float) -> float:
    """
Phase margin in degrees: PM = 180° + φ(ωgc)

Rust: `control_systems::phase_margin`
    """
    ...

def steady_state_error_type0(gain: float) -> float:
    """
Steady-state error for a type-0 system with step input: ess = 1/(1 + K)

Rust: `control_systems::steady_state_error_type0`
    """
    ...

def steady_state_error_type1(gain: float) -> float:
    """
Steady-state error for a type-1 system with ramp input: ess = 1/K

Rust: `control_systems::steady_state_error_type1`
    """
    ...

def is_stable_first_order(tau: float) -> bool:
    """
Check stability of a first-order system: stable when τ > 0.

Rust: `control_systems::is_stable_first_order`
    """
    ...

def is_stable_second_order(zeta: float, omega_n: float) -> bool:
    """
Check stability of a second-order system: stable when ζ > 0 and ωn > 0.

Rust: `control_systems::is_stable_second_order`
    """
    ...

def routh_criterion_2nd(a0: float, a1: float, a2: float) -> bool:
    """
Routh stability criterion for a 2nd-order polynomial: stable when all coefficients > 0.

Rust: `control_systems::routh_criterion_2nd`
    """
    ...

def transfer_function_poles(denominator: list[float]) -> list[complex]:
    """
Poles of a transfer function: the complex roots of the denominator
polynomial (coefficients highest degree first), via
`numerical::roots::polynomial_roots`. A system is BIBO-stable when
every pole has a negative real part.

Rust: `control_systems::transfer_function_poles`
    """
    ...
