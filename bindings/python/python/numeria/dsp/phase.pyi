"""
Phase utilities: unwrapping (1D and Itoh 2D), phase-locked loops, interpolated zero crossings, and phase measurement against a reference tone.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def wrap_phase(p: float) -> float:
    """
Wrap an angle into (−π, π].

Rust: `dsp::phase::wrap_phase`
    """
    ...

def unwrap_phase(p: list[float]) -> list[float]:
    """
1D phase unwrapping: remove 2π jumps between consecutive samples.

Rust: `dsp::phase::unwrap_phase`
    """
    ...

def unwrap_phase_2d(p: list[float], w: int, h: int) -> list[float]:
    """
2D phase unwrapping by Itoh's method: unwrap each row, then unwrap
the columns of the row-unwrapped field. Exact for residue-free
(consistent) phase maps.

Panics:
Panics unless `p.len() == w * h`.

Rust: `dsp::phase::unwrap_phase_2d`
    """
    ...

def phase_difference(a: list[float], b: list[float]) -> list[float]:
    """
Wrapped per-sample phase difference a − b.

Panics:
Panics if the lengths differ.

Rust: `dsp::phase::phase_difference`
    """
    ...

def group_delay_from_phase(phase: list[float], freqs: list[float]) -> list[float]:
    """
Group delay −dφ/dω from unwrapped phase samples on an angular
frequency grid (central differences; one-sided at the ends).

Panics:
Panics if the lengths differ or fewer than 2 points.

Rust: `dsp::phase::group_delay_from_phase`
    """
    ...

def phase_locked_loop(x: list[float], fs: float, f0: float, bandwidth: float) -> tuple[list[float], list[float]]:
    """
Second-order phase-locked loop tracking a real tone near f0:
returns the NCO phase track and the instantaneous frequency estimate
(Hz) per sample. `bandwidth` is the loop bandwidth in Hz.

Panics:
Panics unless the rates are positive.

Rust: `dsp::phase::phase_locked_loop`
    """
    ...

def zero_crossing_times(x: list[float], fs: float) -> list[float]:
    """
Linearly interpolated zero-crossing times (seconds), both directions.

Rust: `dsp::phase::zero_crossing_times`
    """
    ...

def phase_vs_reference(x: list[float], ref_freq: float, fs: float) -> float:
    """
Phase (radians) of the signal's component at `ref_freq` relative to
cos(2π·f·t) starting at the first sample, via single-bin correlation.

Rust: `dsp::phase::phase_vs_reference`
    """
    ...
