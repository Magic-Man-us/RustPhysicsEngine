"""
Thermal radiation and radiative transfer.  The Stefan-Boltzmann law `j = σT⁴`, Wien's displacement of the spectral peak, and the colour and brightness temperatures that invert them.  Transfer through an absorbing medium: optical depth, the Beer-Lambert law, and the photon mean free path. Radiation pressure for absorbing and reflecting surfaces. Surface exchange via Kirchhoff's law (emissivity equals absorptivity at equilibrium), view factors, and net radiative exchange between surfaces.  For the Planck spectrum itself see `quantum`; for reactor and photon shielding see `neutronics`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def total_emissive_power(temperature: float) -> float:
    """
Total emissive power of a perfect blackbody (ε=1): E = σT⁴

Rust: `radiation::total_emissive_power`
    """
    ...

def spectral_peak_frequency(temperature: float) -> float:
    """
Wien's law in the frequency domain: f_max = 5.879×10¹⁰ × T

Rust: `radiation::spectral_peak_frequency`
    """
    ...

def color_temperature(peak_wavelength: float) -> float:
    """
Inverse Wien's law: T = b / λ_max (color temperature from peak wavelength)

Rust: `radiation::color_temperature`
    """
    ...

def brightness_temperature(intensity: float, frequency: float) -> float:
    """
Brightness temperature via the Rayleigh-Jeans approximation: T_b = Ic² / (2kf²)

Rust: `radiation::brightness_temperature`
    """
    ...

def optical_depth(absorption_coeff: float, path_length: float) -> float:
    """
Optical depth: τ = κ × s

Rust: `radiation::optical_depth`
    """
    ...

def beer_lambert(initial_intensity: float, absorption_coeff: float, path_length: float) -> float:
    """
Beer-Lambert law: I = I₀ × e^(-κs)

Rust: `radiation::beer_lambert`
    """
    ...

def mean_free_path_photon(absorption_coeff: float) -> float:
    """
Photon mean free path: l = 1/κ

Rust: `radiation::mean_free_path_photon`
    """
    ...

def radiation_pressure(intensity: float) -> float:
    """
Radiation pressure for fully absorbed radiation: P = I/c

Rust: `radiation::radiation_pressure`
    """
    ...

def radiation_pressure_reflected(intensity: float) -> float:
    """
Radiation pressure for fully reflected radiation: P = 2I/c

Rust: `radiation::radiation_pressure_reflected`
    """
    ...

def emissivity_from_absorptivity(absorptivity: float) -> float:
    """
At thermal equilibrium, emissivity equals absorptivity: ε = α

Rust: `radiation::emissivity_from_absorptivity`
    """
    ...

def view_factor_parallel_plates(width: float, height: float, separation: float) -> float:
    """
View factor for two identical, directly opposed, parallel rectangles of
width W and height H separated by distance D.

Uses the exact analytical formula:
F = (2 / (πXY)) * [ ln(√((1+X²)(1+Y²)/(1+X²+Y²)))
                    + X√(1+Y²) atan(X/√(1+Y²))
                    + Y√(1+X²) atan(Y/√(1+X²))
                    - X atan(X) - Y atan(Y) ]
where X = W/D and Y = H/D.

Rust: `radiation::view_factor_parallel_plates`
    """
    ...

def radiative_exchange(emissivity1: float, emissivity2: float, area: float, t1: float, t2: float) -> float:
    """
Radiative heat exchange between two infinite parallel gray surfaces:
Q = σA(T₁⁴ - T₂⁴) / (1/ε₁ + 1/ε₂ - 1)

Rust: `radiation::radiative_exchange`
    """
    ...

def intensity_at_distance(luminosity: float, distance: float) -> float:
    """
Intensity at distance from a point source: I = L / (4πd²)

Rust: `radiation::intensity_at_distance`
    """
    ...

def luminosity_from_intensity(intensity: float, distance: float) -> float:
    """
Luminosity from measured intensity and distance: L = I × 4πd²

Rust: `radiation::luminosity_from_intensity`
    """
    ...
