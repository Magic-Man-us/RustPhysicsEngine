"""
Laser beams, optical fibre, and interferometry.  Gaussian beam propagation: waist, Rayleigh range, radius and curvature against distance, divergence, the Gouy phase, and on-axis intensity.  Fibre through the numerical aperture, acceptance angle, and the V-number that decides single- versus multi-mode operation, plus attenuation and dispersion broadening. Ray transfer (ABCD) matrices compose optical elements by matrix multiplication.  Coherence length and time, fringe visibility, and the Fabry-Pérot transmission with its free spectral range close the module.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def beam_waist_from_divergence(wavelength: float, divergence_half_angle: float) -> float:
    """
Beam waist from far-field divergence: w₀ = λ/(π×θ)

Rust: `photonics::beam_waist_from_divergence`
    """
    ...

def rayleigh_range(waist: float, wavelength: float) -> float:
    """
Rayleigh range: z_R = πw₀²/λ

Rust: `photonics::rayleigh_range`
    """
    ...

def beam_radius(waist: float, z: float, rayleigh: float) -> float:
    """
Beam radius at axial position z: w(z) = w₀√(1+(z/z_R)²)

Rust: `photonics::beam_radius`
    """
    ...

def beam_divergence(waist: float, wavelength: float) -> float:
    """
Far-field half-angle divergence: θ = λ/(πw₀)

Rust: `photonics::beam_divergence`
    """
    ...

def beam_curvature(z: float, rayleigh: float) -> float:
    """
Radius of curvature of the wavefront: R(z) = z(1+(z_R/z)²)

Rust: `photonics::beam_curvature`
    """
    ...

def gouy_phase(z: float, rayleigh: float) -> float:
    """
Gouy phase shift: ψ(z) = atan(z/z_R)

Rust: `photonics::gouy_phase`
    """
    ...

def beam_intensity(power: float, waist: float, r: float, z: float, rayleigh: float) -> float:
    """
Peak-normalized Gaussian beam intensity at radial offset r and axial position z:
I = (2P/(πw²)) exp(-2r²/w²), where w = w(z).

Rust: `photonics::beam_intensity`
    """
    ...

def beam_parameter(waist: float, z: float, rayleigh: float) -> tuple[float, float]:
    """
Combined beam parameter: returns (w(z), R(z)) at axial position z.

Rust: `photonics::beam_parameter`
    """
    ...

def numerical_aperture(n_core: float, n_clad: float) -> float:
    """
Numerical aperture of a step-index fiber: NA = √(n_core² - n_clad²)

Rust: `photonics::numerical_aperture`
    """
    ...

def acceptance_angle(na: float) -> float:
    """
Maximum acceptance half-angle: θ_max = arcsin(NA)

Rust: `photonics::acceptance_angle`
    """
    ...

def v_number(radius: float, na: float, wavelength: float) -> float:
    """
Normalized frequency (V-number): V = 2πr×NA/λ

Rust: `photonics::v_number`
    """
    ...

def is_single_mode(v_number: float) -> bool:
    """
True when the fiber supports only the fundamental mode (V < 2.405).

Rust: `photonics::is_single_mode`
    """
    ...

def number_of_modes(v_number: float) -> float:
    """
Approximate mode count for a step-index multimode fiber: M ≈ V²/2

Rust: `photonics::number_of_modes`
    """
    ...

def fiber_attenuation(input_power: float, attenuation_db_per_km: float, length_km: float) -> float:
    """
Output power after propagation through a lossy fiber:
P_out = P_in × 10^(-αL/10), where α is in dB/km and L in km.

Rust: `photonics::fiber_attenuation`
    """
    ...

def dispersion_broadening(dispersion: float, length: float, spectral_width: float) -> float:
    """
Chromatic dispersion pulse broadening: Δt = D × L × Δλ
D in ps/(nm·km), L in km, Δλ in nm → Δt in ps.

Rust: `photonics::dispersion_broadening`
    """
    ...

def critical_angle_fiber(n_core: float, n_clad: float) -> float:
    """
Critical angle for total internal reflection inside the fiber core:
θc = arcsin(n_clad / n_core)

Rust: `photonics::critical_angle_fiber`
    """
    ...

def thin_lens_matrix(focal_length: float) -> list[list[float]]:
    """
Thin lens matrix: [[1, 0], [-1/f, 1]]

Rust: `photonics::thin_lens_matrix`
    """
    ...

def free_space_matrix(distance: float) -> list[list[float]]:
    """
Free-space propagation matrix: [[1, d], [0, 1]]

Rust: `photonics::free_space_matrix`
    """
    ...

def image_distance_thick_lens(n: float, r1: float, r2: float, thickness: float, object_dist: float) -> float:
    """
Image distance for a thick lens via ABCD matrix composition.

Constructs the system matrix from: refraction at R1, propagation through
the lens of thickness `t` and index `n`, refraction at R2, then solves
for the image distance using the thin-lens-equivalent focal length.

Rust: `photonics::image_distance_thick_lens`
    """
    ...

def coherence_length(wavelength: float, bandwidth: float) -> float:
    """
Temporal coherence length: L_c = λ²/Δλ

Rust: `photonics::coherence_length`
    """
    ...

def coherence_time(bandwidth_hz: float) -> float:
    """
Coherence time from frequency bandwidth: τ_c = 1/Δf

Rust: `photonics::coherence_time`
    """
    ...

def fringe_visibility(i_max: float, i_min: float) -> float:
    """
Fringe visibility (contrast): V = (I_max - I_min) / (I_max + I_min)

Rust: `photonics::fringe_visibility`
    """
    ...

def fabry_perot_transmission(reflectance: float, phase: float) -> float:
    """
Fabry-Perot etalon transmission (Airy function):
T = (1-R)² / ((1-R)² + 4R sin²(δ/2))

Rust: `photonics::fabry_perot_transmission`
    """
    ...

def free_spectral_range(cavity_length: float, n: float) -> float:
    """
Free spectral range of a Fabry-Perot cavity: FSR = c/(2nL) in Hz.

Rust: `photonics::free_spectral_range`
    """
    ...
