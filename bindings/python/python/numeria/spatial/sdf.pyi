"""
Signed distance fields: primitives, combinators, domain operators, and queries (sphere tracing, normals, AO, soft shadows).  Primitive formulas follow Inigo Quilez's reference catalogue (iquilezles.org/articles/distfunctions). Negative inside, positive outside; all primitive SDFs are exact unless noted.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.spatial.primitives import Aabb
from numeria.spatial.primitives import Capsule
from numeria.spatial.primitives import Circle
from numeria.geometry.geodesy import Ellipsoid
from numeria.spatial.primitives import Polygon2
from numeria.spatial.primitives import Ray
from numeria.spatial.primitives import Rect
from numeria.spatial.primitives import Sphere
from numeria.math import Vec2
from numeria.math import Vec3

def sd_sphere(p: Vec3 | Sequence[float], r: float) -> float:
    """
Sphere of radius r at the origin: |p| − r.

Rust: `spatial::sdf::sd_sphere`
    """
    ...

def sd_box(p: Vec3 | Sequence[float], half: Vec3 | Sequence[float]) -> float:
    """
Axis-aligned box with the given half extents.

Rust: `spatial::sdf::sd_box`
    """
    ...

def sd_rounded_box(p: Vec3 | Sequence[float], half: Vec3 | Sequence[float], r: float) -> float:
    """
Box with edges rounded by radius r.

Rust: `spatial::sdf::sd_rounded_box`
    """
    ...

def sd_torus(p: Vec3 | Sequence[float], major: float, minor: float) -> float:
    """
Torus in the xz-plane: major radius to the tube center, minor tube
radius.

Rust: `spatial::sdf::sd_torus`
    """
    ...

def sd_capsule(p: Vec3 | Sequence[float], a: Vec3 | Sequence[float], b: Vec3 | Sequence[float], r: float) -> float:
    """
Capsule between a and b with radius r.

Rust: `spatial::sdf::sd_capsule`
    """
    ...

def sd_cylinder(p: Vec3 | Sequence[float], a: Vec3 | Sequence[float], b: Vec3 | Sequence[float], r: float) -> float:
    """
Finite capped cylinder between a and b with radius r (exact).

Rust: `spatial::sdf::sd_cylinder`
    """
    ...

def sd_cone(p: Vec3 | Sequence[float], angle: float, h: float) -> float:
    """
Infinite-precision capped cone with apex at the origin opening
along −y: half-angle `angle`, height h (IQ's sdCone, exact).

Rust: `spatial::sdf::sd_cone`
    """
    ...

def sd_plane(p: Vec3 | Sequence[float], n: Vec3 | Sequence[float], d: float) -> float:
    """
Half-space n·p + d = 0 (n need not be unit; it is normalized).

Rust: `spatial::sdf::sd_plane`
    """
    ...

def sd_ellipsoid(p: Vec3 | Sequence[float], radii: Vec3 | Sequence[float]) -> float:
    """
Ellipsoid with the given semi-axes (IQ's bound-improved
approximation; not exact away from the axes).

Rust: `spatial::sdf::sd_ellipsoid`
    """
    ...

def sd_octahedron(p: Vec3 | Sequence[float], s: float) -> float:
    """
Regular octahedron with "radius" s (exact).

Rust: `spatial::sdf::sd_octahedron`
    """
    ...

def sd_circle(p: Vec2 | Sequence[float], r: float) -> float:
    """
Circle of radius r at the origin.

Rust: `spatial::sdf::sd_circle`
    """
    ...

def sd_rect(p: Vec2 | Sequence[float], half: Vec2 | Sequence[float]) -> float:
    """
Axis-aligned rectangle with the given half extents.

Rust: `spatial::sdf::sd_rect`
    """
    ...

def sd_segment_2d(p: Vec2 | Sequence[float], a: Vec2 | Sequence[float], b: Vec2 | Sequence[float]) -> float:
    """
Unsigned distance to a 2-D segment minus nothing (a "line" SDF).

Rust: `spatial::sdf::sd_segment_2d`
    """
    ...

def sd_polygon_2d(p: Vec2 | Sequence[float], poly: Polygon2) -> float:
    """
Signed distance to a simple polygon (negative inside).

Rust: `spatial::sdf::sd_polygon_2d`
    """
    ...

def sd_hexagon(p: Vec2 | Sequence[float], r: float) -> float:
    """
Regular hexagon with circumscribed radius derived from apothem r
(IQ's sdHexagon: r is the apothem / inradius).

Rust: `spatial::sdf::sd_hexagon`
    """
    ...

def sd_star(p: Vec2 | Sequence[float], r: float, n: int, m: float) -> float:
    """
n-pointed star with outer radius r and inner-radius factor set by m
(IQ's sdStar; m between 2 and n controls pointiness).

Rust: `spatial::sdf::sd_star`
    """
    ...

def op_union(a: float, b: float) -> float:
    """
Union: min(a, b).

Rust: `spatial::sdf::op_union`
    """
    ...

def op_subtract(a: float, b: float) -> float:
    """
Subtraction (a minus b): max(a, −b).

Rust: `spatial::sdf::op_subtract`
    """
    ...

def op_intersect(a: float, b: float) -> float:
    """
Intersection: max(a, b).

Rust: `spatial::sdf::op_intersect`
    """
    ...

def op_smooth_union(a: float, b: float, k: float) -> float:
    """
Polynomial smooth union with blending radius k.

Rust: `spatial::sdf::op_smooth_union`
    """
    ...

def op_smooth_subtract(a: float, b: float, k: float) -> float:
    """
Smooth subtraction.

Rust: `spatial::sdf::op_smooth_subtract`
    """
    ...

def op_smooth_intersect(a: float, b: float, k: float) -> float:
    """
Smooth intersection.

Rust: `spatial::sdf::op_smooth_intersect`
    """
    ...

def op_round(d: float, r: float) -> float:
    """
Rounds a shape outward by r.

Rust: `spatial::sdf::op_round`
    """
    ...

def op_onion(d: float, thickness: float) -> float:
    """
Hollows a shape into a shell of the given thickness.

Rust: `spatial::sdf::op_onion`
    """
    ...

def op_repeat(p: Vec3 | Sequence[float], period: Vec3 | Sequence[float]) -> Vec3:
    """
Infinite domain repetition with the given period per axis
(returns the point folded into the central cell).

Rust: `spatial::sdf::op_repeat`
    """
    ...

def op_repeat_limited(p: Vec3 | Sequence[float], period: Vec3 | Sequence[float], count: list[int]) -> Vec3:
    """
Limited repetition: at most `count` cells either side per axis.

Rust: `spatial::sdf::op_repeat_limited`
    """
    ...

def op_mirror(p: Vec3 | Sequence[float], axes: list[bool]) -> Vec3:
    """
Mirror the chosen axes (|x| fold).

Rust: `spatial::sdf::op_mirror`
    """
    ...

def op_twist(p: Vec3 | Sequence[float], k: float) -> Vec3:
    """
Twist about the y axis by k radians per unit height.

Rust: `spatial::sdf::op_twist`
    """
    ...

def op_bend(p: Vec3 | Sequence[float], k: float) -> Vec3:
    """
Bend about the z axis with curvature k.

Rust: `spatial::sdf::op_bend`
    """
    ...

def op_elongate(p: Vec3 | Sequence[float], h: Vec3 | Sequence[float]) -> Vec3:
    """
Elongation: stretches the shape by clamping the sample point.

Rust: `spatial::sdf::op_elongate`
    """
    ...

def op_polar_repeat_2d(p: Vec2 | Sequence[float], n: int) -> Vec2:
    """
Polar repetition: folds the plane into one of n angular sectors.

Rust: `spatial::sdf::op_polar_repeat_2d`
    """
    ...

def sdf_normal(f: Callable[[Vec3 | Sequence[float]], float], p: Vec3 | Sequence[float], eps: float) -> Vec3:
    """
Central-difference gradient normalized to a surface normal.

Rust: `spatial::sdf::sdf_normal`
    """
    ...

def sdf_raymarch(f: Callable[[Vec3 | Sequence[float]], float], r: Ray, max_dist: float, eps: float, max_steps: int) -> Optional[RayHit]:
    """
Sphere tracing: march the ray by the SDF value until |f| < eps.

Rust: `spatial::sdf::sdf_raymarch`
    """
    ...

def sdf_to_grid(f: Callable[[Vec3 | Sequence[float]], float], bounds: Aabb, res: list[int]) -> list[float]:
    """
Samples the SDF on a regular grid (x-fastest order:
`data[k*ny*nx + j*nx + i]`), suitable for marching cubes.

Panics:
Panics if any resolution is < 2.

Rust: `spatial::sdf::sdf_to_grid`
    """
    ...

def sdf_to_grid_2d(f: Callable[[Vec2 | Sequence[float]], float], bounds: Rect, res: list[int]) -> list[float]:
    """
2-D grid sampling (row-major, `data[j*nx + i]`).

Panics:
Panics if any resolution is < 2.

Rust: `spatial::sdf::sdf_to_grid_2d`
    """
    ...

def sdf_ambient_occlusion(f: Callable[[Vec3 | Sequence[float]], float], p: Vec3 | Sequence[float], n: Vec3 | Sequence[float], steps: int, step_size: float) -> float:
    """
Screen-space-style ambient occlusion: samples the SDF along the
normal; 1 = fully open, 0 = fully occluded.

Rust: `spatial::sdf::sdf_ambient_occlusion`
    """
    ...

def sdf_soft_shadow(f: Callable[[Vec3 | Sequence[float]], float], origin: Vec3 | Sequence[float], dir: Vec3 | Sequence[float], k: float) -> float:
    """
IQ soft shadow: marches from `origin` along `dir` and darkens by the
closest approach scaled by k (larger k = harder shadow). Returns a
factor in [0, 1].

Rust: `spatial::sdf::sdf_soft_shadow`
    """
    ...
