"""
Bounding volume hierarchy over axis-aligned boxes.  Built top-down with binned surface-area-heuristic splits (12 bins; Wald 2007), falling back to a median split when SAH finds no gain. Leaves store index ranges into a permutation of the input.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.spatial.primitives import Aabb
from numeria.spatial.primitives import Ray
from numeria.spatial.primitives import Sphere
from numeria.spatial.primitives import Triangle
from numeria.math import Vec3

class Bvh:
    """
Binary BVH; query methods return primitive indices into the
original input slice.

Rust: `spatial::bvh::Bvh`
    """
    @staticmethod
    def build(bounds: list[Aabb]) -> Bvh: ...
    @staticmethod
    def build_triangles(tris: list[Triangle]) -> Bvh: ...
    def query_ray(self, r: Ray, max_t: float) -> list[int]: ...
    def query_aabb(self, b: Aabb) -> list[int]: ...
    def query_sphere(self, s: Sphere) -> list[int]: ...
    def closest_hit(self, r: Ray, tris: list[Triangle]) -> Optional[tuple[int, RayHit]]: ...
    def closest_point(self, p: Vec3 | Sequence[float], tris: list[Triangle]) -> tuple[int, Vec3, float]: ...
    def self_overlaps(self) -> list[tuple[int, int]]: ...
    def refit(self, bounds: list[Aabb]) -> None: ...
    def depth(self) -> int: ...
