"""
k-d trees (3-D and 2-D) with median splits, plus a uniform spatial hash for broadphase neighbor queries.  Reference: Bentley 1975; Friedman, Bentley & Finkel 1977 (nearest neighbor search with bounds pruning).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec2
from numeria.math import Vec3

class KdTree:
    """
Median-split k-d tree over `Vec3` points.

Rust: `spatial::kdtree::KdTree`
    """
    @staticmethod
    def build(points: list[Vec3 | Sequence[float]]) -> KdTree: ...
    def nearest(self, p: Vec3 | Sequence[float]) -> Optional[tuple[int, float]]: ...
    def k_nearest(self, p: Vec3 | Sequence[float], k: int) -> list[tuple[int, float]]: ...
    def within_radius(self, p: Vec3 | Sequence[float], r: float) -> list[tuple[int, float]]: ...
    def all_pairs_within(self, r: float) -> list[tuple[int, int]]: ...

class KdTree2:
    """
Median-split k-d tree over `Vec2` points.

Rust: `spatial::kdtree::KdTree2`
    """
    @staticmethod
    def build(points: list[Vec2 | Sequence[float]]) -> KdTree2: ...
    def nearest(self, p: Vec2 | Sequence[float]) -> Optional[tuple[int, float]]: ...
    def k_nearest(self, p: Vec2 | Sequence[float], k: int) -> list[tuple[int, float]]: ...
    def within_radius(self, p: Vec2 | Sequence[float], r: float) -> list[tuple[int, float]]: ...
    def all_pairs_within(self, r: float) -> list[tuple[int, int]]: ...

class SpatialHash:
    """
Uniform-grid spatial hash for 3-D points; O(1) insert, sphere
queries visit only overlapping cells.

Rust: `spatial::kdtree::SpatialHash`
    """
    def __init__(self, cell: float) -> None: ...
    def insert(self, i: int, p: Vec3 | Sequence[float]) -> None: ...
    def query_sphere(self, p: Vec3 | Sequence[float], r: float) -> list[int]: ...
    def clear(self) -> None: ...
    @property
    def cell(self) -> float: ...
