"""
Homogeneous 2-D projective geometry: points, lines, cross ratios, and plane homographies (Hartley & Zisserman, *Multiple View Geometry*, ch. 2 and 4).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.math import Vec2

class Homography:
    """
Plane projective transform p' ~ H·p.

Rust: `spatial::projective::Homography`
    """
    @staticmethod
    def from_four_points(src: list[Vec2 | Sequence[float]], dst: list[Vec2 | Sequence[float]]) -> Optional[Homography]: ...
    def apply(self, p: Vec2 | Sequence[float]) -> Optional[Vec2]: ...
    def inverse(self) -> Optional[Homography]: ...
    def compose(self, other: Homography) -> Homography: ...
    def vanishing_point(self, direction: Vec2 | Sequence[float]) -> Optional[Vec2]: ...
    @property
    def h(self) -> list[list[float]]: ...

def point_h(p: Vec2 | Sequence[float]) -> list[float]:
    """
Lifts a Euclidean point to homogeneous coordinates (w = 1).

Rust: `spatial::projective::point_h`
    """
    ...

def dehomogenize(h: list[float]) -> Optional[Vec2]:
    """
Projects back to Euclidean coordinates; `None` for points at
infinity (w ≈ 0).

Rust: `spatial::projective::dehomogenize`
    """
    ...

def line_through(a: Vec2 | Sequence[float], b: Vec2 | Sequence[float]) -> list[float]:
    """
The line through two points: l = a × b.

Rust: `spatial::projective::line_through`
    """
    ...

def lines_intersect(l1: list[float], l2: list[float]) -> list[float]:
    """
Intersection of two lines: p = l₁ × l₂ (possibly at infinity for
parallel lines).

Rust: `spatial::projective::lines_intersect`
    """
    ...

def point_on_line(p: list[float], l: list[float], tol: float) -> bool:
    """
Incidence test: p·l = 0 within tol (both scale-normalized).

Rust: `spatial::projective::point_on_line`
    """
    ...

def are_collinear(a: Vec2 | Sequence[float], b: Vec2 | Sequence[float], c: Vec2 | Sequence[float], tol: float) -> bool:
    """
Collinearity of three Euclidean points (twice the triangle area
below tol, scale-normalized).

Rust: `spatial::projective::are_collinear`
    """
    ...

def cross_ratio(a: float, b: float, c: float, d: float) -> float:
    """
Cross ratio of four collinear parameters:
(a, b; c, d) = ((a−c)(b−d)) / ((a−d)(b−c)).

Panics:
Panics when the denominator vanishes (repeated points).

Rust: `spatial::projective::cross_ratio`
    """
    ...

def rectify_quad_to_rect(quad: list[Vec2 | Sequence[float]], width: float, height: float) -> Optional[Homography]:
    """
Homography mapping an arbitrary quad (CCW or CW consistent order)
onto the axis-aligned rectangle [0, w]×[0, h] with corner order
(0,0), (w,0), (w,h), (0,h).

Rust: `spatial::projective::rectify_quad_to_rect`
    """
    ...
