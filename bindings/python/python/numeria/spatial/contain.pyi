"""
Orientation predicates and containment tests.  `orient2d_exact` follows Shewchuk's approach: a floating-point filter with a proven error bound, falling back to exact expansion arithmetic (error-free two_sum / two_product transforms) when the filter cannot decide (Shewchuk, "Adaptive precision floating-point arithmetic and fast robust geometric predicates", 1997).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.spatial.primitives import Aabb
from numeria.spatial.primitives import Capsule
from numeria.spatial.primitives import Cylinder
from numeria.spatial.primitives import Obb
from numeria.spatial.primitives import Polygon2
from numeria.spatial.primitives import Sphere
from numeria.spatial.primitives import Triangle
from numeria.spatial.primitives import Triangle2
from numeria.math import Vec2
from numeria.math import Vec3

def orient2d(a: Vec2 | Sequence[float], b: Vec2 | Sequence[float], c: Vec2 | Sequence[float]) -> float:
    """
Twice the signed area of (a, b, c): positive for a CCW turn.

Rust: `spatial::contain::orient2d`
    """
    ...

def orient3d(a: Vec3 | Sequence[float], b: Vec3 | Sequence[float], c: Vec3 | Sequence[float], d: Vec3 | Sequence[float]) -> float:
    """
Six times the signed volume of tetrahedron (a, b, c, d): positive
when d lies on the positive side of the CCW plane (a, b, c).

Rust: `spatial::contain::orient3d`
    """
    ...

def in_circle(a: Vec2 | Sequence[float], b: Vec2 | Sequence[float], c: Vec2 | Sequence[float], d: Vec2 | Sequence[float]) -> float:
    """
In-circle predicate: > 0 iff d lies inside the circumcircle of the
CCW triangle (a, b, c) (4×4 determinant form).

Rust: `spatial::contain::in_circle`
    """
    ...

def in_sphere(a: Vec3 | Sequence[float], b: Vec3 | Sequence[float], c: Vec3 | Sequence[float], d: Vec3 | Sequence[float], e: Vec3 | Sequence[float]) -> float:
    """
In-sphere predicate: > 0 iff e lies inside the circumsphere of the
positively oriented tetrahedron (a, b, c, d).

Rust: `spatial::contain::in_sphere`
    """
    ...

def orient2d_exact(a: Vec2 | Sequence[float], b: Vec2 | Sequence[float], c: Vec2 | Sequence[float]) -> int:
    """
Exact sign of orient2d: −1, 0, or 1, never wrong.

A Shewchuk-style floating-point filter answers the easy cases; the
hard ones are decided by exact expansion evaluation of the 6-term
determinant ax·by − ax·cy + bx·cy − bx·ay + cx·ay − cx·by.

Rust: `spatial::contain::orient2d_exact`
    """
    ...

def point_in_triangle_2d(p: Vec2 | Sequence[float], t: Triangle2) -> bool:
    """
Point in 2-D triangle (boundary counts as inside), robust to either
winding.

Rust: `spatial::contain::point_in_triangle_2d`
    """
    ...

def point_in_triangle(p: Vec3 | Sequence[float], t: Triangle, tol: float) -> bool:
    """
Point in 3-D triangle: within `tol` of the plane and barycentrics
in [0, 1].

Rust: `spatial::contain::point_in_triangle`
    """
    ...

def point_in_polygon_2d(p: Vec2 | Sequence[float], poly: Polygon2) -> bool:
    """
Even-odd (crossing number) point-in-polygon test.

Rust: `spatial::contain::point_in_polygon_2d`
    """
    ...

def winding_number_2d(p: Vec2 | Sequence[float], poly: Polygon2) -> int:
    """
Winding number of a polygon about p (0 for outside points of simple
polygons; ±1 inside depending on orientation).

Rust: `spatial::contain::winding_number_2d`
    """
    ...

def point_in_convex_polygon_2d(p: Vec2 | Sequence[float], poly: Polygon2) -> bool:
    """
O(log n) point-in-convex-polygon by binary search on the fan from
vertex 0 (polygon must be convex and CCW).

Rust: `spatial::contain::point_in_convex_polygon_2d`
    """
    ...

def point_in_convex_hull_3d(p: Vec3 | Sequence[float], hull_tris: list[Triangle]) -> bool:
    """
Point inside a convex hull given as outward-oriented triangles: on
or behind every face plane.

Rust: `spatial::contain::point_in_convex_hull_3d`
    """
    ...

def point_in_mesh(p: Vec3 | Sequence[float], tris: list[Triangle]) -> bool:
    """
Generalized winding number test for closed (possibly non-convex)
triangle meshes: the summed signed solid angle is ±4π inside and ~0
outside (van Oosterom & Strackee 1983 per-triangle solid angle).

Rust: `spatial::contain::point_in_mesh`
    """
    ...

def point_in_aabb(p: Vec3 | Sequence[float], b: Aabb) -> bool:
    """
Point in AABB (closed).

Rust: `spatial::contain::point_in_aabb`
    """
    ...

def point_in_obb(p: Vec3 | Sequence[float], b: Obb) -> bool:
    """
Point in OBB: local coordinates within the half extents.

Rust: `spatial::contain::point_in_obb`
    """
    ...

def point_in_sphere(p: Vec3 | Sequence[float], s: Sphere) -> bool:
    """
Point in sphere (closed).

Rust: `spatial::contain::point_in_sphere`
    """
    ...

def point_in_capsule(p: Vec3 | Sequence[float], c: Capsule) -> bool:
    """
Point in capsule: within radius of the core segment.

Rust: `spatial::contain::point_in_capsule`
    """
    ...

def point_in_cylinder(p: Vec3 | Sequence[float], c: Cylinder) -> bool:
    """
Point in finite cylinder: axial span and radial distance.

Rust: `spatial::contain::point_in_cylinder`
    """
    ...

def point_in_tetrahedron(p: Vec3 | Sequence[float], a: Vec3 | Sequence[float], b: Vec3 | Sequence[float], c: Vec3 | Sequence[float], d: Vec3 | Sequence[float]) -> bool:
    """
Point in tetrahedron: consistent orientation with respect to all
four faces.

Rust: `spatial::contain::point_in_tetrahedron`
    """
    ...

def aabb_contains_aabb(outer: Aabb, inner: Aabb) -> bool:
    """
Full containment of one AABB in another (closed).

Rust: `spatial::contain::aabb_contains_aabb`
    """
    ...

def sphere_contains_aabb(s: Sphere, b: Aabb) -> bool:
    """
Sphere containing an entire AABB (all corners inside).

Rust: `spatial::contain::sphere_contains_aabb`
    """
    ...
