"""
Polyhedra: Platonic/Archimedean/Catalan/Johnson solids, Goldberg and geodesic polyhedra, and Conway polyhedron operators.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.patterns.tilings import Archimedean
from numeria.patterns.symmetry import PointGroup3
from numeria.math import Vec3

class ArchimedeanSolid:
    """
The 13 Archimedean solids.

Rust: `patterns::polyhedra::ArchimedeanSolid`
    """
    ...

class Polyhedron:
    """
A polyhedron with polygonal faces (counterclockwise seen from
outside).

Rust: `patterns::polyhedra::Polyhedron`
    """
    def __init__(self, vertices: list[Vec3 | Sequence[float]], faces: list[list[int]]) -> None: ...
    def to_mesh(self) -> Mesh: ...
    def edges(self) -> list[tuple[int, int]]: ...
    def euler(self) -> int: ...
    def face_centroids(self) -> list[Vec3]: ...
    def face_normals(self) -> list[Vec3]: ...
    def volume(self) -> float: ...
    def surface_area(self) -> float: ...
    def is_convex(self) -> bool: ...
    def faces_around_vertex(self, v: int) -> list[int]: ...
    def vertex_figure(self, v: int) -> list[int]: ...
    def normalize(self, radius: float) -> Polyhedron: ...
    def canonicalize(self, iterations: int) -> Polyhedron: ...
    def dual(self) -> Polyhedron: ...
    def symmetry_group(self) -> Optional[PointGroup3]: ...
    @property
    def vertices(self) -> list[Vec3]: ...
    @property
    def faces(self) -> list[list[int]]: ...

def conway_dual(p: Polyhedron) -> Polyhedron:
    """
Conway dual (alias of `Polyhedron::dual`).

Rust: `patterns::polyhedra::conway_dual`
    """
    ...

def kis(p: Polyhedron, apex_height: float) -> Polyhedron:
    """
Conway kis: a pyramid of the given apex height over every face.

Rust: `patterns::polyhedra::kis`
    """
    ...

def ambo(p: Polyhedron) -> Polyhedron:
    """
Conway ambo (rectification): vertices at edge midpoints.

Rust: `patterns::polyhedra::ambo`
    """
    ...

def truncate(p: Polyhedron, ratio: float) -> Polyhedron:
    """
Conway truncate: cuts each corner, moving `ratio` along every
edge (1/3 turns regular triangles into regular hexagons).

Panics:
Panics unless `0 < ratio < 1/2`.

Rust: `patterns::polyhedra::truncate`
    """
    ...

def chamfer(p: Polyhedron, ratio: float) -> Polyhedron:
    """
Conway chamfer: shrinks faces in-plane by `ratio` and replaces
each edge with a hexagon (original vertices kept).

Panics:
Panics unless `0 < ratio < 1`.

Rust: `patterns::polyhedra::chamfer`
    """
    ...

def gyro(p: Polyhedron) -> Polyhedron:
    """
Conway gyro: pentagonal faces, one per (face, edge) incidence.

Rust: `patterns::polyhedra::gyro`
    """
    ...

def propellor(p: Polyhedron) -> Polyhedron:
    """
Conway propellor: each face spins off a smaller rotated copy
surrounded by quads.

Rust: `patterns::polyhedra::propellor`
    """
    ...

def whirl(p: Polyhedron) -> Polyhedron:
    """
Conway whirl: hexagons spiral around shrunken rotated faces.

Rust: `patterns::polyhedra::whirl`
    """
    ...

def join(p: Polyhedron) -> Polyhedron:
    """
Conway join = dual(ambo): rhombic faces over each original edge.

Rust: `patterns::polyhedra::join`
    """
    ...

def needle(p: Polyhedron) -> Polyhedron:
    """
Conway needle = kis(dual).

Rust: `patterns::polyhedra::needle`
    """
    ...

def zip(p: Polyhedron) -> Polyhedron:
    """
Conway zip = dual(kis).

Rust: `patterns::polyhedra::zip`
    """
    ...

def ortho(p: Polyhedron) -> Polyhedron:
    """
Conway ortho = join(join).

Rust: `patterns::polyhedra::ortho`
    """
    ...

def expand(p: Polyhedron) -> Polyhedron:
    """
Conway expand = ambo(ambo).

Rust: `patterns::polyhedra::expand`
    """
    ...

def bevel(p: Polyhedron) -> Polyhedron:
    """
Conway bevel = truncate(ambo).

Rust: `patterns::polyhedra::bevel`
    """
    ...

def meta(p: Polyhedron) -> Polyhedron:
    """
Conway meta = kis(join).

Rust: `patterns::polyhedra::meta`
    """
    ...

def snub(p: Polyhedron) -> Polyhedron:
    """
Conway snub = dual(gyro).

Rust: `patterns::polyhedra::snub`
    """
    ...

def conway_apply(p: Polyhedron, notation: str) -> Polyhedron:
    """
Applies a Conway notation string, e.g. `"tkT"` or `"dsI"`: the
rightmost character may be a seed (T, C, O, D, I); otherwise the
operators apply to `p`. Operators: d a k t j n z o e b m s g p c w.

Errors:
Returns `GeomError::InvalidArgument` for an unknown character.

Rust: `patterns::polyhedra::conway_apply`
    """
    ...

def tetrahedron() -> Polyhedron:
    """
Regular tetrahedron (edge 2√2).

Rust: `patterns::polyhedra::tetrahedron`
    """
    ...

def cube() -> Polyhedron:
    """
Cube (edge 2).

Rust: `patterns::polyhedra::cube`
    """
    ...

def octahedron() -> Polyhedron:
    """
Regular octahedron (edge √2).

Rust: `patterns::polyhedra::octahedron`
    """
    ...

def icosahedron() -> Polyhedron:
    """
Regular icosahedron.

Rust: `patterns::polyhedra::icosahedron`
    """
    ...

def dodecahedron() -> Polyhedron:
    """
Regular dodecahedron (dual of the icosahedron).

Rust: `patterns::polyhedra::dodecahedron`
    """
    ...

def prism(n: int, h: float) -> Polyhedron:
    """
Right prism over a regular n-gon (unit edge circumcircle scaled so
the polygon edge is 1), height `h`.

Panics:
Panics unless `n >= 3` and `h > 0`.

Rust: `patterns::polyhedra::prism`
    """
    ...

def antiprism(n: int, h: float) -> Polyhedron:
    """
Antiprism over a regular n-gon (unit polygon edge), height `h`.

Panics:
Panics unless `n >= 3` and `h > 0`.

Rust: `patterns::polyhedra::antiprism`
    """
    ...

def pyramid(n: int, h: float) -> Polyhedron:
    """
Pyramid over a regular n-gon (unit edge base), apex height `h`.

Panics:
Panics unless `n >= 3` and `h > 0`.

Rust: `patterns::polyhedra::pyramid`
    """
    ...

def bipyramid(n: int, h: float) -> Polyhedron:
    """
Bipyramid over a regular n-gon (unit edge equator), apexes at ±h.

Panics:
Panics unless `n >= 3` and `h > 0`.

Rust: `patterns::polyhedra::bipyramid`
    """
    ...

def from_convex_points(points: list[Vec3 | Sequence[float]]) -> Polyhedron:
    """
Builds a polyhedron as the convex hull of a point set, merging
coplanar triangles into polygon faces.

Panics:
Panics with fewer than 4 points or degenerate input.

Rust: `patterns::polyhedra::from_convex_points`
    """
    ...

def archimedean(kind: ArchimedeanSolid) -> Polyhedron:
    """
Constructs an Archimedean solid: exact coordinates or exact Conway
constructions everywhere except the snub dodecahedron, which is
built combinatorially by the snub operator and canonicalized (its
coordinates are then approximate).

Rust: `patterns::polyhedra::archimedean`
    """
    ...

def catalan(kind: ArchimedeanSolid) -> Polyhedron:
    """
Catalan solid: the dual of the corresponding Archimedean solid
(canonicalized so faces are planar and congruent).

Rust: `patterns::polyhedra::catalan`
    """
    ...

def johnson(n: int) -> Optional[Polyhedron]:
    """
The first 20 Johnson solids J1..J20 with unit edges; `None` for
n = 0 or n > 20.

Rust: `patterns::polyhedra::johnson`
    """
    ...

def geodesic_sphere(frequency: int) -> Polyhedron:
    """
Class I geodesic sphere: each icosahedron face subdivided into
`frequency`² triangles, projected to the unit sphere.

Panics:
Panics unless `frequency >= 1`.

Rust: `patterns::polyhedra::geodesic_sphere`
    """
    ...

def goldberg(m: int, n: int) -> Polyhedron:
    """
Goldberg polyhedron GP(m, n): hexagons plus 12 pentagons. Class I
(n = 0) and class II (m = n) are supported (class II via a √3
refinement of the class I triangulation); general class III is
not.

Panics:
Panics unless `m >= 1` and (`n == 0` or `n == m`).

Rust: `patterns::polyhedra::goldberg`
    """
    ...
