"""
Vector calculus operators and field theory for physics grids.  Provides discrete differential operators (gradient, laplacian, divergence, curl) on 2D and 3D uniform grids, point-wise numerical differentiation via function pointers, line/surface integrals, and a Jacobi Poisson solver.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def gradient_3d(field: list[float], nx: int, ny: int, nz: int, dx: float, dy: float, dz: float) -> list[tuple[float, float, float]]:
    """
Compute the gradient of a scalar field on a uniform 3-D grid.

`field` is row-major with index mapping `i*ny*nz + j*nz + k`.
Returns a `Vec` of `(∂f/∂x, ∂f/∂y, ∂f/∂z)` tuples, one per grid point.

Rust: `vector_calculus::gradient_3d`
    """
    ...

def laplacian_3d(field: list[float], nx: int, ny: int, nz: int, dx: float, dy: float, dz: float) -> list[float]:
    """
Compute the Laplacian (∇²f) of a scalar field on a uniform 3-D grid.

Rust: `vector_calculus::laplacian_3d`
    """
    ...

def divergence_3d(fx: list[float], fy: list[float], fz: list[float], nx: int, ny: int, nz: int, dx: float, dy: float, dz: float) -> list[float]:
    """
Divergence of a vector field on a uniform 3-D grid.

Each component (`fx`, `fy`, `fz`) is a flat array with the same index mapping
as scalar fields.

Rust: `vector_calculus::divergence_3d`
    """
    ...

def curl_3d(fx: list[float], fy: list[float], fz: list[float], nx: int, ny: int, nz: int, dx: float, dy: float, dz: float) -> tuple[list[float], list[float], list[float]]:
    """
Curl of a vector field on a uniform 3-D grid.

Returns `(curl_x, curl_y, curl_z)` as separate flat arrays.

Rust: `vector_calculus::curl_3d`
    """
    ...

def gradient_2d(field: list[float], nx: int, ny: int, dx: float, dy: float) -> list[tuple[float, float]]:
    """
Gradient of a scalar field on a uniform 2-D grid.

Index mapping: `i*ny + j`. Returns `(∂f/∂x, ∂f/∂y)` per grid point.

Rust: `vector_calculus::gradient_2d`
    """
    ...

def laplacian_2d(field: list[float], nx: int, ny: int, dx: float, dy: float) -> list[float]:
    """
Laplacian of a scalar field on a uniform 2-D grid.

Rust: `vector_calculus::laplacian_2d`
    """
    ...

def divergence_2d(fx: list[float], fy: list[float], nx: int, ny: int, dx: float, dy: float) -> list[float]:
    """
Divergence of a 2-D vector field.

Rust: `vector_calculus::divergence_2d`
    """
    ...

def curl_2d(fx: list[float], fy: list[float], nx: int, ny: int, dx: float, dy: float) -> list[float]:
    """
Scalar curl of a 2-D vector field: ∂Fy/∂x - ∂Fx/∂y.

Rust: `vector_calculus::curl_2d`
    """
    ...

def poisson_jacobi_2d(rhs: list[float], nx: int, ny: int, dx: float, dy: float, max_iter: int, tol: float) -> list[float]:
    """
Solve ∇²φ = ρ on a 2-D grid with zero (Dirichlet) boundary
conditions.

The interior unknowns are assembled into a sparse SPD system
(−∇²φ = −ρ, 5-point stencil) and solved with Jacobi-preconditioned
conjugate gradient (`linalg::sparse::pcg_jacobi`); if CG does not
reach `tol` within `max_iter` iterations, the classic point-Jacobi
sweep is used as a fallback so the historical behavior (best-effort
answer, never an error) is preserved.

Rust: `vector_calculus::poisson_jacobi_2d`
    """
    ...

def line_integral(f: Callable[[float, float, float], tuple[float, float, float]], path: list[tuple[float, float, float]]) -> float:
    """
Numerical line integral ∫F·dr along a piecewise-linear path in 3-D.

`f` returns the vector field value (Fx, Fy, Fz) at a given point.
`path` is an ordered list of waypoints. The integral is evaluated at the
midpoint of each segment.

Rust: `vector_calculus::line_integral`
    """
    ...

def flux_integral_2d(fn_field: Callable[[float, float], tuple[float, float]], path: list[tuple[float, float]]) -> float:
    """
Flux integral ∫F·n̂ ds along a 2-D curve.

The outward normal is computed by rotating each segment's tangent 90 degrees
clockwise: tangent (tx, ty) -> normal (ty, -tx). The integral is evaluated
at the midpoint of each segment.

Rust: `vector_calculus::flux_integral_2d`
    """
    ...

def numerical_gradient(f: Callable[[float, float, float], float], x: float, y: float, z: float, h: float) -> tuple[float, float, float]:
    """
Finite-difference gradient of a scalar function at a point.

Rust: `vector_calculus::numerical_gradient`
    """
    ...

def numerical_laplacian(f: Callable[[float, float, float], float], x: float, y: float, z: float, h: float) -> float:
    """
Finite-difference Laplacian of a scalar function at a point.

Rust: `vector_calculus::numerical_laplacian`
    """
    ...

def numerical_divergence(fx: Callable[[float, float, float], float], fy: Callable[[float, float, float], float], fz: Callable[[float, float, float], float], x: float, y: float, z: float, h: float) -> float:
    """
Finite-difference divergence of a vector field at a point.

Each component of the field is given as a separate function.

Rust: `vector_calculus::numerical_divergence`
    """
    ...
