"""
Time-domain signal operations and test waveforms.  Convolution, cross- and autocorrelation, normalization, windowing, and the simple smoothers -- moving average, exponential moving average, and the median filter, which unlike the other two removes impulsive noise without smearing an edge.  Waveform generators (sine, square, sawtooth, triangle, noise, chirp) provide test signals.  This module is the elementary layer and re-exports the pieces of `transforms` and `dsp` most often wanted alongside it. For FFTs of any length go to `transforms::fft`; for filter *design* go to `dsp`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.statistics.distributions import Exponential
from numeria.dsp.windows import blackman_window as blackman_window
from numeria.transforms.fft import fft as fft
from numeria.transforms.fft import fft_convolve as fft_convolve
from numeria.dsp.iir import first_order_highpass as first_order_highpass
from numeria.dsp.iir import first_order_lowpass as first_order_lowpass
from numeria.dsp.windows import hamming_window as hamming_window
from numeria.dsp.windows import hann_window as hann_window
from numeria.transforms.fft import ifft as ifft
from numeria.transforms.fft import next_power_of_two as next_power_of_two
from numeria.dsp.windows import rectangular_window as rectangular_window
from numeria.transforms.fft import rfft as rfft

def convolve(signal: list[float], kernel: list[float]) -> list[float]:
    """
Linear convolution of signal with kernel: `y[n] = Σ s[i]·k[n-i]`

Rust: `signal_processing::convolve`
    """
    ...

def cross_correlate(x: list[float], y: list[float]) -> list[float]:
    """
Cross-correlation of x and y via convolution with time-reversed y

Rust: `signal_processing::cross_correlate`
    """
    ...

def auto_correlate(signal: list[float]) -> list[float]:
    """
Auto-correlation of a signal: cross-correlation of the signal with itself

Rust: `signal_processing::auto_correlate`
    """
    ...

def normalize_signal(signal: MutableSequence[float]) -> None:
    """
Normalize signal amplitude to [-1, 1] by dividing by peak absolute value

Rust: `signal_processing::normalize_signal`
    """
    ...

def apply_window(signal: list[float], window: list[float]) -> list[float]:
    """
Element-wise multiplication of signal by window coefficients

Rust: `signal_processing::apply_window`
    """
    ...

def moving_average(signal: list[float], window_size: int) -> list[float]:
    """
Simple moving average filter with specified window size

Rust: `signal_processing::moving_average`
    """
    ...

def exponential_moving_average(signal: list[float], alpha: float) -> list[float]:
    """
Exponential moving average filter: `y[n] = α·x[n] + (1-α)·y[n-1]`

Rust: `signal_processing::exponential_moving_average`
    """
    ...

def median_filter(signal: list[float], window_size: int) -> list[float]:
    """
Median filter for impulse noise removal with specified window size

Rust: `signal_processing::median_filter`
    """
    ...

def sine_wave(frequency: float, sample_rate: float, duration: float, amplitude: float) -> list[float]:
    """
Generate a sine wave: `x[n] = A·sin(2πf·n/fs)` for n samples over given duration

Rust: `signal_processing::sine_wave`
    """
    ...

def square_wave(frequency: float, sample_rate: float, duration: float, amplitude: float) -> list[float]:
    """
Generate a square wave: +A for first half-period, -A for second half

Rust: `signal_processing::square_wave`
    """
    ...

def sawtooth_wave(frequency: float, sample_rate: float, duration: float, amplitude: float) -> list[float]:
    """
Generate a sawtooth wave: linearly ramps from -A to +A each period

Rust: `signal_processing::sawtooth_wave`
    """
    ...

def white_noise(n: int, amplitude: float, seed: int) -> list[float]:
    """
Generate deterministic pseudo-random white noise using a linear congruential generator

Rust: `signal_processing::white_noise`
    """
    ...

def zero_crossings(signal: list[float]) -> int:
    """
Count the number of zero crossings (sign changes) in the signal

Rust: `signal_processing::zero_crossings`
    """
    ...

def rms_level(signal: list[float]) -> float:
    """
Root mean square level: RMS = sqrt(Σx²/n)

Rust: `signal_processing::rms_level`
    """
    ...

def peak_to_peak(signal: list[float]) -> float:
    """
Peak-to-peak amplitude: max(x) - min(x)

Rust: `signal_processing::peak_to_peak`
    """
    ...

def crest_factor(signal: list[float]) -> float:
    """
Crest factor: ratio of peak absolute value to RMS level

Rust: `signal_processing::crest_factor`
    """
    ...
