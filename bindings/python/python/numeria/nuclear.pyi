"""
Radioactive decay, nuclear binding, and dosimetry.  Exponential decay in its several parameterisations -- decay constant, half-life, mean lifetime -- and activity. Binding energy from the mass defect, binding energy per nucleon (the curve whose peak at iron-56 is why both fission and fusion release energy), and reaction Q-values.  Nuclear size follows `R = R₀A^(1/3)`, giving a roughly constant nuclear density. Dosimetry covers absorbed and equivalent dose and the inverse-square falloff of intensity with distance.  For reactor-scale neutron transport see `neutronics`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from numeria.statistical_mechanics.kinetics import Reaction

def remaining_nuclei(initial_count: float, decay_constant: float, time: float) -> float:
    """
Number of remaining nuclei: N(t) = N_0 * e^(-λt)

Rust: `nuclear::remaining_nuclei`
    """
    ...

def activity(initial_count: float, decay_constant: float, time: float) -> float:
    """
Activity (decay rate): A = λ * N = λ * N_0 * e^(-λt)

Rust: `nuclear::activity`
    """
    ...

def half_life(decay_constant: float) -> float:
    """
Half-life from decay constant: t_1/2 = ln(2) / λ

Rust: `nuclear::half_life`
    """
    ...

def decay_constant(half_life: float) -> float:
    """
Decay constant from half-life: λ = ln(2) / t_1/2

Rust: `nuclear::decay_constant`
    """
    ...

def mean_lifetime(decay_constant: float) -> float:
    """
Mean lifetime: τ = 1 / λ

Rust: `nuclear::mean_lifetime`
    """
    ...

def remaining_after_half_lives(initial_count: float, num_half_lives: float) -> float:
    """
Number of nuclei remaining after n half-lives: N = N_0 / 2^n

Rust: `nuclear::remaining_after_half_lives`
    """
    ...

def num_half_lives(time: float, half_life: float) -> float:
    """
Number of half-lives elapsed: n = t / t_1/2

Rust: `nuclear::num_half_lives`
    """
    ...

def mass_defect(num_protons: int, num_neutrons: int, nucleus_mass: float) -> float:
    """
Mass defect: Δm = Z*m_p + N*m_n - M_nucleus

Rust: `nuclear::mass_defect`
    """
    ...

def binding_energy(mass_defect: float) -> float:
    """
Binding energy from mass defect: E_b = Δm * c^2

Rust: `nuclear::binding_energy`
    """
    ...

def binding_energy_per_nucleon(total_binding_energy: float, mass_number: int) -> float:
    """
Binding energy per nucleon: E_b / A

Rust: `nuclear::binding_energy_per_nucleon`
    """
    ...

def q_value(reactant_mass: float, product_mass: float) -> float:
    """
Q-value of a nuclear reaction: Q = (m_reactants - m_products) * c^2

Rust: `nuclear::q_value`
    """
    ...

def mass_energy(mass: float) -> float:
    """
Energy released from mass conversion: E = Δm * c^2

Rust: `nuclear::mass_energy`
    """
    ...

def energy_from_amu(mass_difference_amu: float) -> float:
    """
Energy from fission/fusion (given mass difference in amu):
E = Δm(amu) * 931.5 MeV

Rust: `nuclear::energy_from_amu`
    """
    ...

def reaction_rate(number_density: float, cross_section: float, flux: float) -> float:
    """
Reaction rate: R = n * σ * Φ
n = target number density, σ = cross section, Φ = flux

Rust: `nuclear::reaction_rate`
    """
    ...

def nuclear_mean_free_path(number_density: float, cross_section: float) -> float:
    """
Mean free path in nuclear context: λ = 1 / (n * σ)

Rust: `nuclear::nuclear_mean_free_path`
    """
    ...

def nuclear_radius(mass_number: int) -> float:
    """
Nuclear radius (empirical): R = R_0 * A^(1/3) where R_0 ≈ 1.2 fm

Rust: `nuclear::nuclear_radius`
    """
    ...

def nuclear_density() -> float:
    """
Nuclear density (approximately constant): ρ ≈ 3m_p / (4π * R_0^3)

Rust: `nuclear::nuclear_density`
    """
    ...

def absorbed_dose(energy: float, mass: float) -> float:
    """
Absorbed dose: D = E / m (Gray, Gy = J/kg)

Rust: `nuclear::absorbed_dose`
    """
    ...

def equivalent_dose(absorbed_dose: float, weighting_factor: float) -> float:
    """
Equivalent dose: H = D * w_R (Sievert, Sv)
w_R is the radiation weighting factor

Rust: `nuclear::equivalent_dose`
    """
    ...

def radiation_intensity_distance(initial_intensity: float, initial_distance: float, new_distance: float) -> float:
    """
Inverse square law for radiation intensity: I = I_0 * (r_0 / r)^2

Rust: `nuclear::radiation_intensity_distance`
    """
    ...
