"""
Probability distributions: densities, mass functions, and CDFs.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class Beta:
    """
Beta distribution on [0, 1] with shape parameters (a, b).

Rust: `statistics::distributions::Beta`
    """
    def __init__(self, a: float, b: float) -> None: ...
    @property
    def a(self) -> float: ...
    @property
    def b(self) -> float: ...

class Binomial:
    """
Binomial distribution (discrete) with n trials and success
probability p; CDF via the regularized incomplete beta.

Rust: `statistics::distributions::Binomial`
    """
    def __init__(self, n: int, p: float) -> None: ...
    def pmf(self, k: int) -> float: ...
    @property
    def n(self) -> int: ...
    @property
    def p(self) -> float: ...

class ChiSquared:
    """
Chi-squared distribution with k degrees of freedom; CDF via P(k/2, x/2).

Rust: `statistics::distributions::ChiSquared`
    """
    def __init__(self, k: float) -> None: ...
    @property
    def k(self) -> float: ...

class Exponential:
    """
Exponential distribution with the given rate λ (mean 1/λ), wrapping
the module's free `exponential_pdf`/`exponential_cdf`.

Rust: `statistics::distributions::Exponential`
    """
    def __init__(self, rate: float) -> None: ...
    @property
    def rate(self) -> float: ...

class FDist:
    """
Fisher-Snedecor F distribution with (d1, d2) degrees of freedom.

Rust: `statistics::distributions::FDist`
    """
    def __init__(self, d1: float, d2: float) -> None: ...
    @property
    def d1(self) -> float: ...
    @property
    def d2(self) -> float: ...

class Gamma:
    """
Gamma distribution with shape α and rate β (mean α/β).

Rust: `statistics::distributions::Gamma`
    """
    def __init__(self, shape: float, rate: float) -> None: ...
    @property
    def shape(self) -> float: ...
    @property
    def rate(self) -> float: ...

class LogNormal:
    """
Log-normal distribution: ln X ~ N(μ, σ²).

Rust: `statistics::distributions::LogNormal`
    """
    def __init__(self, mu: float, sigma: float) -> None: ...
    @property
    def mu(self) -> float: ...
    @property
    def sigma(self) -> float: ...

class Normal:
    """
Normal distribution N(μ, σ²); quantile via `erfinv`.

Rust: `statistics::distributions::Normal`
    """
    def __init__(self, mu: float, sigma: float) -> None: ...
    @property
    def mu(self) -> float: ...
    @property
    def sigma(self) -> float: ...

class Poisson:
    """
Poisson distribution (discrete); `pdf` is the mass at round(x) and
`cdf` uses P(X ≤ k) = Q(k+1, λ).

Rust: `statistics::distributions::Poisson`
    """
    def __init__(self, lambda_: float) -> None: ...
    def pmf(self, k: int) -> float: ...
    @property
    def lambda_(self) -> float: ...

class StudentT:
    """
Student's t distribution with ν degrees of freedom; CDF via the
regularized incomplete beta function.

Rust: `statistics::distributions::StudentT`
    """
    def __init__(self, nu: float) -> None: ...
    @property
    def nu(self) -> float: ...

class Weibull:
    """
Weibull distribution with shape k and scale λ.

Rust: `statistics::distributions::Weibull`
    """
    def __init__(self, k: float, lambda_: float) -> None: ...
    @property
    def k(self) -> float: ...
    @property
    def lambda_(self) -> float: ...

def gaussian(x: float, mu: float, sigma: float) -> float:
    """
Gaussian probability density function: f(x) = (1/(σ√(2π))) · exp(-½((x-μ)/σ)²)

Rust: `statistics::distributions::gaussian`
    """
    ...

def gaussian_cdf(x: float, mu: float, sigma: float) -> float:
    """
Gaussian CDF: Φ(x) = ½·erfc(−(x−μ)/(σ√2)), full double precision.

Rust: `statistics::distributions::gaussian_cdf`
    """
    ...

def gaussian_cdf_approx(x: float, mu: float, sigma: float) -> float:
    """
Deprecated alias of `gaussian_cdf`; the historical Abramowitz &
Stegun approximation has been replaced by the exact erfc form.

Rust: `statistics::distributions::gaussian_cdf_approx`
    """
    ...

def poisson_pmf(k: int, lambda_: float) -> float:
    """
Poisson probability mass function: P(k;λ) = λᵏ · e⁻λ / k!

Rust: `statistics::distributions::poisson_pmf`
    """
    ...

def exponential_pdf(x: float, lambda_: float) -> float:
    """
Exponential probability density function: f(x;λ) = λ · e⁻ˡˣ for x ≥ 0

Rust: `statistics::distributions::exponential_pdf`
    """
    ...

def exponential_cdf(x: float, lambda_: float) -> float:
    """
Exponential cumulative distribution function: F(x;λ) = 1 - e⁻ˡˣ for x ≥ 0

Rust: `statistics::distributions::exponential_cdf`
    """
    ...

def chi_squared_pdf(x: float, k: int) -> float:
    """
Chi-squared PDF: f(x;k) = x^(k/2-1)·e^(-x/2) / (2^(k/2)·Γ(k/2))

Rust: `statistics::distributions::chi_squared_pdf`
    """
    ...
