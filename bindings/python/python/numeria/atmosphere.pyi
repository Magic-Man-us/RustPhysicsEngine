"""
The standard atmosphere, humidity, and near-surface wind.  The barometric formula and the ISA lapse-rate model give pressure, temperature and density against altitude, plus the pressure and density altitudes an aircraft altimeter reports. Humidity is covered by the Magnus formulation for dew point and relative humidity.  Wind includes the power-law shear profile, the wind power density that sets a turbine's available energy (`P/A = ½ρv³`, so a doubling of wind speed is eight times the power), the Beaufort scale, and the Coriolis parameter `f = 2Ω sin φ`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def wind_chill(temperature_c: float, wind_speed_kmh: float) -> float:
    """
Environment Canada / NWS wind chill formula.
Valid for temperature <= 10°C and wind speed >= 4.8 km/h.
Returns the wind chill temperature in °C.

Rust: `atmosphere::wind_chill`
    """
    ...

def beaufort_to_speed(beaufort: int) -> float:
    """
Convert Beaufort scale number to approximate wind speed in m/s.
Uses v = 0.836 × B^(3/2).

Rust: `atmosphere::beaufort_to_speed`
    """
    ...

def speed_to_beaufort(speed_ms: float) -> int:
    """
Convert wind speed in m/s to Beaufort scale number (clamped 0–12).
Inverse of `beaufort_to_speed`.

Rust: `atmosphere::speed_to_beaufort`
    """
    ...

def wind_shear(v_top: float, v_bottom: float, height_diff: float) -> float:
    """
Vertical wind shear: dv/dz = (v_top - v_bottom) / Δz.
Returns shear in s⁻¹.

Rust: `atmosphere::wind_shear`
    """
    ...

def wind_power_density(density: float, velocity: float) -> float:
    """
Wind power density: P/A = ½ρv³ (W/m²).

Rust: `atmosphere::wind_power_density`
    """
    ...

def coriolis_parameter(latitude_rad: float) -> float:
    """
Coriolis parameter: f = 2Ω sin(φ).

Rust: `atmosphere::coriolis_parameter`
    """
    ...

def coriolis_acceleration(velocity: float, latitude_rad: float) -> float:
    """
Coriolis acceleration: a = 2Ωv sin(φ).

Rust: `atmosphere::coriolis_acceleration`
    """
    ...

def barometric_pressure(p0: float, molar_mass: float, g: float, height: float, temperature: float) -> float:
    """
Barometric pressure at a given height using the hypsometric equation.
P = P₀ × exp(-Mgh / (RT))

Rust: `atmosphere::barometric_pressure`
    """
    ...

def dry_adiabatic_lapse_rate(g: float, cp: float) -> float:
    """
Dry adiabatic lapse rate: Γ = g / cp (K/m).

Rust: `atmosphere::dry_adiabatic_lapse_rate`
    """
    ...

def temperature_at_altitude(t0: float, lapse_rate: float, altitude: float) -> float:
    """
Temperature at altitude: T = T₀ - Γ × h.

Rust: `atmosphere::temperature_at_altitude`
    """
    ...

def pressure_altitude(p0: float, pressure: float, lapse_rate: float, t0: float) -> float:
    """
Pressure altitude using the standard atmosphere simplification:
h = (T₀ / Γ) × (1 - (P / P₀)^0.1903)

Rust: `atmosphere::pressure_altitude`
    """
    ...

def density_altitude(pressure_alt: float, temperature_c: float, standard_temp_c: float) -> float:
    """
Density altitude from pressure altitude and temperature deviation:
DA = PA + 36.576 × (T - T_std) meters.

Rust: `atmosphere::density_altitude`
    """
    ...

def scale_height(temperature: float, molar_mass: float, g: float) -> float:
    """
Scale height: H = RT / (Mg).

Rust: `atmosphere::scale_height`
    """
    ...

def dew_point(temperature_c: float, relative_humidity: float) -> float:
    """
Dew point via the Magnus formula.
α = (a×T)/(b+T) + ln(RH), then Td = (b×α)/(a-α).
`relative_humidity` is fractional (0.0–1.0).

Rust: `atmosphere::dew_point`
    """
    ...

def relative_humidity(temperature_c: float, dew_point_c: float) -> float:
    """
Relative humidity from temperature and dew point (returns fractional 0.0–1.0).
RH = exp((a×Td)/(b+Td) - (a×T)/(b+T))

Rust: `atmosphere::relative_humidity`
    """
    ...

def heat_index(temperature_c: float, relative_humidity: float) -> float:
    """
Heat index via the Rothfusz regression (simplified).
Takes temperature in °C and relative_humidity as percentage (0–100).

Rust: `atmosphere::heat_index`
    """
    ...

def absolute_humidity(relative_humidity: float, temperature_c: float) -> float:
    """
Absolute humidity in g/m³.
AH = (6.112 × e^(17.67T/(T+243.5)) × RH × 2.1674) / (273.15 + T)
`relative_humidity` is fractional (0.0–1.0).

Rust: `atmosphere::absolute_humidity`
    """
    ...

EARTH_ROTATION_RATE: float

STANDARD_PRESSURE: float

STANDARD_TEMPERATURE: float

DRY_AIR_MOLAR_MASS: float
