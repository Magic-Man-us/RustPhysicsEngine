"""
The solid Earth: gravity, seismology, and heat.  Gravity surveying -- the latitude formula, the free-air and Bouguer corrections, the resulting anomaly, and Airy isostatic compensation.  Seismology: P- and S-wave travel times, epicentral distance from the S−P lag, the Richter and moment magnitude scales, and seismic moment and energy. The moment magnitude is the one to use for large events, because Richter saturates.  Heat flow: pressure and temperature with depth, the geothermal gradient, and geothermal power. Plate tectonics closes the module with Euler-pole plate velocities and the square-root-of-age law for seafloor depth.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def gravity_at_latitude(latitude_rad: float) -> float:
    """
International gravity formula: g = 9.780327(1 + 0.0053024 sin²φ − 0.0000058 sin²2φ).
`latitude_rad` is the geodetic latitude in radians.

Rust: `geophysics::gravity_at_latitude`
    """
    ...

def free_air_correction(height: float) -> float:
    """
Free-air gravity correction: Δg = −0.3086 × h (mGal).
`height` is meters above the geoid.

Rust: `geophysics::free_air_correction`
    """
    ...

def bouguer_correction(height: float, density: float) -> float:
    """
Bouguer slab correction: Δg = 2πGρh.
Accounts for the gravitational attraction of material between the
station and the geoid. Returns the correction in m/s².

Rust: `geophysics::bouguer_correction`
    """
    ...

def bouguer_anomaly(observed_g: float, latitude_g: float, free_air: float, bouguer: float) -> float:
    """
Bouguer anomaly: Δg_B = g_obs − g_lat + free_air − bouguer.
All values in consistent units (typically mGal or m/s²).

Rust: `geophysics::bouguer_anomaly`
    """
    ...

def isostatic_compensation_depth(elevation: float, crust_density: float, mantle_density: float) -> float:
    """
Airy isostatic compensation depth: d = elevation × ρ_c / (ρ_m − ρ_c).
Returns the depth of the crustal root below normal Moho.

Rust: `geophysics::isostatic_compensation_depth`
    """
    ...

def p_wave_travel_time(distance: float, velocity: float) -> float:
    """
P-wave travel time: t = d / v.

Rust: `geophysics::p_wave_travel_time`
    """
    ...

def s_wave_travel_time(distance: float, velocity: float) -> float:
    """
S-wave travel time: t = d / v.

Rust: `geophysics::s_wave_travel_time`
    """
    ...

def epicentral_distance_from_lag(t_s_minus_p: float, vp: float, vs: float) -> float:
    """
Epicentral distance from S−P lag time: d = Δt × vp × vs / (vp − vs).

Rust: `geophysics::epicentral_distance_from_lag`
    """
    ...

def richter_magnitude(amplitude: float, distance_km: float) -> float:
    """
Simplified Richter local magnitude: M_L = log₁₀(A) + 2.76 log₁₀(Δ) − 2.48.
`amplitude` is the maximum trace amplitude in mm, `distance_km` in km.

Rust: `geophysics::richter_magnitude`
    """
    ...

def moment_magnitude(seismic_moment: float) -> float:
    """
Moment magnitude from seismic moment (N·m): M_w = (2/3) log₁₀(M₀) − 6.07.

Rust: `geophysics::moment_magnitude`
    """
    ...

def seismic_moment(magnitude: float) -> float:
    """
Seismic moment from moment magnitude (returns N·m): M₀ = 10^(1.5(M_w + 6.07)).

Rust: `geophysics::seismic_moment`
    """
    ...

def seismic_energy(magnitude: float) -> float:
    """
Seismic energy from magnitude (Gutenberg-Richter): E = 10^(1.5M + 4.8) joules.

Rust: `geophysics::seismic_energy`
    """
    ...

def pressure_at_depth(depth: float, surface_density: float, g: float) -> float:
    """
Lithostatic pressure at depth (simplified): P ≈ ρgd.
`depth` in meters, `surface_density` in kg/m³, `g` in m/s².

Rust: `geophysics::pressure_at_depth`
    """
    ...

def temperature_at_depth(surface_temp: float, geothermal_gradient: float, depth: float) -> float:
    """
Temperature at depth assuming a linear geothermal gradient:
T = T₀ + (dT/dz) × z.
`geothermal_gradient` is in K/m (typical ~0.025–0.030 K/m).

Rust: `geophysics::temperature_at_depth`
    """
    ...

def moho_depth_continental() -> float:
    """
Average continental Moho depth: ~35 km.

Rust: `geophysics::moho_depth_continental`
    """
    ...

def moho_depth_oceanic() -> float:
    """
Average oceanic Moho depth: ~7 km.

Rust: `geophysics::moho_depth_oceanic`
    """
    ...

def core_mantle_boundary_depth() -> float:
    """
Core-mantle boundary depth: 2891 km.

Rust: `geophysics::core_mantle_boundary_depth`
    """
    ...

def heat_flow(conductivity: float, temperature_gradient: float) -> float:
    """
Fourier heat flow: q = k × dT/dz (W/m²).
`conductivity` in W/(m·K), `temperature_gradient` in K/m.

Rust: `geophysics::heat_flow`
    """
    ...

def geothermal_power(flow_rate: float, specific_heat: float, delta_temp: float) -> float:
    """
Geothermal power extracted from a fluid: P = ṁ c ΔT.
`flow_rate` in kg/s, `specific_heat` in J/(kg·K), `delta_temp` in K.

Rust: `geophysics::geothermal_power`
    """
    ...

def plate_velocity_euler(omega: float, radius: float, angular_distance: float) -> float:
    """
Plate velocity from an Euler pole: v = ω R sin(θ).
`omega` in rad/s, `radius` in meters, `angular_distance` in radians.

Rust: `geophysics::plate_velocity_euler`
    """
    ...

def age_of_seafloor(distance: float, spreading_rate: float) -> float:
    """
Age of seafloor from distance to ridge: t = d / (2v).
`distance` in meters, `spreading_rate` is the full rate in m/s.
Division by 2 accounts for the half-spreading rate.

Rust: `geophysics::age_of_seafloor`
    """
    ...

def ocean_depth_from_age(age_myr: float) -> float:
    """
Ocean depth from seafloor age (Parsons-Sclater model):
d = 2500 + 350√t.
`age_myr` is in millions of years; returns depth in meters.

Rust: `geophysics::ocean_depth_from_age`
    """
    ...
