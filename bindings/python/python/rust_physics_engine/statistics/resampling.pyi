"""
Resampling methods: bootstrap, BCa bootstrap, permutation tests, and the jackknife.  Reference: Efron & Tibshirani, *An Introduction to the Bootstrap* (1993), ch. 6 (percentile), ch. 14 (BCa), ch. 15 (permutation).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.monte_carlo import Rng

class BootstrapResult:
    """
Bootstrap summary: point estimate on the original data, bootstrap
standard error, and the confidence interval bounds.

Rust: `statistics::resampling::BootstrapResult`
    """
    def __init__(self, estimate: float, se: float, ci_low: float, ci_high: float) -> None: ...
    @property
    def estimate(self) -> float: ...
    @property
    def se(self) -> float: ...
    @property
    def ci_low(self) -> float: ...
    @property
    def ci_high(self) -> float: ...

def bootstrap(data: list[float], statistic: Callable[[list[float]], float], n_resamples: int, level: float, rng: Rng) -> BootstrapResult:
    """
Percentile bootstrap for an arbitrary statistic at the given
confidence level (e.g. 0.95).

Panics:
Panics unless data is non-empty, n_resamples ≥ 2, and
level ∈ (0, 1).

Rust: `statistics::resampling::bootstrap`
    """
    ...

def bootstrap_bca(data: list[float], statistic: Callable[[list[float]], float], n_resamples: int, level: float, rng: Rng) -> BootstrapResult:
    """
Bias-corrected and accelerated (BCa) bootstrap: percentile interval
with the bias correction z₀ from the replicate distribution and the
acceleration a from the jackknife influence values.

Panics:
Panics unless data has ≥ 2 points, n_resamples ≥ 2, and
level ∈ (0, 1).

Rust: `statistics::resampling::bootstrap_bca`
    """
    ...

def permutation_test(x: list[float], y: list[float], statistic: Callable[[list[float], list[float]], float], n_perm: int, rng: Rng) -> float:
    """
Two-sample permutation test. `statistic` maps (x, y) to a test
statistic (e.g. difference of means); the returned p-value is the
fraction of label permutations with |T*| ≥ |T| (with the +1
continuity correction).

Panics:
Panics unless both samples are non-empty and n_perm ≥ 1.

Rust: `statistics::resampling::permutation_test`
    """
    ...

def jackknife(data: list[float], statistic: Callable[[list[float]], float]) -> tuple[float, float]:
    """
Jackknife estimate and standard error of a statistic:
SE² = (n−1)/n · Σ (θ₍ᵢ₎ − θ̄)².

Panics:
Panics unless data has at least 2 points.

Rust: `statistics::resampling::jackknife`
    """
    ...
