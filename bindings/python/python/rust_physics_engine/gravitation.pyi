"""
Newtonian gravity and two-body orbits.  The inverse-square force and its potential energy, the field of a point mass, escape and circular orbital velocity, and Kepler's third law in both directions. The vis-viva equation `v² = μ(2/r − 1/a)` ties speed to position on any conic orbit, and the specific orbital energy fixes which conic it is.  Also the Roche limit, the Hill sphere, the Schwarzschild radius and gravitational time dilation -- the last two are the points at which Newtonian gravity stops being enough; see `general_relativity`.  For orbits propagated rather than characterised, and for transfers between them, see `astrophysics`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.math import Vec3

def gravitational_force(m1: float, m2: float, distance: float) -> float:
    """
Gravitational force magnitude between two masses: F = G * m1 * m2 / r^2

Rust: `gravitation::gravitational_force`
    """
    ...

def gravitational_force_vec(m1: float, pos1: Vec3 | Sequence[float], m2: float, pos2: Vec3 | Sequence[float]) -> Vec3:
    """
Gravitational force vector from body1 toward body2.

Rust: `gravitation::gravitational_force_vec`
    """
    ...

def gravitational_potential_energy(m1: float, m2: float, distance: float) -> float:
    """
Gravitational potential energy: U = -G * m1 * m2 / r

Rust: `gravitation::gravitational_potential_energy`
    """
    ...

def gravitational_field(mass: float, distance: float) -> float:
    """
Gravitational field strength at distance r from mass M: g = G * M / r^2

Rust: `gravitation::gravitational_field`
    """
    ...

def escape_velocity(mass: float, radius: float) -> float:
    """
Escape velocity from a body of mass M and radius r: v = sqrt(2GM/r)

Rust: `gravitation::escape_velocity`
    """
    ...

def orbital_velocity(central_mass: float, orbital_radius: float) -> float:
    """
Orbital velocity for a circular orbit: v = sqrt(GM/r)

Rust: `gravitation::orbital_velocity`
    """
    ...

def orbital_period(central_mass: float, orbital_radius: float) -> float:
    """
Orbital period (Kepler's third law): T = 2π * sqrt(r^3 / (G*M))

Rust: `gravitation::orbital_period`
    """
    ...

def semi_major_axis_from_period(central_mass: float, period: float) -> float:
    """
Semi-major axis from orbital period (inverse Kepler's third law):
a = (G*M*T^2 / (4π^2))^(1/3)

Rust: `gravitation::semi_major_axis_from_period`
    """
    ...

def schwarzschild_radius(mass: float) -> float:
    """
Schwarzschild radius of a black hole: r_s = 2GM / c^2

Rust: `gravitation::schwarzschild_radius`
    """
    ...

def gravitational_time_dilation(mass: float, distance: float) -> float:
    """
Gravitational time dilation factor at distance r from mass M:
sqrt(1 - 2GM/(rc^2))

Rust: `gravitation::gravitational_time_dilation`
    """
    ...

def roche_limit(primary_radius: float, primary_density: float, satellite_density: float) -> float:
    """
Roche limit (fluid body): d = R * (2 * ρ_M / ρ_m)^(1/3)
R = radius of primary, ρ_M = density of primary, ρ_m = density of satellite

Rust: `gravitation::roche_limit`
    """
    ...

def vis_viva(central_mass: float, distance: float, semi_major_axis: float) -> float:
    """
Vis-viva equation: v^2 = GM * (2/r - 1/a)
Returns the orbital speed at distance r for an orbit with semi-major axis a.

Rust: `gravitation::vis_viva`
    """
    ...

def specific_orbital_energy(central_mass: float, semi_major_axis: float) -> float:
    """
Specific orbital energy: ε = -GM / (2a)

Rust: `gravitation::specific_orbital_energy`
    """
    ...

def hill_sphere_radius(semi_major_axis: float, orbiting_mass: float, central_mass: float) -> float:
    """
Hill sphere radius: r_H ≈ a * (m / (3M))^(1/3)

Rust: `gravitation::hill_sphere_radius`
    """
    ...
