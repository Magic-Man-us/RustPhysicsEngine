"""
Relativistic kinematics and scattering for particle collisions.  Invariant mass -- the quantity every collider analysis is built on, because it is the same in every frame -- along with centre-of-mass energy for colliding and fixed-target geometries, and the Lorentz boost of energy and longitudinal momentum.  The collider coordinates: rapidity, pseudorapidity and transverse momentum, chosen because rapidity differences are boost invariant along the beam. Scattering by the Rutherford cross section and the Breit-Wigner resonance shape, with the width-lifetime relation `Γτ = ħ` and branching ratios.  Also the conservation-law checks -- charge, lepton number, baryon number -- that say whether a proposed reaction can happen at all.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def invariant_mass(energy: float, momentum: float) -> float:
    """
Invariant mass from total energy and scalar momentum magnitude.
m = √(E² - p²c²) / c²

Rust: `particle_physics::invariant_mass`
    """
    ...

def invariant_mass_two_body(e1: float, px1: float, py1: float, pz1: float, e2: float, px2: float, py2: float, pz2: float) -> float:
    """
Invariant mass of a two-body system from individual four-momenta.
m² = (E1+E2)² - |p1+p2|²c²,  return m/c².

Rust: `particle_physics::invariant_mass_two_body`
    """
    ...

def center_of_mass_energy(e_beam: float, e_target: float, p_beam: float, p_target: float) -> float:
    """
Center-of-mass energy √s for two particles with given energies and momenta.
√s = √((E1+E2)² - (p1+p2)²c²)

Rust: `particle_physics::center_of_mass_energy`
    """
    ...

def fixed_target_com_energy(beam_energy: float, target_mass: float) -> float:
    """
Fixed-target center-of-mass energy (high-energy approximation).
√s ≈ √(2 × E_beam × m_target × c²)

Rust: `particle_physics::fixed_target_com_energy`
    """
    ...

def lorentz_boost_energy(energy: float, momentum_z: float, beta: float) -> float:
    """
Lorentz boost of energy along z.  E' = γ(E - β pz c)

Rust: `particle_physics::lorentz_boost_energy`
    """
    ...

def lorentz_boost_pz(energy: float, momentum_z: float, beta: float) -> float:
    """
Lorentz boost of z-momentum.  pz' = γ(pz - β E/c)

Rust: `particle_physics::lorentz_boost_pz`
    """
    ...

def rapidity(energy: float, pz: float) -> float:
    """
Rapidity y = 0.5 × ln((E + pz c) / (E - pz c))

Rust: `particle_physics::rapidity`
    """
    ...

def pseudorapidity(theta: float) -> float:
    """
Pseudorapidity η = -ln(tan(θ/2))

Rust: `particle_physics::pseudorapidity`
    """
    ...

def transverse_momentum(px: float, py: float) -> float:
    """
Transverse momentum pT = √(px² + py²)

Rust: `particle_physics::transverse_momentum`
    """
    ...

def rutherford_cross_section(z1: float, z2: float, energy: float, angle: float) -> float:
    """
Rutherford scattering differential cross section.
dσ/dΩ = (Z1 Z2 k_e e² / (4E))² / sin⁴(θ/2)

Rust: `particle_physics::rutherford_cross_section`
    """
    ...

def breit_wigner(energy: float, mass: float, width: float) -> float:
    """
Non-relativistic Breit-Wigner resonance (normalized to peak = 1).
BW(E) = (Γ/2)² / ((E - M)² + (Γ/2)²)

Rust: `particle_physics::breit_wigner`
    """
    ...

def decay_rate_from_lifetime(lifetime: float) -> float:
    """
Decay rate from lifetime.  Γ = ℏ / τ

Rust: `particle_physics::decay_rate_from_lifetime`
    """
    ...

def lifetime_from_width(width_joules: float) -> float:
    """
Lifetime from decay width.  τ = ℏ / Γ

Rust: `particle_physics::lifetime_from_width`
    """
    ...

def branching_ratio(partial_width: float, total_width: float) -> float:
    """
Branching ratio BR = Γ_i / Γ_total

Rust: `particle_physics::branching_ratio`
    """
    ...

def mean_free_path_particle(cross_section: float, number_density: float) -> float:
    """
Mean free path λ = 1 / (n σ)

Rust: `particle_physics::mean_free_path_particle`
    """
    ...

def luminosity_to_event_rate(luminosity: float, cross_section: float) -> float:
    """
Event rate R = L × σ

Rust: `particle_physics::luminosity_to_event_rate`
    """
    ...

def is_charge_conserved(charges_in: list[float], charges_out: list[float]) -> bool:
    """
Check charge conservation: sum of input charges ≈ sum of output charges.

Rust: `particle_physics::is_charge_conserved`
    """
    ...

def is_lepton_number_conserved(leptons_in: list[int], leptons_out: list[int]) -> bool:
    """
Check lepton number conservation.

Rust: `particle_physics::is_lepton_number_conserved`
    """
    ...

def is_baryon_number_conserved(baryons_in: list[int], baryons_out: list[int]) -> bool:
    """
Check baryon number conservation.

Rust: `particle_physics::is_baryon_number_conserved`
    """
    ...

def four_momentum_magnitude(energy: float, px: float, py: float, pz: float) -> float:
    """
Four-momentum magnitude (invariant mass × c).
√(E² - p²c²) / c  =  mc

Rust: `particle_physics::four_momentum_magnitude`
    """
    ...

M_MUON: float

M_TAU: float

M_PION_CHARGED: float

M_PION_NEUTRAL: float

M_KAON: float

M_W_BOSON: float

M_Z_BOSON: float

M_HIGGS: float

M_TOP_QUARK: float

CHARGE_UP: float

CHARGE_DOWN: float

FINE_STRUCTURE: float

WEAK_MIXING_ANGLE_SIN2: float

STRONG_COUPLING: float
