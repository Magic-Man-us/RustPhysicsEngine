"""
Discrete exterior calculus on triangle meshes: exterior derivatives, diagonal Hodge stars, Laplacians, Hodge decomposition, harmonic forms and Betti numbers, heat and Poisson solves, spectral shape analysis, curvature flows, and persistent homology.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.linalg.sparse import CsrMatrix
from rust_physics_engine.math import Vec3
from rust_physics_engine.manifold.vecn import VecN

class DecMesh:
    """
A triangle mesh with its DEC operators: primal edges, exterior
derivatives d0 and d1, and diagonal Hodge stars.

Rust: `manifold::dec::DecMesh`
    """
    def __init__(self, mesh: Mesh) -> None: ...
    def d0(self) -> CsrMatrix: ...
    def d1(self) -> CsrMatrix: ...
    def hodge0(self) -> list[float]: ...
    def hodge1(self) -> list[float]: ...
    def hodge2(self) -> list[float]: ...
    def dual_areas(self) -> list[float]: ...
    def laplace_beltrami(self) -> CsrMatrix: ...
    def laplace_1form(self) -> CsrMatrix: ...
    def gradient(self, f: list[float]) -> list[float]: ...
    def curl(self, w: list[float]) -> list[float]: ...
    def divergence(self, w: list[float]) -> list[float]: ...
    def hodge_decomposition(self, w: list[float]) -> tuple[list[float], list[float], list[float]]: ...
    def harmonic_forms(self) -> list[list[float]]: ...
    def betti_numbers(self) -> list[int]: ...
    def simplicial_cohomology_rank(self, k: int) -> int: ...
    def interpolate_1form_to_vectors(self, w: list[float]) -> list[Vec3]: ...
    def vector_field_to_1form(self, v: list[Vec3 | Sequence[float]]) -> list[float]: ...
    def heat_flow(self, f0: list[float], t: float, steps: int) -> list[float]: ...
    def poisson_solve(self, rho: list[float], fixed: list[tuple[int, float]]) -> list[float]: ...
    def eigenmodes(self, n: int) -> tuple[list[float], list[list[float]]]: ...
    def geodesic_heat_method(self, source: int, t: float) -> list[float]: ...
    def vector_heat_method(self, source: int, v0: Vec3 | Sequence[float], t: float) -> list[Vec3]: ...
    def trivial_connection(self, singularities: list[tuple[int, float]]) -> list[float]: ...
    def smoothest_direction_field(self, n_rosy: int) -> list[Vec3]: ...
    def stream_function(self, v: list[float]) -> list[float]: ...
    def fluid_step_dec(self, w: MutableSequence[float], dt: float, nu: float) -> None: ...
    def mean_curvature_flow_step(self, dt: float) -> Mesh: ...
    def willmore_energy(self) -> float: ...
    def discrete_gauss_bonnet_check(self) -> float: ...
    @property
    def mesh(self) -> Mesh: ...
    @property
    def edges(self) -> list[tuple[int, int]]: ...

def persistent_homology_vietoris_rips(points: list[VecN | Sequence[float]], max_eps: float, max_dim: int) -> list[tuple[int, float, float]]:
    """
Vietoris-Rips persistent homology in dimensions 0 and 1: returns
(dimension, birth, death) pairs (essential classes get death =
`max_eps`).

Rust: `manifold::dec::persistent_homology_vietoris_rips`
    """
    ...

def persistence_diagram_bottleneck(a: list[tuple[float, float]], b: list[tuple[float, float]]) -> float:
    """
Bottleneck distance between two persistence diagrams (same dimension),
by binary search over candidate distances with greedy augmenting-path
matching (diagonal projections allowed).

Rust: `manifold::dec::persistence_diagram_bottleneck`
    """
    ...

def betti_curve(pairs: list[tuple[int, float, float]], eps_range: list[float]) -> list[tuple[float, list[int]]]:
    """
Betti curve: Betti numbers (dims 0..2) as a function of the filtration
parameter, from the persistence pairs.

Rust: `manifold::dec::betti_curve`
    """
    ...
