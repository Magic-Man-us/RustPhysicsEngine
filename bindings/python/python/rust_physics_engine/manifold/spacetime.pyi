"""
Special and general relativity: four-vectors and Lorentz transforms, Rindler and Kruskal coordinates, Schwarzschild and Kerr geodesics, gravitational lensing, black hole thermodynamics, cosmological distances, inspiral waveforms, and Kaluza-Klein reduction.  Kinematics and geometry use geometric units (G = c = 1) with the mostly-minus signature (+, -, -, -) unless stated otherwise; the thermodynamic and cosmological helpers use SI units.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.manifold.geodesic import GeodesicState
from rust_physics_engine.manifold.metric import Sig
from rust_physics_engine.manifold.lie import Sl2C
from rust_physics_engine.manifold.lie import So3
from rust_physics_engine.math import Vec3
from rust_physics_engine.manifold.vecn import VecN

class Causal:
    """
Causal relation of event `b` relative to event `a`.

Rust: `manifold::spacetime::Causal`
    """
    ...

class FourVector:
    """
A spacetime four-vector (t, x) in units with c = 1.

Rust: `manifold::spacetime::FourVector`
    """
    def __init__(self, t: float, x: Vec3 | Sequence[float]) -> None: ...
    def minkowski_dot(self, o: FourVector) -> float: ...
    def minkowski_dot_sig(self, o: FourVector, sig: Sig) -> float: ...
    def norm_squared(self) -> float: ...
    def is_timelike(self) -> bool: ...
    def is_spacelike(self) -> bool: ...
    def is_null(self, tol: float) -> bool: ...
    def boost(self, v: Vec3 | Sequence[float]) -> FourVector: ...
    def rotate(self, r: So3) -> FourVector: ...
    @staticmethod
    def from_velocity(v: Vec3 | Sequence[float]) -> FourVector: ...
    @staticmethod
    def from_momentum(m: float, v: Vec3 | Sequence[float]) -> FourVector: ...
    def energy(self) -> float: ...
    def spatial_momentum(self) -> Vec3: ...
    def rapidity(self) -> float: ...
    def scale(self, k: float) -> FourVector: ...
    def proper_time_to(self, o: FourVector) -> float: ...
    def __add__(self, o: FourVector) -> FourVector: ...
    def __sub__(self, o: FourVector) -> FourVector: ...
    @property
    def t(self) -> float: ...
    @property
    def x(self) -> Vec3: ...

class KerrConstants:
    """
Carter constants of motion for a timelike Kerr geodesic: energy `e`,
axial angular momentum `l`, and Carter constant `q` per unit mass.

Rust: `manifold::spacetime::KerrConstants`
    """
    def __init__(self, m: float, a: float, e: float, l: float, q: float) -> None: ...
    def radial_potential(self, r: float) -> float: ...
    def theta_potential(self, theta: float) -> float: ...
    @property
    def m(self) -> float: ...
    @property
    def a(self) -> float: ...
    @property
    def e(self) -> float: ...
    @property
    def l(self) -> float: ...
    @property
    def q(self) -> float: ...

class LorentzTransform:
    """
A Lorentz transformation as a 4x4 matrix acting on (t, x, y, z).

Rust: `manifold::spacetime::LorentzTransform`
    """
    @staticmethod
    def identity() -> LorentzTransform: ...
    @staticmethod
    def boost(v: Vec3 | Sequence[float]) -> LorentzTransform: ...
    @staticmethod
    def boost_x(beta: float) -> LorentzTransform: ...
    @staticmethod
    def rotation(r: So3) -> LorentzTransform: ...
    def compose(self, o: LorentzTransform) -> LorentzTransform: ...
    def inverse(self) -> LorentzTransform: ...
    def apply(self, v: FourVector) -> FourVector: ...
    def is_lorentz(self, tol: float) -> bool: ...
    @staticmethod
    def thomas_wigner_rotation(v1: Vec3 | Sequence[float], v2: Vec3 | Sequence[float]) -> So3: ...
    @staticmethod
    def velocity_addition(u: Vec3 | Sequence[float], v: Vec3 | Sequence[float]) -> Vec3: ...
    @staticmethod
    def from_sl2c(m: Sl2C) -> LorentzTransform: ...
    def to_sl2c(self) -> Sl2C: ...

class Plane:
    """
A hyperplane of events: all x with normal . x = offset (Minkowski dot).

Rust: `manifold::spacetime::Plane`
    """
    def __init__(self, normal: FourVector, offset: float) -> None: ...
    @property
    def normal(self) -> FourVector: ...
    @property
    def offset(self) -> float: ...

def light_cone_check(a: FourVector, b: FourVector) -> Causal:
    """
Classify the separation b - a on the light cone of `a`.

Rust: `manifold::spacetime::light_cone_check`
    """
    ...

def simultaneity_plane(observer_vel: Vec3 | Sequence[float], event: FourVector) -> Plane:
    """
The simultaneity hyperplane through `event` for an observer moving at
`observer_vel`: events x with u . (x - event) = 0 for the observer
four-velocity u.

Rust: `manifold::spacetime::simultaneity_plane`
    """
    ...

def twin_paradox_ages(v: float, t_coordinate: float) -> tuple[float, float]:
    """
Ages (stay-at-home, traveler) after coordinate time `t_coordinate` with
the traveler cruising at speed `v`.

Rust: `manifold::spacetime::twin_paradox_ages`
    """
    ...

def relativistic_rocket(accel_proper: float, tau: float) -> tuple[float, float, float]:
    """
Relativistic rocket with constant proper acceleration: returns
(coordinate time, distance, speed) after proper time `tau`.

Rust: `manifold::spacetime::relativistic_rocket`
    """
    ...

def rindler_coords(t: float, x: float, a: float) -> tuple[float, float]:
    """
Rindler coordinates (eta, xi) of the Minkowski event (t, x) in the
right wedge x > |t|, normalized so the observer at proper acceleration
`a` sits at xi = 1/a: t = xi sinh(a eta), x = xi cosh(a eta).

Rust: `manifold::spacetime::rindler_coords`
    """
    ...

def rindler_horizon(a: float) -> float:
    """
Distance from a uniformly accelerated observer to their Rindler
horizon: c^2 / a (geometric units: 1/a).

Rust: `manifold::spacetime::rindler_horizon`
    """
    ...

def unruh_temperature(a: float) -> float:
    """
Unruh temperature of a uniformly accelerated observer (SI units):
T = hbar a / (2 pi c k_B).

Rust: `manifold::spacetime::unruh_temperature`
    """
    ...

def kruskal_from_schwarzschild(t: float, r: float, m: float) -> tuple[float, float]:
    """
Kruskal-Szekeres coordinates (T, X) of the Schwarzschild event (t, r),
smooth across the horizon r = 2M (exterior region I and interior
region II).

Rust: `manifold::spacetime::kruskal_from_schwarzschild`
    """
    ...

def penrose_diagram_coords(t: float, r: float, m: float) -> tuple[float, float]:
    """
Penrose diagram coordinates: Kruskal null coordinates compactified with
arctangent; returns (T, X) of the conformal diagram.

Rust: `manifold::spacetime::penrose_diagram_coords`
    """
    ...

def eddington_finkelstein(t: float, r: float, m: float) -> float:
    """
Ingoing Eddington-Finkelstein null coordinate v = t + r* with the
tortoise coordinate r* = r + 2M ln|r/2M - 1|.

Rust: `manifold::spacetime::eddington_finkelstein`
    """
    ...

def schwarzschild_geodesic_metric(m: float) -> Metric:
    """
The Schwarzschild metric wired into the finite-difference `Metric`
machinery (coordinates t, r, theta, phi; signature -+++).

Rust: `manifold::spacetime::schwarzschild_geodesic_metric`
    """
    ...

def orbit_schwarzschild_full(m: float, e: float, l: float, r0: float, tau_end: float, dt: float) -> list[tuple[float, float, float, float]]:
    """
Full timelike Schwarzschild orbit in the equatorial plane from the
first integrals: energy `e` and angular momentum `l` per unit mass,
starting at r0 (infalling if r0 is not a turning point). Returns
(t, r, phi, tau) samples every proper-time step `dt`.

Rust: `manifold::spacetime::orbit_schwarzschild_full`
    """
    ...

def photon_ray_trace_schwarzschild(m: float, b: float, phi_max: float) -> list[tuple[float, float]]:
    """
Photon trajectory around a Schwarzschild black hole with impact
parameter `b`, from the orbit equation u'' + u = 3 M u^2 starting at
infinity. Returns (phi, r) samples; stops at `phi_max`, escape, or
capture inside the photon sphere.

Rust: `manifold::spacetime::photon_ray_trace_schwarzschild`
    """
    ...

def black_hole_shadow_radius(m: float, a: float, inclination: float) -> float:
    """
Apparent black hole shadow radius for a Kerr hole of spin `a` seen at
`inclination` (radians from the spin axis), averaged over the shadow
boundary via Bardeen's celestial coordinates; sqrt(27) M for a = 0.

Rust: `manifold::spacetime::black_hole_shadow_radius`
    """
    ...

def kerr_geodesic_constants(m: float, a: float, e: float, l: float, q: float) -> KerrConstants:
    """
Bundle the Kerr geodesic constants (energy, axial angular momentum, and
Carter constant per unit rest mass) with their potentials.

Rust: `manifold::spacetime::kerr_geodesic_constants`
    """
    ...

def gravitational_lens_einstein_radius(m: float, d_l: float, d_s: float, d_ls: float) -> float:
    """
Einstein ring angular radius (geometric units, angles in radians):
theta_E = sqrt(4 M d_ls / (d_l d_s)).

Rust: `manifold::spacetime::gravitational_lens_einstein_radius`
    """
    ...

def point_lens_magnification(u: float) -> float:
    """
Total point-lens magnification at impact parameter u (in Einstein
radii): (u^2 + 2) / (u sqrt(u^2 + 4)).

Rust: `manifold::spacetime::point_lens_magnification`
    """
    ...

def lens_equation_solve(beta: float, mass_model: Callable[[float], float]) -> list[float]:
    """
Solve the lens equation beta = theta - alpha(theta) for image positions
given the deflection profile `mass_model` (alpha as a function of
theta, odd in theta). Returns all real images found on both sides.

Rust: `manifold::spacetime::lens_equation_solve`
    """
    ...

def hawking_temperature(m: float) -> float:
    """
Hawking temperature of a Schwarzschild black hole (SI):
T = hbar c^3 / (8 pi G M k_B).

Rust: `manifold::spacetime::hawking_temperature`
    """
    ...

def bekenstein_entropy(m: float) -> float:
    """
Bekenstein-Hawking entropy (SI): S = 4 pi G M^2 k_B / (hbar c).

Rust: `manifold::spacetime::bekenstein_entropy`
    """
    ...

def evaporation_time(m: float) -> float:
    """
Black hole evaporation time (SI): t = 5120 pi G^2 M^3 / (hbar c^4).

Rust: `manifold::spacetime::evaporation_time`
    """
    ...

def cosmological_distances(z: float, h0: float, omega_m: float, omega_l: float) -> tuple[float, float, float, float]:
    """
Cosmological distances in a flat-ish FRW universe (SI: `h0` in 1/s,
distances in meters, lookback time in seconds): returns (comoving,
angular-diameter, luminosity, lookback).

Rust: `manifold::spacetime::cosmological_distances`
    """
    ...

def gw_chirp_mass(m1: float, m2: float) -> float:
    """
Chirp mass (m1 m2)^(3/5) / (m1 + m2)^(1/5).

Rust: `manifold::spacetime::gw_chirp_mass`
    """
    ...

def gw_waveform_inspiral(m1: float, m2: float, d: float, t: list[float]) -> tuple[list[float], list[float]]:
    """
Leading-order (Newtonian chirp) inspiral waveform at luminosity
distance `d` (SI units, face-on): returns (h_plus, h_cross) sampled at
the times `t`, with coalescence at the last sample.

Rust: `manifold::spacetime::gw_waveform_inspiral`
    """
    ...

def kk_reduce_geodesic_to_charged(geo5: list[GeodesicState], radius: float) -> tuple[float, list[VecN]]:
    """
Reduce a 5D Kaluza-Klein geodesic to 4D charged-particle data: the
conserved fifth momentum gives the charge-to-mass ratio (valid when
the gauge potential vanishes at the initial point and phi = 1), and
the positions project to the 4D worldline.

Rust: `manifold::spacetime::kk_reduce_geodesic_to_charged`
    """
    ...

def kk_compactification_mass_spectrum(radius: float, n_max: int) -> list[float]:
    """
Kaluza-Klein tower masses n / R for mode numbers 0..=n_max.

Rust: `manifold::spacetime::kk_compactification_mass_spectrum`
    """
    ...

def extra_dimension_gravity_law(r: float, n_extra: int, size: float) -> float:
    """
Gravitational force law with `n_extra` compact extra dimensions of size
`size` (normalized to 1/r^2 at large r): 1/r^2 outside, continuously
matched to size^n / r^(2+n) inside.

Rust: `manifold::spacetime::extra_dimension_gravity_law`
    """
    ...

def sta_vs_matrix_lorentz_check(v: Vec3 | Sequence[float]) -> float:
    """
Cross-validate the spacetime algebra boost rotor against the matrix
Lorentz boost: the maximum component difference over a set of basis
events (the STA rotor R e R~ realizes the inverse boost, so it is
compared against B(-v)).

Rust: `manifold::spacetime::sta_vs_matrix_lorentz_check`
    """
    ...
