"""
Manifold learning and dimensionality reduction: spectral embeddings (MDS, Isomap, LLE, Laplacian eigenmaps, diffusion maps), PCA and kernel PCA, stochastic neighbor embeddings, intrinsic-dimension estimators, embedding quality metrics, benchmark datasets, and optimization on matrix manifolds.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.cfd.sph import Kernel
from rust_physics_engine.linalg.matrix import Matrix
from rust_physics_engine.monte_carlo import Rng
from rust_physics_engine.manifold.vecn import VecN

def dist_matrix(points: list[VecN | Sequence[float]]) -> Matrix:
    """
Pairwise Euclidean distance matrix.

Rust: `manifold::embedding::dist_matrix`
    """
    ...

def classical_mds(dist: Matrix | Sequence[Sequence[float]], dim: int) -> list[VecN]:
    """
Classical (Torgerson) multidimensional scaling from a distance matrix.

Rust: `manifold::embedding::classical_mds`
    """
    ...

def metric_mds_smacof(dist: Matrix | Sequence[Sequence[float]], dim: int, iters: int, rng: Rng) -> tuple[list[VecN], float]:
    """
Metric MDS by SMACOF stress majorization. Returns (embedding, stress).

Rust: `manifold::embedding::metric_mds_smacof`
    """
    ...

def nonmetric_mds(dist: Matrix | Sequence[Sequence[float]], dim: int, iters: int, rng: Rng) -> list[VecN]:
    """
Nonmetric MDS: SMACOF against monotone-regressed disparities.

Rust: `manifold::embedding::nonmetric_mds`
    """
    ...

def knn_graph(points: list[VecN | Sequence[float]], k: int) -> list[list[tuple[int, float]]]:
    """
k-nearest-neighbor graph: for each point, its k neighbors and distances.

Rust: `manifold::embedding::knn_graph`
    """
    ...

def geodesic_distance_matrix(knn: list[list[tuple[int, float]]]) -> Matrix:
    """
All-pairs shortest paths over a kNN graph (Floyd-Warshall; symmetrized).

Rust: `manifold::embedding::geodesic_distance_matrix`
    """
    ...

def isomap(points: list[VecN | Sequence[float]], k_neighbors: int, dim: int) -> list[VecN]:
    """
Isomap: geodesic distances over the kNN graph fed to classical MDS.

Rust: `manifold::embedding::isomap`
    """
    ...

def lle(points: list[VecN | Sequence[float]], k: int, dim: int, reg: float) -> list[VecN]:
    """
Locally linear embedding.

Rust: `manifold::embedding::lle`
    """
    ...

def laplacian_eigenmaps(points: list[VecN | Sequence[float]], k: int, dim: int, sigma: float) -> list[VecN]:
    """
Laplacian eigenmaps with heat-kernel weights.

Rust: `manifold::embedding::laplacian_eigenmaps`
    """
    ...

def diffusion_maps(points: list[VecN | Sequence[float]], eps: float, dim: int, t: float) -> list[VecN]:
    """
Diffusion maps: eigenfunctions of the diffusion operator, scaled by
lambda^t.

Rust: `manifold::embedding::diffusion_maps`
    """
    ...

def spectral_embedding(adjacency: Matrix | Sequence[Sequence[float]], dim: int) -> list[VecN]:
    """
Spectral embedding of a graph adjacency matrix.

Rust: `manifold::embedding::spectral_embedding`
    """
    ...

def pca(points: list[VecN | Sequence[float]], dim: int) -> tuple[list[VecN], list[float], Matrix]:
    """
Principal component analysis: returns (projected points, explained
variance per component, components as rows).

Rust: `manifold::embedding::pca`
    """
    ...

def kernel_pca(points: list[VecN | Sequence[float]], kernel: Callable[[VecN | Sequence[float], VecN | Sequence[float]], float], dim: int) -> list[VecN]:
    """
Kernel PCA with a user-supplied kernel.

Rust: `manifold::embedding::kernel_pca`
    """
    ...

def tsne(points: list[VecN | Sequence[float]], dim: int, perplexity: float, iters: int, lr: float, rng: Rng) -> list[VecN]:
    """
t-SNE (exact gradients; suitable for small point sets).

Rust: `manifold::embedding::tsne`
    """
    ...

def umap_lite(points: list[VecN | Sequence[float]], k: int, dim: int, min_dist: float, epochs: int, rng: Rng) -> list[VecN]:
    """
Lightweight UMAP: fuzzy kNN weights optimized by SGD attraction and
random-negative repulsion.

Rust: `manifold::embedding::umap_lite`
    """
    ...

def intrinsic_dimension_mle(points: list[VecN | Sequence[float]], k: int) -> float:
    """
Levina-Bickel maximum-likelihood intrinsic dimension using k neighbors.

Rust: `manifold::embedding::intrinsic_dimension_mle`
    """
    ...

def intrinsic_dimension_correlation(points: list[VecN | Sequence[float]], r_range: tuple[float, float]) -> float:
    """
Correlation-dimension estimate: log-log slope of the correlation
integral over the radius range.

Rust: `manifold::embedding::intrinsic_dimension_correlation`
    """
    ...

def intrinsic_dimension_two_nn(points: list[VecN | Sequence[float]]) -> float:
    """
TwoNN intrinsic dimension (Facco et al.): d = n / sum ln(r2/r1).

Rust: `manifold::embedding::intrinsic_dimension_two_nn`
    """
    ...

def trustworthiness(high: list[VecN | Sequence[float]], low: list[VecN | Sequence[float]], k: int) -> float:
    """
Trustworthiness of a low-dimensional embedding (1 = perfect).

Rust: `manifold::embedding::trustworthiness`
    """
    ...

def continuity(high: list[VecN | Sequence[float]], low: list[VecN | Sequence[float]], k: int) -> float:
    """
Continuity of an embedding (trustworthiness with roles swapped).

Rust: `manifold::embedding::continuity`
    """
    ...

def stress(dist_high: Matrix | Sequence[Sequence[float]], dist_low: Matrix | Sequence[Sequence[float]]) -> float:
    """
Kruskal stress between two distance matrices.

Rust: `manifold::embedding::stress`
    """
    ...

def neighborhood_preservation(high: list[VecN | Sequence[float]], low: list[VecN | Sequence[float]], k: int) -> float:
    """
Fraction of k-nearest neighbors preserved by the embedding.

Rust: `manifold::embedding::neighborhood_preservation`
    """
    ...

def procrustes_align(a: list[VecN | Sequence[float]], b: list[VecN | Sequence[float]]) -> tuple[list[VecN], float]:
    """
Procrustes alignment of b onto a (rotation + scale + translation);
returns (aligned b, residual).

Rust: `manifold::embedding::procrustes_align`
    """
    ...

def swiss_roll(n: int, noise: float, rng: Rng) -> tuple[list[VecN], list[float]]:
    """
Swiss roll in R3 with the unrolled arc-length parameter as ground truth.

Rust: `manifold::embedding::swiss_roll`
    """
    ...

def s_curve(n: int, noise: float, rng: Rng) -> tuple[list[VecN], list[float]]:
    """
S-curve dataset with the curve parameter as ground truth.

Rust: `manifold::embedding::s_curve`
    """
    ...

def torus_sample(n: int, big_r: float, small_r: float, rng: Rng) -> tuple[list[VecN], list[tuple[float, float]]]:
    """
Points on a torus with (u, v) angles as ground truth.

Rust: `manifold::embedding::torus_sample`
    """
    ...

def sphere_sample(n: int, rng: Rng) -> list[VecN]:
    """
Uniform points on the unit 2-sphere in R3.

Rust: `manifold::embedding::sphere_sample`
    """
    ...

def helix_sample(n: int, rng: Rng) -> tuple[list[VecN], list[float]]:
    """
Helix in R3 with the parameter as ground truth.

Rust: `manifold::embedding::helix_sample`
    """
    ...

def mobius_sample(n: int, rng: Rng) -> list[VecN]:
    """
Points on a Mobius band.

Rust: `manifold::embedding::mobius_sample`
    """
    ...

def klein_sample(n: int, rng: Rng) -> list[VecN]:
    """
Points on the figure-8 immersion of the Klein bottle in R3.

Rust: `manifold::embedding::klein_sample`
    """
    ...

def two_moons(n: int, noise: float, rng: Rng) -> tuple[list[VecN], list[int]]:
    """
The two-moons dataset with labels.

Rust: `manifold::embedding::two_moons`
    """
    ...

def blobs(n: int, centers: list[VecN | Sequence[float]], spread: float, rng: Rng) -> tuple[list[VecN], list[int]]:
    """
Isotropic Gaussian blobs with labels.

Rust: `manifold::embedding::blobs`
    """
    ...

def tangent_space_estimate(points: list[VecN | Sequence[float]], idx: int, k: int, dim: int) -> Matrix:
    """
Local tangent space at a point by PCA of its k neighbors: rows are an
orthonormal basis.

Rust: `manifold::embedding::tangent_space_estimate`
    """
    ...

def manifold_curvature_estimate(points: list[VecN | Sequence[float]], idx: int, k: int) -> float:
    """
Curvature proxy at a point: residual variance fraction outside the local
tangent plane.

Rust: `manifold::embedding::manifold_curvature_estimate`
    """
    ...

def grassmann_distance(a: Matrix | Sequence[Sequence[float]], b: Matrix | Sequence[Sequence[float]]) -> float:
    """
Grassmann distance between subspaces spanned by the rows of a and b
(square root of the sum of squared principal angles).

Rust: `manifold::embedding::grassmann_distance`
    """
    ...

def stiefel_project(m: Matrix | Sequence[Sequence[float]]) -> Matrix:
    """
Project a matrix onto the Stiefel manifold (nearest orthonormal-column
matrix, via the polar factor).

Rust: `manifold::embedding::stiefel_project`
    """
    ...

def riemannian_gradient_descent_sphere(f: Callable[[VecN | Sequence[float]], float], grad: Callable[[VecN | Sequence[float]], VecN | Sequence[float]], x0: VecN | Sequence[float], iters: int, lr: float) -> VecN:
    """
Riemannian gradient descent on the unit sphere.

Rust: `manifold::embedding::riemannian_gradient_descent_sphere`
    """
    ...

def geodesic_kmeans(metric: Metric, points: list[VecN | Sequence[float]], k: int, iters: int, rng: Rng) -> tuple[list[VecN], list[int]]:
    """
k-means with distances and means taken in a Riemannian metric (uses the
metric's exp/log maps). Returns (centroids, labels).

Rust: `manifold::embedding::geodesic_kmeans`
    """
    ...

def manifold_interpolation_rbf(points: list[VecN | Sequence[float]], values: list[float], query: VecN | Sequence[float], kernel: Callable[[float], float]) -> float:
    """
Radial-basis interpolation of scattered manifold data.

Rust: `manifold::embedding::manifold_interpolation_rbf`
    """
    ...
