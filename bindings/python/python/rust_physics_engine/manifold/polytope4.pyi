"""
Four-dimensional polytopes: the six regular 4-polytopes with their full combinatorics, prisms and products, projections and cross-sections, duals, Coxeter-plane pictures, exceptional root systems and lattices, and curse-of-dimensionality demonstrations.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.manifold.lie import So4
from rust_physics_engine.math import Vec2
from rust_physics_engine.math import Vec3
from rust_physics_engine.manifold.vecn import VecN

class Polytope4:
    """
A 4-polytope: vertices with edge, face (2D), and cell (3D facet)
combinatorics. Faces and cells list vertex indices.

Rust: `manifold::polytope4::Polytope4`
    """
    def __init__(self, vertices: list[Vec4 | Sequence[float]], edges: list[tuple[int, int]], faces: list[list[int]], cells: list[list[int]]) -> None: ...
    @staticmethod
    def tesseract() -> Polytope4: ...
    @staticmethod
    def cell16() -> Polytope4: ...
    @staticmethod
    def cell24() -> Polytope4: ...
    @staticmethod
    def simplex5() -> Polytope4: ...
    @staticmethod
    def cell600() -> Polytope4: ...
    @staticmethod
    def cell120() -> Polytope4: ...
    @staticmethod
    def duoprism(p: int, q: int) -> Polytope4: ...
    @staticmethod
    def duocylinder(n: int) -> Polytope4: ...
    @staticmethod
    def grand_antiprism() -> Polytope4: ...
    @staticmethod
    def cubinder(n: int) -> Polytope4: ...
    @staticmethod
    def spherinder(n: int) -> Polytope4: ...
    def rectified(self) -> Polytope4: ...
    def truncated(self) -> Polytope4: ...
    def dual(self) -> Polytope4: ...
    def euler_characteristic(self) -> int: ...
    def f_vector(self) -> list[int]: ...
    def edge_length(self) -> float: ...
    def rotate(self, r: So4) -> Polytope4: ...
    def project_perspective(self, distance: float) -> Mesh: ...
    def project_orthographic(self, drop_axis: int) -> list[Vec3]: ...
    def project_stereographic(self) -> list[Vec3]: ...
    def cross_section(self, w: float) -> Mesh: ...
    def cross_section_oriented(self, normal: Vec4 | Sequence[float], offset: float) -> Mesh: ...
    def unfold_net(self) -> list[Mesh]: ...
    def schlegel_diagram(self, cell: int) -> list[Vec3]: ...
    def vertex_figure(self, v: int) -> Mesh: ...
    def symmetry_order(self) -> int: ...
    def coxeter_group_generators(self) -> list[So4]: ...
    @staticmethod
    def from_wythoff(symbol: str) -> Optional[Polytope4]: ...
    def dihedral_angle(self) -> float: ...
    def circumradius(self) -> float: ...
    def inradius(self) -> float: ...
    def hypervolume(self) -> float: ...
    def surface_volume(self) -> float: ...
    @property
    def vertices(self) -> list[Vec4]: ...
    @property
    def edges(self) -> list[tuple[int, int]]: ...
    @property
    def faces(self) -> list[list[int]]: ...
    @property
    def cells(self) -> list[list[int]]: ...

class Vec4:
    """
A 4D vector.

Rust: `manifold::polytope4::Vec4`
    """
    def __init__(self, x: float, y: float, z: float, w: float) -> None: ...
    def dot(self, o: Vec4 | Sequence[float]) -> float: ...
    def norm(self) -> float: ...
    def normalized(self) -> Vec4: ...
    def add(self, o: Vec4 | Sequence[float]) -> Vec4: ...
    def sub(self, o: Vec4 | Sequence[float]) -> Vec4: ...
    def scale(self, k: float) -> Vec4: ...
    @property
    def x(self) -> float: ...
    @property
    def y(self) -> float: ...
    @property
    def z(self) -> float: ...
    @property
    def w(self) -> float: ...

def rotation_4d_planes() -> list[tuple[int, int]]:
    """
The six coordinate rotation planes of R4.

Rust: `manifold::polytope4::rotation_4d_planes`
    """
    ...

def rotate_4d(p: Vec4 | Sequence[float], plane: tuple[int, int], angle: float) -> Vec4:
    """
Rotate a point in a single coordinate plane.

Rust: `manifold::polytope4::rotate_4d`
    """
    ...

def rotate_4d_double(p: Vec4 | Sequence[float], angle_xy: float, angle_zw: float) -> Vec4:
    """
Double rotation: xy-plane by `angle_xy`, zw-plane by `angle_zw`.

Rust: `manifold::polytope4::rotate_4d_double`
    """
    ...

def clifford_torus(u: float, v: float, r: float) -> Vec4:
    """
Point on the Clifford torus in S3: parameter angles (u, v), aspect r.

Rust: `manifold::polytope4::clifford_torus`
    """
    ...

def clifford_torus_mesh(nu: int, nv: int, r: float) -> list[Vec4]:
    """
Sampled Clifford torus grid.

Rust: `manifold::polytope4::clifford_torus_mesh`
    """
    ...

def hypersphere_s3_points(n: int) -> list[Vec4]:
    """
Near-uniform points on S3 (from the super-Fibonacci quaternions).

Rust: `manifold::polytope4::hypersphere_s3_points`
    """
    ...

def hypersphere_volume(r: float, n: int) -> float:
    """
Volume of the n-ball of radius r (alias into the spherical module).

Rust: `manifold::polytope4::hypersphere_volume`
    """
    ...

def simplex_n(n: int) -> tuple[list[VecN], list[tuple[int, int]]]:
    """
Regular n-simplex: n+1 vertices in R^n, unit circumradius, with edges.

Rust: `manifold::polytope4::simplex_n`
    """
    ...

def hypercube_n(n: int) -> tuple[list[VecN], list[tuple[int, int]]]:
    """
n-cube vertices (coordinates +-1/2) with edges.

Rust: `manifold::polytope4::hypercube_n`
    """
    ...

def cross_polytope_n(n: int) -> tuple[list[VecN], list[tuple[int, int]]]:
    """
n-dimensional cross-polytope (unit vertices +-e_i) with edges.

Rust: `manifold::polytope4::cross_polytope_n`
    """
    ...

def hypercube_graph_n(n: int) -> list[tuple[int, int]]:
    """
Edges of the n-cube graph (bitmask vertices, Hamming distance 1).

Rust: `manifold::polytope4::hypercube_graph_n`
    """
    ...

def project_n_to_3(points: list[VecN | Sequence[float]], basis: list[VecN | Sequence[float]]) -> list[Vec3]:
    """
Project n-dimensional points into 3D with the given (orthonormal) basis.

Rust: `manifold::polytope4::project_n_to_3`
    """
    ...

def project_n_to_2(points: list[VecN | Sequence[float]], basis: list[VecN | Sequence[float]]) -> list[Vec2]:
    """
Project n-dimensional points into 2D.

Rust: `manifold::polytope4::project_n_to_2`
    """
    ...

def petrie_polygon_projection(p: Polytope4) -> list[Vec2]:
    """
Petrie polygon projection of a regular 4-polytope: its vertices
projected into the Coxeter plane of the matching symmetry group, using
root systems realized in the polytope's own coordinates.

Rust: `manifold::polytope4::petrie_polygon_projection`
    """
    ...

def coxeter_plane_projection(vertices: list[VecN | Sequence[float]], group: str) -> list[Vec2]:
    """
Project points into the Coxeter plane of the given group ("B4", "D4",
"F4", "H4", "E8"), with root systems in standard coordinates. The plane
is the invariant plane of a Coxeter element, found as the 2D eigenspace
of (w + w^T)/2 with eigenvalue cos(2 pi/h).

Rust: `manifold::polytope4::coxeter_plane_projection`
    """
    ...

def e8_roots() -> list[VecN]:
    """
The 240 roots of E8 (norm sqrt 2).

Rust: `manifold::polytope4::e8_roots`
    """
    ...

def e8_lattice_nearest(p: VecN | Sequence[float]) -> VecN:
    """
Nearest E8 lattice point (D8 plus glue-vector decoding).

Rust: `manifold::polytope4::e8_lattice_nearest`
    """
    ...

def leech_lattice_min_vectors_count() -> int:
    """
The Leech lattice minimal-vector count (kissing number in 24D).

Rust: `manifold::polytope4::leech_lattice_min_vectors_count`
    """
    ...

def d4_lattice_points(r: float) -> list[Vec4]:
    """
D4 lattice points (integer coordinates, even sum) within radius r.

Rust: `manifold::polytope4::d4_lattice_points`
    """
    ...

def f4_roots() -> list[Vec4]:
    """
The 48 roots of F4.

Rust: `manifold::polytope4::f4_roots`
    """
    ...

def h4_roots() -> list[Vec4]:
    """
The 120 roots of H4 (the unit icosians).

Rust: `manifold::polytope4::h4_roots`
    """
    ...

def kissing_number_known(n: int) -> Optional[int]:
    """
Known kissing numbers by dimension.

Rust: `manifold::polytope4::kissing_number_known`
    """
    ...

def hypercube_slicing_volume(n: int, s: float) -> float:
    """
(n-1)-volume of the slice of the unit n-cube `[0,1]^n` by the hyperplane
sum(x) = s, times sqrt(n) (the Irwin-Hall density scaled to a volume).

Rust: `manifold::polytope4::hypercube_slicing_volume`
    """
    ...

def hypersphere_cap_fraction(n: int, theta: float) -> float:
    """
Fraction of the (n-1)-sphere's surface within angle theta of a pole:
regularized incomplete beta I_{sin^2 theta}((n-1)/2, 1/2) / 2 for
theta <= pi/2.

Rust: `manifold::polytope4::hypersphere_cap_fraction`
    """
    ...

def gaussian_concentration_radius(n: int) -> float:
    """
Gaussian mass concentrates at radius sqrt(n).

Rust: `manifold::polytope4::gaussian_concentration_radius`
    """
    ...

def random_walk_n_return_prob(n: int, steps: int) -> float:
    """
Monte Carlo probability that a simple random walk on Z^n returns to the
origin within `steps` steps (deterministic internal seed).

Rust: `manifold::polytope4::random_walk_n_return_prob`
    """
    ...

def volume_ball_vs_cube_ratio(n: int) -> float:
    """
Ratio of the volume of the inscribed ball to the unit cube in n
dimensions (goes to zero fast).

Rust: `manifold::polytope4::volume_ball_vs_cube_ratio`
    """
    ...
