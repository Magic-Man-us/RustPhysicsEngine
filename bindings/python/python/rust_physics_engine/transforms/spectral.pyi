"""
Spectral estimation: periodogram, Welch averaging, multitaper (DPSS), parametric AR models (Burg, Yule-Walker), MUSIC, cross-spectra, coherence, cepstra, Lomb-Scargle, and spectrum descriptors.  All PSDs are one-sided densities in units²/Hz: integrating them over frequency (trapezoid over the returned grid) recovers the signal's variance/power.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.dsp.windows import WindowKind

def periodogram(x: list[float], fs: float, window_kind: WindowKind) -> tuple[list[float], list[float]]:
    """
Windowed periodogram: (frequencies, one-sided PSD).

Panics:
Panics on an empty signal.

Rust: `transforms::spectral::periodogram`
    """
    ...

def welch(x: list[float], fs: float, nperseg: int, noverlap: int, window_kind: WindowKind) -> tuple[list[float], list[float]]:
    """
Welch's method: average windowed periodograms of overlapping
segments (detrended by segment mean removal).

Panics:
Panics unless `0 < noverlap < nperseg <= x.len()`.

Rust: `transforms::spectral::welch`
    """
    ...

def dpss(n: int, nw: float, k: int) -> list[list[float]]:
    """
Discrete prolate spheroidal (Slepian) sequences: the first k tapers
of length n at time-bandwidth product nw, from the tridiagonal
eigenproblem. Each taper has unit energy; sign convention: positive
mean (even tapers) / positive first lag (odd tapers).

Panics:
Panics if `k == 0`, `k > n`, or the eigen solve fails.

Rust: `transforms::spectral::dpss`
    """
    ...

def multitaper(x: list[float], fs: float, nw: float, k: int) -> tuple[list[float], list[float]]:
    """
Thomson multitaper PSD estimate with k DPSS tapers.

Rust: `transforms::spectral::multitaper`
    """
    ...

def burg_ar(x: list[float], order: int) -> tuple[list[float], float]:
    """
Burg's method AR(p) fit: returns (a, σ²) for the model
x\\[n\\] = Σ a\\[k\\]·x\\[n−1−k\\] + e\\[n\\] with prediction-error variance σ².

Panics:
Panics unless `0 < order < x.len()`.

Rust: `transforms::spectral::burg_ar`
    """
    ...

def yule_walker_ar(x: list[float], order: int) -> tuple[list[float], float]:
    """
Yule-Walker AR(p) fit via the autocorrelation method (Levinson-style
dense solve): same conventions as `burg_ar`.

Panics:
Panics unless `0 < order < x.len()` and the autocorrelation system is
nonsingular.

Rust: `transforms::spectral::yule_walker_ar`
    """
    ...

def ar_psd(coeffs: list[float], sigma2: float, fs: float, n: int) -> tuple[list[float], list[float]]:
    """
One-sided PSD of an AR model (a, σ²) on n frequency points up to
Nyquist: σ²/(fs·|1 − Σ a\\[k\\] e^(−jω(k+1))|²), doubled off DC/Nyquist.

Rust: `transforms::spectral::ar_psd`
    """
    ...

def music(x: list[float], n_sources: int, order: int, fs: float, n: int) -> tuple[list[float], list[float]]:
    """
MUSIC pseudospectrum for real sinusoids: correlation matrix of
dimension `order`, signal subspace of dimension 2·n_sources, and the
noise-subspace projection evaluated at n frequencies up to Nyquist.

Panics:
Panics unless `2*n_sources < order < x.len()`.

Rust: `transforms::spectral::music`
    """
    ...

def cross_spectral_density(x: list[float], y: list[float], fs: float, nperseg: int) -> tuple[list[float], list[complex]]:
    """
Welch-averaged cross-spectral density S_xy(f) = E\\[X*(f)·Y(f)\\]
(Hann window, 50% overlap): (frequencies, complex CSD).

Panics:
Panics unless both signals have at least `nperseg` samples.

Rust: `transforms::spectral::cross_spectral_density`
    """
    ...

def coherence(x: list[float], y: list[float], fs: float, nperseg: int) -> tuple[list[float], list[float]]:
    """
Magnitude-squared coherence |S_xy|²/(S_xx·S_yy) on the Welch grid.

Rust: `transforms::spectral::coherence`
    """
    ...

def transfer_function_estimate(input: list[float], output: list[float], fs: float, nperseg: int) -> tuple[list[float], list[complex]]:
    """
H1 transfer-function estimate S_xy/S_xx from input to output.

Rust: `transforms::spectral::transfer_function_estimate`
    """
    ...

def cepstrum_real(x: list[float]) -> list[float]:
    """
Real cepstrum: IFFT of log |X(f)| (real part).

Rust: `transforms::spectral::cepstrum_real`
    """
    ...

def cepstrum_power(x: list[float]) -> list[float]:
    """
Power cepstrum: IFFT of log |X(f)|², i.e. twice the real cepstrum.

Rust: `transforms::spectral::cepstrum_power`
    """
    ...

def lomb_scargle(t: list[float], y: list[float], freqs: list[float]) -> list[float]:
    """
Lomb-Scargle normalized periodogram for unevenly sampled data at the
requested frequencies (Hz). Values are in the classical normalization
(power / 2σ²).

Panics:
Panics if `t` and `y` lengths differ or fewer than 2 samples.

Rust: `transforms::spectral::lomb_scargle`
    """
    ...

def spectral_entropy(psd: list[float]) -> float:
    """
Spectral entropy of a PSD, normalized to \\[0, 1\\].

Rust: `transforms::spectral::spectral_entropy`
    """
    ...

def spectral_flatness(psd: list[float]) -> float:
    """
Spectral flatness (Wiener entropy): geometric over arithmetic mean.

Rust: `transforms::spectral::spectral_flatness`
    """
    ...

def detrend(x: list[float], order: int) -> list[float]:
    """
Remove a least-squares polynomial trend of the given order.

Panics:
Panics if the fit system is singular (order too high for the data).

Rust: `transforms::spectral::detrend`
    """
    ...

def power_law_fit(f: list[float], psd: list[float], f_min: float, f_max: float) -> tuple[float, float]:
    """
Fit PSD ≈ A·f^(−α) over \\[f_min, f_max\\] by log-log linear regression;
returns (α, A).

Rust: `transforms::spectral::power_law_fit`
    """
    ...
