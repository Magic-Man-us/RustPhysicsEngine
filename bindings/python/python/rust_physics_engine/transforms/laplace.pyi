"""
Laplace-domain tools: numerical inverse transforms (fixed-Talbot and Gaver-Stehfest), the z-transform, transfer-function responses, and a discrete fractional Fourier transform.  Polynomial coefficient conventions: s-domain polynomials are highest-power-first (like `numerical::polynomial_roots`); digital filter coefficient arrays are in z⁻¹ powers (b\\[0\\] + b\\[1\\]z⁻¹ + …).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def inverse_laplace_talbot(f: Callable[[complex], complex], t: float, m: int) -> float:
    """
Fixed-Talbot inverse Laplace transform (Abate & Valkó 2004) with m
contour nodes: f(t) from F(s) for t > 0.

Panics:
Panics if `t <= 0` or `m < 2`.

Rust: `transforms::laplace::inverse_laplace_talbot`
    """
    ...

def inverse_laplace_stehfest(f: Callable[[float], float], t: float, n: int) -> float:
    """
Gaver-Stehfest inverse Laplace transform: needs only real F(s)
evaluations. `n` must be even (12-16 is typical; larger n needs more
precision than f64 can give).

Panics:
Panics if `t <= 0`, n is odd, or n > 18.

Rust: `transforms::laplace::inverse_laplace_stehfest`
    """
    ...

def laplace_numeric(f: Callable[[float], float], s: float, t_max: float, n: int) -> float:
    """
Forward Laplace transform F(s) = ∫₀^tmax f(t)e^(−st) dt by composite
Simpson quadrature (n panels, n rounded up to even).

Rust: `transforms::laplace::laplace_numeric`
    """
    ...

def z_transform_eval(x: list[float], z: complex) -> complex:
    """
Evaluate the (one-sided) z-transform X(z) = Σ x\\[n\\]·z^(−n).

Rust: `transforms::laplace::z_transform_eval`
    """
    ...

def impulse_response_from_tf(num: list[float], den: list[float], n: int) -> list[float]:
    """
Impulse response of the digital transfer function
H(z) = (num\\[0\\] + num\\[1\\]z⁻¹ + …)/(den\\[0\\] + den\\[1\\]z⁻¹ + …),
by running the difference equation for n samples.

Panics:
Panics if `den` is empty or `den[0] == 0`.

Rust: `transforms::laplace::impulse_response_from_tf`
    """
    ...

def s_domain_freq_response(num: list[float], den: list[float], omega: list[float]) -> list[complex]:
    """
Continuous-time frequency response H(jω) of num(s)/den(s)
(highest-power-first coefficients) at each ω.

Rust: `transforms::laplace::s_domain_freq_response`
    """
    ...

def digital_freq_response(b: list[float], a: list[float], n_points: int) -> tuple[list[float], list[complex]]:
    """
Digital frequency response of b(z⁻¹)/a(z⁻¹) at `n_points` normalized
frequencies spanning [0, 0.5]; returns (frequencies, response).

Panics:
Panics if `n_points < 2`.

Rust: `transforms::laplace::digital_freq_response`
    """
    ...

def fractional_fourier(x: list[complex], alpha: float) -> list[complex]:
    """
Discrete fractional Fourier transform of angle `alpha` (α = π/2 is
the unitary DFT), via the eigendecomposition of the Candan-Kutay-
Ozaktas commuting matrix: F^α = Σ_k e^(−i·k·α)·u_k·(u_kᵀx). O(n²)
after an O(n³) eigen solve, intended for moderate n.

Errors:
Returns an error if the eigen decomposition fails to converge.

Rust: `transforms::laplace::fractional_fourier`
    """
    ...
