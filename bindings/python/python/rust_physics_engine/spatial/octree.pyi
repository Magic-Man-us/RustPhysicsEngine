"""
Barnes-Hut octree for N-body force approximation.  Direct summation costs O(N²). The octree groups distant bodies and treats each group as a single mass at its centre of mass, which brings the cost to O(N log N).  The approximation is controlled by `theta`: a node is used as a whole when its width divided by the distance to it is below that threshold. Smaller `theta` is more accurate and slower, and `theta = 0` degenerates to direct summation. The conventional default of 0.5 is `BH_THETA`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.astrophysics.nbody import Body
from rust_physics_engine.math import Vec3

class Octree:
    """

Rust: `spatial::octree::Octree`
    """
    @staticmethod
    def build(bodies: list[Body]) -> Octree: ...
    def compute_acceleration(self, bodies: list[Body], idx: int, theta: float, softening: float) -> Vec3: ...

def compute_all_accelerations(bodies: list[Body], theta: float, softening: float) -> list[Vec3]:
    """
Computes accelerations for all bodies, using direct summation below the crossover threshold or Barnes-Hut above it.

Rust: `spatial::octree::compute_all_accelerations`
    """
    ...

BH_THETA: float

BH_CROSSOVER: int
