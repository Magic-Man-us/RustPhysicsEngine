"""
Closest-point queries and set distances.  References: Ericson, *Real-Time Collision Detection*, ch. 5 (point and segment queries); Eiter & Mannila 1994 (discrete Fréchet distance).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.spatial.primitives import Aabb
from rust_physics_engine.spatial.primitives import Obb
from rust_physics_engine.spatial.primitives import Polygon2
from rust_physics_engine.spatial.primitives import Polyline
from rust_physics_engine.spatial.primitives import Segment
from rust_physics_engine.spatial.primitives import Sphere
from rust_physics_engine.spatial.primitives import Triangle
from rust_physics_engine.math import Vec2
from rust_physics_engine.math import Vec3

def closest_point_segment(p: Vec3 | Sequence[float], s: Segment) -> tuple[Vec3, float]:
    """
Closest point on a segment and its parameter t ∈ [0, 1].

Rust: `spatial::distance::closest_point_segment`
    """
    ...

def closest_point_segment_2d(p: Vec2 | Sequence[float], s: Segment2) -> tuple[Vec2, float]:
    """
2-D closest point on a segment and its parameter.

Rust: `spatial::distance::closest_point_segment_2d`
    """
    ...

def closest_point_triangle(p: Vec3 | Sequence[float], t: Triangle) -> Vec3:
    """
Closest point on a triangle (Ericson RTCD §5.1.5 Voronoi-region
walk).

Rust: `spatial::distance::closest_point_triangle`
    """
    ...

def closest_point_plane(p: Vec3 | Sequence[float], pl: Plane) -> Vec3:
    """
Orthogonal projection onto a plane.

Rust: `spatial::distance::closest_point_plane`
    """
    ...

def closest_point_aabb(p: Vec3 | Sequence[float], b: Aabb) -> Vec3:
    """
Componentwise clamp onto an AABB.

Rust: `spatial::distance::closest_point_aabb`
    """
    ...

def closest_point_obb(p: Vec3 | Sequence[float], b: Obb) -> Vec3:
    """
Clamp in the box's local frame (RTCD §5.1.4).

Rust: `spatial::distance::closest_point_obb`
    """
    ...

def closest_point_sphere(p: Vec3 | Sequence[float], s: Sphere) -> Vec3:
    """
Closest point on a sphere's surface (center maps to +radius·x̂).

Rust: `spatial::distance::closest_point_sphere`
    """
    ...

def closest_points_segments(s1: Segment, s2: Segment) -> tuple[Vec3, Vec3, float]:
    """
Closest points between two segments and their distance
(RTCD §5.1.9).

Rust: `spatial::distance::closest_points_segments`
    """
    ...

def closest_points_lines(p1: Vec3 | Sequence[float], d1: Vec3 | Sequence[float], p2: Vec3 | Sequence[float], d2: Vec3 | Sequence[float]) -> Optional[tuple[Vec3, Vec3]]:
    """
Closest points between two infinite lines; `None` when parallel.

Rust: `spatial::distance::closest_points_lines`
    """
    ...

def distance_point_segment(p: Vec3 | Sequence[float], s: Segment) -> float:
    """
Distance from a point to a segment.

Rust: `spatial::distance::distance_point_segment`
    """
    ...

def distance_point_triangle(p: Vec3 | Sequence[float], t: Triangle) -> float:
    """
Distance from a point to a triangle.

Rust: `spatial::distance::distance_point_triangle`
    """
    ...

def distance_point_polyline(p: Vec3 | Sequence[float], pl: Polyline) -> tuple[float, int, float]:
    """
Distance from a point to a polyline, with the segment index and
parameter of the closest point.

Panics:
Panics on a polyline with no segments.

Rust: `spatial::distance::distance_point_polyline`
    """
    ...

def distance_point_polygon_2d(p: Vec2 | Sequence[float], poly: Polygon2) -> float:
    """
Unsigned distance from a point to the boundary of a polygon.

Rust: `spatial::distance::distance_point_polygon_2d`
    """
    ...

def distance_segment_triangle(s: Segment, t: Triangle) -> float:
    """
Distance between a segment and a triangle: minimum over segment vs
the three edges and the endpoints vs the face.

Rust: `spatial::distance::distance_segment_triangle`
    """
    ...

def distance_aabb_aabb(a: Aabb, b: Aabb) -> float:
    """
Distance between two AABBs (0 when overlapping).

Rust: `spatial::distance::distance_aabb_aabb`
    """
    ...

def hausdorff_distance(a: list[Vec3 | Sequence[float]], b: list[Vec3 | Sequence[float]]) -> float:
    """
Symmetric Hausdorff distance between two 3-D point sets.

Panics:
Panics when either set is empty.

Rust: `spatial::distance::hausdorff_distance`
    """
    ...

def hausdorff_distance_2d(a: list[Vec2 | Sequence[float]], b: list[Vec2 | Sequence[float]]) -> float:
    """
Symmetric Hausdorff distance between two 2-D point sets.

Panics:
Panics when either set is empty.

Rust: `spatial::distance::hausdorff_distance_2d`
    """
    ...

def frechet_distance_2d(a: list[Vec2 | Sequence[float]], b: list[Vec2 | Sequence[float]]) -> float:
    """
Discrete Fréchet distance between two 2-D polygonal curves
(Eiter & Mannila dynamic program).

Panics:
Panics when either curve is empty.

Rust: `spatial::distance::frechet_distance_2d`
    """
    ...
