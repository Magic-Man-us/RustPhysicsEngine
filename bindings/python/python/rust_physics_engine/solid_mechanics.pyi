"""
Strength of materials: stress, strain, elastic constants and beams.  Engineering and true stress and strain, and the elastic constants with the identities that connect them -- any two of `E`, `G`, `K` and `ν` determine the other two for an isotropic material, and the conversions are all here.  Beam bending: cantilever and simply-supported deflections, bending moment and stress, and second moments of area for rectangular and circular sections. Design closes with the von Mises equivalent stress, the safety factor, and strain energy density.  For the tensor formulation and yield surfaces see `continuum_mechanics`; for finite-element beams and modal analysis see `resonance::structural`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.statistics.distributions import Poisson

def tensile_stress(force: float, area: float) -> float:
    """
Tensile stress: σ = F/A

Rust: `solid_mechanics::tensile_stress`
    """
    ...

def tensile_strain(delta_l: float, original_l: float) -> float:
    """
Tensile strain: ε = ΔL/L₀

Rust: `solid_mechanics::tensile_strain`
    """
    ...

def shear_stress(force: float, area: float) -> float:
    """
Shear stress: τ = F/A

Rust: `solid_mechanics::shear_stress`
    """
    ...

def shear_strain(displacement: float, height: float) -> float:
    """
Shear strain: γ = Δx/h

Rust: `solid_mechanics::shear_strain`
    """
    ...

def volumetric_strain(delta_v: float, original_v: float) -> float:
    """
Volumetric strain: εv = ΔV/V₀

Rust: `solid_mechanics::volumetric_strain`
    """
    ...

def true_stress(engineering_stress: float, engineering_strain: float) -> float:
    """
True stress from engineering values: σ_true = σ_eng(1 + ε_eng)

Rust: `solid_mechanics::true_stress`
    """
    ...

def true_strain(engineering_strain: float) -> float:
    """
True strain from engineering strain: ε_true = ln(1 + ε_eng)

Rust: `solid_mechanics::true_strain`
    """
    ...

def youngs_modulus(stress: float, strain: float) -> float:
    """
Young's modulus (elastic modulus): E = σ/ε

Rust: `solid_mechanics::youngs_modulus`
    """
    ...

def shear_modulus(shear_stress: float, shear_strain: float) -> float:
    """
Shear modulus: G = τ/γ

Rust: `solid_mechanics::shear_modulus`
    """
    ...

def bulk_modulus(pressure: float, volumetric_strain: float) -> float:
    """
Bulk modulus: K = -P/εv

Rust: `solid_mechanics::bulk_modulus`
    """
    ...

def poisson_ratio_from_moduli(e: float, g: float) -> float:
    """
Poisson's ratio from elastic moduli: ν = E/(2G) - 1

Rust: `solid_mechanics::poisson_ratio_from_moduli`
    """
    ...

def e_from_k_and_g(bulk: float, shear: float) -> float:
    """
Young's modulus from bulk and shear moduli: E = 9KG/(3K + G)

Rust: `solid_mechanics::e_from_k_and_g`
    """
    ...

def bulk_from_e_and_nu(e: float, nu: float) -> float:
    """
Bulk modulus from Young's modulus and Poisson's ratio: K = E/(3(1 - 2ν))

Rust: `solid_mechanics::bulk_from_e_and_nu`
    """
    ...

def shear_from_e_and_nu(e: float, nu: float) -> float:
    """
Shear modulus from Young's modulus and Poisson's ratio: G = E/(2(1 + ν))

Rust: `solid_mechanics::shear_from_e_and_nu`
    """
    ...

def beam_deflection_cantilever_point(force: float, length: float, e: float, i: float) -> float:
    """
Cantilever beam tip deflection under point load: δ = FL³/(3EI)

Rust: `solid_mechanics::beam_deflection_cantilever_point`
    """
    ...

def beam_deflection_simply_supported_center(force: float, length: float, e: float, i: float) -> float:
    """
Simply supported beam center deflection under point load: δ = FL³/(48EI)

Rust: `solid_mechanics::beam_deflection_simply_supported_center`
    """
    ...

def bending_moment(force: float, distance: float) -> float:
    """
Bending moment: M = F·d

Rust: `solid_mechanics::bending_moment`
    """
    ...

def bending_stress(moment: float, y: float, i: float) -> float:
    """
Bending stress at distance y from neutral axis: σ = My/I

Rust: `solid_mechanics::bending_stress`
    """
    ...

def second_moment_rectangle(width: float, height: float) -> float:
    """
Second moment of area for a rectangle: I = bh³/12

Rust: `solid_mechanics::second_moment_rectangle`
    """
    ...

def second_moment_circle(radius: float) -> float:
    """
Second moment of area for a circle: I = πr⁴/4

Rust: `solid_mechanics::second_moment_circle`
    """
    ...

def von_mises_stress(s1: float, s2: float, s3: float) -> float:
    """
Von Mises equivalent stress: σ_vm = √(((σ₁-σ₂)² + (σ₂-σ₃)² + (σ₃-σ₁)²)/2)

Rust: `solid_mechanics::von_mises_stress`
    """
    ...

def safety_factor(yield_strength: float, applied_stress: float) -> float:
    """
Safety factor: n = σ_yield/σ_applied

Rust: `solid_mechanics::safety_factor`
    """
    ...

def strain_energy_density(stress: float, strain: float) -> float:
    """
Elastic strain energy density: u = σε/2

Rust: `solid_mechanics::strain_energy_density`
    """
    ...
