"""
Semiconductor device physics.  Carrier statistics -- the intrinsic concentration, Fermi-Dirac occupancy, the thermal voltage `kT/q` -- and transport by drift and diffusion, linked by the Einstein relation `D/μ = kT/q`.  Then the devices: the PN junction's built-in potential and depletion width, the Shockley diode equation, MOSFET drain current in the linear and saturation regimes, and solar cells through open-circuit voltage, fill factor and efficiency.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def intrinsic_carrier_concentration(nc: float, nv: float, band_gap: float, temperature: float) -> float:
    """
Intrinsic carrier concentration: ni = sqrt(Nc * Nv) * exp(-Eg / (2kT))

Rust: `electronics::intrinsic_carrier_concentration`
    """
    ...

def fermi_dirac(energy: float, fermi_level: float, temperature: float) -> float:
    """
Fermi-Dirac distribution: f(E) = 1 / (1 + exp((E - Ef) / (kT)))

Rust: `electronics::fermi_dirac`
    """
    ...

def thermal_voltage(temperature: float) -> float:
    """
Thermal voltage: Vt = kT / q

Rust: `electronics::thermal_voltage`
    """
    ...

def conductivity(carrier_density: float, mobility: float, charge: float) -> float:
    """
Electrical conductivity: sigma = n * q * mu

Rust: `electronics::conductivity`
    """
    ...

def resistivity(conductivity: float) -> float:
    """
Resistivity: rho = 1 / sigma

Rust: `electronics::resistivity`
    """
    ...

def drift_velocity(mobility: float, electric_field: float) -> float:
    """
Drift velocity: vd = mu * E

Rust: `electronics::drift_velocity`
    """
    ...

def diffusion_coefficient_einstein(mobility: float, temperature: float) -> float:
    """
Einstein relation for diffusion coefficient: D = mu * kT / q

Rust: `electronics::diffusion_coefficient_einstein`
    """
    ...

def built_in_potential(na: float, nd: float, ni: float, temperature: float) -> float:
    """
Built-in potential of a p-n junction: Vbi = (kT/q) * ln(Na * Nd / ni^2)

Rust: `electronics::built_in_potential`
    """
    ...

def depletion_width(epsilon: float, vbi: float, na: float, nd: float, charge: float) -> float:
    """
Depletion region width: W = sqrt(2 * epsilon * Vbi * (1/Na + 1/Nd) / q)

Rust: `electronics::depletion_width`
    """
    ...

def diode_current(is_: float, voltage: float, temperature: float, n: float) -> float:
    """
Shockley diode equation: I = Is * (exp(V / (n * Vt)) - 1)

Rust: `electronics::diode_current`
    """
    ...

def diode_reverse_saturation(area: float, ni: float, dn: float, dp: float, ln: float, lp: float, charge: float) -> float:
    """
Reverse saturation current (symmetric approximation):
Is = q * A * ni^2 * (Dn/Ln + Dp/Lp)

Rust: `electronics::diode_reverse_saturation`
    """
    ...

def mosfet_drain_current_linear(mu: float, cox: float, w: float, l: float, vgs: float, vth: float, vds: float) -> float:
    """
MOSFET drain current in the linear region:
Id = mu * Cox * (W/L) * ((Vgs - Vth) * Vds - Vds^2 / 2)

Rust: `electronics::mosfet_drain_current_linear`
    """
    ...

def mosfet_drain_current_saturation(mu: float, cox: float, w: float, l: float, vgs: float, vth: float) -> float:
    """
MOSFET drain current in the saturation region:
Id = (mu * Cox / 2) * (W/L) * (Vgs - Vth)^2

Rust: `electronics::mosfet_drain_current_saturation`
    """
    ...

def solar_cell_current(photocurrent: float, dark_current: float, voltage: float, temperature: float) -> float:
    """
Solar cell current: I = Iph - I0 * (exp(V / Vt) - 1)

Rust: `electronics::solar_cell_current`
    """
    ...

def open_circuit_voltage(photocurrent: float, dark_current: float, temperature: float) -> float:
    """
Open-circuit voltage: Voc = Vt * ln(Iph / I0 + 1)

Rust: `electronics::open_circuit_voltage`
    """
    ...

def fill_factor(voc: float, isc: float, pmax: float) -> float:
    """
Fill factor: FF = Pmax / (Voc * Isc)

Rust: `electronics::fill_factor`
    """
    ...

def solar_cell_efficiency(pmax: float, incident_power: float) -> float:
    """
Solar cell efficiency: eta = Pmax / Pin

Rust: `electronics::solar_cell_efficiency`
    """
    ...
