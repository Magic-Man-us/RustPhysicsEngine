"""
Optimization: continuous, combinatorial, and strategic.  The module root holds the scalar and unconstrained-gradient methods -- golden section and Brent for a bracketed minimum of one variable, then gradient descent with and without momentum, Adam, numerical gradients, and the regression and curve fitting built on them.  The submodules take it further: `lp` for linear programming and duality, `integer` for branch-and-bound and dynamic programming, `network` for flows and scheduling, `convex` for L-BFGS, proximal methods and ADMM, `metaheuristics` for the derivative-free and population-based methods, `game_theory` for equilibria and cooperative solutions, and `least_squares` for Levenberg-Marquardt.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import convex, game_theory, integer, least_squares, lp, metaheuristics, network
from rust_physics_engine.optimization.least_squares import LmResult as LmResult
from rust_physics_engine.optimization.least_squares import fit_exponential_decay as fit_exponential_decay
from rust_physics_engine.optimization.least_squares import fit_gaussian_peak as fit_gaussian_peak

def golden_section_min(f: Callable[[float], float], a: float, b: float, tol: float, max_iter: int) -> float:
    """
Golden-section search for the minimum of `f` on `[a, b]`.

Returns the x value that minimizes f within tolerance `tol`.

Rust: `optimization::golden_section_min`
    """
    ...

def brent_min(f: Callable[[float], float], a: float, b: float, tol: float, max_iter: int) -> float:
    """
Brent's method for 1-D minimization, combining golden-section search with
parabolic interpolation.

Rust: `optimization::brent_min`
    """
    ...

def numerical_gradient_vec(f: Callable[[list[float]], float], x: list[float], h: float) -> list[float]:
    """
Central-difference numerical gradient of a scalar function of n variables.

Rust: `optimization::numerical_gradient_vec`
    """
    ...

def gradient_descent(f: Callable[[list[float]], float], grad: Callable[[list[float]], list[float]], x0: list[float], learning_rate: float, tol: float, max_iter: int) -> list[float]:
    """
Vanilla gradient descent: x ← x − α∇f.

Rust: `optimization::gradient_descent`
    """
    ...

def gradient_descent_momentum(f: Callable[[list[float]], float], grad: Callable[[list[float]], list[float]], x0: list[float], learning_rate: float, momentum: float, tol: float, max_iter: int) -> list[float]:
    """
Gradient descent with momentum: v ← μv − α∇f, x ← x + v.

Rust: `optimization::gradient_descent_momentum`
    """
    ...

def adam(f: Callable[[list[float]], float], grad: Callable[[list[float]], list[float]], x0: list[float], learning_rate: float, tol: float, max_iter: int) -> list[float]:
    """
Adam optimizer (β1=0.9, β2=0.999, ε=1e-8).

Rust: `optimization::adam`
    """
    ...

def nelder_mead(f: Callable[[list[float]], float], x0: list[float], step: float, tol: float, max_iter: int) -> list[float]:
    """
Nelder-Mead simplex algorithm for unconstrained minimization.

Rust: `optimization::nelder_mead`
    """
    ...

def simulated_annealing(f: Callable[[list[float]], float], x0: list[float], temp_initial: float, cooling_rate: float, step_size: float, max_iter: int) -> list[float]:
    """
Simulated annealing for unconstrained minimization.

T = temp_initial × cooling_rate^iter. Accepts uphill moves with probability
exp(−ΔE / T). LCG seeded deterministically from `x0`.

Rust: `optimization::simulated_annealing`
    """
    ...

def linear_regression(x: list[float], y: list[float]) -> tuple[float, float]:
    """
Ordinary linear regression: minimizes ‖a0 + a1·x − y‖₂ via
Householder-QR least squares (`linalg::qr::least_squares`).

Returns `(slope, intercept)`.

Rust: `optimization::linear_regression`
    """
    ...

def polynomial_fit(x: list[float], y: list[float], degree: int) -> list[float]:
    """
Fit a polynomial of the given degree to (x, y) data by QR least
squares on the Vandermonde matrix, falling back to normal equations
with Gaussian elimination when the system is rank deficient.

Returns coefficients `[a0, a1, …, a_degree]` such that
ŷ = a0 + a1·x + a2·x² + …

Rust: `optimization::polynomial_fit`
    """
    ...

def r_squared(y_actual: list[float], y_predicted: list[float]) -> float:
    """
Coefficient of determination R² = 1 − SS_res / SS_tot.

Rust: `optimization::r_squared`
    """
    ...
