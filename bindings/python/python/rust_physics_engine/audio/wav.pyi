"""
WAV (RIFF) reading and writing: PCM 8/16/24/32-bit, IEEE float 32/64-bit, and WAVE_FORMAT_EXTENSIBLE containers. Samples are normalized to −1..1 per channel.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class WavData:
    """
Decoded audio: sample rate, channel count, and per-channel samples
in −1..1.

Rust: `audio::wav::WavData`
    """
    def __init__(self, fs: int, channels: int, samples: list[list[float]]) -> None: ...
    @property
    def fs(self) -> int: ...
    @property
    def channels(self) -> int: ...
    @property
    def samples(self) -> list[list[float]]: ...

def wav_read(bytes: list[int]) -> WavData:
    """
Decode a WAV byte stream.

Errors:
`InvalidArgument` for malformed containers or unsupported encodings.

Rust: `audio::wav::wav_read`
    """
    ...

def wav_write(data: WavData, bits: int, float: bool) -> list[int]:
    """
Encode to WAV bytes: PCM at 8/16/24/32 bits, or IEEE float at 32/64.

Panics:
Panics for unsupported bit depths or mismatched channel lengths.

Rust: `audio::wav::wav_write`
    """
    ...

def wav_read_file(path: str) -> WavData:
    """
Read a WAV file from disk.

Errors:
I/O errors from the filesystem; decode failures become
`InvalidData`.

Rust: `audio::wav::wav_read_file`
    """
    ...

def wav_write_file(path: str, data: WavData, bits: int, float: bool) -> None:
    """
Write a WAV file to disk.

Errors:
I/O errors from the filesystem.

Rust: `audio::wav::wav_write_file`
    """
    ...

def wav_info(bytes: list[int]) -> tuple[int, int, int, int]:
    """
Header summary (fs, channels, bits, frames) without decoding samples.

Errors:
`InvalidArgument` for malformed containers.

Rust: `audio::wav::wav_info`
    """
    ...

def to_mono(d: WavData) -> list[float]:
    """
Average all channels down to one.

Rust: `audio::wav::to_mono`
    """
    ...

def to_interleaved(d: WavData) -> list[float]:
    """
Interleave channels (frame-major).

Rust: `audio::wav::to_interleaved`
    """
    ...

def from_interleaved(x: list[float], channels: int) -> list[list[float]]:
    """
Split an interleaved stream into per-channel vectors.

Panics:
Panics if `channels == 0`.

Rust: `audio::wav::from_interleaved`
    """
    ...
