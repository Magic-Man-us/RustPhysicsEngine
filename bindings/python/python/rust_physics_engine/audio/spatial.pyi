"""
Spatial audio: panning laws, VBAP, ambisonics, simple binaural cues, Doppler, distance/air attenuation, geometric room acoustics (image source and ray tracing), microphone arrays (beamforming, TDOA localization), sonar, and loudspeaker system responses.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.monte_carlo import Rng
from rust_physics_engine.dsp.iir import Sos
from rust_physics_engine.math import Vec3

def pan_linear(x: float, pos: float) -> tuple[float, float]:
    """
Linear pan law; `pos` in [-1 (left), 1 (right)].

Rust: `audio::spatial::pan_linear`
    """
    ...

def pan_constant_power(x: float, pos: float) -> tuple[float, float]:
    """
Constant-power (-3 dB center) pan law.

Rust: `audio::spatial::pan_constant_power`
    """
    ...

def pan_minus_4_5_db(x: float, pos: float) -> tuple[float, float]:
    """
-4.5 dB-center compromise pan law (geometric mean of the linear and
constant-power laws).

Rust: `audio::spatial::pan_minus_4_5_db`
    """
    ...

def pan_vbap_2d(angle: float, speaker_angles: list[float]) -> list[float]:
    """
2D VBAP: gains for `speaker_angles` (radians, unsorted) reproducing a
source at `angle`; only the flanking pair is nonzero.

Rust: `audio::spatial::pan_vbap_2d`
    """
    ...

def pan_vbap_3d(dir: Vec3 | Sequence[float], speakers: list[Vec3 | Sequence[float]]) -> list[float]:
    """
3D VBAP over speaker triplets: picks the triplet giving all-positive
gains with the best conditioning, normalized to unit power.

Rust: `audio::spatial::pan_vbap_3d`
    """
    ...

def ambisonics_encode_1st(x: float, azimuth: float, elevation: float) -> list[float]:
    """
First-order B-format (FuMa WXYZ) encoding of one sample.

Rust: `audio::spatial::ambisonics_encode_1st`
    """
    ...

def ambisonics_encode(x: float, az: float, el: float, order: int) -> list[float]:
    """
Higher-order ambisonic encoding (ACN channel order, SN3D weights) of
one sample; (order+1)² channels.

Rust: `audio::spatial::ambisonics_encode`
    """
    ...

def ambisonics_decode(b: list[float], speakers: list[tuple[float, float]], order: int) -> list[float]:
    """
Basic projection decode of an ACN/SN3D signal set to speakers at
(azimuth, elevation) pairs.

Rust: `audio::spatial::ambisonics_decode`
    """
    ...

def ambisonics_rotate(b: list[float], yaw: float, pitch: float, roll: float, order: int) -> list[float]:
    """
Rotate an ACN/SN3D ambisonic frame by yaw/pitch/roll, via projection
onto a Fibonacci sphere sampling (exact for band-limited fields as the
sampling is dense; 256 points).

Rust: `audio::spatial::ambisonics_rotate`
    """
    ...

def itd_woodworth(azimuth: float, head_radius: float, c: float) -> float:
    """
Woodworth interaural time difference (s) for a spherical head of
radius `head_radius`; `azimuth` in radians from the median plane.

Rust: `audio::spatial::itd_woodworth`
    """
    ...

def spherical_head_hrtf(azimuth: float, freq: float, head_radius: float, c: float) -> complex:
    """
Duda-Martens (Brown-Duda) spherical-head shadowing filter response at
one ear; `azimuth` is measured from that ear's axis (0 = ipsilateral).

Rust: `audio::spatial::spherical_head_hrtf`
    """
    ...

def ild_spherical_head(azimuth: float, freq: float, head_radius: float) -> float:
    """
Interaural level difference (dB, positive = louder in the near ear)
from the spherical-head model.

Rust: `audio::spatial::ild_spherical_head`
    """
    ...

def binaural_simple(x: list[float], azimuth: float, elevation: float, fs: float) -> tuple[list[float], list[float]]:
    """
Simple binaural rendering: ITD (fractional delay) plus first-order
head-shadow filtering per ear.

Rust: `audio::spatial::binaural_simple`
    """
    ...

def doppler_resample(x: list[float], source_path: Callable[[float], Vec3 | Sequence[float]], listener: Vec3 | Sequence[float], c: float, fs: float) -> list[float]:
    """
Doppler by retarded-time resampling: the source moves along
`source_path(t)`; each output sample reads the emission-time signal
value with 1/r distance attenuation.

Rust: `audio::spatial::doppler_resample`
    """
    ...

def distance_gain(d: float, ref_d: float, rolloff: float) -> float:
    """
Inverse-distance gain with reference distance and rolloff exponent.

Rust: `audio::spatial::distance_gain`
    """
    ...

def air_absorption_filter(d: float, humidity: float, temp: float, fs: float) -> Sos:
    """
Atmospheric absorption over distance `d` approximated as a 2nd-order
Butterworth lowpass whose cutoff gives 3 dB of ISO 9613-style
high-frequency loss at that range.

Rust: `audio::spatial::air_absorption_filter`
    """
    ...

def image_source_ir(room: Vec3 | Sequence[float], source: Vec3 | Sequence[float], listener: Vec3 | Sequence[float], absorption: list[float], max_order: int, fs: float, c: float) -> list[float]:
    """
Shoebox image-source impulse response (Allen-Berkley). `absorption`
holds wall absorption coefficients in the order
[-x, +x, -y, +y, -z, +z].

Rust: `audio::spatial::image_source_ir`
    """
    ...

def ray_tracing_ir(room_mesh: Mesh, source: Vec3 | Sequence[float], listener: Vec3 | Sequence[float], absorption: list[float], n_rays: int, max_bounces: int, fs: float, c: float, rng: Rng) -> list[float]:
    """
Stochastic ray-traced energy impulse response in an arbitrary closed
mesh; `absorption[i]` indexes by triangle material. Amplitude is the
square root of collected energy per sample bin.

Rust: `audio::spatial::ray_tracing_ir`
    """
    ...

def early_reflections(room: Vec3 | Sequence[float], source: Vec3 | Sequence[float], listener: Vec3 | Sequence[float], c: float) -> list[tuple[float, float, Vec3]]:
    """
Direct sound plus the six first-order reflections of a shoebox room:
(arrival time s, 1/(4πd) gain, unit direction of arrival).

Rust: `audio::spatial::early_reflections`
    """
    ...

def beamforming_delay_sum(mics: list[Vec3 | Sequence[float]], signals: list[list[float]], steer: Vec3 | Sequence[float], fs: float, c: float) -> list[float]:
    """
Delay-and-sum beamformer steered toward the unit direction `steer`
(plane-wave model): aligns and averages the mic signals.

Rust: `audio::spatial::beamforming_delay_sum`
    """
    ...

def beamforming_mvdr(mics: list[Vec3 | Sequence[float]], signals: list[list[float]], steer: Vec3 | Sequence[float], freq: float, fs: float, c: float, diagonal_loading: float) -> list[float]:
    """
Narrowband frequency-domain MVDR beamformer at `freq`: per-block
spatial covariance with diagonal loading, steering toward `steer`.

Rust: `audio::spatial::beamforming_mvdr`
    """
    ...

def tdoa_gcc_phat(a: list[float], b: list[float], fs: float) -> float:
    """
GCC-PHAT time difference of arrival: delay of `b` relative to `a` in
seconds (positive = b lags a).

Rust: `audio::spatial::tdoa_gcc_phat`
    """
    ...

def localize_tdoa(mics: list[Vec3 | Sequence[float]], tdoas: list[float], c: float) -> Vec3:
    """
Least-squares source localization from TDOAs relative to `mics[0]`
(`tdoas[i]` is the extra delay at mic i+1), by Gauss-Newton.

Rust: `audio::spatial::localize_tdoa`
    """
    ...

def sonar_range(t_echo: float, c: float) -> float:
    """
Round-trip echo time to range.

Rust: `audio::spatial::sonar_range`
    """
    ...

def sonar_equation(sl: float, tl: float, ts: float, nl: float, di: float) -> float:
    """
Active sonar equation: echo excess = SL - 2 TL + TS - (NL - DI), dB.

Rust: `audio::spatial::sonar_equation`
    """
    ...

def speaker_crossover_lr4(fc: float, fs: float) -> tuple[Sos, Sos]:
    """
Linkwitz-Riley 4th-order crossover: (lowpass, highpass), each two
cascaded 2nd-order Butterworth sections; the pair sums to allpass.

Rust: `audio::spatial::speaker_crossover_lr4`
    """
    ...

def speaker_baffle_step(width_m: float, fs: float) -> Sos:
    """
Baffle-step compensation target: the +6 dB diffraction step of a
baffle of width `width_m`, centered at f3 = 115/width, as a high
shelf.

Rust: `audio::spatial::speaker_baffle_step`
    """
    ...

def thiele_small_response(fs_driver: float, qts: float, vas: float, box_volume: float, f: float) -> float:
    """
Sealed-box (2nd-order highpass) response magnitude in dB of a driver
with free-air resonance `fs_driver`, total Q `qts`, and compliance
volume `vas` in a box of `box_volume` (same units), at frequency `f`.

Rust: `audio::spatial::thiele_small_response`
    """
    ...
