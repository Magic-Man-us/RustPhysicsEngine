"""
Adaptive Runge-Kutta integration: Dormand-Prince 5(4).  Reference: Dormand & Prince, "A family of embedded Runge-Kutta formulae" (1980); Hairer, Nørsett & Wanner, *Solving ODEs I*, §II.4. The embedded 4th-order solution provides the error estimate; steps use the FSAL (first-same-as-last) property.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class AdaptiveResult:
    """
Result of an adaptive integration: accepted step times, states, and
the number of rejected trial steps.

Rust: `numerical::ode::adaptive::AdaptiveResult`
    """
    def __init__(self, t: list[float], y: list[list[float]], steps_rejected: int) -> None: ...
    @property
    def t(self) -> list[float]: ...
    @property
    def y(self) -> list[list[float]]: ...
    @property
    def steps_rejected(self) -> int: ...

def dormand_prince(f: Callable[[float, list[float]], list[float]], t0: float, t1: float, y0: list[float], rtol: float, atol: float, h0: float) -> AdaptiveResult:
    """
Integrates dy/dt = f(t, y) from t0 to t1 with adaptive step size,
recording every accepted step.

The local error per step is kept near `atol + rtol·|y|` (RMS over
components). Fails with `NoConvergence` if the step count budget is
exhausted or the step size underflows.

Rust: `numerical::ode::adaptive::dormand_prince`
    """
    ...

def dormand_prince_dense(f: Callable[[float, list[float]], list[float]], t0: float, t1: float, y0: list[float], rtol: float, atol: float, h0: float, sample_times: list[float]) -> AdaptiveResult:
    """
Like `dormand_prince` but returns the solution interpolated at
`sample_times` (cubic Hermite between accepted steps, using the
stored derivatives at the step endpoints).

`sample_times` must be non-decreasing and lie within [t0, t1].

Rust: `numerical::ode::adaptive::dormand_prince_dense`
    """
    ...
