"""
Biophysics: the elementary membrane, transport and mechanics relations here, with the population-scale models in submodules.  The roadmap calls this area `bio`; it lives under the existing `biophysics` module instead, so that there is one home for the subject rather than two.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import epidemiology, neuro, phylo, population, seq_align
from rust_physics_engine.statistics.distributions import Exponential

def nernst_potential(temperature: float, z: float, c_out: float, c_in: float) -> float:
    """
Nernst equation: E = (RT/(zF)) × ln(c_out/c_in)

Rust: `biophysics::nernst_potential`
    """
    ...

def goldman_potential(temperature: float, pk: float, pna: float, pcl: float, k_out: float, k_in: float, na_out: float, na_in: float, cl_out: float, cl_in: float) -> float:
    """
Goldman-Hodgkin-Katz voltage equation for K⁺, Na⁺, and Cl⁻.
`Vm = (RT/F) × ln((Pk[K]o + Pna[Na]o + Pcl[Cl]i) / (Pk[K]i + Pna[Na]i + Pcl[Cl]o))`

Rust: `biophysics::goldman_potential`
    """
    ...

def resting_membrane_potential_typical() -> float:
    """
Typical resting membrane potential for a neuron: -70 mV

Rust: `biophysics::resting_membrane_potential_typical`
    """
    ...

def michaelis_menten(vmax: float, km: float, substrate: float) -> float:
    """
Michaelis-Menten kinetics: `v = Vmax × [S] / (Km + [S])`

Rust: `biophysics::michaelis_menten`
    """
    ...

def michaelis_menten_inhibited(vmax: float, km: float, substrate: float, inhibitor: float, ki: float) -> float:
    """
Competitive inhibition: `v = Vmax × [S] / (Km(1 + [I]/Ki) + [S])`

Rust: `biophysics::michaelis_menten_inhibited`
    """
    ...

def lineweaver_burk(vmax: float, km: float, substrate: float) -> tuple[float, float]:
    """
Lineweaver-Burk transform: returns `(1/[S], 1/v)` for double-reciprocal plot

Rust: `biophysics::lineweaver_burk`
    """
    ...

def hill_equation(vmax: float, k: float, substrate: float, n: float) -> float:
    """
Hill equation for cooperative binding: `v = Vmax × [S]^n / (K^n + [S]^n)`

Rust: `biophysics::hill_equation`
    """
    ...

def hill_coefficient_from_data(s1: float, v1: float, s2: float, v2: float, vmax: float) -> float:
    """
Derive Hill coefficient from two (substrate, velocity) data points.
n = log((v1/(Vmax-v1)) / (v2/(Vmax-v2))) / log(s1/s2)

Rust: `biophysics::hill_coefficient_from_data`
    """
    ...

def exponential_growth(n0: float, rate: float, time: float) -> float:
    """
Exponential growth: N = N₀ × e^(rt)

Rust: `biophysics::exponential_growth`
    """
    ...

def logistic_growth(n0: float, k: float, r: float, time: float) -> float:
    """
Logistic growth: N = K / (1 + ((K - N₀)/N₀) × e^(-rt))

Rust: `biophysics::logistic_growth`
    """
    ...

def doubling_time_population(rate: float) -> float:
    """
Doubling time: td = ln(2) / r

Rust: `biophysics::doubling_time_population`
    """
    ...

def lotka_volterra_prey(prey: float, predator: float, alpha: float, beta: float) -> float:
    """
Lotka-Volterra prey rate: dx/dt = αx - βxy

Rust: `biophysics::lotka_volterra_prey`
    """
    ...

def lotka_volterra_predator(prey: float, predator: float, delta: float, gamma: float) -> float:
    """
Lotka-Volterra predator rate: dy/dt = δxy - γy

Rust: `biophysics::lotka_volterra_predator`
    """
    ...

def cardiac_output(stroke_volume: float, heart_rate: float) -> float:
    """
Cardiac output: CO = SV × HR (L/min when SV in L/beat and HR in bpm)

Rust: `biophysics::cardiac_output`
    """
    ...

def mean_arterial_pressure(systolic: float, diastolic: float) -> float:
    """
Mean arterial pressure: MAP = DBP + (SBP - DBP)/3

Rust: `biophysics::mean_arterial_pressure`
    """
    ...

def vascular_resistance(pressure_drop: float, flow: float) -> float:
    """
Vascular resistance: R = ΔP / Q

Rust: `biophysics::vascular_resistance`
    """
    ...

def poiseuille_blood_flow(radius: float, pressure_drop: float, viscosity: float, length: float) -> float:
    """
Poiseuille flow in a vessel: Q = πr⁴ΔP / (8μL)

Rust: `biophysics::poiseuille_blood_flow`
    """
    ...

def sigmoid(x: float, x50: float, slope: float) -> float:
    """
Sigmoid function: f = 1 / (1 + exp(-slope × (x - x50)))

Rust: `biophysics::sigmoid`
    """
    ...

def ld50_probit(dose: float, ld50: float, slope: float) -> float:
    """
LD50 probit model: probability = sigmoid(ln(dose), ln(ld50), slope)

Rust: `biophysics::ld50_probit`
    """
    ...

FARADAY_BIO: float

GAS_CONSTANT: float

BODY_TEMP: float
