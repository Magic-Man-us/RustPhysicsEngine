"""
Engineering solids: metals, alloys, polymers and ceramics.  Density, Young's modulus, yield and tensile strength, Poisson's ratio, thermal conductivity and expansion, and specific heat. Room-temperature values; a specific alloy, temper or grade will differ, sometimes substantially.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.statistics.distributions import Poisson

class Material:
    """
Engineering solids: metals, alloys, polymers and ceramics.

Density, Young's modulus, yield and tensile strength, Poisson's ratio,
thermal conductivity and expansion, and specific heat. Room-temperature
values; a specific alloy, temper or grade will differ, sometimes
substantially.
An engineering material with mechanical and thermal properties.

All values use SI units:
- `density`: kg/m^3
- `youngs_modulus`, `yield_strength`, `tensile_strength`: Pa
- `thermal_conductivity`: W/(m*K)
- `specific_heat`: J/(kg*K)
- `thermal_expansion`: 1/K
- `melting_point`: K

Rust: `materials::common::Material`
    """
    @property
    def name(self) -> str: ...
    @property
    def density(self) -> float: ...
    @property
    def youngs_modulus(self) -> float: ...
    @property
    def poisson_ratio(self) -> float: ...
    @property
    def yield_strength(self) -> float: ...
    @property
    def tensile_strength(self) -> float: ...
    @property
    def thermal_conductivity(self) -> float: ...
    @property
    def specific_heat(self) -> float: ...
    @property
    def thermal_expansion(self) -> float: ...
    @property
    def melting_point(self) -> float: ...

def by_name(name: str) -> Optional[Material]:
    """
Looks up a material by name using case-insensitive ASCII comparison.

Zero-allocation: uses `eq_ignore_ascii_case` instead of `to_lowercase`.

Rust: `materials::common::by_name`
    """
    ...

def all() -> list[Material]:
    """
Returns a slice of all common engineering materials.

Rust: `materials::common::all`
    """
    ...
