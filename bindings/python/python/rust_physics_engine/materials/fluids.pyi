"""
Common liquids.  Density, dynamic and kinematic viscosity, surface tension, speed of sound, specific heat, and boiling and freezing points, at room temperature and one atmosphere.  Viscosity is the strongly temperature-dependent one: it can change by a factor of several over a few tens of degrees, so a single figure is only a starting point.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class Fluid:
    """
Common liquids.

Density, dynamic and kinematic viscosity, surface tension, speed of
sound, specific heat, and boiling and freezing points, at room
temperature and one atmosphere.

Viscosity is the strongly temperature-dependent one: it can change by a
factor of several over a few tens of degrees, so a single figure is
only a starting point.
A fluid (liquid) with its mechanical and thermal properties at 20 degrees C
unless otherwise noted in the entry.

All values use SI units:
- `density`: kg/m^3
- `dynamic_viscosity`: Pa*s
- `kinematic_viscosity`: m^2/s
- `surface_tension`: N/m
- `specific_heat`: J/(kg*K)
- `thermal_conductivity`: W/(m*K)
- `boiling_point`, `freezing_point`: K

Rust: `materials::fluids::Fluid`
    """
    @property
    def name(self) -> str: ...
    @property
    def density(self) -> float: ...
    @property
    def dynamic_viscosity(self) -> float: ...
    @property
    def kinematic_viscosity(self) -> float: ...
    @property
    def surface_tension(self) -> float: ...
    @property
    def specific_heat(self) -> float: ...
    @property
    def thermal_conductivity(self) -> float: ...
    @property
    def boiling_point(self) -> float: ...
    @property
    def freezing_point(self) -> float: ...

def by_name(name: str) -> Optional[Fluid]:
    """
Looks up a fluid by name using case-insensitive ASCII comparison.

Zero-allocation: uses `eq_ignore_ascii_case` instead of `to_lowercase`.

Rust: `materials::fluids::by_name`
    """
    ...

def all() -> list[Fluid]:
    """
Returns a slice of all fluids in the database.

Rust: `materials::fluids::all`
    """
    ...
