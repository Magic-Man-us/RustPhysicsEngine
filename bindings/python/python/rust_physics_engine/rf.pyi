"""
Radio-frequency engineering: links, lines and noise.  Link budgets built from free-space path loss, the Friis transmission equation, antenna gain and effective area, EIRP, beamwidth, directivity and fade margin.  Transmission lines: characteristic impedance of coax, velocity factor, guide wavelength, and the mismatch quantities -- VSWR, return loss, mismatch loss. Conductors are covered by the skin depth `δ = √(2ρ/ωμ)`, which is why RF current flows in a thin surface layer.  Noise and units: thermal noise power and floor in dBm, signal-to-noise ratio, the Shannon capacity of the resulting channel, and conversions between watts, dBm, ratios and decibels.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def wavelength_to_frequency(wavelength: float) -> float:
    """
Wavelength to frequency: f = c / λ

Rust: `rf::wavelength_to_frequency`
    """
    ...

def frequency_to_wavelength(frequency: float) -> float:
    """
Frequency to wavelength: λ = c / f

Rust: `rf::frequency_to_wavelength`
    """
    ...

def frequency_to_energy(frequency: float) -> float:
    """
Photon energy from frequency: E = hf

Rust: `rf::frequency_to_energy`
    """
    ...

def free_space_path_loss(distance: float, frequency: float) -> float:
    """
Free-space path loss in dB: FSPL = 20log₁₀(d) + 20log₁₀(f) + 20log₁₀(4π/c)

Rust: `rf::free_space_path_loss`
    """
    ...

def friis_received_power(pt: float, gt: float, gr: float, wavelength: float, distance: float) -> float:
    """
Friis transmission equation (linear): Pr = Pt × Gt × Gr × (λ/(4πd))²

Rust: `rf::friis_received_power`
    """
    ...

def link_budget_db(pt_dbm: float, gt_dbi: float, gr_dbi: float, path_loss_db: float) -> float:
    """
Link budget in dB: Pr = Pt + Gt + Gr - PathLoss

Rust: `rf::link_budget_db`
    """
    ...

def skin_depth_conductor(frequency: float, permeability: float, conductivity: float) -> float:
    """
Skin depth in a conductor: δ = 1 / √(πfμσ)

Rust: `rf::skin_depth_conductor`
    """
    ...

def fade_margin_db(transmitted_dbm: float, received_dbm: float, sensitivity_dbm: float) -> float:
    """
Fade margin: FM = received_dBm - sensitivity_dBm

Rust: `rf::fade_margin_db`
    """
    ...

def antenna_gain_from_area(effective_area: float, wavelength: float) -> float:
    """
Antenna gain from effective area: G = 4πA_e / λ²

Rust: `rf::antenna_gain_from_area`
    """
    ...

def effective_area_from_gain(gain: float, wavelength: float) -> float:
    """
Effective aperture from gain: A_e = Gλ² / (4π)

Rust: `rf::effective_area_from_gain`
    """
    ...

def half_wave_dipole_gain() -> float:
    """
Returns the half-wave dipole gain (linear): G ≈ 1.64 (2.15 dBi).

Rust: `rf::half_wave_dipole_gain`
    """
    ...

def eirp(power: float, gain: float) -> float:
    """
Effective isotropic radiated power: EIRP = P × G

Rust: `rf::eirp`
    """
    ...

def beamwidth_approximate(wavelength: float, aperture: float) -> float:
    """
Approximate antenna beamwidth in degrees: θ ≈ 70λ / D.

Rust: `rf::beamwidth_approximate`
    """
    ...

def antenna_directivity(gain: float, efficiency: float) -> float:
    """
Antenna directivity from gain and efficiency: D = G / η

Rust: `rf::antenna_directivity`
    """
    ...

def characteristic_impedance_coax(outer_radius: float, inner_radius: float, permittivity_rel: float) -> float:
    """
Characteristic impedance of coaxial cable: Z₀ = (138/√εr) × log₁₀(D/d).

Rust: `rf::characteristic_impedance_coax`
    """
    ...

def velocity_factor(permittivity_rel: float) -> float:
    """
Velocity factor: VF = 1 / √εr

Rust: `rf::velocity_factor`
    """
    ...

def wavelength_in_line(free_space_wavelength: float, velocity_factor: float) -> float:
    """
Wavelength in a transmission line: λ_line = λ₀ × VF

Rust: `rf::wavelength_in_line`
    """
    ...

def vswr(reflection_coeff: float) -> float:
    """
Voltage standing wave ratio: VSWR = (1 + |Γ|) / (1 - |Γ|)

Rust: `rf::vswr`
    """
    ...

def return_loss(reflection_coeff: float) -> float:
    """
Return loss in dB: RL = -20log₁₀(|Γ|)

Rust: `rf::return_loss`
    """
    ...

def mismatch_loss(vswr: float) -> float:
    """
Mismatch loss in dB: ML = -10log₁₀(1 - ((VSWR-1)/(VSWR+1))²)

Rust: `rf::mismatch_loss`
    """
    ...

def dbm_to_watts(dbm: float) -> float:
    """
Convert dBm to watts: P = 10^((dBm - 30) / 10)

Rust: `rf::dbm_to_watts`
    """
    ...

def watts_to_dbm(watts: float) -> float:
    """
Convert watts to dBm: dBm = 10log₁₀(P) + 30

Rust: `rf::watts_to_dbm`
    """
    ...

def db_to_ratio(db: float) -> float:
    """
Convert dB to linear ratio: ratio = 10^(dB / 10)

Rust: `rf::db_to_ratio`
    """
    ...

def ratio_to_db(ratio: float) -> float:
    """
Convert linear ratio to dB: dB = 10log₁₀(ratio)

Rust: `rf::ratio_to_db`
    """
    ...

def noise_power(bandwidth: float, temperature: float) -> float:
    """
Thermal noise power: N = k_B × T × B

Rust: `rf::noise_power`
    """
    ...

def snr_db(signal_power: float, noise_power: float) -> float:
    """
Signal-to-noise ratio in dB: SNR = 10log₁₀(S / N)

Rust: `rf::snr_db`
    """
    ...

def thermal_noise_floor_dbm(bandwidth: float, temperature: float) -> float:
    """
Thermal noise floor in dBm: 10log₁₀(k_B × T × B) + 30

Rust: `rf::thermal_noise_floor_dbm`
    """
    ...

def shannon_capacity(bandwidth: float, snr_linear: float) -> float:
    """
Shannon-Hartley channel capacity: C = B × log₂(1 + SNR)

Rust: `rf::shannon_capacity`
    """
    ...
