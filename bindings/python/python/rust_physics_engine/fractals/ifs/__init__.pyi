"""
Iterated function systems: the chaos game and deterministic attractor construction (Barnsley, "Fractals Everywhere", 1988), Moran similarity dimension, collage error, a library of classic IFS presets in 2-D and 3-D, and Draves-style fractal flames.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from . import presets
from rust_physics_engine.spatial.transform2d import Affine2
from rust_physics_engine.spatial.primitives import Polygon2
from rust_physics_engine.spatial.primitives import Rect
from rust_physics_engine.monte_carlo import Rng
from rust_physics_engine.math import Vec2
from rust_physics_engine.math import Vec3

class Ifs:
    """
A 2-D iterated function system: contractive affine maps with
selection probabilities.

Rust: `fractals::ifs::Ifs`
    """
    def __init__(self, maps: list[tuple[Affine2, float]]) -> None: ...
    def chaos_game(self, n: int, burn_in: int, rng: Rng) -> list[Vec2]: ...
    def chaos_game_colored(self, n: int, burn_in: int, rng: Rng) -> list[tuple[Vec2, int]]: ...
    def deterministic(self, depth: int, seed: Polygon2) -> list[Polygon2]: ...
    def deterministic_points(self, depth: int) -> list[Vec2]: ...
    def similarity_dimension(self) -> Optional[float]: ...
    def bounding_rect(self, n: int, rng: Rng) -> Rect: ...
    def collage_error(self, target: list[Vec2 | Sequence[float]]) -> float: ...
    def render_density(self, n: int, res: tuple[int, int], rng: Rng) -> list[int]: ...
    @property
    def maps(self) -> list[tuple[Affine2, float]]: ...

class Ifs3:
    """
A 3-D IFS with affine maps stored as `Mat4`.

Rust: `fractals::ifs::Ifs3`
    """
    def __init__(self, maps: list[tuple[Mat4, float]]) -> None: ...
    def chaos_game(self, n: int, burn_in: int, rng: Rng) -> list[Vec3]: ...
    def deterministic_points(self, depth: int) -> list[Vec3]: ...
    @property
    def maps(self) -> list[tuple[Mat4, float]]: ...

class Variation:
    """
The nonlinear variations of Draves & Reckase, "The Fractal Flame
Algorithm". Variations with free parameters use the fixed values
noted below; Julia uses the Ω = 0 branch so results are
deterministic.

Rust: `fractals::ifs::Variation`
    """
    ...

def apply_variation(v: Variation, p: Vec2 | Sequence[float]) -> Vec2:
    """
Applies one flame variation to a point. Formulas follow the flame
paper's conventions: r = |p|, θ = atan2(x, y).

Rust: `fractals::ifs::apply_variation`
    """
    ...

def fractal_flame(maps: list[tuple[Affine2, float, Variation]], n: int, rng: Rng) -> list[tuple[Vec2, float]]:
    """
Fractal flame chaos game: each step applies a randomly chosen
affine map followed by its variation, and blends a per-map color
coordinate c ← (c + cᵢ)/2 with cᵢ = i/(m−1). Returns points with
their color coordinates; non-finite excursions restart from the
origin.

Panics:
Panics on an empty map list or non-positive total probability.

Rust: `fractals::ifs::fractal_flame`
    """
    ...
