"""
Beta function and regularized incomplete beta.  B(a,b) = Γ(a)Γ(b)/Γ(a+b), evaluated in log space. The regularized incomplete beta I_x(a,b) uses the modified Lentz continued fraction of Numerical Recipes §6.4.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.statistics.distributions import Beta

def beta(a: float, b: float) -> float:
    """
Complete beta function B(a,b) = Γ(a)Γ(b)/Γ(a+b).

Panics:
Panics unless a > 0 and b > 0.

Rust: `special::beta::beta`
    """
    ...

def beta_inc(a: float, b: float, x: float) -> float:
    """
Regularized incomplete beta function I_x(a,b), the CDF of the Beta
distribution.

Panics:
Panics unless a > 0, b > 0, and x ∈ [0, 1].

Rust: `special::beta::beta_inc`
    """
    ...
