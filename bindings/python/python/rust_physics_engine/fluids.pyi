"""
Fluid statics and single-phase flow.  Statics: hydrostatic pressure, buoyancy and flotation, Pascal's principle. Inviscid flow: continuity, Bernoulli, Torricelli, the Venturi meter. Viscous flow: Stokes drag, the drag equation and terminal velocity, Poiseuille's law, the Darcy-Weisbach head loss. Compressible flow: Mach number, stagnation and isentropic ratios.  Surface tension, capillary rise, vorticity, circulation and the Kutta-Joukowski lift round it out, along with the dimensionless groups that decide which regime you are in -- Reynolds, Froude, Weber, Bond, Peclet, Marangoni, Archimedes.  These are the closed-form relations. For flow solved on a grid or with particles see `cfd`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.audio.envelope import Ar

def hydrostatic_pressure(density: float, g: float, depth: float) -> float:
    """
Hydrostatic pressure: P = ρ * g * h

Rust: `fluids::hydrostatic_pressure`
    """
    ...

def total_pressure(atmospheric_pressure: float, density: float, g: float, depth: float) -> float:
    """
Total pressure at depth: P = P_atm + ρ * g * h

Rust: `fluids::total_pressure`
    """
    ...

def pascal_force(f1: float, a1: float, a2: float) -> float:
    """
Pascal's principle: F2 = F1 * (A2 / A1)

Rust: `fluids::pascal_force`
    """
    ...

def pressure(force: float, area: float) -> float:
    """
Pressure: P = F / A

Rust: `fluids::pressure`
    """
    ...

def buoyant_force(fluid_density: float, displaced_volume: float, g: float) -> float:
    """
Buoyant force (Archimedes' principle): F_b = ρ_fluid * V_displaced * g

Rust: `fluids::buoyant_force`
    """
    ...

def fraction_submerged(object_density: float, fluid_density: float) -> float:
    """
Fraction of object submerged (floating): f = ρ_object / ρ_fluid

Rust: `fluids::fraction_submerged`
    """
    ...

def apparent_weight(mass: float, object_volume: float, fluid_density: float, g: float) -> float:
    """
Apparent weight in fluid: W_app = W - F_b = mg - ρ_fluid * V * g

Rust: `fluids::apparent_weight`
    """
    ...

def continuity_velocity(a1: float, v1: float, a2: float) -> float:
    """
Continuity equation: A1 * v1 = A2 * v2 → v2 = A1 * v1 / A2

Rust: `fluids::continuity_velocity`
    """
    ...

def flow_rate(area: float, velocity: float) -> float:
    """
Volume flow rate: Q = A * v

Rust: `fluids::flow_rate`
    """
    ...

def mass_flow_rate(density: float, area: float, velocity: float) -> float:
    """
Mass flow rate: ṁ = ρ * A * v

Rust: `fluids::mass_flow_rate`
    """
    ...

def bernoulli_pressure(p1: float, density: float, v1: float, h1: float, v2: float, h2: float, g: float) -> float:
    """
Bernoulli's equation: P1 + 0.5*ρ*v1^2 + ρ*g*h1 = P2 + 0.5*ρ*v2^2 + ρ*g*h2
Returns P2 given all other quantities.

Rust: `fluids::bernoulli_pressure`
    """
    ...

def torricelli_velocity(g: float, height: float) -> float:
    """
Torricelli's theorem: v = sqrt(2 * g * h)

Rust: `fluids::torricelli_velocity`
    """
    ...

def venturi_velocity(p1: float, p2: float, density: float, a1: float, a2: float) -> float:
    """
Venturi effect velocity from pressure difference:
v2 = sqrt(2 * (P1 - P2) / (ρ * (1 - (A2/A1)^2)))

Rust: `fluids::venturi_velocity`
    """
    ...

def drag_force(drag_coefficient: float, density: float, area: float, velocity: float) -> float:
    """
Drag force: F_d = 0.5 * C_d * ρ * A * v^2

Rust: `fluids::drag_force`
    """
    ...

def terminal_velocity(mass: float, g: float, density: float, area: float, drag_coefficient: float) -> float:
    """
Terminal velocity: v_t = sqrt(2 * m * g / (ρ * A * C_d))

Rust: `fluids::terminal_velocity`
    """
    ...

def stokes_drag(dynamic_viscosity: float, radius: float, velocity: float) -> float:
    """
Stokes' drag (low Reynolds number): F = 6π * μ * r * v

Rust: `fluids::stokes_drag`
    """
    ...

def reynolds_number(density: float, velocity: float, length: float, dynamic_viscosity: float) -> float:
    """
Reynolds number: Re = ρ * v * L / μ

Rust: `fluids::reynolds_number`
    """
    ...

def poiseuille_flow_rate(radius: float, pressure_drop: float, dynamic_viscosity: float, length: float) -> float:
    """
Poiseuille's law (volume flow rate through a pipe):
Q = π * r^4 * ΔP / (8 * μ * L)

Rust: `fluids::poiseuille_flow_rate`
    """
    ...

def surface_tension_force(surface_tension: float, length: float) -> float:
    """
Surface tension force along a line: F = γ * L

Rust: `fluids::surface_tension_force`
    """
    ...

def capillary_rise(surface_tension: float, contact_angle_rad: float, density: float, g: float, tube_radius: float) -> float:
    """
Capillary rise: h = 2 * γ * cos(θ) / (ρ * g * r)

Rust: `fluids::capillary_rise`
    """
    ...

def mach_number(velocity: float, speed_of_sound: float) -> float:
    """
Mach number: M = v / a

Rust: `fluids::mach_number`
    """
    ...

def dynamic_pressure(density: float, velocity: float) -> float:
    """
Dynamic pressure: q = ½ρv²

Rust: `fluids::dynamic_pressure`
    """
    ...

def stagnation_pressure(static_pressure: float, dynamic_pressure: float) -> float:
    """
Stagnation pressure: P₀ = P + q

Rust: `fluids::stagnation_pressure`
    """
    ...

def isentropic_pressure_ratio(mach: float, gamma: float) -> float:
    """
Isentropic pressure ratio: P/P₀ = (1 + (γ-1)/2 × M²)^(-γ/(γ-1))

Rust: `fluids::isentropic_pressure_ratio`
    """
    ...

def isentropic_temperature_ratio(mach: float, gamma: float) -> float:
    """
Isentropic temperature ratio: T/T₀ = (1 + (γ-1)/2 × M²)^(-1)

Rust: `fluids::isentropic_temperature_ratio`
    """
    ...

def vorticity_2d(dvx_dy: float, dvy_dx: float) -> float:
    """
Vorticity in 2D: ω = ∂v_y/∂x - ∂v_x/∂y

Rust: `fluids::vorticity_2d`
    """
    ...

def circulation(vorticity: float, area: float) -> float:
    """
Circulation (uniform vorticity): Γ = ω × A

Rust: `fluids::circulation`
    """
    ...

def kutta_joukowski_lift(density: float, velocity: float, circulation: float) -> float:
    """
Kutta-Joukowski lift per unit span: L = ρ × V × Γ

Rust: `fluids::kutta_joukowski_lift`
    """
    ...

def kinematic_viscosity(dynamic_viscosity: float, density: float) -> float:
    """
Kinematic viscosity: ν = μ/ρ

Rust: `fluids::kinematic_viscosity`
    """
    ...

def pressure_gradient_pipe(flow_rate: float, dynamic_viscosity: float, radius: float, length: float) -> float:
    """
Pressure gradient in a pipe (Poiseuille inverse): dP = 8μLQ/(πr⁴)

Rust: `fluids::pressure_gradient_pipe`
    """
    ...

def hydraulic_diameter(area: float, wetted_perimeter: float) -> float:
    """
Hydraulic diameter: D_h = 4A/P

Rust: `fluids::hydraulic_diameter`
    """
    ...

def darcy_friction_factor_laminar(reynolds: float) -> float:
    """
Darcy friction factor for laminar pipe flow: f = 64/Re

Rust: `fluids::darcy_friction_factor_laminar`
    """
    ...

def darcy_weisbach_head_loss(friction_factor: float, length: float, diameter: float, velocity: float, g: float) -> float:
    """
Darcy-Weisbach head loss: h_L = f × (L/D) × v²/(2g)

Rust: `fluids::darcy_weisbach_head_loss`
    """
    ...

def buoyancy_velocity(g: float, beta: float, delta_temp: float, length: float) -> float:
    """
Characteristic buoyancy velocity: v = √(gβΔTL)

Rust: `fluids::buoyancy_velocity`
    """
    ...

def thermal_expansion_coefficient_ideal_gas(temperature: float) -> float:
    """
Thermal expansion coefficient for ideal gas: β = 1/T

Rust: `fluids::thermal_expansion_coefficient_ideal_gas`
    """
    ...

def viscosity_sutherland(mu0: float, t0: float, t: float, s: float) -> float:
    """
Sutherland's law for viscosity: μ = μ₀ × (T/T₀)^(3/2) × (T₀ + S)/(T + S)

Rust: `fluids::viscosity_sutherland`
    """
    ...

def thermal_conductivity_gas(k0: float, t0: float, t: float, s: float) -> float:
    """
Sutherland's law for thermal conductivity (same form as viscosity)

Rust: `fluids::thermal_conductivity_gas`
    """
    ...

def natural_convection_nu_vertical(rayleigh: float) -> float:
    """
Churchill-Chu correlation for natural convection on a vertical plate (air, Pr≈0.71):
Nu = (0.825 + 0.387 × Ra^(1/6) / 1.1936)²

Rust: `fluids::natural_convection_nu_vertical`
    """
    ...

def natural_convection_nu_horizontal_hot(rayleigh: float) -> float:
    """
Natural convection Nusselt number for hot horizontal plate facing up:
Nu = 0.54 × Ra^(1/4), valid for 10⁴ ≤ Ra ≤ 10⁷

Rust: `fluids::natural_convection_nu_horizontal_hot`
    """
    ...

def marangoni_number(surface_tension_gradient: float, length: float, delta_temp: float, dynamic_viscosity: float, thermal_diffusivity: float) -> float:
    """
Marangoni number: Ma = -(dσ/dT) × L × ΔT / (μ × α)

Rust: `fluids::marangoni_number`
    """
    ...

def bond_number(density_diff: float, g: float, length: float, surface_tension: float) -> float:
    """
Bond number: Bo = Δρ × g × L² / σ (gravity vs surface tension)

Rust: `fluids::bond_number`
    """
    ...

def weber_number(density: float, velocity: float, length: float, surface_tension: float) -> float:
    """
Weber number: We = ρv²L / σ (inertia vs surface tension)

Rust: `fluids::weber_number`
    """
    ...

def froude_number(velocity: float, g: float, length: float) -> float:
    """
Froude number: Fr = v / √(gL) (inertia vs gravity in free surface flow)

Rust: `fluids::froude_number`
    """
    ...

def archimedes_number(density_fluid: float, density_particle: float, diameter: float, dynamic_viscosity: float, g: float) -> float:
    """
Archimedes number: Ar = g × d³ × ρf × (ρp - ρf) / μ²

Rust: `fluids::archimedes_number`
    """
    ...

def peclet_number(velocity: float, length: float, diffusivity: float) -> float:
    """
Peclet number: Pe = vL/α (advection vs diffusion)

Rust: `fluids::peclet_number`
    """
    ...
