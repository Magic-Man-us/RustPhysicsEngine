"""
LU decomposition with partial pivoting (Doolittle form).  Reference: Golub & Van Loan, *Matrix Computations*, §3.4.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.linalg.matrix import Matrix

class Lu:
    """
Packed LU factorization P·A = L·U.

`lu` stores U on and above the diagonal and the unit-lower-triangular
L (implicit ones on the diagonal) below it. `perm[i]` is the row of A
that ended up in position i; `sign` is the permutation's parity (±1).

Rust: `linalg::lu::Lu`
    """
    def __init__(self, lu: Matrix | Sequence[Sequence[float]], perm: list[int], sign: float) -> None: ...
    def solve(self, b: list[float]) -> list[float]: ...
    def solve_matrix(self, b: Matrix | Sequence[Sequence[float]]) -> Matrix: ...
    def determinant(self) -> float: ...
    def inverse(self) -> Matrix: ...
    @property
    def lu(self) -> Matrix: ...
    @property
    def perm(self) -> list[int]: ...
    @property
    def sign(self) -> float: ...

def lu_decompose(a: Matrix | Sequence[Sequence[float]]) -> Lu:
    """
Factors a square matrix as P·A = L·U with partial (row) pivoting.

Returns `SolveError::InvalidArgument` for non-square input and
`SolveError::Singular` when a pivot falls below the threshold.

Rust: `linalg::lu::lu_decompose`
    """
    ...

def solve(a: Matrix | Sequence[Sequence[float]], b: list[float]) -> list[float]:
    """
Convenience wrapper: factor `a` and solve A·x = b in one call.

Rust: `linalg::lu::solve`
    """
    ...
