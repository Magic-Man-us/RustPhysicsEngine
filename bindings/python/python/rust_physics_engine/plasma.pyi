"""
Plasma parameters: the characteristic lengths, frequencies and speeds.  The Debye length is where it starts -- the distance over which a plasma screens a charge, and therefore the scale below which "plasma" stops being the right description. The Debye number counts particles in that sphere, and a plasma is only collective if that number is large.  Frequencies: electron and ion plasma frequencies, and the cyclotron frequencies in a magnetic field, with the associated Larmor radius. Speeds: thermal, ion-acoustic, Alfvén and magnetosonic. Plus magnetic pressure, plasma beta, the skin depth, the Coulomb logarithm and the collision frequency.  For a conducting fluid treated as a continuum see `magnetohydrodynamics`.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


def debye_length(temperature: float, density: float, charge: float) -> float:
    """
λD = √(ε₀kT / (nq²))

Rust: `plasma::debye_length`
    """
    ...

def plasma_frequency_electron(density: float) -> float:
    """
ωp = √(ne² / (mₑε₀))

Rust: `plasma::plasma_frequency_electron`
    """
    ...

def plasma_frequency_ion(density: float, ion_mass: float) -> float:
    """
ωp = √(ne² / (m_ion × ε₀))

Rust: `plasma::plasma_frequency_ion`
    """
    ...

def cyclotron_frequency_electron(b_field: float) -> float:
    """
ωc = eB / mₑ

Rust: `plasma::cyclotron_frequency_electron`
    """
    ...

def cyclotron_frequency_ion(b_field: float, ion_mass: float, charge: float) -> float:
    """
ωc = qB / m

Rust: `plasma::cyclotron_frequency_ion`
    """
    ...

def larmor_radius(velocity_perp: float, mass: float, charge: float, b_field: float) -> float:
    """
rL = mv⊥ / (|q|B)

Rust: `plasma::larmor_radius`
    """
    ...

def plasma_beta(pressure: float, b_field: float) -> float:
    """
β = 2μ₀p / B²

Rust: `plasma::plasma_beta`
    """
    ...

def alfven_speed(b_field: float, density: float) -> float:
    """
vA = B / √(μ₀ρ)

Rust: `plasma::alfven_speed`
    """
    ...

def sound_speed_plasma(gamma: float, temperature: float, ion_mass: float) -> float:
    """
cs = √(γkT / m)

Rust: `plasma::sound_speed_plasma`
    """
    ...

def thermal_velocity(temperature: float, mass: float) -> float:
    """
vth = √(2kT / m)

Rust: `plasma::thermal_velocity`
    """
    ...

def debye_number(density: float, debye_len: float) -> float:
    """
ND = n × (4π/3)λD³

Rust: `plasma::debye_number`
    """
    ...

def coulomb_logarithm(temperature: float, density: float) -> float:
    """
lnΛ = ln(12π × ND)

Rust: `plasma::coulomb_logarithm`
    """
    ...

def magnetic_pressure(b_field: float) -> float:
    """
Pm = B² / (2μ₀)

Rust: `plasma::magnetic_pressure`
    """
    ...

def magnetosonic_speed(alfven: float, sound: float) -> float:
    """
vms = √(vA² + cs²)

Rust: `plasma::magnetosonic_speed`
    """
    ...

def skin_depth_plasma(plasma_freq: float) -> float:
    """
δ = c / ωp

Rust: `plasma::skin_depth_plasma`
    """
    ...

def collision_frequency(density: float, temperature: float, coulomb_log: float, mass: float, charge: float) -> float:
    """
ν = nq⁴lnΛ / (4πε₀²m²vth³)

Rust: `plasma::collision_frequency`
    """
    ...
