"""
Union-find over `0..n` with path compression and union by size.  Shared infrastructure: graph minimum spanning trees, percolation cluster labelling, and single-linkage clustering all reduce to the same "merge these two, are these two together" question.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional


class DisjointSet:
    """
Union-find over `0..n` with path compression and union by size.

Shared infrastructure: graph minimum spanning trees, percolation cluster
labelling, and single-linkage clustering all reduce to the same
"merge these two, are these two together" question.
Disjoint-set forest over the elements `0..n`.

Rust: `discrete::disjoint_set::DisjointSet`
    """
    def __init__(self, n: int) -> None: ...
    def len(self) -> int: ...
    def is_empty(self) -> bool: ...
    def count(self) -> int: ...
    def find(self, x: int) -> int: ...
    def union(self, a: int, b: int) -> bool: ...
    def connected(self, a: int, b: int) -> bool: ...
    def set_size(self, x: int) -> int: ...
    def sets(self) -> list[list[int]]: ...
    def labels(self) -> list[int]: ...
