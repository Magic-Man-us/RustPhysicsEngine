"""
Phyllotactic patterns and spirals: Vogel sunflowers, Fibonacci point sets, the classical spiral family, and parastichy analysis.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.patterns.tilings import Archimedean
from rust_physics_engine.math import Vec2
from rust_physics_engine.math import Vec3

def vogel_sunflower(n: int, scale: float) -> list[Vec2]:
    """
Vogel's sunflower model (Vogel 1979): floret i at radius
`scale · √i` and angle `i · GOLDEN_ANGLE`.

Rust: `patterns::phyllotaxis::vogel_sunflower`
    """
    ...

def vogel_sunflower_angle(n: int, scale: float, angle: float) -> list[Vec2]:
    """
Vogel model with an arbitrary divergence angle.

Rust: `patterns::phyllotaxis::vogel_sunflower_angle`
    """
    ...

def fibonacci_sphere(n: int) -> list[Vec3]:
    """
Near-uniform points on the unit sphere: latitude strips of equal
area, longitude advanced by the golden angle.

Panics:
Panics unless `n >= 1`.

Rust: `patterns::phyllotaxis::fibonacci_sphere`
    """
    ...

def fibonacci_disk(n: int) -> list[Vec2]:
    """
Near-uniform points on the unit disk (Vogel pattern scaled so the
n-th floret reaches radius 1).

Panics:
Panics unless `n >= 1`.

Rust: `patterns::phyllotaxis::fibonacci_disk`
    """
    ...

def fibonacci_hemisphere(n: int) -> list[Vec3]:
    """
Near-uniform points on the upper (y > 0) unit hemisphere.

Panics:
Panics unless `n >= 1`.

Rust: `patterns::phyllotaxis::fibonacci_hemisphere`
    """
    ...

def golden_spiral(turns: float, points_per_turn: int, a: float) -> list[Vec2]:
    """
Golden spiral: logarithmic spiral growing by φ every quarter turn,
starting radius `a`.

Panics:
Panics unless `turns > 0`, `points_per_turn >= 1`, `a > 0`.

Rust: `patterns::phyllotaxis::golden_spiral`
    """
    ...

def archimedean_spiral(a: float, b: float, theta_max: float, n: int) -> list[Vec2]:
    """
Archimedean spiral r = a + bθ sampled on `n` points over
θ ∈ [0, theta_max].

Panics:
Panics unless `n >= 2`.

Rust: `patterns::phyllotaxis::archimedean_spiral`
    """
    ...

def logarithmic_spiral(a: float, b: float, theta_max: float, n: int) -> list[Vec2]:
    """
Logarithmic spiral r = a e^{bθ}.

Panics:
Panics unless `n >= 2`.

Rust: `patterns::phyllotaxis::logarithmic_spiral`
    """
    ...

def fermat_spiral(a: float, theta_max: float, n: int) -> list[Vec2]:
    """
Fermat (parabolic) spiral r = a √θ.

Panics:
Panics unless `n >= 2` and `theta_max >= 0`.

Rust: `patterns::phyllotaxis::fermat_spiral`
    """
    ...

def hyperbolic_spiral(a: float, theta_range: tuple[float, float], n: int) -> list[Vec2]:
    """
Hyperbolic spiral r = a/θ over `theta_range` (which must exclude
0).

Panics:
Panics unless `n >= 2` and the range excludes zero.

Rust: `patterns::phyllotaxis::hyperbolic_spiral`
    """
    ...

def lituus(a: float, theta_range: tuple[float, float], n: int) -> list[Vec2]:
    """
Lituus r = a/√θ over `theta_range` (positive).

Panics:
Panics unless `n >= 2` and the range is positive.

Rust: `patterns::phyllotaxis::lituus`
    """
    ...

def euler_spiral(length: float, n: int) -> list[Vec2]:
    """
Euler spiral (clothoid): curvature grows linearly with arclength,
κ(s) = s. Points via composite-Simpson evaluation of the Fresnel
integrals x = ∫cos(t²/2)dt, y = ∫sin(t²/2)dt.

Panics:
Panics unless `n >= 2` and `length > 0`.

Rust: `patterns::phyllotaxis::euler_spiral`
    """
    ...

def spiral_of_theodorus(n: int) -> list[Vec2]:
    """
Spiral of Theodorus (square-root spiral): `n` right triangles with
unit legs; vertex k lies at radius √(k+1).

Panics:
Panics unless `n >= 1`.

Rust: `patterns::phyllotaxis::spiral_of_theodorus`
    """
    ...

def conical_spiral(a: float, b: float, h: float, turns: float, n: int) -> list[Vec3]:
    """
Conical spiral: radius `a + b t`, height `h t`, `turns` full turns
over t ∈ [0, 1], axis y.

Panics:
Panics unless `n >= 2`.

Rust: `patterns::phyllotaxis::conical_spiral`
    """
    ...

def spherical_spiral(turns: float, n: int) -> list[Vec3]:
    """
Spherical spiral on the unit sphere: polar angle sweeps 0..π while
the azimuth makes `turns` turns (axis y).

Panics:
Panics unless `n >= 2`.

Rust: `patterns::phyllotaxis::spherical_spiral`
    """
    ...

def parastichy_counts(points: list[Vec2 | Sequence[float]]) -> tuple[int, int]:
    """
Detects the two dominant parastichy (visible spiral) families:
the two most common index differences between each floret and its
nearest neighbors, returned ascending. For golden-angle patterns
these are consecutive Fibonacci numbers.

Panics:
Panics unless at least 8 points are given.

Rust: `patterns::phyllotaxis::parastichy_counts`
    """
    ...

def cylinder_phyllotaxis(n: int, rise: float, angle: float, radius: float) -> list[Vec3]:
    """
Helical (cylindrical) phyllotaxis: point i at height `i · rise`
and azimuth `i · angle` on a cylinder of the given radius.

Rust: `patterns::phyllotaxis::cylinder_phyllotaxis`
    """
    ...

def spiral_interpolate_sequence(a: Vec2 | Sequence[float], b: Vec2 | Sequence[float], n: int) -> list[Vec2]:
    """
Logarithmic-spiral arc from `a` to `b` about the origin: `n`
points (inclusive) interpolating radius geometrically and angle
linearly (shortest way around).

Panics:
Panics unless `n >= 2` and both points are away from the origin.

Rust: `patterns::phyllotaxis::spiral_interpolate_sequence`
    """
    ...

GOLDEN_ANGLE: float
