"""
Space-filling curves and locality-preserving orders: Hilbert (2-D and 3-D), Peano, Morton/Z-order, Gray codes, and L-system curves (Sierpiński arrowhead, Moore, Gosper).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.math import Vec2
from rust_physics_engine.math import Vec3

def hilbert_d2xy(order: int, d: int) -> tuple[int, int]:
    """
Hilbert curve index to grid coordinates on a `2^order` square grid
(Wikipedia's iterative rotate-and-flip formulation).

Panics:
Panics unless `1 <= order <= 31` and `d < 4^order`.

Rust: `patterns::space_filling::hilbert_d2xy`
    """
    ...

def hilbert_xy2d(order: int, x: int, y: int) -> int:
    """
Grid coordinates to Hilbert index (inverse of `hilbert_d2xy`).

Panics:
Panics unless `1 <= order <= 31` and both coordinates are below
`2^order`.

Rust: `patterns::space_filling::hilbert_xy2d`
    """
    ...

def hilbert_curve_2d(order: int) -> list[Vec2]:
    """
The full Hilbert curve as points in the unit square (cell
centers), in curve order.

Panics:
Panics unless `1 <= order <= 10` (2^20 points at most).

Rust: `patterns::space_filling::hilbert_curve_2d`
    """
    ...

def hilbert_3d_d2xyz(order: int, d: int) -> tuple[int, int, int]:
    """
3-D Hilbert index to grid coordinates on a `2^order` cube.

Panics:
Panics unless `1 <= order <= 21` and `d < 8^order`.

Rust: `patterns::space_filling::hilbert_3d_d2xyz`
    """
    ...

def hilbert_3d_xyz2d(order: int, x: int, y: int, z: int) -> int:
    """
3-D grid coordinates to Hilbert index (inverse of
`hilbert_3d_d2xyz`).

Panics:
Panics unless `1 <= order <= 21` and all coordinates are below
`2^order`.

Rust: `patterns::space_filling::hilbert_3d_xyz2d`
    """
    ...

def hilbert_curve_3d(order: int) -> list[Vec3]:
    """
The 3-D Hilbert curve as points in the unit cube, in curve order.

Panics:
Panics unless `1 <= order <= 6` (2^18 points at most).

Rust: `patterns::space_filling::hilbert_curve_3d`
    """
    ...

def peano_curve(order: int) -> list[Vec2]:
    """
Peano curve on a `3^order` grid via the ternary digit formula
(Peano 1890): points in the unit square in curve order.

Panics:
Panics unless `1 <= order <= 6`.

Rust: `patterns::space_filling::peano_curve`
    """
    ...

def morton_encode_2d(x: int, y: int) -> int:
    """
Interleaves the bits of x (even positions) and y (odd positions).

Rust: `patterns::space_filling::morton_encode_2d`
    """
    ...

def morton_decode_2d(m: int) -> tuple[int, int]:
    """
Inverse of `morton_encode_2d`.

Rust: `patterns::space_filling::morton_decode_2d`
    """
    ...

def morton_encode_3d(x: int, y: int, z: int) -> int:
    """
Interleaves 21 bits each of x, y, z.

Panics:
Panics when any coordinate exceeds 21 bits.

Rust: `patterns::space_filling::morton_encode_3d`
    """
    ...

def morton_decode_3d(m: int) -> tuple[int, int, int]:
    """
Inverse of `morton_encode_3d`.

Rust: `patterns::space_filling::morton_decode_3d`
    """
    ...

def z_order_curve(order: int) -> list[Vec2]:
    """
The Z-order (Morton) traversal of a `2^order` grid as unit-square
points.

Panics:
Panics unless `1 <= order <= 10`.

Rust: `patterns::space_filling::z_order_curve`
    """
    ...

def gray_code(n: int) -> int:
    """
Binary reflected Gray code.

Rust: `patterns::space_filling::gray_code`
    """
    ...

def gray_decode(g: int) -> int:
    """
Inverse Gray code (prefix xor by doubling).

Rust: `patterns::space_filling::gray_decode`
    """
    ...

def sierpinski_curve(order: int) -> list[Vec2]:
    """
Sierpiński arrowhead curve (traverses the Sierpiński triangle),
unit steps from the origin.

Panics:
Panics unless `1 <= order <= 10`.

Rust: `patterns::space_filling::sierpinski_curve`
    """
    ...

def moore_curve(order: int) -> list[Vec2]:
    """
Moore curve: the closed variant of the Hilbert curve (last point
adjacent to the first), unit grid steps.

Panics:
Panics unless `1 <= order <= 8`.

Rust: `patterns::space_filling::moore_curve`
    """
    ...

def gosper_curve(order: int) -> list[Vec2]:
    """
Gosper (flowsnake) curve, unit steps.

Panics:
Panics unless `1 <= order <= 6`.

Rust: `patterns::space_filling::gosper_curve`
    """
    ...

def hilbert_locality_ratio(points: list[Vec2 | Sequence[float]], order: int) -> float:
    """
Locality measure of the Hilbert order: mean |index difference|
(normalized by the index range) divided by mean spatial distance
(normalized by the bounding-box diagonal) over all point pairs.
Lower means indices track spatial proximity better.

Panics:
Panics unless `1 <= order <= 31` and at least 2 points are given.

Rust: `patterns::space_filling::hilbert_locality_ratio`
    """
    ...
