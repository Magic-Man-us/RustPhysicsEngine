"""
Plane tilings: regular and Archimedean (uniform) tilings, their Laves duals, hex-grid coordinate algebra, and a few classic non-edge-to-edge patterns (brick, herringbone).
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.spatial.primitives import Polygon2
from rust_physics_engine.spatial.primitives import Rect
from rust_physics_engine.math import Vec2

class Archimedean:
    """
The 11 Archimedean (uniform) tilings by vertex configuration.

Rust: `patterns::tilings::Archimedean`
    """
    ...

class Hex:
    """
Axial hex-grid coordinate (Red Blob Games convention); the third
cube coordinate is `s = -q - r`.

Rust: `patterns::tilings::Hex`
    """
    def __init__(self, q: int, r: int) -> None: ...
    def s(self) -> int: ...
    def add(self, other: Hex) -> Hex: ...
    def sub(self, other: Hex) -> Hex: ...
    def scale(self, k: int) -> Hex: ...
    def neighbors(self) -> list[Hex]: ...
    def distance(self, other: Hex) -> int: ...
    def to_pixel(self, size: float, pointy: bool) -> Vec2: ...
    @staticmethod
    def from_pixel(p: Vec2 | Sequence[float], size: float, pointy: bool) -> Hex: ...
    def ring(self, radius: int) -> list[Hex]: ...
    def spiral(self, radius: int) -> list[Hex]: ...
    def line_to(self, other: Hex) -> list[Hex]: ...
    def rotate60(self) -> Hex: ...
    def reflect_q(self) -> Hex: ...
    @property
    def q(self) -> int: ...
    @property
    def r(self) -> int: ...

class Tiling:
    """
A tiling as an indexed face set: `faces` are counterclockwise
vertex loops, `edges` the unique undirected edges.

Rust: `patterns::tilings::Tiling`
    """
    def __init__(self, vertices: list[Vec2 | Sequence[float]], faces: list[list[int]], edges: list[tuple[int, int]]) -> None: ...
    def clip_to_rect(self, rect: Rect) -> Tiling: ...
    def polygons(self) -> list[Polygon2]: ...
    def face_centroids(self) -> list[Vec2]: ...
    def dual(self) -> Tiling: ...
    @property
    def vertices(self) -> list[Vec2]: ...
    @property
    def faces(self) -> list[list[int]]: ...
    @property
    def edges(self) -> list[tuple[int, int]]: ...

def square_grid(nx: int, ny: int, size: float) -> Tiling:
    """
Square grid of `nx` x `ny` cells with the given cell size.

Rust: `patterns::tilings::square_grid`
    """
    ...

def triangular_grid(nx: int, ny: int, size: float) -> Tiling:
    """
Triangular grid: `nx` x `ny` rhombi split into unit triangles of
the given edge length.

Rust: `patterns::tilings::triangular_grid`
    """
    ...

def hexagonal_grid(nx: int, ny: int, size: float, pointy_top: bool) -> Tiling:
    """
Hexagonal grid: `nx` x `ny` hexagons of circumradius `size`.
`pointy_top` orients a vertex upward; otherwise an edge is up.

Rust: `patterns::tilings::hexagonal_grid`
    """
    ...

def archimedean(kind: Archimedean, extent: Rect, size: float) -> Tiling:
    """
Archimedean (uniform) tiling of the given kind with edge length
`size`, covering `extent` (faces with centroid inside).

Rust: `patterns::tilings::archimedean`
    """
    ...

def laves(kind: Archimedean, extent: Rect, size: float) -> Tiling:
    """
Laves tiling: the dual of the corresponding Archimedean tiling.

Rust: `patterns::tilings::laves`
    """
    ...

def hex_range(center: Hex, radius: int) -> list[Hex]:
    """
All hexes within `radius` of `center` (hex-distance ball).

Panics:
Panics for negative radius.

Rust: `patterns::tilings::hex_range`
    """
    ...

def cairo_pentagonal(extent: Rect, size: float) -> Tiling:
    """
Cairo pentagonal tiling: the dual of the snub square tiling.

Rust: `patterns::tilings::cairo_pentagonal`
    """
    ...

def rhombille(extent: Rect, size: float) -> Tiling:
    """
Rhombille (tumbling blocks) tiling: the dual of the trihexagonal
tiling.

Rust: `patterns::tilings::rhombille`
    """
    ...

def brick(extent: Rect, w: float, h: float, offset: float) -> Tiling:
    """
Running-bond brick pattern: rows of `w` x `h` bricks, each row
shifted by `offset` (in units of `w`).

Panics:
Panics unless `w, h > 0`.

Rust: `patterns::tilings::brick`
    """
    ...

def herringbone(extent: Rect, w: float, h: float) -> Tiling:
    """
Herringbone pattern of `w` x `h` bricks (alternating horizontal
and vertical along the diagonals).

Panics:
Panics unless `0 < h < w`.

Rust: `patterns::tilings::herringbone`
    """
    ...
