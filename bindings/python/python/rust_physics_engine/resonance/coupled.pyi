"""
Coupled linear oscillators: normal modes, modal superposition, receptance, classic two-body systems, Kuramoto synchronization, and tuned-mass-damper design.
"""
# @generated by bindings/python/generate.py -- do not edit.
from __future__ import annotations
from collections.abc import Callable, Sequence
from fractions import Fraction
from typing import Any, Optional

from rust_physics_engine.linalg.matrix import Matrix

class CoupledOscillators:
    """
N-degree-of-freedom system M·x″ + C·x′ + K·x = F.

Rust: `resonance::coupled::CoupledOscillators`
    """
    def __init__(self, masses: list[float], stiffness: Matrix | Sequence[Sequence[float]], damping: Matrix | Sequence[Sequence[float]]) -> None: ...
    @staticmethod
    def chain(n: int, m: float, k: float, k_coupling: float) -> CoupledOscillators: ...
    @staticmethod
    def chain_fixed_ends(n: int, m: float, k: float) -> CoupledOscillators: ...
    @staticmethod
    def ring(n: int, m: float, k: float) -> CoupledOscillators: ...
    @staticmethod
    def from_springs(masses: list[float], springs: list[tuple[int, int, float]]) -> CoupledOscillators: ...
    def normal_modes(self) -> tuple[list[float], Matrix]: ...
    def mode_shape(self, i: int) -> list[float]: ...
    def modal_participation(self, x0: list[float]) -> list[float]: ...
    def modal_damping_ratios(self) -> list[float]: ...
    def response(self, x0: list[float], v0: list[float], t: float) -> list[float]: ...
    def forced_response(self, force: list[float], omega: float) -> list[complex]: ...
    def frequency_response_matrix(self, omega: float) -> Matrix: ...
    def beat_frequency(self) -> Optional[float]: ...
    def energy_transfer_time(self) -> Optional[float]: ...
    def dispersion_relation(self, k_wave: float) -> float: ...
    def simulate(self, x0: list[float], v0: list[float], t_end: float, dt: float) -> list[list[float]]: ...
    def dunkerley_estimate(self) -> float: ...
    def rayleigh_quotient(self, shape: list[float]) -> float: ...
    def anti_resonance_frequencies(self, i: int, j: int) -> list[float]: ...
    @property
    def masses(self) -> list[float]: ...
    @property
    def stiffness(self) -> Matrix: ...
    @property
    def damping(self) -> Matrix: ...

def two_pendulums_coupled(l: float, g: float, k: float, m: float) -> CoupledOscillators:
    """
Two pendulums (length l, mass m) coupled by a spring k: 2-dof
small-angle system in the displacement coordinates.

Rust: `resonance::coupled::two_pendulums_coupled`
    """
    ...

def wilberforce_pendulum(m: float, k: float, i: float, kappa: float, eps: float) -> CoupledOscillators:
    """
Wilberforce pendulum: vertical bounce (mass m, spring k) coupled to
torsion (inertia i, stiffness kappa) through the cross term eps.

Rust: `resonance::coupled::wilberforce_pendulum`
    """
    ...

def huygens_sync_simulate(omega1: float, omega2: float, coupling: float, t_end: float, dt: float) -> list[tuple[float, float]]:
    """
Two weakly coupled phase oscillators (Huygens' clocks abstraction):
φ̇₁ = ω₁ + κ·sin(φ₂−φ₁), φ̇₂ = ω₂ + κ·sin(φ₁−φ₂). Returns
(t, wrapped phase difference) per step.

Rust: `resonance::coupled::huygens_sync_simulate`
    """
    ...

def kuramoto(n: int, k: float, omegas: list[float], theta0: list[float], t_end: float, dt: float) -> tuple[list[list[float]], list[float]]:
    """
Kuramoto model of n phase oscillators with global coupling K:
returns the phase history (per step) and the order parameter r(t).

Panics:
Panics unless `omegas` and `theta0` both have length n.

Rust: `resonance::coupled::kuramoto`
    """
    ...

def kuramoto_critical_coupling(omegas: list[float]) -> float:
    """
Critical coupling estimate K_c = 2/(π·g(0)) with the frequency
density at the center estimated by a Gaussian kernel (Silverman
bandwidth) around the mean frequency.

Rust: `resonance::coupled::kuramoto_critical_coupling`
    """
    ...

def tuned_mass_damper_design(m_primary: float, k_primary: float, mass_ratio: float) -> tuple[float, float, float]:
    """
Den Hartog tuned-mass-damper design for an undamped primary
(m_primary, k_primary) and absorber mass ratio μ: returns the
absorber stiffness, damping, and optimal tuning ratio f = 1/(1+μ).

Rust: `resonance::coupled::tuned_mass_damper_design`
    """
    ...
